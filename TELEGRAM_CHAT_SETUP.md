# ✅ Установка TELEGRAM_ALERT_CHAT_ID

## 📋 Задача

Добавить переменную окружения `TELEGRAM_ALERT_CHAT_ID` в n8n для отправки уведомлений в Telegram чат.

**Chat ID:** `-5004140602`  
**Chat Name:** "TG Alert Chat"  
**Type:** group

---

## 🎯 Способ 1: Через n8n UI (Рекомендуется)

1. Откройте n8n: https://n8n.rentflow.rentals
2. Перейдите в **Settings** (⚙️)
3. Выберите **Variables** в меню слева
4. Нажмите **Add Variable**
5. Заполните:
   - **Key:** `TELEGRAM_ALERT_CHAT_ID`
   - **Value:** `-5004140602`
6. Нажмите **Save**

**✅ Готово!** Переменная сразу доступна в workflow через `{{ $env.TELEGRAM_ALERT_CHAT_ID }}`

---

## 🖥️ Способ 2: Через SSH на сервере

### Шаг 1: Подключиться к серверу

```bash
ssh root@46.224.17.15
# Password: Geodrive2024SecurePass
```

### Шаг 2: Перейти в директорию проекта

```bash
cd /root/geodrive_n8n-agents
```

### Шаг 3: Проверить текущую конфигурацию

```bash
grep 'TELEGRAM_ALERT_CHAT_ID' docker-compose.yml
```

### Шаг 4: Добавить переменную

Если переменной нет, добавьте её вручную в `docker-compose.yml`:

```bash
nano docker-compose.yml
```

Найдите секцию `n8n:` → `environment:` и добавьте:

```yaml
services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    environment:
      - TELEGRAM_ALERT_CHAT_ID=-5004140602  # <- Добавьте эту строку
      # ... остальные переменные ...
```

Сохраните файл: `Ctrl+X` → `Y` → `Enter`

### Шаг 5: Перезапустить n8n

```bash
docker compose restart n8n
```

### Шаг 6: Проверить переменную

```bash
docker exec n8n printenv | grep TELEGRAM
```

**Ожидаемый вывод:**
```
TELEGRAM_ALERT_CHAT_ID=-5004140602
```

---

## 🔍 Проверка в workflow

1. Откройте workflow: https://n8n.rentflow.rentals/workflow/8jkfmWF2dTtnlMHj
2. Найдите ноду **Send Telegram Alert**
3. В поле **Chat ID** должно быть: `{{ $env.TELEGRAM_ALERT_CHAT_ID }}`
4. Запустите workflow (Test workflow)
5. Проверьте, что уведомления приходят в чат **"TG Alert Chat"**

---

## 🐛 Troubleshooting

### Ошибка: "credential with ID 'undefined' could not exist for type 'telegramApi'"

**Причина:** Переменная `TELEGRAM_ALERT_CHAT_ID` не установлена в n8n

**Решение:** Используйте Способ 1 (через n8n UI)

---

### Ошибка: Chat not found

**Причина:** Бот не добавлен в чат или неправильный Chat ID

**Решение:**
1. Убедитесь, что бот `@n8n_alert_geodrive_bot` добавлен в чат "TG Alert Chat"
2. Проверьте права бота (должен иметь возможность отправлять сообщения)
3. Проверьте Chat ID через команду `/getid` в чате (если бот поддерживает)

---

### Ошибка: Переменная не видна в workflow

**Причина:** n8n не перезагрузился после изменения docker-compose.yml

**Решение:**
```bash
docker compose restart n8n
# Подождите 10-15 секунд
docker ps | grep n8n  # Проверьте, что контейнер запущен
```

---

## 📊 Текущий статус

**Workflow:** "Ночной парсинг сотрудников и их касс"  
**ID:** `8jkfmWF2dTtnlMHj`  
**Нода:** "Send Telegram Alert"  
**Использование:** `{{ $env.TELEGRAM_ALERT_CHAT_ID }}`

**Когда срабатывает:**
- При обнаружении расхождений касс сотрудников между RentProg и БД
- Отправляет детальное уведомление с информацией о сотруднике и расхождениях
- Автоматически исправляет кассы в БД

---

## ✅ После установки

1. Запустите workflow вручную для проверки
2. Убедитесь, что уведомления приходят в чат
3. Проверьте формат сообщений
4. Настройте ежедневный запуск (cron: `0 4 * * *` уже настроен)

---

**Документация создана:** 2025-11-08  
**Последнее обновление:** 2025-11-08

