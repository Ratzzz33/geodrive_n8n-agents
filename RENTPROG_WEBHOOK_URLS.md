# RentProg Webhook URLs - Адреса для настройки в RentProg UI

## 📋 Адреса вебхуков для каждого филиала

Используйте эти URL при настройке вебхуков в RentProg для каждого филиала:

### 🏢 Тбилиси (tbilisi)
```
https://geodrive.netlify.app/webhooks/rentprog/tbilisi
```

### 🏢 Батуми (batumi)
```
https://geodrive.netlify.app/webhooks/rentprog/batumi
```

### 🏢 Кутаиси (kutaisi)
```
https://geodrive.netlify.app/webhooks/rentprog/kutaisi
```

### 🏢 Сервисный центр (service-center)
```
https://geodrive.netlify.app/webhooks/rentprog/service-center
```

---

## ⚙️ Как настроить в RentProg UI

1. Войдите в RentProg для каждого филиала
2. Перейдите в **Настройки компании** → **Webhooks** (или **Интеграции**)
3. Добавьте новый webhook:
   - **URL**: используйте соответствующий адрес выше
   - **Метод**: `POST`
   - **Формат**: `JSON`
   - **События**: выберите нужные (обычно все события)

4. Повторите для каждого филиала с соответствующим URL

---

## ✅ Проверка работы

После настройки вебхуков в RentProg:

1. **Проверьте логи Jarvis бота:**
   ```
   [INFO] 🔗 RentProg Webhook URLs (для настройки в RentProg UI):
   [INFO]    ⚠️  RentProg отправляет JSON без секретов, просто укажите URL ниже
   [INFO]   tbilisi: https://geodrive.netlify.app/webhooks/rentprog/tbilisi
   [INFO]   batumi: https://geodrive.netlify.app/webhooks/rentprog/batumi
   [INFO]   kutaisi: https://geodrive.netlify.app/webhooks/rentprog/kutaisi
   [INFO]   service-center: https://geodrive.netlify.app/webhooks/rentprog/service-center
   ```

2. **Проверьте логи Netlify Functions:**
   - Netlify Dashboard → Functions → rentprog-webhook → Logs
   - Должны видеть: `[branch] Webhook received: <event_type>`

3. **Проверьте обработку в основном приложении:**
   - Логи Jarvis бота должны показать обработку события
   - Проверьте БД - должны появиться записи

---

## 🔧 Тест вебхука вручную

Для проверки работы вебхука можно отправить тестовый запрос:

```bash
# Тест для Тбилиси
curl -X POST https://geodrive.netlify.app/webhooks/rentprog/tbilisi \
  -H "Content-Type: application/json" \
  -d '{"id":"test_123","event":"booking.issue.planned","car_id":1,"client_id":1}'

# Ожидаемый ответ:
# {"ok":true,"queued":true,"syncMs":<число>}
```

---

## ⚠️ Важно

1. **RentProg отправляет JSON без секретов** - просто укажите URL, дополнительная валидация не требуется
2. **Все вебхуки на эти адреса считаются от RentProg** - система автоматически их обработает
3. **Убедитесь что:**
   - Основное приложение Jarvis запущено
   - `ORCHESTRATOR_URL` настроен в Netlify Environment Variables
   - Миграция БД применена (`npm run db:push`)

---

## 📝 Полный список для копирования

```
Тбилиси:
https://geodrive.netlify.app/webhooks/rentprog/tbilisi

Батуми:
https://geodrive.netlify.app/webhooks/rentprog/batumi

Кутаиси:
https://geodrive.netlify.app/webhooks/rentprog/kutaisi

Сервисный центр:
https://geodrive.netlify.app/webhooks/rentprog/service-center
```

---

**Готово к использованию!** 🚀

После настройки вебхуки будут автоматически обрабатываться:
1. Netlify Function принимает вебхук → возвращает 200 OK
2. Вызывает основное приложение через `ORCHESTRATOR_URL`
3. Оркестратор: дедупликация → auto-fetch → upsert в БД
4. События логируются и отправляются в n8n

