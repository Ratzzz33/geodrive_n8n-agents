# Change Log: Обновление адресов вебхуков RentProg

**Дата:** 2025-01-15  
**Версия:** 2.0  
**Тип изменения:** Обновление инфраструктуры (Breaking Change для настроек RentProg)

---

## 🔄 Изменения

### Вебхуки RentProg

#### Старая конфигурация (v1.x)
```
Tbilisi:  https://geodrive.netlify.app/webhooks/rentprog/tbilisi
Batumi:   https://geodrive.netlify.app/webhooks/rentprog/batumi
Kutaisi:  https://geodrive.netlify.app/webhooks/rentprog/kutaisi
Service:  https://geodrive.netlify.app/webhooks/rentprog/service-center
```

#### Новая конфигурация (v2.0)
```
Все филиалы: https://webhook.rentflow.rentals
```

---

## 📝 Описание изменений

### 1. Унификация URL
- **Было:** 4 разных URL (по одному на филиал)
- **Стало:** 1 общий URL для всех филиалов
- **Причина:** Упрощение настройки, централизованный мониторинг

### 2. Смена домена
- **Было:** geodrive.netlify.app (Netlify Functions)
- **Стало:** webhook.rentflow.rentals (собственный домен + Nginx)
- **Причина:** Лучшая производительность, контроль, надежность

### 3. Архитектура обработки
- **Было:** RentProg → Netlify Function → HTTP POST к оркестратору
- **Стало:** RentProg → Nginx → n8n → PostgreSQL → Асинхронная обработка
- **Причина:** Быстрый ACK, дедупликация, устойчивость к сбоям

---

## 🚀 Новые возможности

✨ **Дедупликация на уровне БД**
- Unique constraint `(branch, type, ext_id)`
- `ON CONFLICT DO NOTHING` при вставке
- Защита от повторной обработки

✨ **Двухэтапная обработка**
- Быстрое сохранение события (< 100ms)
- Отложенная обработка через cron (5 минут)
- Возможность retry при ошибках

✨ **Улучшенный мониторинг**
- n8n executions для каждого вебхука
- PostgreSQL таблица `events` с историей
- Telegram alerts при ошибках
- Nginx логи для отладки

✨ **SSL из коробки**
- Let's Encrypt сертификат
- Автоматическое обновление
- HTTPS только

---

## 💻 Технические детали

### Инфраструктура

**Сервер:**
- IP: 46.224.17.15
- OS: Ubuntu 20.04+ LTS
- Nginx: 1.18+
- n8n: 1.x
- PostgreSQL: 15+ (Neon)

**DNS:**
```
webhook.rentflow.rentals  A  46.224.17.15
```

**Nginx конфигурация:**
```nginx
server {
    listen 80;
    listen [::]:80;
    server_name webhook.rentflow.rentals;
    
    location / {
        proxy_pass http://localhost:5678/webhook/rentprog-webhook;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**n8n Webhook:**
- Path: `rentprog-webhook`
- Method: POST
- Full URL: http://localhost:5678/webhook/rentprog-webhook

**PostgreSQL Schema:**
```sql
CREATE TABLE events (
  id BIGSERIAL PRIMARY KEY,
  ts TIMESTAMPTZ NOT NULL DEFAULT now(),
  branch TEXT,
  type TEXT,
  ext_id TEXT,
  ok BOOLEAN DEFAULT TRUE,
  reason TEXT,
  processed BOOLEAN DEFAULT FALSE,
  UNIQUE (branch, type, ext_id)
);

CREATE INDEX idx_events_processed 
ON events(processed) 
WHERE processed = FALSE;
```

---

## 🔧 Миграция

### Для администратора RentProg

**Действия (5-10 минут):**

1. Откройте админку RentProg
2. Настройки → Интеграции → Webhooks
3. Для каждого филиала:
   - **Старый URL:** Удалить или деактивировать
   - **Новый URL:** `https://webhook.rentflow.rentals`
   - Метод: `POST`
   - Активен: ✅
4. Сохранить
5. Отправить тестовый вебхук

**Инструкция:** [WEBHOOKS_SETUP_GUIDE.md](./WEBHOOKS_SETUP_GUIDE.md)

### Для разработчиков

**Переменные окружения:**
```env
# Обновить в .env
WEBHOOK_URL=https://webhook.rentflow.rentals/
N8N_WEBHOOK_URL=https://webhook.rentflow.rentals/

# Оставить без изменений
ORCHESTRATOR_URL=http://46.224.17.15:3000
```

**Docker Compose:**
```yaml
environment:
  - WEBHOOK_URL=https://webhook.rentflow.rentals/
  - N8N_WEBHOOK_URL=https://webhook.rentflow.rentals/
```

**Обратная совместимость:**
Тестовый адрес сохранен:
```
https://geodrive.netlify.app/webhook/rentprog-webhook
```

---

## ✅ Тестирование

### Pre-deployment (выполнено)
- [x] DNS настроен и резолвится
- [x] SSL сертификат получен и действителен
- [x] Nginx конфигурация протестирована
- [x] n8n workflow создан и активен
- [x] PostgreSQL таблица создана
- [x] Ручной curl тест успешен

### Post-deployment (требуется)
- [ ] Настройка в RentProg выполнена
- [ ] Тестовый вебхук из RentProg получен
- [ ] n8n executions показывают успешную обработку
- [ ] События появляются в БД
- [ ] Обработка событий работает (processed = true)
- [ ] Telegram alerts работают при ошибках

---

## 📊 Ожидаемые результаты

### Производительность
- Latency вебхука: < 100ms (vs 200-500ms через Netlify)
- Обработка события: < 30 секунд (через cron)
- Пропускная способность: 1000+ событий/минуту

### Надежность
- Дедупликация: 100% (на уровне БД)
- Uptime: 99.9% (Nginx + n8n)
- Retry: автоматический (через cron)

### Мониторинг
- n8n executions: в реальном времени
- PostgreSQL: история за 90 дней
- Telegram alerts: мгновенно при ошибках
- Nginx logs: детальная диагностика

---

## 🐛 Известные проблемы

### Issue #1: Филиал не определяется автоматически
**Описание:** RentProg может не передавать branch в теле вебхука  
**Workaround:** Добавить branch в query параметры в настройках RentProg  
**Пример:** `https://webhook.rentflow.rentals?branch=tbilisi`  
**Статус:** Под наблюдением

---

## 📚 Документация

### Для администраторов RentProg
- [WEBHOOKS_SETUP_GUIDE.md](./WEBHOOKS_SETUP_GUIDE.md) - Краткая инструкция

### Для разработчиков
- [WEBHOOK_URLS_UPDATE.md](./WEBHOOK_URLS_UPDATE.md) - Полное техническое руководство
- [README.md](./README.md) - Обновленная документация проекта

### Для DevOps
- [SESSION_REPORT_2025-01-15.md](./SESSION_REPORT_2025-01-15.md) - Отчет о всей работе
- [docs/DATABASE_MIGRATIONS.md](./docs/DATABASE_MIGRATIONS.md) - Миграции БД
- [claude.md](./claude.md) - Решения для Cursor Agent

---

## 🔐 Безопасность

### SSL/TLS
- Сертификат: Let's Encrypt
- Протокол: TLS 1.2+
- Cipher suites: Modern (рекомендации Mozilla)
- HSTS: Рекомендуется добавить

### Аутентификация
- RentProg → webhook: без токенов (IP whitelist рекомендуется)
- n8n → PostgreSQL: пароль в переменных окружения
- n8n → Jarvis API: localhost (не требует auth)

### Rate Limiting
- Nginx: 10 req/sec на IP (рекомендуется)
- n8n: не ограничено
- PostgreSQL: connection pooling

---

## 📞 Поддержка

### При возникновении проблем:

1. **Проверить статус системы:**
   ```bash
   curl -I https://webhook.rentflow.rentals
   curl http://46.224.17.15:5678/health
   ```

2. **Проверить логи:**
   ```bash
   tail -f /var/log/nginx/webhook-access.log
   tail -f /var/log/nginx/webhook-error.log
   ```

3. **Проверить n8n:**
   - http://46.224.17.15:5678/projects/YeYimRJroeGbDN4w/executions

4. **Проверить БД:**
   ```sql
   SELECT COUNT(*) FROM events 
   WHERE ts > NOW() - INTERVAL '1 hour';
   ```

5. **Связаться с командой:**
   - GitHub Issues
   - Telegram: `/status` в боте

---

## 📅 Версионность

### v2.0 (2025-01-15) - Current
- Новый домен webhook.rentflow.rentals
- Унифицированный URL для всех филиалов
- Двухэтапная обработка через n8n
- Дедупликация на уровне БД

### v1.x (до 2025-01-15) - Deprecated
- Netlify Functions
- Отдельные URL для филиалов
- Прямая обработка в оркестраторе

---

## 🎯 Roadmap

### Q1 2025
- [ ] Добавить IP whitelist для RentProg
- [ ] Настроить HSTS в Nginx
- [ ] Добавить rate limiting
- [ ] Настроить мониторинг (Grafana + Prometheus)

### Q2 2025
- [ ] Миграция на единую систему вебхуков для всех интеграций
- [ ] API для ручной повторной обработки событий
- [ ] Dashboard для мониторинга вебхуков

---

**Версия:** 2.0  
**Дата релиза:** 2025-01-15  
**Статус:** ✅ Готово к использованию (требуется настройка в RentProg)  
**Breaking Changes:** Да (изменение URL вебхуков)  
**Обратная совместимость:** Тестовый адрес сохранен

