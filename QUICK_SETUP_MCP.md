# Быстрая настройка MCP Chrome DevTools

## ✅ Что уже сделано

1. Node.js установлен (v24.11.0)
2. Зависимости установлены (`mcp-server/node_modules`)
3. MCP сервер готов к работе

## 📋 Настройка в Cursor

### Шаг 1: Откройте настройки MCP в Cursor

1. Нажмите `Ctrl+,` (или `Cmd+,` на Mac)
2. Перейдите в раздел **"Features"** → **"Model Context Protocol"**
3. Найдите секцию **"MCP Servers"** или **"Servers"**

### Шаг 2: Добавьте конфигурацию

Скопируйте и вставьте следующую конфигурацию:

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "C:\\Program Files\\nodejs\\node.exe",
      "args": ["mcp-server/chrome-devtools-server.js"],
      "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents"
    }
  }
}
```

**Или**, если Node.js уже добавлен в PATH (после перезапуска Cursor), используйте упрощенную версию:

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "node",
      "args": ["mcp-server/chrome-devtools-server.js"],
      "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents"
    }
  }
}
```

### Шаг 3: Перезапустите Cursor

**Важно:** Полностью закройте и снова откройте Cursor, чтобы изменения вступили в силу.

## ✅ Проверка работы

После перезапуска Cursor:

1. Откройте чат с AI
2. Попробуйте выполнить запрос, например:
   ```
   Открой страницу https://google.com и сделай скриншот
   ```

Если MCP сервер работает, AI сможет использовать инструменты для управления браузером.

## 🔧 Troubleshooting

### Ошибка "node is not recognized"

**Решение:** Используйте конфигурацию с полным путем (см. Шаг 2 выше).

### MCP сервер не подключается

1. Проверьте, что файл `mcp-server/chrome-devtools-server.js` существует
2. Проверьте, что путь `cwd` правильный (абсолютный путь)
3. Перезапустите Cursor полностью

### Puppeteer ошибки при запуске

Puppeteer требует системные зависимости. На Windows обычно работает из коробки, но если есть проблемы:
1. Установите [Google Chrome](https://www.google.com/chrome/)
2. Перезапустите Cursor

## 📚 Дополнительная документация

- [MCP_CHROME_DEVTOOLS_SETUP.md](./MCP_CHROME_DEVTOOLS_SETUP.md) - Полная документация
- [INSTALL_NODEJS.md](./INSTALL_NODEJS.md) - Инструкции по установке Node.js
- [mcp-server/README.md](./mcp-server/README.md) - Документация MCP сервера

