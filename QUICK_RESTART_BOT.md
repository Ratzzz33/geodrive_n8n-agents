# Быстрый перезапуск бота

## Способ 1: В Git Bash (скопируйте и вставьте)

Откройте Git Bash и выполните эти команды:

```bash
ssh root@46.224.17.15
# Введите пароль: enebit7Lschwrkb93vnm

# После подключения выполните:
cd /root/geodrive_n8n-agents
git pull
pkill -f "tsx.*index.ts" || pkill -f "node.*dist/index.js" || true
sleep 2
npm run dev > /root/bot.log 2>&1 &
sleep 3
tail -n 20 /root/bot.log
```

## Способ 2: Автоматический через скрипт

Если у вас установлен sshpass (для Linux) или plink (для Windows):

```bash
bash setup/restart-bot-final.sh
```

## Что должно быть в логах:

После выполнения должны увидеть:
```
✅ Webhook удален, переходим на polling режим
🤖 Bot started (polling mode)
📱 Bot @test_geodrive_check_bot connected (ID: ...)
```

## Проверка работы:

Отправьте боту в Telegram команду `/start` или `/status` - он должен ответить.

