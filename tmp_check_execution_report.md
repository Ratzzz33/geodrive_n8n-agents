# Отчет по выполнению workflow #17658

## Статус выполнения
- ✅ **Успешно завершено** (status: "success")
- ⏱️ **Длительность:** 14 секунд
- 📊 **Всего items:** 24
- 🔢 **Обработано нод:** 14 из 14

## Проблемы обнаружены

### 1. ⚠️ КРИТИЧНО: NULL значения затирают существующие данные

**Проблема:** В нодах "Save Snapshot" и "Save Cars" используется обычный `upsert` без COALESCE. Это означает, что если поле приходит как NULL, оно **затирает существующее значение** в БД.

**Пример из выполнения:**
- В Save Snapshot приходят NULL значения для: `deposit`, `price_hour`, `hourly_deposit`, `monthly_deposit`, `investor_id`, `purchase_price`, `purchase_date`, `age_limit`, `driver_year_limit`
- Эти NULL значения **затирают** существующие данные в БД при upsert

**Решение:** Нужно использовать `executeQuery` с COALESCE вместо `upsert`, чтобы NULL значения не затирали существующие данные.

### 2. ⚠️ Обработана только одна машина

**Проблема:** В выполнении обработана только одна машина (Volkswagen Tiguan, rentprog_id: 50169) из филиала Kutaisi.

**Причина:** В "Wrap Kutaisi Cars" приходит объект `cars` (один объект), а не массив машин. Это означает, что:
- Либо API вернул только одну машину
- Либо Normalize Cars неправильно обрабатывает данные (ожидает массив, но получает объект)

**Нужно проверить:**
- Что приходит из API для всех филиалов
- Как Normalize Cars обрабатывает данные (должен обрабатывать и объект, и массив)

### 3. ✅ Поля цен удаляются правильно

**Проверено:** Code-нода "Remove Price Values" правильно удаляет поля `price_values`, `prices`, `seasons`, `price_periods` из данных перед сохранением в таблицу `cars`.

## Данные по нодам

### Normalize Cars
- ✅ **Статус:** success
- 📥 **Вход:** 1 item
- 📤 **Выход:** 1 item
- 📋 **Данные:** Обработана 1 машина (Volkswagen Tiguan, rentprog_id: 50169)

**Поля в данных:**
- ✅ Основные поля присутствуют: `car_name`, `code`, `number`, `vin`, `color`, `year`, `transmission`, `fuel`, `car_type`, `car_class`
- ✅ Состояния: `state`, `tank_state`, `clean_state`, `mileage`
- ✅ Технические детали: `tire_type`, `tire_size`, `engine_capacity`, `number_doors`, `tank_value`
- ⚠️ NULL значения: `last_inspection`, `deposit`, `price_hour`, `hourly_deposit`, `monthly_deposit`, `investor_id`, `purchase_price`, `purchase_date`, `age_limit`, `driver_year_limit`

### Save Snapshot
- ✅ **Статус:** success
- 📥 **Вход:** 1 item
- 📤 **Выход:** 1 item
- ⚠️ **Проблема:** Много NULL значений, которые могут затереть существующие данные

### Remove Price Values
- ✅ **Статус:** success
- 📥 **Вход:** 1 item
- 📤 **Выход:** 1 item
- ✅ **Проверено:** Поля `price_values`, `prices`, `seasons`, `price_periods` удалены

### Save Cars
- ✅ **Статус:** success
- 📥 **Вход:** 1 item
- 📤 **Выход:** 1 item
- ⚠️ **Проблема:** NULL значения могут затереть существующие данные

## Рекомендации

1. **КРИТИЧНО:** Переключить "Save Snapshot" и "Save Cars" на `executeQuery` с COALESCE, чтобы NULL значения не затирали существующие данные
2. Проверить, почему обработана только одна машина - возможно, проблема в Normalize Cars или в API
3. Проверить в БД, не затерлись ли существующие данные NULL значениями

