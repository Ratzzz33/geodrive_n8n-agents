# Инструкция по запуску Jarvis API

## Проблема
Jarvis API не запускается автоматически через скрипты из-за особенностей PowerShell.

## Решение

### Вариант 1: Запуск вручную (рекомендуется)

Откройте **новое окно терминала** и выполните:

```bash
cd C:\Users\33pok\geodrive_n8n-agents
npm start
```

Оставьте это окно открытым - API будет работать в нем.

### Вариант 2: Запуск через отдельное окно PowerShell

```powershell
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd C:\Users\33pok\geodrive_n8n-agents; npm start"
```

### Вариант 3: Запуск через cmd

```cmd
cd C:\Users\33pok\geodrive_n8n-agents
npm start
```

---

## Проверка запуска

После запуска подождите 10-15 секунд и проверьте:

```bash
node setup/check_system_status.mjs
```

Или вручную:

```bash
curl http://localhost:3000/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"2025-11-11T..."}
```

---

## Что должно быть в логах

После успешного запуска в консоли должно появиться:

```
🚀 Starting Jarvis Bot...
✅ Umnico Realtime Sync started
✅ Jarvis Bot is running
🌐 API server listening on port 3000 (0.0.0.0)
```

---

## Если API не запускается

1. Проверьте что проект собран:
   ```bash
   npm run build
   ```

2. Проверьте что файл существует:
   ```bash
   Test-Path dist/index.js
   ```

3. Проверьте переменные окружения:
   ```bash
   node setup/check_umnico_env.mjs
   ```

4. Проверьте логи на ошибки

