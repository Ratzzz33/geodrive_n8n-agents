# Бэкфилл исторических данных

Эти скрипты применяют автоматическую обработку к **уже существующим данным** в базе данных.

## 📋 Что обрабатывается

### 1. Event Links Backfill (`backfill_event_links.mjs`)

**Назначение:** Связывает старые платежи с событиями из `events` и `history` таблиц.

**Проблема:** 
- Автоматическое связывание работает только для НОВЫХ платежей (после реализации системы)
- **100+ старых платежей** остались несвязанными

**Что делает скрипт:**
1. Находит все несвязанные платежи через view `unlinked_records`
2. Для каждого платежа ищет совпадающие события в `events` и `history`:
   - По RentProg ID (count_id)
   - В временном окне ±5 минут
3. Создает связи в таблице `event_links` с уровнем уверенности (high/medium/low)
4. Показывает статистику обработки

**Результат:**
- Все исторические платежи связаны с соответствующими событиями
- Можно отслеживать полный контекст операций

---

### 2. Entity Timeline Backfill (`backfill_entity_timeline.mjs`)

**Назначение:** Добавляет старые платежи в денормализованный лог `entity_timeline`.

**Проблема:**
- В timeline записываются только новые события (после реализации)
- Старые платежи не видны в хронологии

**Что делает скрипт:**
1. Находит все платежи из `payments`, которых нет в `entity_timeline`
2. Для каждого платежа:
   - Формирует `summary` (краткое описание)
   - Заполняет `details` (детали платежа)
   - Находит связанные сущности (booking, car, client)
   - Определяет филиал и исполнителя
3. Записывает события в `entity_timeline`
4. Показывает статистику по источникам и типам событий

**Результат:**
- Полная хронология всех платежей доступна через timeline API
- Оркестратор и агенты видят полную историю

---

## 🚀 Запуск

### Предварительные требования

```bash
# Убедитесь что установлен postgres драйвер
npm install postgres
```

### Запуск Event Links Backfill

```bash
# Из корня проекта
node setup/backfill_event_links.mjs
```

**Пример вывода:**
```
🔗 Event Links Backfill - Связывание старых данных

📊 Получаем несвязанные платежи...

Найдено несвязанных платежей: 100

🔄 Начинаем связывание...

  Связываем платеж abc-123 (branch: tbilisi, rp_id: 470049, date: 2024-11-01)
    ✓ Связь с event 1234 (high, 45s)
    ✓ Связь с history 5678 (medium, 120s)
  ✅ Создано связей: 2

--- Обработано: 10/100 ---

============================================================
📈 ИТОГИ БЭКФИЛЛА
============================================================
Обработано платежей: 100
Создано связей: 187
Ошибок: 0
Средн. связей на платеж: 1.87

Несвязанных платежей осталось: 23

⚠️  Некоторые платежи остались несвязанными (нет совпадающих events/history)
```

**Примечание:** Некоторые платежи могут остаться несвязанными, если для них нет соответствующих событий в `events` или `history` (например, очень старые записи).

---

### Запуск Entity Timeline Backfill

```bash
# Из корня проекта
node setup/backfill_entity_timeline.mjs
```

**Пример вывода:**
```
📝 Entity Timeline Backfill - Добавление старых платежей

Платежей уже в timeline: 8
Всего платежей в БД: 250
Платежей для добавления: 242

🔄 Начинаем добавление...

  ✓ abc-123 - 500.00 GEL
  ✓ def-456 - 1200.00 USD
  ✓ ghi-789 - 350.50 GEL

--- Обработано: 20/242 ---

============================================================
📈 ИТОГИ БЭКФИЛЛА
============================================================
Обработано платежей: 242
Добавлено в timeline: 242
Ошибок: 0

Платежей в timeline: 250 (было 8)
Добавлено: 242

📊 События в timeline по источникам:
  rentprog_history: 242
  starline: 8

📊 События в timeline по сущностям:
  payment: 250

🎉 Бэкфилл завершен успешно!
```

---

## 📊 Проверка результатов

### После Event Links Backfill

```bash
# Проверить статистику связей
curl http://46.224.17.15:3000/event-links/stats

# Проверить оставшиеся несвязанные записи
curl http://46.224.17.15:3000/event-links/unlinked
```

### После Entity Timeline Backfill

```bash
# Проверить статистику timeline
curl http://46.224.17.15:3000/entity-timeline/stats

# Получить timeline для конкретного платежа
curl http://46.224.17.15:3000/entity-timeline/entity/payment/{payment_id}
```

---

## ⚠️ Важные замечания

### Безопасность
- ✅ Скрипты используют `ON CONFLICT DO NOTHING` - безопасно запускать многократно
- ✅ Не изменяют существующие данные
- ✅ Только добавляют новые связи/записи

### Производительность
- ⏱️ Event Links: ~0.5-1 сек на платеж (поиск + вставка)
- ⏱️ Timeline: ~0.1-0.2 сек на платеж (вставка + поиск связей)
- 📊 Для 100 платежей: 1-2 минуты общее время

### Идемпотентность
- ✅ Можно запускать несколько раз
- ✅ Дубликаты не создаются (unique constraints)
- ✅ Безопасно прерывать и перезапускать

---

## 🔄 Когда запускать

### Event Links Backfill
- **Сейчас:** Один раз для обработки всех старых платежей
- **В будущем:** Не требуется (автоматическое связывание работает для новых записей)

### Entity Timeline Backfill
- **Сейчас:** Один раз для добавления всех старых платежей
- **В будущем:** Не требуется (timeline заполняется автоматически)

---

## 🐛 Troubleshooting

### Ошибка подключения к БД
```bash
# Проверить connection string
echo $DATABASE_URL
```

### Таймаут при большом объеме данных
- Увеличить `max: 1` → `max: 5` в конфиге `postgres()`
- Добавить батчинг (обработка по 50 записей)

### Нет совпадающих events/history
- Это нормально для старых данных
- События могли быть удалены или еще не синхронизированы
- Такие платежи останутся в `unlinked_records`

---

## 📖 См. также

- [EVENT_LINKS.md](../docs/EVENT_LINKS.md) - Документация по системе связывания
- [ENTITY_TIMELINE.md](../docs/ENTITY_TIMELINE.md) - Документация по timeline
- [EVENT_LINKS_IMPLEMENTATION.md](../EVENT_LINKS_IMPLEMENTATION.md) - Детали реализации

