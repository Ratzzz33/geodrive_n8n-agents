# Анализ цветовых индикаторов в интерфейсе RentProg

## 🎨 Цветовая система RentProg

### 1. **ОПЛАТА (колонка "Оплата")**

#### Цвета и их значения:

| Текст | Цвет | Значение | Класс CSS |
|-------|------|----------|-----------|
| **"Оплачено"** | Серый (disabled) | Полностью оплачено, нет действий | `v-btn--disabled` |
| **"Не оплачено"** | Серый (outlined) | Не оплачено, требуется оплата | `v-btn--outlined` |
| **"+ X"** (положительное число) | 🟡 **Warning (желтый/оранжевый)** | Переплата - клиент заплатил больше, чем нужно | `warning` |
| **"- X"** (отрицательное число) | 🔴 **Error (красный)** | Недоплата - клиент должен доплатить | `error` |
| **"0"** | 🔴 **Error (красный)** | Баланс равен нулю, но есть проблемы | `error` |

**Логика:**
- ✅ **Зеленый/Success** - НЕ используется для оплаты
- 🟡 **Желтый/Warning** - Переплата (клиент заплатил больше)
- 🔴 **Красный/Error** - Недоплата или проблемы с оплатой
- ⚪ **Серый/Disabled** - Оплачено (нет действий)
- ⚪ **Серый/Outlined** - Не оплачено (требуется действие)

**Примеры:**
- `+ 60` → Warning (желтый) - переплата 60
- `- 50` → Error (красный) - недоплата 50
- `0` → Error (красный) - баланс 0, но есть проблемы
- `Оплачено` → Disabled (серый) - все оплачено
- `Не оплачено` → Outlined (серый) - требуется оплата

---

### 2. **СТАТУС КЛИЕНТА (колонка "Кл сост")**

#### Иконки и цвета:

| Иконка | Цвет текста | Значение | Класс CSS |
|--------|-------------|----------|-----------|
| **mdi-new-box** | Без цвета | Новый клиент (первая бронь) | - |
| **mdi-alpha-a-circle-outline** | 🔵 **Light-blue** | Клиент категории A (надежный, проверенный) | `light-blue--text` |
| **mdi-account-remove-outline** | Без цвета | Клиент отклонен/заблокирован | - |
| **mdi-cancel** | Без цвета | Бронь отменена | - |
| **mdi-garage-variant** | 🟢 **Green** | Автомобиль в гараже/сервисе | `green` |
| **mdi-head-question-outline** | Без цвета | Требуется проверка клиента | - |

**Логика:**
- 🔵 **Light-blue** - надежный клиент (категория A)
- 🟢 **Green** - автомобиль в сервисе/гараже
- ⚪ **Без цвета** - стандартный статус (новый, отклонен, отменен)

---

### 3. **СТАТУС БРОНИ (колонка "Сост." - только в активных)**

#### Иконки и цвета:

| Иконка | Цвет | Значение | Класс CSS |
|--------|------|----------|-----------|
| **mdi-car** | 🔵 **Primary (синий)** | Активная бронь (автомобиль в аренде) | `primary--text` |
| **mdi-clock** | ⚪ **Grey** | Ожидание (бронь запланирована) | - |

**Логика:**
- 🔵 **Primary (синий)** - активная бронь, автомобиль сейчас в аренде
- ⚪ **Grey** - запланированная бронь, еще не началась

---

### 4. **ОТВЕТСТВЕННЫЙ (колонка "Отв.")**

#### Иконки и цвета:

| Иконка | Цвет | Значение | Класс CSS |
|--------|------|----------|-----------|
| **mdi-account-check** | 🟢 **Success (зеленый)** | Ответственный назначен | `success--text` |
| **mdi-account-question** | ⚪ **Secondary (серый)** | Ответственный не назначен | `secondary--text` |

**Логика:**
- 🟢 **Success (зеленый)** - ответственный сотрудник назначен
- ⚪ **Secondary (серый)** - ответственный не назначен, требуется назначение

---

## 📊 Сводная таблица цветов

| Цвет | Использование | Значение |
|------|---------------|----------|
| 🟢 **Success (зеленый)** | Ответственный назначен | Все хорошо, действие выполнено |
| 🔴 **Error (красный)** | Недоплата, баланс 0 | Проблема, требуется действие |
| 🟡 **Warning (желтый/оранжевый)** | Переплата | Внимание, но не критично |
| 🔵 **Primary (синий)** | Активная бронь | Текущее состояние |
| 🔵 **Light-blue (светло-синий)** | Клиент категории A | Надежный клиент |
| ⚪ **Secondary/Grey (серый)** | Не назначен, не оплачено | Нейтральное состояние, требуется действие |
| ⚪ **Disabled (серый)** | Оплачено | Действие завершено, нет необходимости в действиях |

---

## 🎯 Практическое применение

### Для парсинга и сохранения в БД:

1. **Поле `payment_status`:**
   - `paid` - если "Оплачено" (disabled)
   - `unpaid` - если "Не оплачено" (outlined)
   - `overpaid` - если "+ X" (warning)
   - `underpaid` - если "- X" или "0" (error)
   - Сохранять также числовое значение в `payment_balance`

2. **Поле `client_category`:**
   - `new` - если `mdi-new-box`
   - `A` - если `mdi-alpha-a-circle-outline` (light-blue)
   - `blocked` - если `mdi-account-remove-outline`
   - `cancelled` - если `mdi-cancel`

3. **Поле `booking_status`:**
   - `active` - если `mdi-car` (primary)
   - `planned` - если `mdi-clock` (grey)

4. **Поле `responsible_status`:**
   - `assigned` - если `mdi-account-check` (success)
   - `not_assigned` - если `mdi-account-question` (secondary)

---

## 💾 Рекомендации для БД

### Новые поля для сохранения:

1. **`payment_status`** (TEXT) - статус оплаты: `paid`, `unpaid`, `overpaid`, `underpaid`
2. **`payment_balance`** (NUMERIC) - баланс оплаты (положительное/отрицательное число)
3. **`client_category`** (TEXT) - категория клиента: `new`, `A`, `blocked`, `cancelled`
4. **`booking_status_icon`** (TEXT) - иконка статуса: `car`, `clock`
5. **`responsible_status`** (TEXT) - статус ответственного: `assigned`, `not_assigned`

### Сохранение в `data` JSONB:

```json
{
  "payment": {
    "status": "overpaid",
    "balance": 60,
    "color": "warning"
  },
  "client": {
    "category": "A",
    "icon": "alpha-a-circle-outline",
    "color": "light-blue"
  },
  "booking": {
    "status_icon": "car",
    "color": "primary"
  },
  "responsible": {
    "status": "assigned",
    "icon": "account-check",
    "color": "success"
  }
}
```

---

## ✅ ИТОГ: Новые поля для парсинга

1. **`payment_status`** - статус оплаты (paid/unpaid/overpaid/underpaid)
2. **`payment_balance`** - баланс оплаты (число)
3. **`payment_color`** - цвет индикатора оплаты (success/error/warning/disabled/outlined)
4. **`client_category`** - категория клиента (new/A/blocked/cancelled)
5. **`client_status_color`** - цвет статуса клиента (light-blue/green)
6. **`booking_status_icon`** - иконка статуса брони (car/clock)
7. **`booking_status_color`** - цвет статуса брони (primary/grey)
8. **`responsible_status`** - статус ответственного (assigned/not_assigned)
9. **`responsible_color`** - цвет индикатора ответственного (success/secondary)

Все эти данные можно сохранять в `data` JSONB или создать отдельные поля в таблице `bookings`.

