# Анализ полей страницы броней RentProg

## Поля из таблицы активных броней (АКТИВНЫЕ БРОНИ)

На основе анализа интерфейса страницы `https://web.rentprog.ru/bookings`:

### Колонки таблицы:

1. **№ (Number)** - номер брони
   - В БД: `number` ✅
   - Тип: TEXT

2. **Создана (Created)** - дата и время создания брони
   - В БД: `created_at` ✅ (TIMESTAMPTZ)
   - Формат на странице: "09 нояб. 13:33" (русская дата)

3. **Кл сост (Client Status)** - статус клиента
   - В БД: `client_category` ✅ (возможно)
   - Может содержать: "NEW", иконки (синий круг, красный круг с диагональю)
   - Это HTML элемент с иконками, не просто текст

4. **Оплата (Payment)** - статус оплаты
   - В БД: `paid`, `aggr_paid`, `payment_job_count` ✅
   - Может быть: "НЕ ОПЛАЧЕНО", числовые значения (+30, 0, -68)
   - Формат: TEXT или NUMERIC

5. **Название (Name)** - название/модель автомобиля
   - В БД: `car_name` ✅
   - Формат: "Volkswagen Tiguan (VW Tiguan 713 4x4)", "Hyundai Tucson (Hyundai Tucson 325)"
   - Содержит модель и внутренний код

6. **Клиент (Client)** - имя клиента
   - В БД: `client_id` (FK), но нет прямого поля имени
   - На странице: "Martsinovich Y.", "Namchavadze T.", "Неизвестный С.", "Сотрудник С."
   - Это ссылка на клиента

7. **Начало (Start)** - дата и время начала брони
   - В БД: `start_at` ✅ (TIMESTAMPTZ), `start_date` ✅ (TEXT)
   - Формат на странице: "09 нояб. 14:30" (русская дата)

8. **Конец (End)** - дата и время окончания брони
   - В БД: `end_at` ✅ (TIMESTAMPTZ), `end_date` ✅ (TEXT)
   - Формат на странице: "12 нояб. 14:30" (русская дата)

9. **Суток (Days)** - количество дней бронирования
   - В БД: `days` ✅
   - Тип: INTEGER или TEXT

10. **Выдача (Issue)** - место выдачи автомобиля
    - В БД: `issue_location` ✅, `location_start` ✅
    - Примеры: "Batumi, 6010, 90 Lermontov Str, Batumi 6004", "Tbilisi Office, 1 Merab Aleksidze St, Tbilisi"
    - Может содержать адрес и код филиала

11. **Приём (Reception/Return)** - место приема автомобиля
    - В БД: `return_location` ✅, `location_end` ✅
    - Примеры: "Batumi, 6010, 90 Lermontov Str, Batumi 6004", "Tbilisi, Merab Aleksidze str., 1"

12. **Примечание (Note)** - дополнительные заметки
    - В БД: `notes` ✅, `description` ✅
    - Примеры: "111", "перегон для Алексея, что...", "ДЛЯ СТРАХОВОЙ ФОТО И...", "Техническая бронь по шт..."

13. **Отв. (Responsible)** - ответственный сотрудник
    - В БД: `responsible` ✅, `responsible_id` ✅
    - Примеры: "Eliseev Aleksei Jr", "Lukas Chila", иконка вопроса

### Дополнительные данные (не в таблице, но доступны):

- **Ссылка на детальную страницу брони** - `detail_url`
  - Формат: `https://web.rentprog.ru/bookings/{booking_id}`
  - В БД: можно извлечь из `external_refs` или сохранить в `data` JSONB

- **Booking ID** - ID брони в RentProg
  - Извлекается из URL: `/bookings/{id}`
  - В БД: `external_refs.external_id` ✅

---

## Поля из таблицы неактивных броней (НЕАКТИВНЫЕ БРОНИ)

Дополнительные колонки:

1. **Сост. (Status Icon)** - иконка статуса брони
   - В БД: `status` ✅, `state` ✅
   - Может быть: иконка машины (активная), иконка часов (ожидание)
   - Это HTML элемент, не просто текст

2. **Кл сост (Client Status)** - статус клиента (та же колонка, но позиция другая)
   - В БД: `client_category` ✅
   - Может содержать: 'A' в круге, 'NEW' в боксе

---

## ❌ НОВЫЕ/НЕИЗВЕСТНЫЕ ПОЛЯ (требуют уточнения):

### 1. **client_status_html** - HTML содержимое статуса клиента
   - Содержит иконки и теги
   - Нет прямого соответствия в БД
   - Можно сохранить в `data` JSONB

### 2. **status_icon_html** - HTML иконки статуса (для неактивных)
   - Нет прямого соответствия в БД
   - Можно сохранить в `data` JSONB

### 3. **row_html** - HTML строки таблицы (для отладки)
   - Временное поле для анализа структуры
   - Не нужно сохранять в БД

### 4. **detail_url** - URL детальной страницы брони
   - Можно сохранить в `data` JSONB или создать отдельное поле
   - Полезно для последующего парсинга детальной страницы

---

## 🔍 ПОЛЯ, КОТОРЫЕ НУЖНО ПАРСИТЬ С ДЕТАЛЬНОЙ СТРАНИЦЫ БРОНИ:

Когда находим новую бронь, нужно открыть её детальную страницу и получить:

1. **Полная информация о клиенте** (если не найдено по имени)
2. **Полная информация об автомобиле** (VIN, номер, пробег)
3. **Финансовые данные** (стоимость, депозит, оплачено)
4. **Пробег** (начальный, конечный, лимит)
5. **Топливо** (начальный уровень, конечный уровень)
6. **Дополнительное оборудование** (детские кресла, навигатор, и т.д.)
7. **Страховки** (КАСКО, ОСАГО, супер-КАСКО)
8. **Фото и документы** (если доступны)
9. **История изменений** (если доступна)

---

## 📝 РЕКОМЕНДАЦИИ:

1. **Сохранять HTML статусов в `data` JSONB** для последующего анализа
2. **Использовать `external_refs`** для связи с RentProg ID
3. **Парсить детальные страницы** только для новых броней
4. **Сохранять `detail_url`** в `data` для быстрого доступа
5. **Нормализовать даты** из русского формата в TIMESTAMPTZ

---

## 🔄 СРАВНЕНИЕ С БД:

**Поля, которые ЕСТЬ в БД:**
- ✅ number, created_at, start_at, end_at, start_date, end_date
- ✅ days, issue_location, return_location, location_start, location_end
- ✅ notes, description, responsible, responsible_id
- ✅ car_name, client_id, status, state
- ✅ paid, aggr_paid, payment_job_count
- ✅ client_category

**Поля, которых НЕТ в БД (или требуют уточнения):**
- ❓ client_status_html (HTML статуса клиента)
- ❓ status_icon_html (HTML иконки статуса)
- ❓ detail_url (URL детальной страницы)
- ❓ booking_id (ID из URL, но есть в external_refs)

**Поля, которые нужно ПАРСИТЬ с детальной страницы:**
- Все финансовые данные
- Пробег и топливо
- Дополнительное оборудование
- Страховки
- Фото и документы

