# Скрипты для Telegram Alerts

**Каталог всех скриптов, созданных для реализации и управления Telegram алертами**

---

## 📋 Категории

- [Основные скрипты](#основные-скрипты)
- [Диагностика](#диагностика)
- [Обработка данных](#обработка-данных)
- [Исправление багов](#исправление-багов)

---

## Основные скрипты

### `add_telegram_alerts_to_processors.mjs`

**Назначение:** Добавление Telegram нод во все 4 processor workflows

**Что делает:**
- Добавляет ноду "Format Telegram Alert" (Code node)
- Добавляет ноду "Send Telegram Alert" (HTTP Request node)
- Создаёт connections от Insert Fetched к Format Alert

**Использование:**
```bash
node setup/add_telegram_alerts_to_processors.mjs
```

**Результат:**
- Tbilisi: ✅ Nodes добавлены
- Batumi: ✅ Nodes добавлены
- Kutaisi: ✅ Nodes добавлены
- Service Center: ✅ Nodes добавлены

---

### `fix_telegram_nodes_use_credential.mjs`

**Назначение:** Замена HTTP Request на стандартную Telegram ноду

**Что делает:**
- Находит HTTP Request ноду "Send Telegram Alert"
- Заменяет на стандартную Telegram ноду
- Использует credential "Telegram account"

**Использование:**
```bash
node setup/fix_telegram_nodes_use_credential.mjs
```

**Причина создания:**  
HTTP Request требовал `$env.TELEGRAM_BOT_TOKEN`, но проще использовать готовый credential из n8n UI.

---

### `fix_alert_connection_point.mjs`

**Назначение:** Исправление точки подключения алертов

**Что делает:**
- Отключает Format Alert от Insert Fetched
- Подключает Format Alert к Parse Webhook
- Это даёт доступ к оригинальным данным с `[old, new]` массивами

**Использование:**
```bash
node setup/fix_alert_connection_point.mjs
```

**Важно:**  
Это критическое исправление! Без него алерты не показывают старые значения.

---

### `fix_alert_format_code.mjs`

**Назначение:** Обновление кода форматирования сообщений

**Что делает:**
- Обновляет JavaScript код в ноде "Format Telegram Alert"
- Парсит `payload` правильно
- Убирает лишние поля (eventhash, eventname)
- Показывает только реальные изменения

**Использование:**
```bash
node setup/fix_alert_format_code.mjs
```

**Когда использовать:**
- После изменения формата уведомлений
- Для исправления багов в парсинге

---

### `add_telegram_chat_id.py`

**Назначение:** Добавление переменной `TELEGRAM_ALERT_CHAT_ID` на production сервер

**Что делает:**
- Подключается к серверу через SSH
- Добавляет переменную в `docker-compose.yml`
- Перезапускает n8n контейнер
- Проверяет результат

**Использование:**
```bash
python setup/add_telegram_chat_id.py
```

**Результат:**
```
TELEGRAM_ALERT_CHAT_ID = -5004140602
✅ Переменная настроена в контейнере
```

---

## Диагностика

### `list_processor_workflows.mjs`

**Назначение:** Список всех processor workflows

**Что показывает:**
- ID workflow
- Название
- Статус (active/inactive)
- URL для открытия в n8n

**Использование:**
```bash
node setup/list_processor_workflows.mjs
```

**Вывод:**
```
BATUMI:
  ID: YsBma7qYsdsDykTq
  Name: Batumi Processor Rentprog
  Active: true
  URL: https://n8n.rentflow.rentals/workflow/...
```

---

### `find_kutaisi_workflow.mjs`

**Назначение:** Поиск и проверка workflow Kutaisi

**Что проверяет:**
- Наличие workflow
- Текущий токен в Get RentProg Token node
- Правильность токена

**Использование:**
```bash
node setup/find_kutaisi_workflow.mjs
```

**Вывод:**
```
✅ Найден: Kutaisi Processor Rentprog
   ID: gJPvJwGQSi8455s9
   
✅ Токен ПРАВИЛЬНЫЙ!
```

---

### `get_telegram_credential_id.mjs`

**Назначение:** Получение ID Telegram credential из n8n

**Что делает:**
- Получает список всех credentials
- Ищет Telegram credential
- Показывает ID для использования в скриптах

**Использование:**
```bash
node setup/get_telegram_credential_id.mjs
```

**Примечание:**  
Может не работать если n8n API ограничивает доступ к credentials (405 error).

---

### `check_processing_progress.mjs`

**Назначение:** Проверка прогресса обработки броней

**Что показывает:**
- Всего броней
- Обработано (data = {})
- Осталось обработать
- Примеры обработанных и необработанных

**Использование:**
```bash
node setup/check_processing_progress.mjs
```

**Вывод:**
```
📦 Всего броней: 3449
✅ Обработано: 3449 (100.0%)
⏳ Осталось: 0
```

---

## Обработка данных

### `fix_trigger_on_conflict.mjs`

**Назначение:** Исправление триггера БД для обработки nested данных

**Что делает:**
- Обновляет функцию `process_booking_nested_entities`
- Добавляет `ON CONFLICT (system, external_id) DO NOTHING`
- Парсит JSONB строки в объекты
- Очищает `data` до `{}` после обработки

**Использование:**
```bash
node setup/fix_trigger_on_conflict.mjs
```

**Важно:**  
Критический скрипт для автоматической обработки вложенных данных!

---

### `fix_jsonb_strings_to_objects.mjs`

**Назначение:** Конвертация JSONB строк в объекты

**Что делает:**
- Находит все брони где `data` - JSONB строка
- Конвертирует в JSONB объект: `(data#>>'{}')::JSONB`
- Временно отключает триггер для избежания ошибок

**Использование:**
```bash
node setup/fix_jsonb_strings_to_objects.mjs
```

**Когда использовать:**
- После импорта броней через `import_active_bookings.mjs`
- Если видите `data` в формате `"{\"id\":...}"`

---

### `process_all_bookings_with_data.mjs`

**Назначение:** Обработка всех существующих броней с populated `data`

**Что делает:**
- Находит брони где `data != '{}'`
- Вызывает `UPDATE` для активации триггера
- Триггер парсит данные и очищает `data`

**Использование:**
```bash
node setup/process_all_bookings_with_data.mjs
```

**Время выполнения:**  
~15 минут для 3449 броней (~230 броней/минуту)

---

### `force_process_remaining.mjs`

**Назначение:** Принудительная обработка оставшихся необработанных броней

**Что делает:**
- То же что `process_all_bookings_with_data.mjs`
- Но с более детальным логированием ошибок
- Показывает финальную статистику

**Использование:**
```bash
node setup/force_process_remaining.mjs
```

**Когда использовать:**
- После исправления триггера
- Для обработки оставшихся записей

---

### `import_active_bookings.mjs`

**Назначение:** Импорт активных броней из RentProg API

**Что делает:**
- Запрашивает активные брони от RentProg
- Использует `dynamic_upsert_entity` для сохранения
- Распаковывает car и client данные
- Обновляет foreign keys в bookings

**Использование:**
```bash
node setup/import_active_bookings.mjs
```

**Результат:**
```
TBILISI: 520 броней обработано
BATUMI: 380 броней обработано
KUTAISI: 290 броней обработано
SERVICE-CENTER: 150 броней обработано
```

---

## Исправление багов

### `fix_kutaisi_token.mjs`

**Назначение:** Исправление неверного company_token для Kutaisi

**Что делает:**
- Получает workflow Kutaisi
- Находит Get RentProg Token node
- Обновляет `companyToken` на правильный
- Сохраняет workflow

**Использование:**
```bash
node setup/fix_kutaisi_token.mjs
```

**Правильный токен:**  
`5599ebb7b94827fdfd49ca3a5b7e259cfa99d8ea78edeb50`

---

### `fix_bookings_table.mjs`

**Назначение:** Исправление структуры таблицы bookings

**Что делает:**
- Добавляет `PRIMARY KEY (id)`
- Меняет тип полей с `BIGINT` на `NUMERIC`
  - price
  - days
  - total
  - deposit

**Использование:**
```bash
node setup/fix_bookings_table.mjs
```

**Причина:**  
`BIGINT` не поддерживает дробные числа, а цены могут быть типа `105.50`.

---

### `fix_cars_clients_pk.mjs`

**Назначение:** Проверка и добавление PRIMARY KEY для cars и clients

**Что делает:**
- Проверяет наличие PRIMARY KEY
- Добавляет если отсутствует

**Использование:**
```bash
node setup/fix_cars_clients_pk.mjs
```

**Примечание:**  
Обычно PRIMARY KEY уже есть, скрипт для проверки.

---

### `check_external_refs_constraints.mjs`

**Назначение:** Проверка UNIQUE constraints для external_refs

**Что показывает:**
- Список всех constraints
- Детали UNIQUE constraints
- Соответствие с ON CONFLICT в триггере

**Использование:**
```bash
node setup/check_external_refs_constraints.mjs
```

**Важно:**  
Триггер использует `ON CONFLICT (system, external_id)` - должен быть соответствующий UNIQUE constraint!

---

## Вспомогательные скрипты

### `check_trigger_status.mjs`

**Назначение:** Проверка статуса триггера

**Что показывает:**
- Enabled/Disabled
- Определение триггера (SQL)
- Существование функции

**Использование:**
```bash
node setup/check_trigger_status.mjs
```

---

### `check_import_progress.mjs`

**Назначение:** Проверка прогресса импорта данных

**Что показывает:**
- Количество броней, машин, клиентов
- Прогресс обработки

**Использование:**
```bash
node setup/check_import_progress.mjs
```

---

### `check_bookings_details.mjs`

**Назначение:** Детальная проверка броней

**Что показывает:**
- Брони с датами
- Брони без дат
- Примеры записей

**Использование:**
```bash
node setup/check_bookings_details.mjs
```

---

## Устаревшие/Тестовые скрипты

Эти скрипты были созданы для диагностики и больше не нужны в production:

- `check_data_content.mjs` - проверка содержимого data поля
- `check_raw_jsonb.mjs` - проверка raw JSONB данных
- `test_jsonb_extraction.mjs` - тестирование JSONB extraction
- `test_insert_with_nested.mjs` - тестирование INSERT с nested данными
- `test_direct_insert_with_trigger.mjs` - тестирование триггера
- `add_trigger_debug.mjs` - добавление debug логов в триггер
- `fix_trigger_parse_jsonb_string.mjs` - старая версия парсинга JSONB
- `update_trigger_*.mjs` - различные версии обновления триггера

**Можно удалить** если всё работает стабильно.

---

## Порядок выполнения (для новой установки)

Если нужно настроить систему с нуля:

### 1. Базовая настройка
```bash
# Добавить Telegram алерты
node setup/add_telegram_alerts_to_processors.mjs
node setup/fix_telegram_nodes_use_credential.mjs
node setup/fix_alert_connection_point.mjs
node setup/fix_alert_format_code.mjs

# Настроить переменные
python setup/add_telegram_chat_id.py
```

### 2. Исправление токенов (если нужно)
```bash
# Проверить Kutaisi
node setup/find_kutaisi_workflow.mjs

# Исправить если неверный
node setup/fix_kutaisi_token.mjs
```

### 3. Настройка БД
```bash
# Исправить структуру таблиц
node setup/fix_bookings_table.mjs

# Настроить триггер
node setup/fix_trigger_on_conflict.mjs
```

### 4. Импорт данных
```bash
# Импортировать брони
node setup/import_active_bookings.mjs

# Конвертировать JSONB строки
node setup/fix_jsonb_strings_to_objects.mjs

# Обработать все данные
node setup/process_all_bookings_with_data.mjs
```

### 5. Проверка
```bash
# Проверить прогресс
node setup/check_processing_progress.mjs

# Проверить workflows
node setup/list_processor_workflows.mjs
```

---

## Troubleshooting

### Ошибка: "Cannot find module"

**Решение:**
```bash
npm install
# или
npm install node-fetch postgres
```

### Ошибка: "Connection refused" к БД

**Проверить:**
- Connection string правильный?
- Neon PostgreSQL доступен?

**Исправить:**  
Обновить `CONNECTION_STRING` в скриптах.

### Ошибка: "401 Unauthorized" n8n API

**Проверить:**
- API key актуален?
- Проверить срок действия

**Исправить:**
```javascript
const N8N_API_KEY = 'NEW_KEY';
```

### Ошибка: "duplicate key value violates"

**Причина:**  
Триггер пытается создать дубликат в `external_refs`.

**Решение:**
```bash
node setup/fix_trigger_on_conflict.mjs
```

---

## Автоматизация

### Создание алиасов

Добавьте в `.bashrc` или `.zshrc`:

```bash
alias n8n-alerts-add="node setup/add_telegram_alerts_to_processors.mjs"
alias n8n-alerts-fix="node setup/fix_alert_format_code.mjs"
alias n8n-check-progress="node setup/check_processing_progress.mjs"
alias n8n-list-workflows="node setup/list_processor_workflows.mjs"
```

### GitHub Actions (будущее)

Можно создать workflow для автоматического деплоя изменений в алертах:

```yaml
name: Update Telegram Alerts
on:
  push:
    paths:
      - 'setup/fix_alert_format_code.mjs'
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: node setup/fix_alert_format_code.mjs
```

---

**Автор:** Cursor Agent  
**Дата:** 5 ноября 2025  
**Версия:** 1.0

