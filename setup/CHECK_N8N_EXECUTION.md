# 🔍 Проверка Execution в n8n UI

## Откройте workflow в n8n:
https://n8n.rentflow.rentals/workflow/SLW5V3xUSKsyVYGE

## Что проверить:

### 1. Посмотрите последний execution
- Нажмите на "Executions" справа
- Найдите последний запуск (должен быть только что)
- Откройте его

### 2. Проверьте ноду "Get RentProg Tokens"
- Кликните на ноду
- Посмотрите Output данные
- Должно быть что-то вроде:
```json
{
  "rentprog_id": "501190",
  "entity_type": "booking",
  "tokens": {
    "tbilisi": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "batumi": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "kutaisi": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "service-center": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

### 3. Проверьте ноды "Try Tbilisi", "Try Batumi", etc.
- Кликните на каждую Try ноду
- Посмотрите HTTP Request details
- Проверьте URL:
  - Должен быть: `https://rentprog.net/api/v1/public/bookings/501190`
  - С header: `Authorization: Bearer <token>`

### 4. Посмотрите HTTP Response
- В каждой Try ноде посмотрите что вернулось
- Статус код: 200/404/401?
- Тело ответа: что вернул RentProg?

## ⚠️ Возможные проблемы:

### Если токены пустые или undefined:
❌ **Проблема:** RentProg API не возвращает токены
📝 **Решение:** Проверить company tokens в Code ноде

### Если 401 Unauthorized:
❌ **Проблема:** Токен невалидный или истек
📝 **Решение:** Проверить формат Authorization header

### Если 404 Not Found:
❌ **Проблема:** 
1. Endpoint неправильный
2. Бронь действительно не существует в этом филиале
3. Нужен параметр `?branch=xxx`

📝 **Решение:** Попробуем добавить branch parameter

### Если 200 OK но пустой ответ:
❌ **Проблема:** API возвращает success но нет данных
📝 **Решение:** Проверить формат ответа

---

## 🔄 Альтернативный подход

Если ничего не помогает, попробуем:
1. Запросить список броней: `GET /api/v1/public/bookings?limit=10`
2. Посмотреть какие ID реально есть
3. Протестировать с реальным ID из списка

---

**Что вы видите в execution логах?**

