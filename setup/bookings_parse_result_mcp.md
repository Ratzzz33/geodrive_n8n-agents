# Результаты парсинга броней через MCP Chrome

## ✅ Успешно спарсено:

### Активные брони: 50 записей
### Неактивные брони: 326 записей

---

## 📋 Найденные поля на странице броней:

### Поля из таблицы (активные и неактивные):

1. **number** (№) - номер брони
   - ✅ В БД: `number` (BIGINT)
   - Пример: "510638", "509606"

2. **created** (Создана) - дата создания
   - ✅ В БД: `created_at` (TIMESTAMPTZ)
   - Формат: "11 нояб. 14:49" (русская дата)

3. **status_icon_html** (Сост.) - HTML иконки статуса
   - ❌ НОВОЕ поле - нет прямого соответствия в БД
   - Содержит: `<i class="mdi mdi-car">` (иконка машины)
   - Рекомендация: сохранять в `data` JSONB

4. **client_status_html** (Кл сост) - HTML статуса клиента
   - ❌ НОВОЕ поле - нет прямого соответствия в БД
   - Содержит: `<i class="mdi mdi-alpha-a-circle-outline">` (иконка "A")
   - Рекомендация: сохранять в `data` JSONB

5. **payment** (Оплата) - статус оплаты
   - ✅ В БД: `paid`, `aggr_paid`, `payment_job_count`
   - Значения: "Оплачено", "Не оплачено", "+ 60", "+ 3", "-5", "-50"

6. **car_name** (Название) - название автомобиля
   - ✅ В БД: `car_name` (TEXT)
   - Формат: "Mini Countryman S(Mini Countryman 360)"

7. **client_name** (Клиент) - имя клиента
   - ✅ В БД: `client_id` (FK), но нет прямого поля имени
   - Формат: "Dmitrievskaia A.", "Ulasevici A."

8. **start_date** (Начало) - дата начала брони
   - ✅ В БД: `start_at` (TIMESTAMPTZ), `start_date` (TEXT)
   - Формат: "11 нояб.  15:30" (русская дата)

9. **end_date** (Конец) - дата окончания брони
   - ✅ В БД: `end_at` (TIMESTAMPTZ), `end_date` (TEXT)
   - Формат: "18 нояб.  15:30" (русская дата)

10. **days** (Суток) - количество дней
    - ✅ В БД: `days` (NUMERIC)
    - Формат: "7", "6", "10"

11. **issue_location** (Выдача) - место выдачи
    - ✅ В БД: `issue_location` (TEXT), `location_start` (TEXT)
    - Пример: "Tbilisi, Nadira Khosharauli Str., 29"

12. **return_location** (Приём) - место приема
    - ✅ В БД: `return_location` (TEXT), `location_end` (TEXT)
    - Пример: "Tbilisi, Nadira Khosharauli Str., 29"

13. **notes** (Примечание) - заметки
    - ✅ В БД: `notes` (TEXT), `description` (TEXT)
    - Пример: "285", "250 + 100", "-"

14. **responsible_html** (Отв.) - HTML ответственного
    - ❌ НОВОЕ поле - нет прямого соответствия в БД
    - Содержит: HTML с иконками и именем
    - Рекомендация: сохранять в `data` JSONB

15. **responsible** (Отв.) - имя ответственного
    - ✅ В БД: `responsible` (TEXT), `responsible_id` (UUID)
    - Пример: "Станислав Гунько", null (если иконка вопроса)

16. **detail_url** - URL детальной страницы
    - ❌ НОВОЕ поле - нет прямого соответствия в БД
    - Формат: "https://web.rentprog.ru/bookings/510638"
    - Рекомендация: сохранять в `data` JSONB

17. **booking_id** - ID брони из URL
    - ✅ В БД: `external_refs.external_id` (через external_refs)
    - Формат: "510638", "509606"

---

## ❌ НОВЫЕ/НЕИЗВЕСТНЫЕ ПОЛЯ (требуют уточнения):

### 1. **status_icon_html**
   - HTML содержимое иконки статуса брони
   - Содержит: `<i class="mdi mdi-car">` (иконка машины для активных)
   - Нет прямого соответствия в БД
   - **Рекомендация:** Сохранять в `data` JSONB как `status_icon_html`

### 2. **client_status_html**
   - HTML содержимое статуса клиента
   - Содержит: `<i class="mdi mdi-alpha-a-circle-outline">` (иконка "A")
   - Нет прямого соответствия в БД
   - **Рекомендация:** Сохранять в `data` JSONB как `client_status_html`

### 3. **responsible_html**
   - HTML содержимое поля ответственного
   - Содержит: HTML с иконками и именем (или иконкой вопроса)
   - Нет прямого соответствия в БД (есть только `responsible` TEXT)
   - **Рекомендация:** Сохранять в `data` JSONB как `responsible_html`

### 4. **detail_url**
   - URL детальной страницы брони
   - Формат: `https://web.rentprog.ru/bookings/{id}`
   - Нет прямого соответствия в БД
   - **Рекомендация:** Сохранять в `data` JSONB как `detail_url` или извлекать из `external_refs`

### 5. **payment_status** ⭐ (ВАЖНО - цветовая логика!)
   - Статус оплаты на основе цвета и текста
   - Значения: `paid`, `unpaid`, `overpaid`, `underpaid`
   - **Логика:**
     - `paid` - "Оплачено" (success/disabled)
     - `unpaid` - "Не оплачено" (outlined)
     - `overpaid` - "+ X" (warning - желтый)
     - `underpaid` - "- X" или "0" (error - красный)
   - **Рекомендация:** Создать поле `payment_status` TEXT в БД

### 6. **payment_balance** ⭐ (ВАЖНО!)
   - Числовое значение баланса оплаты
   - Положительное число - переплата
   - Отрицательное число - недоплата
   - 0 - баланс нулевой (но может быть проблемой)
   - **Рекомендация:** Создать поле `payment_balance` NUMERIC в БД

### 7. **payment_color** ⭐ (ВАЖНО - для визуализации!)
   - Цвет индикатора оплаты
   - Значения: `success`, `error`, `warning`, `disabled`, `outlined`
   - **Логика:**
     - `success` - зеленый (оплачено)
     - `error` - красный (недоплата)
     - `warning` - желтый (переплата)
     - `disabled` - серый (оплачено, нет действий)
     - `outlined` - серый контур (не оплачено)
   - **Рекомендация:** Сохранять в `data` JSONB

### 8. **client_category** ⭐ (ВАЖНО!)
   - Категория клиента на основе иконки
   - Значения: `new`, `A`, `blocked`, `cancelled`
   - **Логика:**
     - `new` - `mdi-new-box` (новый клиент)
     - `A` - `mdi-alpha-a-circle-outline` light-blue (надежный клиент)
     - `blocked` - `mdi-account-remove-outline` (клиент заблокирован)
     - `cancelled` - `mdi-cancel` (бронь отменена)
   - **Рекомендация:** Использовать существующее поле `client_category` TEXT

### 9. **client_status_color** ⭐ (ВАЖНО!)
   - Цвет статуса клиента
   - Значения: `light-blue`, `green`, `primary`, `none`
   - **Логика:**
     - `light-blue` - клиент категории A (надежный)
     - `green` - автомобиль в сервисе
     - `primary` - новый клиент
   - **Рекомендация:** Сохранять в `data` JSONB

### 10. **booking_status_icon** ⭐ (ВАЖНО!)
   - Иконка статуса брони
   - Значения: `car`, `clock`
   - **Логика:**
     - `car` - активная бронь (primary - синий)
     - `clock` - запланированная бронь (grey)
   - **Рекомендация:** Сохранять в `data` JSONB или создать поле `booking_status_icon` TEXT

### 11. **booking_status_color** ⭐ (ВАЖНО!)
   - Цвет статуса брони
   - Значения: `primary`, `grey`
   - **Рекомендация:** Сохранять в `data` JSONB

### 12. **responsible_status** ⭐ (ВАЖНО!)
   - Статус ответственного
   - Значения: `assigned`, `not_assigned`
   - **Логика:**
     - `assigned` - `mdi-account-check` success (зеленый) - назначен
     - `not_assigned` - `mdi-account-question` secondary (серый) - не назначен
   - **Рекомендация:** Сохранять в `data` JSONB

### 13. **responsible_color** ⭐ (ВАЖНО!)
   - Цвет индикатора ответственного
   - Значения: `success`, `secondary`
   - **Рекомендация:** Сохранять в `data` JSONB

---

## ✅ ПОЛЯ, КОТОРЫЕ ЕСТЬ В БД:

- `number` ✅
- `created_at` ✅ (из `created`)
- `start_at`, `start_date` ✅ (из `start_date`)
- `end_at`, `end_date` ✅ (из `end_date`)
- `days` ✅
- `issue_location`, `location_start` ✅
- `return_location`, `location_end` ✅
- `notes`, `description` ✅
- `responsible`, `responsible_id` ✅
- `car_name` ✅
- `client_id` ✅ (через поиск по `client_name`)
- `payment` → `paid`, `aggr_paid` ✅
- `booking_id` → `external_refs.external_id` ✅

---

## 📝 ИТОГОВЫЙ СПИСОК НОВЫХ ПОЛЕЙ:

### HTML поля (для отладки, можно не сохранять):
1. **status_icon_html** - HTML иконки статуса (сохранять в `data` JSONB)
2. **client_status_html** - HTML статуса клиента (сохранять в `data` JSONB)
3. **responsible_html** - HTML ответственного (сохранять в `data` JSONB)
4. **detail_url** - URL детальной страницы (сохранять в `data` JSONB)

### ⭐ КРИТИЧНЫЕ ПОЛЯ (обязательно парсить и сохранять):

5. **payment_status** - статус оплаты: `paid`, `unpaid`, `overpaid`, `underpaid` (создать поле в БД)
6. **payment_balance** - баланс оплаты: числовое значение (создать поле в БД)
7. **payment_color** - цвет индикатора: `success`, `error`, `warning`, `disabled`, `outlined` (в `data` JSONB)
8. **client_category** - категория клиента: `new`, `A`, `blocked`, `cancelled` (использовать существующее поле)
9. **client_status_color** - цвет статуса: `light-blue`, `green`, `primary` (в `data` JSONB)
10. **booking_status_icon** - иконка статуса: `car`, `clock` (в `data` JSONB)
11. **booking_status_color** - цвет статуса: `primary`, `grey` (в `data` JSONB)
12. **responsible_status** - статус ответственного: `assigned`, `not_assigned` (в `data` JSONB)
13. **responsible_color** - цвет индикатора: `success`, `secondary` (в `data` JSONB)

**Всего новых полей: 13**

---

## 🔍 ДОПОЛНИТЕЛЬНЫЕ ЗАМЕЧАНИЯ:

- **Формат дат:** Русский формат ("11 нояб. 14:49") требует парсинга в TIMESTAMPTZ
- **Поле payment:** Может содержать текст ("Оплачено", "Не оплачено") или числа ("+ 60", "-5")
- **Поле responsible:** Может быть null, если показана иконка вопроса
- **Структура неактивных броней:** Та же, что и активных, но без колонки "Сост." в начале

---

## 📊 СТАТИСТИКА:

- **Активные брони:** 50 записей
- **Неактивные брони:** 326 записей
- **Всего уникальных полей:** 17
- **Новых полей (нет в БД):** 4
- **Полей, которые есть в БД:** 13

