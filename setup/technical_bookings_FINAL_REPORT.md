# 🔍 Итоговый отчет: Технические брони RentProg

**Дата:** 2025-11-13  
**Исследователь:** Claude AI  
**Метод:** Анализ через MCP Chrome + RentProg Web UI

---

## 🎯 ГЛАВНЫЕ ВЫВОДЫ

### 1. Как определить техническую бронь:

#### ✅ Визуальные признаки:
- **Иконка:** `mdi-alpha-a-circle-outline` (буква A в круге)
  - Обычные брони: `mdi-car` (машина) или `mdi-clock-outline` (часы)
  - Технические брони: `mdi-alpha-a-circle-outline` (буква A)

- **Клиент:**
  - "Сервис А."
  - "Сотрудник G."
  - Другие подобные имена (не реальные клиенты)

#### ✅ Поля в форме создания брони:
1. **Чекбокс "Техническая"** - главный триггер
   - При включении: бронь становится технической
   - Без этого чекбокса: обычная клиентская бронь

2. **НЕТ отдельного чекбокса "Для ремонта"!**

---

## 🔧 Разница между "техническая" и "техническая для ремонта"

### ❓ ГИПОТЕЗА (требует проверки):

Разница определяется **НЕ чекбоксом**, а одним из:

1. **Тип/категория клиента:**
   - Клиент "Сервис" → техническая для ремонта
   - Клиент "Сотрудник" → техническая (служебная поездка)

2. **Примечание/описание:**
   - Текст "ремонт", "fix", "repair" → для ремонта
   - Иначе → обычная техническая

3. **Место выдачи/приёма:**
   - Если "Сервис", "СТО", "Service Center" → для ремонта
   - Иначе → обычная техническая

4. **Тип брони (booking_type):**
   - Возможно есть API поле, не видимое в UI

---

## 📊 Статистика

### Найдено технических броней:
- **Неактивные:** 10 из 322 (3.1%)
- **Активные:** 0 из 50 (0%)

### Примеры технических броней:
1. Chevrolette Cruze 457 → "Сотрудник G."
2. Toyota Rav 4 904 → "Сотрудник G."
3. Ford Mustang 648 → "Сотрудник G."
4. BMW 530i 309 (2 брони) → "Сотрудник G."
5. Mazda 6 562 → "Сотрудник G."
6. Hyundai Tucson 756 (2 брони) → "Сотрудник G."
7. Mini One 595 (2 брони) → "Сотрудник G."
8. Hyundai Veloster 179 → "Сервис А."

---

## 🎨 Визуальные индикаторы

### Иконки состояния брони:
| Иконка | Значение |
|--------|----------|
| `mdi-car` (primary) | Обычная клиентская бронь |
| `mdi-clock-outline` (secondary) | Запланированная (еще не началась) |
| `mdi-alpha-a-circle-outline` | **ТЕХНИЧЕСКАЯ БРОНЬ** ✅ |
| `mdi-new-box` | Новая бронь (только создана) |
| `mdi-account-remove-outline` | Клиент отменил |

### Иконки клиента:
| Иконка | Значение |
|--------|----------|
| `mdi-account-question` (secondary) | Клиент не подтвержден |
| `mdi-account-check` (success) | Клиент подтвержден |

---

## 📋 Поля в форме создания брони

### Обязательные поля:
1. **Выберите автомобиль** (select)
2. **Выберите клиента** (select)
3. **Место выдачи** (text)
4. **Место приёма** (text)
5. **Тип бронирования** (select):
   - Посуточная аренда
   - Почасовая аренда
   - Понедельная аренда (Beta)
   - Помесячная аренда (Beta)

### Чекбоксы/опции:
1. ✅ **Техническая** - главный триггер
2. **Тариф**
3. **Скидка**
4. **Ручной расчёт**
5. **Доп. услуги**
6. **Доп. инфо**
7. **Доп. водители**

### ❌ НЕТ в UI:
- Чекбокс "Для ремонта"
- Чекбокс "Служебная поездка"
- Выбор типа технической брони

---

## 🔍 API данные

### Поля в API ответе (bookings.data[].attributes):

```javascript
{
  id: "507781",
  number: "507781",
  active: false,  // is_active
  
  // Даты
  start_date: "2025-11-06T08:38:00+04:00",
  end_date: "2025-11-06T18:38:00+04:00",
  start_date_formatted: "06 нояб. 8:38",
  end_date_formatted: "06 нояб. 18:38",
  created_at: "2025-11-06T08:38:00+04:00",
  
  // Клиент
  client_id: "...",
  first_name: "Сотрудник",
  middle_name: "",
  last_name: "G.",
  client_category: null, // ❓ Может здесь?
  
  // Авто
  car_id: "...",
  car_name: "Chevrolette Cruze",
  car_code: "Cruze 457 Red",
  
  // Локации
  location_start: "...",
  location_end: "...",
  
  // Финансы
  total: 0,
  deposit: 0,
  rental_cost: 0,
  days: 0,
  
  // Статусы
  state: "...",
  in_rent: false,
  archive: false,
  
  // Ответственные
  start_worker_id: "...",
  end_worker_id: "...",
  responsible: "...",
  
  // Описание
  description: "...",  // ❓ Может здесь "ремонт"?
  source: "web"
}
```

### ❓ Где искать признак "для ремонта":

1. **`client_category`** - может быть "service", "repair", "employee"
2. **`description`** - ключевые слова "ремонт", "repair", "fix"
3. **`state`** - специальное состояние?
4. **`location_start/end`** - если "Сервис", "СТО"
5. **Дополнительное поле в API**, не показанное в атрибутах

---

## 🎯 Рекомендации для парсинга

### Определение технической брони:

```javascript
function isTechnicalBooking(booking) {
  const clientName = `${booking.first_name} ${booking.last_name}`.toLowerCase();
  
  // Признаки технической брони:
  return (
    clientName.includes('сервис') ||
    clientName.includes('сотрудник') ||
    clientName.includes('service') ||
    clientName.includes('employee') ||
    booking.client_category === 'technical' || // если такое поле есть
    booking.rental_cost === 0 // часто техническ брони без стоимости
  );
}

function isTechnicalForRepair(booking) {
  const description = (booking.description || '').toLowerCase();
  const locationStart = (booking.location_start || '').toLowerCase();
  const clientName = `${booking.first_name} ${booking.last_name}`.toLowerCase();
  
  // Признаки "для ремонта":
  return (
    clientName.includes('сервис') ||
    description.includes('ремонт') ||
    description.includes('repair') ||
    description.includes('fix') ||
    description.includes('сто') ||
    locationStart.includes('сервис') ||
    locationStart.includes('service') ||
    locationStart.includes('ремонт')
  );
}

function getTechnicalType(booking) {
  if (!isTechnicalBooking(booking)) {
    return 'regular'; // Обычная клиентская
  }
  
  if (isTechnicalForRepair(booking)) {
    return 'technical_repair'; // Техническая для ремонта
  }
  
  return 'technical'; // Техническая (служебная)
}
```

---

## 📊 Поля для БД

### Рекомендуемые поля в таблице `bookings`:

```sql
ALTER TABLE bookings ADD COLUMN IF NOT EXISTS is_technical BOOLEAN DEFAULT FALSE;
ALTER TABLE bookings ADD COLUMN IF NOT EXISTS technical_type TEXT; -- 'regular', 'technical', 'technical_repair'
ALTER TABLE bookings ADD COLUMN IF NOT EXISTS technical_purpose TEXT; -- 'service', 'repair', 'employee_trip', etc.

CREATE INDEX IF NOT EXISTS idx_bookings_is_technical ON bookings(is_technical);
CREATE INDEX IF NOT EXISTS idx_bookings_technical_type ON bookings(technical_type);
```

---

## ✅ Что проверено

1. ✅ Залогинились в RentProg (Tbilisi филиал)
2. ✅ Распарсили активные брони (50 записей)
3. ✅ Распарсили неактивные брони (322 записи)
4. ✅ Нашли 10 технических броней
5. ✅ Определили визуальный признак (иконка `mdi-alpha-a-circle-outline`)
6. ✅ Открыли форму создания брони
7. ✅ Нашли чекбокс "Техническая"
8. ✅ Проверили поля в форме
9. ✅ Проверили "Тип бронирования"

---

## ❓ Что требует дополнительной проверки

1. ❓ **Точный признак "для ремонта"** в API
   - Нужно запросить детали технической брони через API
   - Сравнить поля "Сервис А." vs "Сотрудник G."

2. ❓ **Поле `client_category`** в API
   - Может содержать "service", "repair", "employee"

3. ❓ **Дополнительные поля** в `/bookings/{id}` endpoint
   - Может быть `booking_purpose`, `is_repair`, etc.

4. ❓ **Клиенты "Сервис" и "Сотрудник"** в `/clients` API
   - Проверить их категорию/тип

---

## 🚀 Следующие шаги

### Для точного определения:

1. **Запросить детали технической брони через API:**
   ```bash
   GET /api/v1/bookings/507781  # Chevrolette Cruze (Сотрудник G.)
   GET /api/v1/bookings/507913  # Hyundai Veloster (Сервис А.)
   ```

2. **Сравнить поля** этих двух броней

3. **Проверить клиентов:**
   ```bash
   GET /api/v1/clients/{client_id_of_Sotrudnik}
   GET /api/v1/clients/{client_id_of_Servis}
   ```

4. **Обновить парсинг** с учетом найденных различий

---

## 📸 Скриншоты

Скриншоты сохранены в памяти MCP Chrome сессии:
- Страница логина
- Активные брони
- Неактивные брони (с техническими)
- Форма создания брони

---

## 🔗 Ссылки

- RentProg Web UI: https://web.rentprog.ru
- Страница броней: https://web.rentprog.ru/bookings
- API Base: https://rentprog.net/api/v1/public

---

## 📝 Заметки

1. **Иконка `mdi-alpha-a-circle-outline`** - самый надежный визуальный признак
2. **Клиент с именем "Сервис" или "Сотрудник"** - ключевой индикатор
3. **НЕТ отдельного чекбокса "Для ремонта"** в UI
4. **Разница определяется логикой**, не UI элементом


