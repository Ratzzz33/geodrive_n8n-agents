# Статус парсинга geodrive.info

## Запущен парсинг с OpenAI API ключом

**Время запуска:** Сейчас  
**Статус:** В процессе

### Что происходит:

1. ✅ Старые данные очищены (было 28 страниц, 1590 чанков)
2. 🔄 Парсинг запущен в фоновом режиме
3. 🔄 Обход всех страниц сайта geodrive.info
4. 🔄 Извлечение контента и разбивка на чанки
5. 🔄 Создание эмбеддингов через OpenAI API (text-embedding-3-small)

### Проверка прогресса:

```bash
node setup/quick_check.mjs
```

Или через SQL:
```sql
SELECT 
  (SELECT COUNT(*) FROM website_pages) as pages,
  (SELECT COUNT(*) FROM website_content_chunks) as chunks,
  (SELECT COUNT(*) FROM website_content_chunks WHERE embedding IS NOT NULL) as with_embeddings;
```

### Ожидаемый результат:

- Все страницы сайта geodrive.info будут сохранены
- Контент разбит на чанки по ~1000 символов
- Для каждого чанка создан эмбеддинг (вектор 1536 размерности)
- Данные готовы для использования в RAG для обучения агента

