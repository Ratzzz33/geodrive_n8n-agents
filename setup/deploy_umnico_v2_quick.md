# 🚀 Быстрый деплой Umnico Parsing V2

## ✅ Что уже сделано

1. ✅ Миграция БД применена (`telegram_username` добавлено)
2. ✅ Код обновлён (`services/playwright-umnico-optimized.ts`)
3. ✅ Скрипты синхронизации обновлены

## ⚠️ Проблема с компиляцией

TypeScript не компилируется из-за ошибок в других файлах (не связанных с нашими изменениями).

## 🔧 Решение: Деплой напрямую на сервер

### Вариант 1: Скопировать файл и пересобрать контейнер

```bash
# 1. Подключиться к серверу
ssh root@46.224.17.15

# 2. Перейти в директорию
cd /root/geodrive_n8n-agents

# 3. Обновить код
git pull

# 4. Собрать только playwright-umnico
cd services
npx tsc playwright-umnico-optimized.ts --outDir ../dist --module commonjs --target es2020 --esModuleInterop --skipLibCheck

# 5. Скопировать в нужное место
cp dist/playwright-umnico-optimized.js ../dist/playwright-umnico.js

# 6. Пересобрать и перезапустить контейнер
cd ..
docker compose build playwright-umnico
docker compose restart playwright-umnico

# 7. Проверить логи
docker logs playwright-umnico --tail 50 -f
```

### Вариант 2: Использовать ts-node (если установлен)

```bash
# На сервере
cd /root/geodrive_n8n-agents
git pull

# Обновить Dockerfile.umnico чтобы использовать ts-node
# Или просто скопировать .ts файл и использовать tsx
```

### Вариант 3: Временно использовать старый файл с патчами

Можно применить изменения напрямую в `playwright-umnico.ts` (базовый файл) вместо создания optimized версии.

## 📋 Проверка после деплоя

```bash
# Health check
curl http://46.224.17.15:3001/health

# Тест нового API
curl http://46.224.17.15:3001/api/conversations/61965921/messages | jq
```

Должен вернуть новый формат с полями:
- `total`
- `incomplete`
- `clientPhone` / `clientTelegram`
- `channel`

## 🎯 Рекомендация

**Лучше всего:** Исправить ошибки TypeScript в других файлах, чтобы `npm run build` работал полностью.

**Быстрое решение:** Задеплоить только playwright-umnico сервис через вариант 1 выше.

