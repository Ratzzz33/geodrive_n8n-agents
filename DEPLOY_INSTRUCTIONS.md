# 🚀 HTTP Scraper - Деплой через Git Bash

**Проблема:** Cursor Terminal зависает на SSH/Git командах  
**Решение:** Запуск через Git Bash в отдельном окне

---

## ⚡ Быстрый запуск

### Дважды кликните на файл:

```
RUN_DEPLOY.bat
```

Откроется **новое окно Git Bash** с процессом деплоя.

---

## 📋 Что делает скрипт:

1. ✅ `git push origin master` - пуш на GitHub
2. ✅ SSH подключение к серверу (автоматическая авторизация)
3. ✅ `git pull origin master` на сервере
4. ✅ `npm install` - установка зависимостей  
5. ✅ `npm run build` - компиляция TypeScript
6. ✅ `pm2 start http-scraper` - запуск сервиса
7. ✅ Health check - проверка работы

---

## ✅ Ожидаемый результат:

```bash
================================================
  Deploying HTTP Scraper Service
================================================

1. Git push...
✓ Pushed to GitHub

2. SSH Deployment...

→ Git pull...
Already up to date.

→ NPM install...
up to date, audited 272 packages in 2s

→ Build...
> jarvis-bot@0.1.0 build
> tsc

→ Check compiled files...
-rw-r--r--  1 root root 8574 httpScraperService.js
-rw-r--r--  1 root root 9301 rentprogScraper.js

→ PM2 restart...
[PM2] Done.

→ PM2 status...
┌────┬───────────────┬─────────┬─────────┬──────────┐
│ id │ name          │ mode    │ status  │ memory   │
├────┼───────────────┼─────────┼─────────┼──────────┤
│ 2  │ http-scraper  │ fork    │ online  │ 18.2mb   │
└────┴───────────────┴─────────┴─────────┴──────────┘

→ Health check...
{"status":"ok","service":"http-scraper-service","method":"HTTP requests (no browser)"}

================================================
  Deploy completed!
================================================
```

---

## 🔧 После успешного деплоя:

### Обновите n8n workflow:

1. https://n8n.rentflow.rentals
2. Workflow: **Company Cash Register Parser**
3. Нода: **Scrape Company Cash (Playwright)**
4. URL: `http://172.17.0.1:3001` → `http://172.17.0.1:3002`
5. **Сохраните** и протестируйте

---

## ❌ Если Git Bash не установлен:

Установите Git for Windows:
https://git-scm.com/download/win

Или используйте альтернативу - запустите команды вручную в обычном CMD.

---

## 🎯 Готово!

**Просто запустите `RUN_DEPLOY.bat` двойным кликом!**


