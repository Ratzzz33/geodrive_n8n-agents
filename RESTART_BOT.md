# Инструкция по перезапуску бота

## Быстрый способ (через Git Bash)

1. Откройте **Git Bash** (или любой другой терминал с SSH)

2. Выполните команду:

```bash
ssh root@46.224.17.15
```

3. После подключения к серверу выполните:

```bash
cd /root/geodrive_n8n-agents
git pull
pkill -f "tsx.*index.ts" || pkill -f "node.*dist/index.js" || true
sleep 2
npm run dev > /root/bot.log 2>&1 &
sleep 3
tail -n 20 /root/bot.log
```

4. Проверьте логи - должны быть строки:
   - `✅ Webhook удален, переходим на polling режим`
   - `🤖 Bot started (polling mode)`
   - `📱 Bot @test_geodrive_check_bot connected`

## Или используйте готовый скрипт

Если у вас есть доступ к серверу через SSH ключ (без пароля):

```bash
bash setup/restart-bot-now.sh
```

## Проверка работы

После перезапуска:

1. В Telegram отправьте боту `/start` или `/status`
2. Бот должен ответить
3. Если не отвечает, проверьте логи: `tail -f /root/bot.log` на сервере

