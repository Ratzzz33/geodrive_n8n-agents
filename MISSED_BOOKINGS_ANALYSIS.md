# 🔍 Анализ пропущенных бронирований

**Дата проверки:** 10.11.2025  
**Статус:** ⚠️ Обнаружены пропущенные бронирования

---

## 📊 Общая статистика

| Показатель | Значение |
|------------|----------|
| **Всего бронирований в БД** | 5,361 |
| **Обработано в синхронизации** | 2,020 |
| **Разница** | 3,341 бронирований |
| **Не обновлено с 18:00 UTC** | 3,353 |

---

## ⚠️ Проблемы

### 1. Большое количество не обновленных бронирований

**3,353 бронирования** не обновлялись с начала последней синхронизации (18:00 UTC).

### 2. Отсутствие branch_code в external_refs

**3,544 бронирования** имеют `branch_code = NULL` в таблице `external_refs`:
- Это **94.6%** от всех не обновленных бронирований
- Многие из них имеют RentProg ID, но без указания филиала

### 3. Бронирования без связи с RentProg

**5 бронирований** вообще не имеют связи с RentProg (нет записи в `external_refs`).

---

## 📋 Примеры пропущенных бронирований

### С RentProg ID, но без branch_code:

1. Booking ID: `36a6d905-904d-45b3-a243-5a45f4d76382`
   - RentProg ID: `509620`
   - Branch: `N/A`
   - Последнее обновление: `2025-11-09T11:59:39.144Z`

2. Booking ID: `0feeded1-7caa-47a1-bc32-2449f4371731`
   - RentProg ID: `261863`
   - Branch: `N/A`
   - Последнее обновление: `2025-11-05T16:56:55.460Z`

### Без связи с RentProg:

1. Booking ID: `a5d57989-b313-4fa6-a296-928289e822dd`
   - RentProg ID: `N/A`
   - Последнее обновление: `2025-11-10T14:03:35.538Z`

---

## 🔍 Возможные причины

### 1. Архивные бронирования

API `/all_bookings` может возвращать только активные и неактивные бронирования, но **не архивные**. В БД могут быть старые архивные бронирования, которые больше не возвращаются API.

### 2. Отсутствие branch_code в старых записях

Бронирования, созданные до внедрения сохранения `branch_code` в `external_refs`, не имеют этой информации. Без `branch_code` система не может определить, из какого филиала загружать бронирование.

### 3. Бронирования из других источников

5 бронирований не имеют связи с RentProg - возможно, они были созданы вручную или из других систем.

### 4. Ограничения API

API `/all_bookings` может иметь ограничения:
- Возвращает только определенный период
- Не возвращает удаленные/архивированные бронирования
- Может иметь лимит на количество возвращаемых записей

---

## 📊 Статистика по филиалам

| Филиал | Всего в БД | Не обновлено | % не обновлено |
|--------|------------|--------------|----------------|
| **unknown** | 3,544 | 3,353 | 94.6% |
| **service-center** | 250 | 0 | 0.0% |
| **tbilisi** | 982 | 0 | 0.0% |
| **kutaisi** | 190 | 0 | 0.0% |
| **batumi** | 395 | 0 | 0.0% |

**Вывод:** Все бронирования с указанным филиалом были обновлены. Проблема только с бронированиями без `branch_code`.

---

## 🔧 Рекомендации

### 1. Проверить архивные бронирования

Создать скрипт для проверки, существуют ли эти бронирования в RentProg:
- Проверить через API по RentProg ID
- Определить, являются ли они архивными
- Если архивные - пометить в БД как архивные

### 2. Восстановить branch_code для старых записей

Для бронирований с RentProg ID, но без `branch_code`:
- Попробовать определить филиал из других данных (например, из `bookings.branch_id`)
- Или загрузить полные данные из RentProg API и обновить `branch_code`

### 3. Синхронизация архивных бронирований

Если нужно синхронизировать архивные бронирования:
- Использовать другой endpoint API (если есть)
- Или загружать по конкретным RentProg ID из БД

### 4. Мониторинг

Добавить мониторинг:
- Количество бронирований без `branch_code`
- Количество бронирований, не обновленных за последние N дней
- Алерты при обнаружении пропусков

---

## 📝 Следующие шаги

1. ✅ Проверить, существуют ли пропущенные бронирования в RentProg API
2. ✅ Определить, являются ли они архивными
3. ✅ Восстановить `branch_code` для старых записей
4. ✅ Решить, нужно ли синхронизировать архивные бронирования

---

**Статус:** ⚠️ Требуется дополнительное исследование

