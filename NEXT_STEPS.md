# Следующие шаги для завершения деплоя

## 📍 Roadmap (ноябрь 2025)

### Краткосрочно (1–2 дня)
- Jarvis API: запустить как сервис (pm2/systemd), healthcheck; опционально — проксирование через Nginx (внешний доступ не обязателен).
- RentProg processors → prod-стандарт:
  - Словарь человекочитаемых полей для алертов (company_id → филиал и т.д.).
  - Фильтрация «шумных» полей в UPDATE (например, `updated_at`).
  - Чек-лист автотестов (`send_test_webhook`).
- Snapshot-импорт `cars` (ручной запуск): проверить маппинг в `external_refs` и обязательные поля.
- Entity Timeline & Event Links — post‑deploy задачи:
  - Бэкфилл связей для исторических платежей (сейчас 100 несвязанных)
  - Тюнинг индексов и ретеншн‑политика для таймлайна
  - Метрики/дашборд (кол-во событий, % связей, latency), Telegram‑алерты по ветвям
  - Экспорт агрегатов для агентов (быстрые выборки без сложных JOIN)
- Подсистема задач (этап 1):
  - Миграции: `tasks`, `task_links`, `task_events` (колонка `employees.task_chat_id` уже добавлена в миграции 005)
  - Команды Jarvis: `/task create`, `/mytasks`, `/tasks branch`
  - Создание темы задачи в `Tasks | <ФИО>` и кнопки `Done/Snooze`
- **Улучшения архитектуры агентов** (см. `docs/AGENT_PATTERNS_IMPROVEMENTS.md`):
  - Agent Execution Logging (2-3 часа) — таблица `agent_executions` для трекинга всех вызовов
  - Agent Memory/Context (2-3 часа) — таблица `agent_context` для сохранения состояния между вызовами
- Обновить конфиги/пример окружения:
  - В `env.example`: актуальные URL (`rentprog.net`, `webhook.rentflow.rentals`)
  - В workflow'ах: убрать fallback на `http://46.224.17.15:5678` в пользу `$env.N8N_URL` (`https://n8n.rentflow.rentals`)
- Документация:
  - Пометить Netlify-доки как LEGACY и/или переместить в `docs/legacy/netlify/`

### Среднесрочно (1 неделя)
- Разнести алерты по чатам филиалов (`TELEGRAM_CHAT_*`), ввести критические алерты c иным форматом.
- Метрики по алертам (n8n → Postgres → дашборд).
- Унифицировать поток: единый webhook → `events` → processors/Jarvis API — выбор по нагрузке.
- Entity Timeline & Event Links — улучшения:
  - Расширенные правила связывания (контекст брони/клиента, окно времени, confidence‑score)
  - Автоэскалации при "платёж без контекста" и "аномальный GPS"
  - Единые агрегаты для оркестратора (пересечения событий/платежей/истории)
- Подсистема задач (этап 2):
  - Напоминания и эскалация; snooze; фильтры списков
  - Связки `task_links` для `car/booking/client/branch`
  - Списки в Jarvis: личные/по филиалу/по сущности; фильтр статус/приоритет
- **Улучшения архитектуры агентов** (продолжение):
  - Agent Tool Registry (3-4 часа) — единый реестр инструментов для всех агентов
  - Agent Retry Strategy (2 часа) — экспоненциальный backoff для временных ошибок

### Долгосрочно (1–2 месяца)
- Кнопки в алертах (Открыть в RentProg, Перейти к теме авто).
- AI-анализ аномалий и группировка событий (batch‑уведомления).
- Дашборд (Grafana) по событиям/филиалам.
- Подсистема задач (этап 3):
  - Шаблоны задач/чек-листы
  - Интеграция с дневным планом; автоподзадачи
  - KPI по закрытию задач; отчеты по филиалам

### Ночной агент продаж — планы

#### Краткосрочно (1–2 дня)
- AmoCRM Snapshot/Incremental: сделки/контакты/диалоги → БД
- Векторизация “победных/проигранных” диалогов (pgvector)
- Night Sales Agent (MVP): автоответы по белому списку интентов через Amo “Чаты в CRM”, логи и метрики

#### Среднесрочно (1 неделя)
- Guardrails: стоп‑слова, лимиты, эскалации
- Расширение шаблонов/сегментаций (язык/источник/ветка воронки)
- Утренние отчёты KPI, A/B‑тестирование ответов

#### Дальше
- Подключение Umnico как отдельного инструмента
- Авто‑офферы и брони (интеграция RentProg/Stripe) с подтверждениями
- LoRA‑дообучение на лучших ответах (после стабилизации RAG)

## ✅ Что уже сделано

### Инфраструктура
1. ✅ Hetzner сервер (`46.224.17.15`)
2. ✅ Nginx + Let's Encrypt SSL (домены: `n8n.rentflow.rentals`, `webhook.rentflow.rentals`)
3. ✅ Docker Compose: n8n, Jarvis API, PostgreSQL (Neon), Playwright Services (Umnico & AmoCRM)

### Интеграции и воркфлоу
4. ✅ RentProg API: 4 филиала, вебхуки, processors (Tbilisi/Batumi/Kutaisi/Service Center)
5. ✅ UI Events система:
   - n8n workflow: `RentProg Events Scraper` (Playwright парсинг "События" каждые 5 мин)
   - API endpoint: `/process-ui-event`
   - БД: `event_processing_log` (дедупликация, классификация событий)
6. ✅ Кассы сотрудников:
   - `employees`: колонки `cash_gel/usd/eur`, `cash_last_updated/synced`, `task_chat_id`
   - n8n workflow: `Cash Register Reconciliation` (ночная сверка в 04:00)
   - API: обновление/сверка касс из UI событий
7. ✅ Entity Timeline & Event Links (деплой завершён):
   - API статус: работает на `http://46.224.17.15:3000`
   - Проверено:
     - `/health` — OK
     - `/entity-timeline/stats` — 8 GPS событий от 7 машин
     - `/event-links/stats` — OK (начальные агрегаты)
     - `/event-links/unlinked` — показывает 100 несвязанных платежей (до внедрения связей)
   - Документация:
     - `docs/ENTITY_TIMELINE.md`, `ENTITY_TIMELINE_IMPLEMENTATION.md`, `ENTITY_TIMELINE_DEPLOYMENT_COMPLETE.md`
     - `docs/EVENT_LINKS.md`, `EVENT_LINKS_IMPLEMENTATION.md`
8. ✅ History Processing System:
   - Таблицы: `history`, `history_operation_mappings`
   - API endpoints: `/process-history`, `/process-history/stats`, `/process-history/unknown`, `/process-history/learn`
   - n8n workflow: `History Matcher & Processor` (каждые 5 минут)
   - Автоматическая обработка: платежи, кассовые операции, техобслуживание, статусы броней
   - Incremental Learning — автосоздание маппингов для новых типов операций
   - Документация: `docs/HISTORY_PROCESSING.md`
9. ✅ Playwright Services (Umnico & AmoCRM):
   - Umnico Service (`:3001`): постоянно работающий браузер, автологин, HTTP API
   - AmoCRM Service (`:3002`): постоянно работающий браузер, автологин, HTTP API + REST API
   - Автозапуск при рестарте сервера (Docker Compose)
10. ✅ Data Collection Workflows:
    - `Umnico Chat Scraper` (сбор диалогов каждые 5 минут)
    - `AmoCRM Deals Scraper` (сбор сделок каждые 30 минут)
    - `AmoCRM All Deals Parser` (полный парсинг всех сделок каждые 6 часов)
    - Таблицы: `conversations`, `messages`, `amocrm_deals`, `amocrm_contacts`, `sync_state`
    - Документация: `docs/WORKFLOWS_README.md`, `docs/AMOCRM_ALL_DEALS_PARSER.md`

### Подсистема задач (частично)
11. ✅ Сервис `taskService.ts`: создание/связки/события задач, обработка maintenance из UI
12. ⏳ Миграции `tasks/task_links/task_events` — требуется создать
13. ⏳ Команды Jarvis и Telegram UI — требуется реализовать

## 🚀 Быстрая установка (5 минут)

### Вариант 1: Через SSH вручную (рекомендуется)

1. **Подключитесь к серверу:**

```bash
ssh root@46.224.17.15
# Пароль: enebit7Lschwrkb93vnm
```

2. **Клонируйте проект:**

```bash
cd /root
git clone https://github.com/Ratzzz33/geodrive_n8n-agents.git
cd geodrive_n8n-agents
```

3. **Установите Docker:**

```bash
chmod +x setup/02-install-docker.sh
./setup/02-install-docker.sh
```

4. **Настройте переменные окружения:**

```bash
cp env.example .env
nano .env
```

Заполните данные подключения к Neon (получите их в [Neon Console](https://console.neon.tech/)):
- `NEON_HOST` - например: `ep-xxx-xxx.us-east-2.aws.neon.tech`
- `NEON_DATABASE` - имя базы данных
- `NEON_USER` - имя пользователя
- `NEON_PASSWORD` - пароль
- `N8N_PASSWORD` - ваш надежный пароль для n8n

5. **Настройте MCP сервер:**

```bash
chmod +x setup/04-setup-mcp-server.sh
./setup/04-setup-mcp-server.sh
```

6. **Настройте firewall:**

```bash
ufw allow 22/tcp
ufw allow 5678/tcp
ufw allow 1880/tcp
ufw --force enable
```

7. **Запустите сервисы:**

```bash
chmod +x setup/03-deploy-services.sh
./setup/03-deploy-services.sh
```

Или напрямую:

```bash
docker compose up -d
```

8. **Проверьте работу:**

```bash
docker compose ps
docker compose logs -f
```

### Вариант 2: Автоматический скрипт (Linux/Mac)

Если у вас есть Linux или Mac с установленным `sshpass`:

```bash
chmod +x setup/deploy-to-server.sh
./setup/deploy-to-server.sh 46.224.17.15 enebit7Lschwrkb93vnm
```

## 📝 После запуска

1. **Откройте n8n:** http://46.224.17.15:5678
2. **Войдите** с учетными данными из `.env`
3. **Создайте API ключ n8n:**
   - Settings → API → Create new API key
   - Обновите `N8N_API_KEY` в `.env`
   - Перезапустите: `docker compose restart`

4. **Проверьте MCP сервер:** http://46.224.17.15:1880/health

## 🔧 Управление

```bash
# Просмотр логов
docker compose logs -f

# Остановка
docker compose down

# Запуск
docker compose up -d

# Перезапуск
docker compose restart
```

## 📚 Документация

- [SERVER_INFO.md](SERVER_INFO.md) - Подробная информация о сервере
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [DEPLOYMENT.md](DEPLOYMENT.md) - Полное руководство по деплою

## ⚠️ Важно

- Сохраните пароль сервера в безопасном месте
- Измените пароль root после первого входа: `passwd`
- Настройте SSH ключи для безопасного доступа
- Регулярно обновляйте систему: `apt update && apt upgrade -y`

## 🆘 Если что-то пошло не так

1. Проверьте логи: `docker compose logs`
2. Проверьте статус контейнеров: `docker compose ps`
3. Убедитесь, что порты открыты: `ufw status`
4. Проверьте подключение к Neon из контейнера

