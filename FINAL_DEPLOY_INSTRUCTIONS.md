# 🚀 Финальный деплой - Инструкция

## Проблема
Cursor Terminal виснет на SSH командах из-за особенностей Windows + paramiko.

## ✅ Решение: Запуск в обычном CMD

### Шаг 1: Открыть CMD
1. Нажмите `Win + R`
2. Введите `cmd`
3. Нажмите Enter

### Шаг 2: Перейти в директорию проекта
```cmd
cd C:\Users\33pok\geodrive_n8n-agents
```

### Шаг 3: Запустить деплой
**Вариант A (рекомендуется):**
```cmd
FINAL_DEPLOY.bat
```

**Вариант B (если не работает):**
```cmd
py deploy_fixes_now.py
```

---

## 📋 Что делает скрипт деплоя:

1. ✅ Подключается к серверу Hetzner
2. ✅ Сохраняет локальные изменения (git stash)
3. ✅ Забирает последний код (git fetch)
4. ✅ Обновляет код (git reset --hard origin/master)
5. ✅ Устанавливает зависимости (npm install)
6. ✅ Компилирует TypeScript (npm run build)
7. ✅ Перезапускает сервисы (pm2 restart)
8. ✅ Проверяет здоровье (health check)

---

## 🔍 Если что-то пошло не так

### Проверить статус на сервере:
```cmd
py quick_check.py
```

### Подключиться к серверу вручную:
```bash
ssh root@46.224.17.15
# Пароль: Geodrive2024SecurePass

# На сервере:
cd /root/geodrive_n8n-agents
npm run build
pm2 restart all
pm2 logs
```

---

## ✅ Ожидаемый результат

В конце вы должны увидеть:
```
✅ Success
{"status":"ok","service":"playwright-service"}

============================================================
  ✅ ДЕПЛОЙ ЗАВЕРШЕН УСПЕШНО!
============================================================
```

---

## 📞 Если нужна помощь

Все исправления уже закоммичены и запушены в GitHub:
- ✅ Исправлен `link_rentprog.ts` (использует external_refs)
- ✅ Исправлен `myinfo.ts` (убраны несуществующие поля)
- ✅ Исправлен `start.ts` (убрано tg_username)
- ✅ Playwright добавлен в зависимости

Достаточно просто запустить деплой из обычного CMD!

