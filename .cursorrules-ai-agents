# Правила для работы с n8n AI Agent нодами (2025)

## 🔴 КРИТИЧЕСКИЕ ПРАВИЛА AI AGENT

### ВСЕ AI Agent работают как Tools Agent (2025)

**Важно:** Начиная с версии 1.82.0 все AI Agent ноды работают **ТОЛЬКО** как Tools Agent. Настройка выбора типа агента удалена.

**Старые workflows:** Если они были настроены как Tools Agent, продолжают работать. Если нет - требуется обновление.

### ОБЯЗАТЕЛЬНО: Подключение инструментов (Tools)

**КРИТИЧНО:** AI Agent **НЕ ЗАПУСТИТСЯ** без подключенных инструментов!

**Требование:**
- ✅ Должен быть подключен **как минимум один** tool sub-node к порту `ai_tool` узла AI Agent
- ❌ Без инструментов выполнение не начнется

**Как подключить инструмент:**
1. Добавь ноду с портом `ai_tool` (например, `HTTP Request`, `Code`, `Data Table`)
2. Соедини её с AI Agent через порт `ai_tool` (не `main`!)
3. В AI Agent укажи LLM-провайдера (OpenAI/Anthropic/Ollama) и credentials

---

## 📋 СТРУКТУРА AI AGENT WORKFLOW

### Базовые паттерны

#### 1. Чат-интерфейс (Chat UX)
```
Chat Trigger → AI Agent → Respond to Chat
```

**Настройка:**
- `Chat Trigger`: `responseMode = "Using Response Nodes"`
- `AI Agent`: `promptType = "auto"` (автоматически берет из Chat Trigger)
- `Respond to Chat`: отправляет ответ пользователю

#### 2. HTTP API
```
Webhook → AI Agent → Respond to Webhook
```

**Настройка:**
- `Webhook`: принимает POST запрос с `{ message: "..." }`
- `AI Agent`: `promptType = "define"`, `text = "={{ $json.body.message }}"`
- `Respond to Webhook`: возвращает JSON ответ

#### 3. Агент как инструмент (AI Agent Tool)
```
AI Agent (главный) → AI Agent Tool (под-агент) → другие tools
```

**Использование:**
- `AI Agent Tool` подключается как tool к главному `AI Agent`
- Позволяет создавать специализированных под-агентов
- Каждый под-агент может иметь свои инструменты

---

## 🛠️ ИНСТРУМЕНТЫ (TOOLS) ДЛЯ AI AGENT

### Доступные инструменты

**Встроенные инструменты:**
- `HTTP Request` - вызовы внешних API
- `Code` - выполнение JavaScript/Python кода
- `Data Table` - работа с табличными данными
- `Calculator` - арифметические операции
- `Wikipedia` - поиск в Wikipedia
- `Wolfram|Alpha` - вычислительные запросы

**Специальные инструменты:**
- `AI Agent Tool` - другой агент как инструмент
- `Call n8n Sub-Workflow Tool` - вызов другого workflow как инструмента
- `Vector Store` - RAG (поиск по векторной базе)
- `Execute Sub-workflow` - вызов под-воркфлоу

**Любая нода с портом `ai_tool`** может быть инструментом!

### Подключение инструментов

**Правило:**
1. Нода должна иметь порт `ai_tool` (не все ноды имеют)
2. Соедини ноду с AI Agent через порт `ai_tool`
3. AI Agent автоматически увидит инструмент и его описание

**Проверка:**
- В AI Agent должен быть виден список доступных инструментов
- Если инструмент не виден - проверь что соединение через `ai_tool`, не `main`

---

## 💬 CHAT TRIGGER И RESPOND TO CHAT

### Chat Trigger

**Типы:**
- `hostedChat` - чат на странице n8n
- `webhook` - чат через виджет или webhook

**Настройки:**
- `responseMode`: 
  - `"lastNode"` - последняя нода возвращает ответ
  - `"Using Response Nodes"` - используй `Respond to Chat` ноды

**Аутентификация:**
- `none` - публичный доступ
- `basicAuth` - username/password
- `n8nUserAuth` - требуется вход в n8n

**Важно:** Для работы с AI Agent используй `responseMode = "Using Response Nodes"`

### Respond to Chat

**Обязательные настройки:**
- `message` - текст ответа (может быть выражение)
- `waitUserReply` - ждать ли ответа пользователя (для диалогов)

**Опции:**
- `limitWaitTime` - ограничение времени ожидания ответа
- `memoryConnection` - подключение памяти для контекста

**Streaming:**
- Поддерживается потоковая выдача в новых версиях
- Включается автоматически если AI Agent имеет `enableStreaming: true`

---

## 🧠 ПАМЯТЬ (MEMORY) ДЛЯ AI AGENT

### Типы памяти

**1. Simple Memory (Buffer Window)**
- Хранит в памяти n8n (не требует credentials)
- Подходит для тестирования и простых случаев
- `sessionKey` - ключ сессии (по умолчанию из Chat Trigger)

**2. Redis Chat Memory**
- Хранит историю в Redis
- Требует Redis credentials
- Подходит для production

**3. Xata Memory**
- Хранит в Xata
- Требует Xata credentials

**4. Zep Memory**
- Хранит в Zep
- Требует Zep credentials

**5. Motorhead Memory**
- Хранит в Motorhead
- Требует Motorhead credentials

**6. Chat Memory Manager**
- Управление памятью в workflow
- Позволяет программно управлять историей

### Подключение памяти

**Способ 1: Автоматическое (Chat Trigger)**
- Chat Trigger автоматически создает память
- AI Agent использует её автоматически
- `Respond to Chat` может подключать память через `memoryConnection`

**Способ 2: Явное подключение**
- Добавь ноду Memory (например, `Simple Memory`)
- Соедини с AI Agent через порт `memory`
- Укажи `sessionKey` для изоляции сессий

**Настройка:**
- `maxTokensFromMemory` - максимум токенов из истории (0 = все)
- `sessionKey` - ключ сессии (по умолчанию из Chat Trigger)

---

## 📤 OUTPUT PARSERS

### Зачем нужны

Output parsers обеспечивают **структурированный вывод** от AI Agent.

**Использование:**
- Когда нужен JSON/список/структурированные данные
- Когда нужна валидация формата ответа
- Когда нужна обработка ошибок парсинга

### Типы Output Parsers

**1. Structured Output Parser**
- Возвращает структурированный JSON
- Требует схему (schema)
- Валидирует формат

**2. Item List Output Parser**
- Возвращает результаты как отдельные items
- Полезно для обработки списков

**3. Auto-fixing Output Parser** (Deprecated)
- ⚠️ Устарел, используй Structured Output Parser

### Подключение

1. В AI Agent включи `Require Specific Output Format = true`
2. Добавь ноду Output Parser
3. Соедини с AI Agent через порт `ai_outputParser`
4. Настрой схему/формат

---

## 🔄 FALLBACK MODEL

### Зачем нужен

Fallback модель используется если основная модель:
- Недоступна
- Вернула ошибку
- Превысила лимиты

### Настройка

1. В AI Agent включи `Enable Fallback Model = true`
2. Добавь вторую LLM ноду (OpenAI/Anthropic/Ollama)
3. Соедини с AI Agent через порт `ai_fallback`
4. Укажи credentials для fallback модели

**Рекомендация:** Используй более дешевую модель как fallback (например, gpt-3.5-turbo как fallback для gpt-4)

---

## ⚙️ НАСТРОЙКИ AI AGENT

### Обязательные настройки

**Prompt (User Message):**
- `promptType = "auto"` - автоматически из Chat Trigger
- `promptType = "define"` - определить вручную
- `text` - текст промпта (может быть выражение)

**LLM Provider:**
- Требует credentials (OpenAI/Anthropic/Ollama)
- Выбирается через подключение LLM ноды к порту `ai_languageModel`

### Опциональные настройки

**System Message:**
- Сообщение перед началом разговора
- Определяет роль и поведение агента
- Рекомендуется всегда указывать

**Max Iterations:**
- Максимум итераций агента (по умолчанию 10)
- Ограничивает количество вызовов инструментов
- Увеличь для сложных задач

**Return Intermediate Steps:**
- Возвращать ли промежуточные шаги
- Полезно для отладки
- По умолчанию `false`

**Enable Streaming:**
- Потоковая выдача ответа
- По умолчанию `true`
- Работает с `Respond to Chat`

**Max Tokens From Memory:**
- Максимум токенов из истории (0 = все)
- Полезно для ограничения контекста
- По умолчанию 0

**Batch Processing:**
- `batchSize` - размер батча (по умолчанию 1)
- `delayBetweenBatches` - задержка между батчами (мс)
- Полезно для rate limiting

---

## 🔗 AI AGENT TOOL (Агент как инструмент)

### Использование

`AI Agent Tool` позволяет использовать другой AI Agent как инструмент для главного агента.

**Сценарии:**
- Специализированные под-агенты (research, data analysis, etc.)
- Разделение ответственности
- Модульная архитектура агентов

### Настройка

**Обязательные поля:**
- `toolDescription` - описание инструмента для LLM
- `text` - промпт для под-агента
- `systemMessage` - системное сообщение под-агента

**Подключение:**
1. Создай `AI Agent Tool` ноду
2. Настрой описание и промпт
3. Подключи инструменты к под-агенту
4. Соедини с главным AI Agent через порт `ai_tool`

**Пример:**
```
AI Agent (главный)
  ↓ ai_tool
AI Agent Tool (research_specialist)
  ↓ ai_tool
HTTP Request (Wikipedia)
HTTP Request (Google Search)
```

---

## 📊 VECTOR STORES (RAG)

### Использование для AI Agent

Vector stores позволяют AI Agent искать информацию в векторной базе данных.

**Типы:**
- `Simple Vector Store` - в памяти n8n (для тестирования)
- `Pinecone Vector Store` - Pinecone
- `Supabase Vector Store` - Supabase
- `Redis Vector Store` - Redis
- `Weaviate Vector Store` - Weaviate
- `MongoDB Atlas Vector Store` - MongoDB Atlas

### Подключение

1. Создай Vector Store ноду
2. Вставь данные через `Insert` операцию
3. Подключи Vector Store к AI Agent через порт `ai_tool`
4. AI Agent автоматически использует для поиска

**Операции:**
- `Insert` - вставка данных
- `Load` - загрузка данных для поиска
- `Similarity Search` - поиск похожих

---

## 🚫 ЧТО НИКОГДА НЕ ДЕЛАТЬ

1. ❌ НЕ создавай AI Agent без подключенных инструментов
2. ❌ НЕ используй `responseMode = "lastNode"` с AI Agent (используй `Respond to Chat`)
3. ❌ НЕ забывай указывать `systemMessage` (критично для поведения агента)
4. ❌ НЕ используй Auto-fixing Output Parser (deprecated, используй Structured)
5. ❌ НЕ подключай инструменты через порт `main` (только `ai_tool`)
6. ❌ НЕ забывай настраивать `sessionKey` для памяти (изоляция сессий)
7. ❌ НЕ используй старые настройки типа агента (удалены в v1.82.0)
8. ❌ НЕ игнорируй `maxIterations` (может привести к бесконечным циклам)

---

## ✅ ЧЕКЛИСТ ПРИ СОЗДАНИИ AI AGENT

### Перед созданием

- [ ] Определен тип интерфейса (Chat/HTTP API)
- [ ] Выбраны необходимые инструменты
- [ ] Подготовлен systemMessage
- [ ] Определена необходимость памяти
- [ ] Определена необходимость output parser

### При создании

- [ ] Добавлен Chat Trigger или Webhook
- [ ] Добавлен AI Agent нода
- [ ] Подключен LLM provider (OpenAI/Anthropic)
- [ ] Подключен хотя бы один tool через `ai_tool`
- [ ] Настроен systemMessage
- [ ] Настроен promptType
- [ ] Добавлен Respond to Chat/Webhook
- [ ] Подключена память (если нужна)
- [ ] Подключен output parser (если нужен)

### После создания

- [ ] Проверено что workflow активирован
- [ ] Протестирован базовый сценарий
- [ ] Проверена работа инструментов
- [ ] Проверена работа памяти (если используется)
- [ ] Проверен формат вывода (если используется parser)

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- [Официальная документация AI Agent](https://docs.n8n.io/integrations/builtin/cluster-nodes/root-nodes/n8n-nodes-langchain.agent/)
- [Tutorial по AI Agent](https://docs.n8n.io/advanced-ai/intro-tutorial/)
- [Примеры workflow](https://n8n.io/workflows/templates/1954)
- [LangChain документация](https://js.langchain.com/docs/concepts/agents/)

---

**ПОМНИ:** AI Agent требует минимум один подключенный инструмент через порт `ai_tool`!

