# ✅ n8n Workflows успешно импортированы через MCP!

**Дата:** 2025-11-05  
**Метод:** MCP n8n-mcp-official  

---

## 🎯 Созданные Workflows

### 1. RentProg Events Scraper
- **ID:** `nZPD1AcSbLo3eSgr`
- **Статус:** Создан и настроен ✅
- **URL:** https://n8n.rentflow.rentals/workflow/nZPD1AcSbLo3eSgr
- **Триггер:** Cron каждые 5 минут
- **Назначение:** Парсинг страницы "События" RentProg для всех 4 филиалов (Playwright)

**Credentials настроены:**
- ✅ Telegram Bot (`1tKryXxL5Gq395nN`)
- ✅ RentProg UI логины (встроены в код):
  - Tbilisi: `eliseevaleksei32@gmail.com`
  - Batumi: `ceo@geodrive.rent`
  - Kutaisi: `geodrivekutaisi2@gmail.com`
  - Service Center: `sofia2020eliseeva@gmail.com`

---

### 2. Cash Register Reconciliation
- **ID:** `9Nrec3H5j2fIQ3Ma`
- **Статус:** Создан и настроен ✅
- **URL:** https://n8n.rentflow.rentals/workflow/9Nrec3H5j2fIQ3Ma
- **Триггер:** Cron ежедневно в 04:00 (Tbilisi timezone)
- **Назначение:** Сверка касс сотрудников с RentProg UI

**Credentials настроены:**
- ✅ Telegram Bot (`1tKryXxL5Gq395nN`)
- ✅ PostgreSQL Neon (`3I9fyXVlGg4Vl4LZ`)
- ✅ RentProg UI логин для Tbilisi (встроен в код)

---

## 📋 Что осталось сделать

### Шаг 1: Активировать workflows ⚠️

**ВАЖНО:** Перед активацией нужно убедиться, что Playwright установлен в n8n!

```bash
# На сервере n8n нужно установить Playwright:
docker exec -it n8n npm install playwright
docker exec -it n8n npx playwright install chromium
```

**Активация через MCP:**
```
mcp_n8n-mcp-official_n8n_update_partial_workflow
id: nZPD1AcSbLo3eSgr
operations: [{"type": "updateSettings", "update": {"active": true}}]
```

**Или вручную в UI:**
1. Откройте https://n8n.rentflow.rentals/workflow/nZPD1AcSbLo3eSgr
2. Нажмите "Active" toggle в правом верхнем углу
3. Повторите для второго workflow

---

### Шаг 2: Проверить DOM селекторы RentProg UI

**Проблема:** Селекторы в коде могут не совпадать с реальной разметкой RentProg.

**Решение:** Запустить тестовый скрипт:
```bash
node setup/test_rentprog_login.mjs
```

Этот скрипт:
- Проверит логин для всех филиалов
- Определит правильные селекторы для полей
- Сохранит скриншоты для анализа

Если селекторы неверные - обновить код в workflows вручную.

---

### Шаг 3: Первый тестовый запуск

**RentProg Events Scraper:**
1. Откройте workflow в n8n UI
2. Нажмите "Execute Workflow" (справа сверху)
3. Проверьте логи выполнения
4. Должны появиться записи в `event_processing_log` таблице

**Cash Register Reconciliation:**
- Ждать до 04:00 следующего дня
- Или запустить вручную через "Execute Workflow"
- Проверить Telegram чат `-5004140602` на алерты

---

## 🔍 Мониторинг

### Проверка выполнений через MCP:
```javascript
// Список последних выполнений
mcp_n8n-mcp-official_n8n_list_executions({
  workflowId: "nZPD1AcSbLo3eSgr",
  limit: 10
})

// Детали конкретного выполнения
mcp_n8n-mcp-official_n8n_get_execution({
  id: "<execution_id>",
  mode: "summary"
})
```

### Проверка через n8n UI:
1. Откройте: https://n8n.rentflow.rentals/executions
2. Фильтр по workflow: "RentProg Events Scraper"
3. Проверьте статус (Success/Error)

---

## ⚙️ Настройка (опционально)

### Изменить интервал парсинга:
Сейчас: каждые 5 минут  
Изменить в node "Every 5 Minutes" → `minutesInterval`

### Изменить время сверки касс:
Сейчас: 04:00 Tbilisi  
Изменить в node "Daily at 04:00 Tbilisi" → `cronExpression`

### Добавить другие филиалы в Cash Reconciliation:
Сейчас обрабатывается только Tbilisi.  
Добавить credentials для других филиалов в node "Scrape Employee Cash"

---

## 🎯 Ожидаемое поведение

### RentProg Events Scraper (каждые 5 минут):
1. Логин в RentProg UI для каждого филиала
2. Переход на страницу "События"
3. Парсинг событий за последние 5 минут
4. Отправка в Jarvis API `/process-ui-event`
5. При ошибке → Telegram alert в чат `-5004140602`

### Cash Register Reconciliation (ежедневно 04:00):
1. Чтение всех активных сотрудников из БД
2. Для каждого сотрудника:
   - Логин в RentProg UI (Tbilisi)
   - Переход на страницу сотрудника
   - Парсинг кассы (GEL, USD, EUR)
   - Сравнение с расчетной кассой
   - При расхождении > 0.01:
     - Автоисправление в БД
     - Telegram alert с деталями

---

## 📊 Таблицы БД

**Используются:**
- `employees` - информация о сотрудниках и их кассы
- `event_processing_log` - лог обработанных UI событий (дедупликация)

**Создать если отсутствуют:**
```sql
-- Уже созданы через миграции 005 и 006
SELECT * FROM employees LIMIT 1;
SELECT * FROM event_processing_log LIMIT 1;
```

---

## 🚨 Возможные проблемы

### 1. Playwright не установлен в n8n
**Симптом:** Ошибка `Cannot find module 'playwright'`  
**Решение:** Установить Playwright в контейнер n8n (см. Шаг 1)

### 2. Неверные селекторы DOM
**Симптом:** Ошибка при парсинге полей  
**Решение:** Запустить `test_rentprog_login.mjs` и обновить селекторы

### 3. Изменились credentials RentProg
**Симптом:** Ошибка авторизации  
**Решение:** Обновить логины/пароли в code nodes workflows

### 4. Нет данных в `employees` таблице
**Симптом:** Пустой результат "Get All Employees"  
**Решение:** Добавить сотрудников в БД или запустить импорт из RentProg

---

## ✅ Чеклист готовности

- [x] Workflows созданы в n8n
- [x] Credentials настроены (Telegram, PostgreSQL)
- [x] RentProg UI логины добавлены в код
- [ ] Playwright установлен в n8n контейнер
- [ ] DOM селекторы проверены
- [ ] Workflows активированы
- [ ] Первый тестовый запуск выполнен
- [ ] Мониторинг настроен

---

## 🎉 Готово!

Оба workflow успешно импортированы и настроены через MCP. Следуйте чеклисту выше для завершения настройки!

**Следующий шаг:** Установите Playwright в n8n и активируйте workflows.

