# Как добавить n8n MCP сервер в Cursor

## 📋 Пошаговая инструкция

Вы видите интерфейс "Tools & MCP" в Cursor. Чтобы добавить n8n сервер:

### Шаг 1: Нажмите "New MCP Server"

В разделе **"Installed MCP Servers"** нажмите на карточку с плюсом:
- **"+ New MCP Server"** → **"Add a Custom MCP Server"**

### Шаг 2: Заполните форму

В появившейся форме добавьте:

**Name:**
```
n8n
```

**Command:**
```
C:\Program Files\nodejs\node.exe
```

**Arguments:**
```
C:\Users\33pok\geodrive_n8n-agents\mcp-server\n8n-mcp-server.js
```

**Working Directory (cwd):**
```
C:\Users\33pok\geodrive_n8n-agents
```

**Environment Variables (если есть секция для этого):**

Добавьте две переменные:
- **Name:** `N8N_BASE_URL`, **Value:** `https://n8n.rentflow.rentals`
- **Name:** `N8N_API_KEY`, **Value:** `ваш_api_ключ_здесь`

**ИЛИ** если нет секции для env переменных, убедитесь, что они в файле `.env` в корне проекта.

### Шаг 3: Сохраните

Нажмите кнопку сохранения (обычно "Save" или "Add").

### Шаг 4: Перезапустите Cursor

Полностью закройте и снова откройте Cursor.

## ✅ После добавления

После перезапуска вы должны увидеть в списке:
- ✅ **chrome-devtools** (9 tools enabled)
- ✅ **n8n** (12 tools enabled) - новый сервер

## 🔍 Если нет формы для ввода

Если при нажатии "New MCP Server" открывается JSON редактор, вставьте:

```json
{
  "command": "C:\\Program Files\\nodejs\\node.exe",
  "args": [
    "C:\\Users\\33pok\\geodrive_n8n-agents\\mcp-server\\n8n-mcp-server.js"
  ],
  "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents",
  "env": {
    "N8N_BASE_URL": "https://n8n.rentflow.rentals",
    "N8N_API_KEY": "ваш_api_ключ_здесь"
  }
}
```

## ⚠️ Важно

1. Замените `ваш_api_ключ_здесь` на реальный API ключ из n8n
2. Замените домен `https://n8n.rentflow.rentals` на ваш домен (если отличается)
3. Убедитесь, что пути правильные (замените `33pok` если нужно)

## 🧪 Проверка

После добавления и перезапуска:
1. В списке MCP серверов должно появиться **n8n**
2. Попробуйте в чате: "Проверь подключение к n8n"
3. Если работает - вы увидите список workflows

