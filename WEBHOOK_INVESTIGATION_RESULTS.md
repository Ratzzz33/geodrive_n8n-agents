# 🔍 Результаты расследования проблемы с вебхуками RentProg

**Дата:** 2025-11-04  
**Статус:** ✅ Проблема найдена

---

## 📊 Краткий итог

### ✅ Что работает:
1. **SSL сертификат валиден** - Let's Encrypt R12, срок действия в норме
2. **Вебхуки ПРИХОДЯТ** - в БД 91 событие за последние 2 дня
3. **Nginx работает** - успешно проксирует запросы на n8n
4. **n8n workflow активен** - обрабатывает вебхуки и сохраняет в БД
5. **БД работает** - события сохраняются корректно

### ❌ Проблема:
**78 из 91 событий (86%) имеют тип "unknown"** - формат вебхука не распознаётся системой.

---

## 🔬 Детальная диагностика

### 1. SSL и доступность

```bash
✅ curl -I https://webhook.rentflow.rentals
HTTP/2 404 (для GET - это нормально)
SSL: TLSv1.3, сертификат валиден

✅ curl -X POST https://webhook.rentflow.rentals
HTTP/2 200 OK
{"ok":true,"received":true}
```

**Вывод:** Домен доступен, SSL работает, n8n отвечает.

---

### 2. Статистика событий в БД

```sql
Всего событий: 91
За последний час: 1
За последний день: 9
Первое событие: 2025-11-02 19:01:37
Последнее событие: 2025-11-04 13:34:28
```

**События по типам:**
- `unknown`: **78** ⚠️
- `null`: 3
- `diagnostic`: 3
- `create`: 2
- `car_update`: 1
- `booking_update`: 1
- `client_update`: 1
- `test.insert`: 1
- `update`: 1

**События по филиалам:**
- Tbilisi (9247): 3 события
- Service Center (11163): 3 события
- Unknown: остальные

**Вывод:** Вебхуки приходят, но в неизвестном формате.

---

### 3. Последние события

```
1. [2025-11-04T13:34:28] create - RentProg ID: 381384, Branch: Service Center ✅
2. [2025-11-04T12:10:08] update - RentProg ID: 381296, Branch: Service Center ✅
3. [2025-11-04T12:07:43] create - RentProg ID: 999999, Branch: Service Center ✅
4. [2025-11-04T06:57:57] null - RentProg ID: 381164, Branch: Tbilisi ⚠️
5. [2025-11-04T05:06:12] null - RentProg ID: 38204, Branch: Tbilisi ⚠️
```

**Вывод:** Некоторые события распознаются (create, update), но большинство приходят в неизвестном формате.

---

## 🎯 Найденная причина

### Проблема: Неизвестный формат вебхука

**Что происходит:**
1. RentProg отправляет вебхуки на `https://webhook.rentflow.rentals` ✅
2. Nginx проксирует на n8n ✅
3. n8n workflow получает вебхук ✅
4. **Parse & Validate Format** пытается определить тип события ❌
5. Не находит совпадения в `knownEventTypes` массиве
6. Событие сохраняется как `type: "unknown"` ⚠️

**Почему это происходит:**
- В workflow определён пустой массив `knownEventTypes`:
  ```javascript
  const knownEventTypes = [];  // Пусто - все вебхуки считаются неизвестными
  ```
- Все события, не совпадающие с этим списком, помечаются как "unknown"

---

## 📋 Что нужно сделать

### 1. Получить примеры реальных вебхуков

**Способ 1: Через Telegram alerts**
- В workflow настроена нода "Debug: Unknown Format"
- Она должна отправлять уведомления в Telegram при каждом unknown вебхуке
- **Проверьте Telegram чат** (TELEGRAM_ALERT_CHAT_ID) - там должны быть примеры

**Способ 2: Через n8n UI**
1. Откройте https://n8n.rentflow.rentals
2. Workflow "RentProg Webhooks Monitor"
3. Executions → последние выполнения
4. Откройте execution с unknown событием
5. Посмотрите данные ноды "Parse & Validate Format"
6. Там будет поле `rawEvent` и `parsedPayload`

**Способ 3: Из БД (если есть таблица raw_webhooks)**
```sql
SELECT * FROM raw_webhooks ORDER BY ts DESC LIMIT 10;
```

---

### 2. Добавить типы событий в workflow

После получения примеров вебхуков, нужно:

1. Определить реальные типы событий (например: `booking_created`, `car_moved`, `payment_received`)
2. Обновить массив `knownEventTypes` в ноде "Parse & Validate Format"
3. Сохранить workflow в n8n

**Пример:**
```javascript
const knownEventTypes = [
  'booking.created',
  'booking.updated',
  'booking.deleted',
  'car.created',
  'car.updated',
  'car.moved',
  'client.created',
  'client.updated',
  'payment.received',
  'payment.refunded'
];
```

---

### 3. Проверить Telegram настройки

**Убедитесь что:**
- Переменная `TELEGRAM_ALERT_CHAT_ID` настроена в n8n
- Telegram credentials назначены в нодах alert
- Бот имеет доступ к чату

**Проверка:**
```bash
# На сервере или через docker
docker exec n8n env | grep TELEGRAM
```

**Если настройки есть:**
- Проверьте Telegram чат - там должны быть уведомления о unknown вебхуках
- Каждое уведомление содержит полный payload вебхука

**Если настроек нет:**
- Unknown вебхуки сохраняются в БД, но нет уведомлений
- Нужно посмотреть executions в n8n UI

---

## 🚀 Следующие шаги

### Шаг 1: Найти примеры unknown вебхуков

**Вариант A: Telegram**
```
Проверить Telegram чат → найти сообщения вида:
"Неизвестный формат вебхука от RentProg"
```

**Вариант B: n8n UI**
```
1. Открыть https://n8n.rentflow.rentals
2. Workflow "RentProg Webhooks Monitor" → Executions
3. Найти execution с недавней датой
4. Посмотреть данные ноды "Parse & Validate Format"
5. Скопировать `rawEvent` и `parsedPayload`
```

**Вариант C: Новый вебхук**
```
1. В RentProg создать/обновить бронирование
2. Сразу проверить n8n executions
3. Посмотреть данные вебхука
```

---

### Шаг 2: Обновить workflow

1. Определить типы событий из примеров
2. Открыть workflow в n8n UI
3. Редактировать ноду "Parse & Validate Format"
4. Обновить массив `knownEventTypes`
5. Сохранить workflow
6. Отправить тестовый вебхук для проверки

---

### Шаг 3: Мониторинг

После обновления:
1. Следить за новыми вебхуками
2. Проверять что они распознаются (type != "unknown")
3. Если появляются новые unknown - добавить их в список

**Команда для мониторинга:**
```bash
watch -n 10 'node /workspace/check_recent_events.mjs | head -40'
```

---

## 📝 Дополнительная информация

### Структура workflow

```
RentProg → webhook.rentflow.rentals
           ↓
        Nginx (proxy)
           ↓
        n8n: Webhook node
           ↓
        Parse & Validate Format
           ↓
    ┌──────┴──────┐
    ↓             ↓
 Known        Unknown
 Format       Format
    ↓             ↓
 Auto       Telegram
 Process     Alert
    ↓             ↓
  Save        Save
  Event       Event
```

### Known vs Unknown

**Known format:**
- `isKnownFormat = true`
- Тип события распознан
- Автоматическая обработка через Jarvis API
- Может trigger upsert processor

**Unknown format:**
- `isKnownFormat = false`
- Тип события не в списке `knownEventTypes`
- Telegram alert (если настроен)
- Сохраняется в БД для анализа
- Требует ручного добавления типа в workflow

---

## ✅ Чеклист для пользователя

### Проверить настройки RentProg:
- [ ] URL вебхука = `https://webhook.rentflow.rentals` ✅
- [ ] Метод = POST ✅
- [ ] Вебхук активен ✅
- [ ] Выбраны нужные типы событий (?)

### Проверить систему:
- [x] SSL сертификат валиден ✅
- [x] Nginx работает ✅
- [x] n8n запущен ✅
- [x] Workflow активен ✅
- [x] События сохраняются в БД ✅

### Получить примеры вебхуков:
- [ ] Проверить Telegram alerts
- [ ] Посмотреть n8n executions
- [ ] Создать тестовое событие в RentProg

### Обновить workflow:
- [ ] Определить типы событий
- [ ] Добавить в `knownEventTypes`
- [ ] Сохранить workflow
- [ ] Протестировать

---

## 📞 Куда обратиться за помощью

### Telegram alerts не приходят?
1. Проверить переменную `TELEGRAM_ALERT_CHAT_ID` в n8n
2. Проверить Telegram credentials в workflow
3. Убедиться что бот добавлен в чат

### Не можете найти примеры вебхуков?
1. Создайте новое событие в RentProg (например, обновите бронь)
2. Сразу откройте n8n UI → Executions
3. Последний execution должен содержать данные вебхука

### Нужна помощь с обновлением workflow?
1. Предоставьте пример `rawEvent` из n8n execution
2. Предоставьте пример `parsedPayload`
3. Можно автоматически сгенерировать список типов событий

---

**Автор:** Claude Sonnet 4.5  
**Дата:** 2025-11-04  
**Статус:** ✅ Проблема идентифицирована - вебхуки приходят, но не распознаются
