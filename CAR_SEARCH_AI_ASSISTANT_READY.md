# 🚗 Car Search AI Assistant - Готов к запуску!

**Дата:** 2025-11-08  
**Статус:** Создан и настроен

---

## 🎉 Что создано:

### 1. Query Car Search Tool ✅
- **ID:** `kI4mXx8MuOBa37lp`
- **URL:** https://n8n.rentflow.rentals/workflow/kI4mXx8MuOBa37lp
- **Функция:** Вызывает `http://localhost:3000/api/cars/search`
- **Параметры:** branch, startDate, endDate, maxPriceUSD, transmission, yearFrom
- **Статус:** ✅ Создан, готов к использованию
- **Действие:** Оставить **ВЫКЛЮЧЕННЫМ** (вызывается автоматически AI Agent)

### 2. Car Search AI Assistant ✅
- **ID:** `z1b7wIj17ppMuU7a`
- **URL:** https://n8n.rentflow.rentals/workflow/z1b7wIj17ppMuU7a
- **Статус:** ✅ Валидация пройдена, 0 ошибок
- **Компоненты:**
  - Telegram Trigger
  - AI Agent (GPT-4o-mini)
  - Tool: Query Car Search
  - Send Response (Telegram)
- **Действие:** **АКТИВИРОВАТЬ**

---

## 🏗️ Архитектура:

```
Telegram Message
    ↓
[Telegram Trigger]
    ↓
[AI Agent] ← [OpenAI GPT-4o-mini]
    ↓         ↑
    ↓    [Tool: Query Car Search]
    ↓         ↓
    ↓    POST http://localhost:3000/api/cars/search
    ↓         ↓
    ↓    [Car Search Module] → База данных
    ↓         ↓
    ↓    Форматированный ответ
    ↓
[Send Response] → Telegram
```

---

## 🔧 Используемые модули:

### 1. Car Search Module (уже работает) ✅
- **Путь:** `src/modules/car-search/index.ts`
- **Функции:**
  - `searchCars()` - поиск с фильтрами
  - `formatForChat()` - форматирование для чатов
- **API Endpoints:**
  - `POST /api/cars/search` - полный поиск
  - `GET /api/cars/search/quick` - быстрый поиск
  - `POST /api/cars/search/natural` - естественный язык

### 2. API Endpoint
- **URL:** `http://localhost:3000/api/cars/search`
- **Method:** POST
- **Body:**
  ```json
  {
    "branch": "batumi",
    "startDate": "2025-11-09",
    "endDate": "2025-11-10",
    "maxPriceUSD": 50,
    "transmission": "Автомат",
    "yearFrom": 2015
  }
  ```

---

## 🚀 Активация (5 минут):

### Шаг 1: Убедиться что Jarvis API запущен

```bash
# Проверить статус
curl http://localhost:3000/health

# Если не запущен - запустить
cd C:\Users\33pok\geodrive_n8n-agents
npm run dev
```

### Шаг 2: Активировать workflow

1. Откройте: https://n8n.rentflow.rentals/workflow/z1b7wIj17ppMuU7a
2. Нажмите toggle **"Active"** в правом верхнем углу
3. Workflow должен стать активным

### Шаг 3: Получить Webhook URL

1. Кликните на **"Telegram Trigger"** ноду
2. Скопируйте **"Webhook URL"**
3. Должен быть вида:
   ```
   https://n8n.rentflow.rentals/webhook-test/79c0340c-e42c-4ce6-bd78-48b79f76aa78
   ```

### Шаг 4: Установить webhook в Telegram

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=<WEBHOOK_URL>"
```

**Проверить:**
```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

**Ожидаемый ответ:**
```json
{
  "ok": true,
  "result": {
    "url": "https://n8n.rentflow.rentals/webhook-test/79c0340c-e42c-4ce6-bd78-48b79f76aa78",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

---

## 🧪 Тестирование:

### 1. Открыть бота в Telegram

Найдите своего бота и откройте чат

### 2. Отправить запрос

**Примеры запросов:**

```
Покажи машины в Батуми до 50 долларов
```

```
Найди автомат на 9-10 ноября
```

```
Кроссоверы в Тбилиси
```

```
Машины в батуми с автоматом до 40 долларов
```

### 3. Ожидаемый ответ

AI Agent должен:
1. Распознать запрос
2. Вызвать Tool: Query Car Search с параметрами
3. Tool вызовет API `/api/cars/search`
4. API вернет форматированный ответ
5. AI Agent отправит результат в Telegram

**Пример ответа:**
```
🚗 Нашел 3 машины в Батуми на 9 - 10 ноября:

1. Ford Fiesta (2015)
   📍 Батуми
   ⚙️ Автомат | 5 мест
   💰 96 GEL/день (≈$35.56)

2. Honda Fit EX (2015)
   📍 Батуми
   ⚙️ Автомат | 5 мест
   💰 107 GEL/день (≈$39.63)

3. Kia Soul (2013)
   📍 Батуми
   ⚙️ Автомат | 5 мест
   💰 107 GEL/день (≈$39.63)

📊 Цены: от 96 до 135 GEL
```

---

## 🔍 Отладка:

### Если бот не отвечает:

1. **Проверить Jarvis API:**
   ```bash
   curl http://localhost:3000/health
   ```
   
   Должен вернуть: `{"status":"ok"}`

2. **Проверить Car Search API:**
   ```bash
   curl -X POST http://localhost:3000/api/cars/search \
     -H "Content-Type: application/json" \
     -d '{"branch":"batumi","maxPriceUSD":50}'
   ```
   
   Должен вернуть список машин

3. **Проверить workflow активирован:**
   - https://n8n.rentflow.rentals/workflow/z1b7wIj17ppMuU7a
   - Toggle **"Active"** должен быть включен

4. **Проверить webhook установлен:**
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
   ```

5. **Проверить executions:**
   - https://n8n.rentflow.rentals/workflow/z1b7wIj17ppMuU7a/executions
   - Найдите последний запуск
   - Проверьте ошибки в нодах

### Если AI Agent не вызывает Tool:

1. **Проверить system message:**
   - Откройте AI Agent ноду
   - Должен быть system message с инструкциями

2. **Проверить toolDescription:**
   - Откройте Tool ноду
   - Должно быть: "Используй этот инструмент ДЛЯ ЛЮБОГО запроса..."

3. **Проверить OpenAI credentials:**
   - Model: `gpt-4o-mini`
   - Temperature: `0.3`
   - API Key валиден

### Если Tool возвращает ошибку:

1. **Проверить URL в Tool:**
   - Должен быть: `http://localhost:3000/api/cars/search`
   - НЕ https!

2. **Проверить Jarvis API запущен:**
   ```bash
   # Проверить процесс
   netstat -ano | findstr :3000
   
   # Запустить если нет
   cd C:\Users\33pok\geodrive_n8n-agents
   npm run dev
   ```

3. **Проверить логи Jarvis API:**
   - Откройте терминал где запущен `npm run dev`
   - Смотрите логи запросов

---

## 📊 Мониторинг:

### Executions

Посмотреть все запуски:
```
https://n8n.rentflow.rentals/workflow/z1b7wIj17ppMuU7a/executions
```

### Логи API

```bash
# В терминале где запущен Jarvis API
# Вы увидите:
POST /api/cars/search
{ branch: 'batumi', maxPriceUSD: 50, ... }
200 OK - 3 cars found
```

### Тестирование API напрямую

```bash
# Без дат (все доступные машины)
curl -X POST http://localhost:3000/api/cars/search \
  -H "Content-Type: application/json" \
  -d '{"branch":"batumi","maxPriceUSD":50}'

# С датами (проверка доступности)
curl -X POST http://localhost:3000/api/cars/search \
  -H "Content-Type: application/json" \
  -d '{
    "branch":"batumi",
    "startDate":"2025-11-09",
    "endDate":"2025-11-10",
    "maxPriceUSD":50,
    "transmission":"Автомат"
  }'
```

---

## 🎯 Доступные фильтры:

### Обязательные:
- `branch` - код филиала (tbilisi, batumi, kutaisi)

### Опциональные:
- `startDate` - дата начала (YYYY-MM-DD)
- `endDate` - дата окончания (YYYY-MM-DD)
- `maxPriceUSD` - максимум в долларах
- `maxPriceGEL` - максимум в лари
- `transmission` - 'Автомат' | 'Механика'
- `yearFrom` - год выпуска от
- `yearTo` - год выпуска до
- `carClass` - класс авто
- `carType` - тип кузова
- `seats` - минимум мест
- `driveUnit` - привод

---

## 📝 Примеры запросов для AI:

### Простые:
- "Покажи машины в Батуми"
- "Что есть в Тбилиси?"
- "Машины в Кутаиси"

### С ценой:
- "Машины до 50 долларов"
- "Найди что-то дешевое"
- "До 100 гел в день"

### С датами:
- "На 9-10 ноября"
- "С 15 по 20 декабря"
- "На эти выходные"

### С характеристиками:
- "Автомат в Батуми"
- "Кроссоверы"
- "Новые машины (от 2015 года)"
- "С автоматом до 40 долларов"

### Комбинированные:
- "Покажи машины в Батуми до 50 долларов на 9-10 ноября"
- "Найди автомат в Тбилиси от 2015 года"
- "Кроссоверы в Батуми с автоматом до 60 долларов"

---

## ✅ Чек-лист:

### Создание:
- [x] Query Car Search Tool - создан ✅
- [x] Car Search AI Assistant - создан ✅
- [x] Валидация пройдена ✅
- [x] OpenAI credentials подключены ✅
- [x] Telegram credentials подключены ✅

### Перед запуском:
- [ ] Jarvis API запущен (`npm run dev`)
- [ ] API endpoint работает (`curl http://localhost:3000/api/cars/search`)
- [ ] Workflow активирован (toggle Active)
- [ ] Telegram webhook настроен
- [ ] Webhook проверен (`getWebhookInfo`)

### Тестирование:
- [ ] Простой запрос ("Покажи машины в Батуми")
- [ ] С фильтрами ("До 50 долларов")
- [ ] С датами ("На 9-10 ноября")
- [ ] Комбинированный запрос
- [ ] Проверить executions в n8n
- [ ] Проверить логи API

---

## 🎉 Готово!

**Car Search AI Assistant** создан и готов к запуску!

**Что осталось:**
1. Запустить Jarvis API (`npm run dev`)
2. Активировать workflow (1 клик)
3. Настроить Telegram webhook (1 команда)
4. Протестировать (5 минут)

**Документация:**
- Car Search Module: `docs/CAR_SEARCH_MODULE.md`
- Исходный код: `src/modules/car-search/`
- API endpoints: `src/api/car-search.ts`

---

**Последнее обновление:** 2025-11-08 12:35  
**Версия:** 1.0.0 (Production Ready)  
**Статус:** ✅ Готов к использованию

