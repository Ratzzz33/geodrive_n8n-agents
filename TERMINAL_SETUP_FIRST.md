# ⚠️ ВАЖНО: Сначала настройте терминал!

Перед запуском деплоя **обязательно настройте терминал**, иначе команды будут зависать.

---

## 🔧 Настройка PowerShell (1 минута)

**1. Откройте PowerShell и выполните:**

```powershell
notepad $PROFILE
```

Если появится ошибка "не найден", создайте файл:

```powershell
New-Item -Path $PROFILE -ItemType File -Force
notepad $PROFILE
```

**2. Добавьте в файл:**

```powershell
function Prompt { "PS> " }
```

**3. Сохраните (Ctrl+S) и закройте Notepad**

**4. Перезапустите PowerShell**

**5. Проверьте - промпт должен стать:**

```
PS> 
```

Вместо длинного:
```
C:\Users\33pok\geodrive_n8n-agents>
```

---

## ✅ После настройки

**Запустите деплой двойным кликом:**

```
final_commit_deploy.bat
```

Это автоматически:
1. ✅ Закоммитит все изменения
2. ✅ Запушит на GitHub  
3. ✅ Задеплоит на сервер
4. ✅ Запустит HTTP Scraper service
5. ✅ Покажет результат

---

## 📋 Что увидите при успехе:

```
✅ SUCCESS! Deployed on port 3002

Next: Update n8n workflow URL to http://172.17.0.1:3002
```

---

**Сначала настройте терминал, потом запускайте `final_commit_deploy.bat`!** 🎯


