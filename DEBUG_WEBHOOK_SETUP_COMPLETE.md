# ✅ Отладочное уведомление добавлено

**Дата:** 2025-11-02  
**Статус:** ✅ Выполнено

---

## Что сделано

### 1. ✅ Добавлена Debug нода Telegram

**Позиция:** Сразу после ноды "Webhook", перед "If Error"

**Функция:** Отправляет уведомление в Telegram при **каждом** получении вебхука от RentProg

**Сообщение содержит:**
- 🔔 Время получения
- Branch (филиал)
- Type (тип события)
- Полный JSON запроса для отладки

**Поток:**
```
Webhook → Debug: Webhook Received → If Error → Save Event → Respond
```

---

## Проверка работы

### Тестовый вебхук отправлен

**Время:** 2025-11-02 21:59:22  
**Статус HTTP:** 200 OK  
**Ответ:** `{"ok":true,"received":true}`

### Что проверить сейчас

1. **Telegram уведомление:**
   - Должно прийти сообщение с префиксом "🔔 DEBUG: Webhook получен от RentProg"
   - Содержит полный JSON запроса
   - Если сообщение пришло → вебхук доходит до n8n ✅
   - Если НЕ пришло → проблема с подключением Telegram или переменной окружения

2. **Execution в n8n:**
   - Откройте workflow "RentProg Webhooks Monitor"
   - Перейдите в "Executions"
   - Последний execution должен быть успешным
   - Проверьте данные ноды "Debug: Webhook Received"

3. **Запись в БД:**
   - Должна появиться запись в таблице `events`
   - Проверить командой: `node setup/check_test_event.mjs`

---

## Если вебхуки от RentProg не приходят

### Проверка URL в RentProg

**Правильный URL для всех филиалов:**
```
https://webhook.rentflow.rentals
```

⚠️ **Важно:**
- Без слэша в конце
- Без пути `/webhook/rentprog-webhook`
- Просто домен: `https://webhook.rentflow.rentals`

### Проверка на сервере

**1. Логи Nginx:**
```bash
ssh root@46.224.17.15
tail -f /var/log/nginx/webhook-access.log
# Должны видеть POST запросы от RentProg IP
```

**2. Логи n8n:**
```bash
docker logs n8n --tail 100 | grep -i webhook
docker logs n8n --tail 100 | grep -i error
```

**3. Проверка Nginx конфигурации:**
```bash
cat /etc/nginx/sites-available/webhook.rentflow.rentals.conf
# Должно быть: proxy_pass http://localhost:5678/webhook/rentprog-webhook;
```

---

## Отключение Debug ноды

После отладки можно:

**Вариант 1: Отключить ноду (не удалять)**
1. Откройте ноду "Debug: Webhook Received"
2. Нажмите кнопку "Disable" (или переключатель)
3. Нода не будет выполняться, но останется в workflow

**Вариант 2: Удалить ноду**
1. Откройте workflow
2. Удалите ноду "Debug: Webhook Received"
3. Подключите "Webhook" напрямую к "If Error"
4. Сохраните workflow

---

## Текущий статус

✅ **Workflow активен**  
✅ **Debug нода добавлена**  
✅ **Credentials назначены** (Postgres account, Telegram account)  
✅ **Error handling настроен**  
✅ **Тестовый вебхук отправлен**

**Следующий шаг:** Проверьте Telegram - должно прийти уведомление с данными вебхука!

---

**Отчет подготовлен:** 2025-11-02 21:59:30
