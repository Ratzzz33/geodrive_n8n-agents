# Проблема с официальным n8n-mcp сервером

## ❌ Проблема

Сервер запускается, но сразу закрывается:
```
Starting new stdio process with command: cmd /c ...
Client closed for command
No server info found
```

## 🔍 Причина

1. `cmd /c` завершает процесс сразу после запуска команды
2. npx выполняется, но не остается в режиме stdio для MCP протокола
3. MCP ожидает, что процесс будет держать stdin/stdout открытыми для коммуникации

## ✅ Решение

Создан wrapper-скрипт `n8n-mcp-official-wrapper.bat`, который правильно запускает npx без немедленного завершения.

### Новая конфигурация:

```json
"n8n-mcp-official": {
  "command": "C:\\Users\\33pok\\geodrive_n8n-agents\\mcp-server\\n8n-mcp-official-wrapper.bat",
  "args": [],
  "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents",
  "env": {
    "MCP_MODE": "stdio",
    "LOG_LEVEL": "error",
    "DISABLE_CONSOLE_OUTPUT": "true",
    "N8N_API_URL": "https://n8n.rentflow.rentals/api/v1",
    "N8N_API_KEY": "ваш_api_ключ"
  }
}
```

## 📝 Wrapper скрипт

Создан файл `mcp-server/n8n-mcp-official-wrapper.bat`:
```batch
@echo off
C:\nvm4w\nodejs\npx.cmd -y n8n-mcp
```

Этот скрипт:
- Не использует `cmd /c` (который закрывается сразу)
- Запускает npx напрямую
- Сохраняет stdio канал для MCP протокола

## 🔄 Что делать сейчас

1. **Перезапустите Cursor полностью**
2. **Проверьте логи** - теперь не должно быть "Client closed for command"
3. **Проверьте Tools & MCP** - сервер должен появиться с инструментами

## 🧪 Проверка

После перезапуска Cursor проверьте логи `n8n-mcp-official`:
- Должно быть: `Server started` или подобное
- НЕ должно быть: `Client closed for command` сразу после старта

## 🎯 Альтернативное решение (если не поможет)

Если wrapper тоже не сработает, можно установить n8n-mcp локально:

```bash
cd mcp-server
npm install n8n-mcp --save-dev
```

И запускать через node напрямую:
```json
"command": "C:\\nvm4w\\nodejs\\node.exe",
"args": [
  "C:\\Users\\33pok\\geodrive_n8n-agents\\mcp-server\\node_modules\\n8n-mcp\\dist\\index.js"
]
```

Но сначала попробуйте с wrapper-скриптом.

