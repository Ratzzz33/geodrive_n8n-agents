# ⚠️ Запуск обновления в Windows CMD

**Проблема:** Cursor Terminal блокирует вывод Python SSH команд.

**Решение:** Запустить в обычном Windows CMD или PowerShell.

---

## 🚀 Инструкция

### Способ 1: Windows CMD (Рекомендуется)

1. Нажми `Win + R`
2. Введи `cmd` и нажми Enter
3. Выполни:

```cmd
cd C:\Users\33pok\geodrive_n8n-agents
python setup\quick_update.py
```

### Способ 2: PowerShell

1. Нажми `Win + X`
2. Выбери "Windows PowerShell"
3. Выполни:

```powershell
cd C:\Users\33pok\geodrive_n8n-agents
python setup\quick_update.py
```

---

## 📋 Что произойдет

Скрипт автоматически:
1. ✅ Подключится к серверу
2. ✅ Обновит код (`git pull`)
3. ✅ Установит Chromium (~2-3 минуты)
4. ✅ Перезапустит сервис
5. ✅ Протестирует работу

**Ожидаемый вывод в конце:**
```
✅ Playwright service is working!
{"success":true,"events":[...]}

==================================================
✅ SETUP COMPLETE!
==================================================
```

---

## ⚠️ Если не работает

### Вариант А: Запуск напрямую на сервере

```cmd
ssh root@46.224.17.15
# Пароль: Geodrive2024SecurePass

cd /root/geodrive_n8n-agents
git pull
npx playwright install chromium
pm2 restart playwright-service
curl http://localhost:3001/health
```

### Вариант Б: GitHub Actions (TODO)

Можно создать GitHub Action для автоматического деплоя.

---

## 📝 После успешного обновления

Открой `FINAL_SETUP_STEPS.md` и выполни настройку n8n workflows в UI.

---

**Файлы для выполнения:**
- ✅ `setup/quick_update.py` - готов
- ✅ `setup/server_ssh.py` - исправлен (убрано переопределение stdout)

