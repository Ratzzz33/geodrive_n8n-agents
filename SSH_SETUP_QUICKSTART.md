# 🔐 Быстрая настройка SSH ключей

## 🚀 Запуск (1 команда)

```bash
cd C:\Users\33pok\geodrive_n8n-agents
python setup/setup_ssh_keys.py
```

Скрипт автоматически:
1. ✅ Создаст SSH ключ (если его нет)
2. ✅ Скопирует ключ на сервер
3. ✅ Протестирует подключение
4. ✅ Отключит пароль (после подтверждения)

---

## 📋 Что происходит

### Шаг 1: Создание ключа
- Проверяет наличие ключа в `~/.ssh/id_rsa`
- Если нет - создает новый (RSA 2048 бит)

### Шаг 2: Копирование на сервер
- Подключается с паролем
- Добавляет публичный ключ в `~/.ssh/authorized_keys` на сервере

### Шаг 3: Тестирование
- Пробует подключиться с ключом
- Проверяет, что всё работает

### Шаг 4: Отключение пароля
- Спрашивает подтверждение
- Настраивает `/etc/ssh/sshd_config`
- Перезагружает SSH сервис

---

## ✅ После настройки

**Подключение:**
```bash
ssh root@46.224.17.15
# Теперь без пароля!
```

**server_ssh.py автоматически использует ключ:**
```python
from setup.server_ssh import ServerSSH
ssh = ServerSSH()
ssh.connect()  # Использует ключ автоматически
```

---

## ⚠️ Важно

1. **Сохраните приватный ключ** (`~/.ssh/id_rsa`) в безопасном месте
2. **Если потеряете ключ** - доступ к серверу будет невозможен
3. **Перед отключением пароля** скрипт проверит, что ключ работает

---

## 🛠️ Если что-то пошло не так

**Проблема: Ключ не работает**
```bash
# Пересоздать ключ
python setup/setup_ssh_keys.py
```

**Проблема: Заблокирован доступ**
- Если есть доступ через консоль сервера (не SSH):
  ```bash
  # Восстановить пароль
  sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
  systemctl restart sshd
  ```

---

## 📚 Подробная документация

См. [docs/SSH_KEYS_SETUP.md](./docs/SSH_KEYS_SETUP.md) для детальной инструкции.

---

**Готово! Запустите скрипт и следуйте инструкциям.** 🎉

