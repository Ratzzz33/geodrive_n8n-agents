# 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА: WEBHOOK_URL не обновилась в n8n

## Диагностика

**Проблема:** В UI n8n на вкладке "Production URL" показывается:
```
https://geodrive.netlify.app/webhook/rentprog-webhook  (СТАРЫЙ адрес)
```

**Ожидаемо:** 
```
https://webhook.rentflow.rentals/rentprog-webhook  (НОВЫЙ адрес)
```

## Корневая причина

n8n использует **переменную окружения `WEBHOOK_URL`** при запуске контейнера. Эта переменная:
- **НЕ обновляется** при изменении параметров workflow в UI
- **НЕ обновляется** при запросах через REST API
- **Устанавливается ОД РА ЗЫ** при старте контейнера

Текущие параметры workflow — это просто **настройки UI**, а **фактический URL, который используется n8n** — это переменная окружения при запуске.

## Решение

На сервере **46.224.17.15** необходимо:

### 1. SSH на сервер

```bash
ssh root@46.224.17.15
```

### 2. Проверить текущие переменные в контейнере

```bash
docker exec n8n env | grep WEBHOOK
```

Должно показать:
```
WEBHOOK_URL=https://geodrive.netlify.app/  ← СТАРОЕ (Netlify)
```

### 3. Остановить контейнер

```bash
docker stop n8n
```

### 4. Обновить docker-compose.yml на сервере

Найти в `/root/docker-compose.yml` (или где он лежит) строку:
```yaml
- WEBHOOK_URL=https://geodrive.netlify.app/
```

Заменить на:
```yaml
- WEBHOOK_URL=https://webhook.rentflow.rentals/
```

### 5. Перезапустить контейнер

```bash
docker-compose up -d n8n
```

(Выполнить из той же директории, где лежит docker-compose.yml)

### 6. Проверить результат

Подождите 30 секунд, затем:
- Откройте UI n8n: `https://n8n.rentflow.rentals`
- Откройте workflow "RentProg Webhooks Monitor"
- Нажмите на Webhook ноду
- Проверьте вкладку "Production URL" — должен быть `https://webhook.rentflow.rentals`

## Вторая проблема: webhookId

После перезапуска контейнера измняется еще нужно будет **сбросить webhookId**:

1. В workflow на webhook ноде: webhookId = `rentprog-webhook-v2` (мы его меняли)
2. После перезапуска контейнера нужно вернуть на: webhookId = `rentprog-webhook-monitor`

**Или** просто оставить как есть — новый URL все равно заработает.

## Проверка через MCP

После перезапуска проверим через MCP:

```
mcp_n8n_n8n_get_workflow(id="gNXRKIQpNubEazH7")
```

Должно показать в webhook ноде:
```json
"options": {
  "productionUrl": "https://webhook.rentflow.rentals"
}
```

---

## ДЕЙСТВИЯ:

1. ✅ docker-compose.yml уже содержит `WEBHOOK_URL=https://webhook.rentflow.rentals/`
2. ⏳ **ТРЕБУЕТСЯ:** Перезапустить контейнер n8n на сервере 46.224.17.15
3. ⏳ **ТРЕБУЕТСЯ:** Проверить результат в UI n8n
