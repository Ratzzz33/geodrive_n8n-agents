# Правила для работы с n8n (2025)

## 🔴 КРИТИЧЕСКИЕ ПРАВИЛА ИМПОРТА WORKFLOW

### ВСЕГДА используй `setup/import_workflow_2025.mjs` для импорта workflow

**НЕ используй старые скрипты:**
- ❌ `setup/import_workflow_working.ps1`
- ❌ `setup/import_new_workflow.mjs`
- ❌ `setup/import_upsert_workflow.mjs`
- ❌ Любые другие старые скрипты импорта

**ПОЧЕМУ:** API n8n изменился в 2025 году. Старые скрипты вызывают ошибки:
- "Could not find workflow"
- "Could not find property option"

### Обязательные поля при создании workflow

**ДОЛЖНЫ быть:**
- `name` (string)
- `nodes` (array)
- `connections` (object)
- `settings` (object с `executionOrder: 'v1'`)

**НЕ ДОЛЖНЫ быть (удаляй перед импортом):**
- `id`
- `versionId`
- `updatedAt`
- `createdAt`
- `triggerCount`
- `meta`
- `staticData`
- `pinData`
- `tags`
- `ownerId`
- `sharedWithProjects`

### Обработка нод

**Удаляй из нод:**
- `id` (генерируется автоматически)
- `notes`, `notesInFlow`
- `disabled`, `continueOnFail`, `alwaysOutputData`
- `executeOnce`, `maxTries`, `waitBetweenTries`, `retryOnFail`

**webhookId:**
- Оставляй только для webhook нод (`n8n-nodes-base.webhook`)
- Для остальных нод - удаляй

**Credentials:**
- Формат: `{ id: "..." }` или `{ name: "..." }`
- Если нет ни id ни name - удаляй credentials
- При обновлении существующего workflow - восстанавливай credentials из текущего workflow

**Options:**
- Удаляй пустые вложенные объекты в `parameters.options`
- Очищай структуру: `options.response.response.responseFormat` → удалить полностью

**Специфичные правила для нод:**

1. **Schedule Trigger (typeVersion 1.2):**
   - Используй `cronExpression` вместо `hours` с `hoursInterval`
   - Формат: `"expression": "0 8-23 * * *"` (каждый час с 8 до 23)

2. **HTTP Request (typeVersion 4.2):**
   - НЕ используй вложенный `options.response.response.responseFormat`
   - Используй только `options.timeout` или убирай `response` полностью

3. **IF нода (typeVersion 2):**
   - ОБЯЗАТЕЛЬНО добавляй `id` к каждому условию в массиве `conditions`
   - Формат: `{ "id": "unique-id", "leftValue": "...", ... }`

### Обновление существующего workflow

**Порядок действий:**
1. Получи текущий workflow через GET `/workflows/{id}`
2. Восстанови credentials из существующих нод
3. Обнови workflow через PUT `/workflows/{id}` с очищенными данными

### Проверка перед импортом

**Всегда проверяй:**
- Workflow файл валидный JSON
- Все обязательные поля присутствуют
- Системные поля удалены
- Options очищены от пустых объектов

---

## 📋 ПРАВИЛА РАБОТЫ С n8n API

### Используй MCP инструменты когда возможно

**Приоритет:**
1. ✅ `mcp_n8n_n8n_...` (основной MCP сервер)
2. ✅ `mcp_n8n-mcp-official_...` (резервный, 271+ инструментов)
3. ⚠️ REST API только если MCP недоступен

### API ключ

**Где взять:**
- https://n8n.rentflow.rentals
- Settings → API → Create API Key

**Текущий ключ (до 2025-12-02):**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI3ZDYyYjM3My0yMDFiLTQ3ZjMtODU5YS1jZGM2OWRkZWE0NGEiLCJpc3MiOiJuOG4iLCJhdWQiOiJwdWJsaWMtYXBpIiwiaWF0IjoxNzYyMDg0MjY4LCJleHAiOjE3NjQ2NTE2MDB9.gsdxltowlQShNi9mil074-cMhnuJJLI5lN6MP7FQEcI
```

### Endpoints

**Base URL:** `https://n8n.rentflow.rentals/api/v1`

**Основные endpoints:**
- `GET /workflows` - список workflow
- `GET /workflows/{id}` - получить workflow
- `POST /workflows` - создать workflow
- `PUT /workflows/{id}` - обновить workflow
- `POST /workflows/{id}/activate` - активировать
- `POST /workflows/{id}/deactivate` - деактивировать

---

## 🎯 ПРАВИЛА СТРУКТУРЫ WORKFLOW

### Минимальная структура для API

```json
{
  "name": "Workflow Name",
  "nodes": [...],
  "connections": {...},
  "settings": {
    "executionOrder": "v1"
  }
}
```

### Settings

**Обязательно:**
- `executionOrder: "v1"` (или `"v0"` для legacy)

**Для Service Center Processor:**
- `executionOrder: "v0"` (legacy) - НЕ МЕНЯТЬ!
- `timezone: "Asia/Tbilisi"`
- `timeoutWorkflow: true`
- `timeoutAfter: "1 hour"`
- ⚠️ **НЕ использовать `errorWorkflow`** — эта настройка больше не используется (вызывала ошибки "Could not find workflow")

### Connections

**Формат:**
```json
{
  "Node Name": {
    "main": [[{ "node": "Next Node", "type": "main", "index": 0 }]]
  }
}
```

**Важно:** Используй имена нод (name), не id!

---

## ⚠️ ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ

### Ошибка: "must have required property 'settings'"

**Решение:** Добавь `settings: { executionOrder: 'v1' }`

### Ошибка: "must NOT have additional properties"

**Решение:** Удали системные поля (id, versionId, updatedAt, createdAt)

### Ошибка: "Could not find property option"

**Решение:** Очисти пустые объекты в `parameters.options`

### Ошибка: "propertyValues[itemName] is not iterable"

**Причина:** Неправильная структура параметров нод

**Решения:**
1. **Schedule Trigger (typeVersion 1.2):**
   - ❌ НЕ использовать: `hours: { start: 8, end: 23 }` с `hoursInterval`
   - ✅ Использовать: `cronExpression: "0 8-23 * * *"` (каждый час с 8 до 23)
   ```json
   "rule": {
     "interval": [{
       "field": "cronExpression",
       "expression": "0 8-23 * * *"
     }]
   }
   ```

2. **HTTP Request (typeVersion 4.2):**
   - ❌ НЕ использовать: `options.response.response.responseFormat`
   - ✅ Использовать: только `options.timeout` или убрать `response` из `options`
   ```json
   "options": {
     "timeout": 3600000
   }
   ```

3. **IF нода (typeVersion 2):**
   - ✅ ОБЯЗАТЕЛЬНО добавлять `id` к каждому условию
   ```json
   "conditions": [{
     "id": "check-errors",
     "leftValue": "={{ $json.errors }}",
     "rightValue": 0,
     "operator": { "type": "number", "operation": "gt" }
   }]
   ```

### Ошибка: "401 Unauthorized"

**Решение:** Проверь API ключ, получи новый если истек

### Ошибка: "Could not find workflow"

**Решение:** 
1. Проверь что workflow файл валидный JSON
2. Используй `setup/import_workflow_2025.mjs`
3. Проверь что все обязательные поля присутствуют

---

## 🔧 ПРАВИЛА ОБРАБОТКИ CREDENTIALS

### При создании нового workflow

**Если credentials есть в файле:**
- Оставь только `{ name: "..." }` (n8n найдет по имени)
- Или `{ id: "..." }` если знаешь точный id

**Если credentials нет:**
- Удали поле `credentials` из ноды
- Credentials нужно будет привязать вручную через UI

### При обновлении существующего workflow

**ВСЕГДА:**
1. Получи текущий workflow через GET
2. Извлеки credentials из существующих нод
3. Примени credentials к новым нодам (по имени/типу)
4. Обнови workflow с сохраненными credentials

**Это критично!** Иначе credentials будут потеряны при обновлении.

---

## 📝 ПРАВИЛА ИМПОРТА

### Перед импортом

1. ✅ Проверь что используешь `setup/import_workflow_2025.mjs`
2. ✅ Удали все системные поля из workflow JSON
3. ✅ Очисти options от пустых объектов
4. ✅ Проверь credentials формат
5. ✅ Убедись что `settings.executionOrder` присутствует

### После импорта

1. ✅ Проверь что workflow создан/обновлен в UI
2. ✅ Проверь что все ноды на месте
3. ✅ Проверь connections
4. ✅ Проверь что credentials привязаны (если были)
5. ✅ Активируй workflow если нужно

---

## 🚫 ЧТО НИКОГДА НЕ ДЕЛАТЬ

1. ❌ НЕ используй старые скрипты импорта
2. ❌ НЕ оставляй системные поля (id, versionId, etc.)
3. ❌ НЕ оставляй пустые options объекты
4. ❌ НЕ теряй credentials при обновлении workflow
5. ❌ НЕ импортируй workflow без проверки структуры
6. ❌ НЕ используй REST API если доступен MCP
7. ❌ НЕ хардкоди API ключи в коде (используй ENV)
8. ❌ **НЕ используй `errorWorkflow` в settings** — эта настройка больше не используется (вызывала ошибки "Could not find workflow")

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- [N8N_IMPORT_2025_FIX.md](./N8N_IMPORT_2025_FIX.md) - Детальное описание исправлений
- [IMPORTANT_MEMORIES.md](./IMPORTANT_MEMORIES.md) - Важные заметки
- [claude.md](./claude.md) - Полная документация

---

## 🤖 ПРАВИЛА ДЛЯ AI AGENT НОД

**ВАЖНО:** См. [.cursorrules-ai-agents](./.cursorrules-ai-agents) для полных правил работы с AI Agent нодами.

### Критичные правила

1. **Все AI Agent работают как Tools Agent** (с v1.82.0)
2. **ОБЯЗАТЕЛЬНО:** Минимум один tool подключен через порт `ai_tool`
3. **Chat Trigger:** Используй `responseMode = "Using Response Nodes"` с `Respond to Chat`
4. **Memory:** Настраивай `sessionKey` для изоляции сессий
5. **Output Parser:** Используй Structured Output Parser (не Auto-fixing, он deprecated)

### Базовые паттерны

- **Чат:** `Chat Trigger → AI Agent → Respond to Chat`
- **HTTP API:** `Webhook → AI Agent → Respond to Webhook`
- **Под-агенты:** `AI Agent → AI Agent Tool → tools`

---

**ПОМНИ:** 
- При работе с n8n workflow ВСЕГДА используй `setup/import_workflow_2025.mjs`!
- **НЕ используй `errorWorkflow` в settings** — эта настройка удаляется автоматически при импорте

