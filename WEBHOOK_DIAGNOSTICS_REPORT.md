# 🔍 Отчет: Диагностика проблем с вебхуками RentProg

**Дата:** 2025-01-15  
**Задача:** Найти причину отсутствия вебхуков от RentProg

---

## ✅ Созданные инструменты

### 1. Скрипт автоматической диагностики
**Файл:** `check_rentprog_webhook_issues.sh`

**Что проверяет:**
- ✅ DNS резолюция домена `webhook.rentflow.rentals`
- ✅ Доступность домена (HTTP/HTTPS)
- ✅ SSL сертификат (действительность и срок действия)
- ✅ Статус Nginx (запущен ли, правильность конфигурации)
- ✅ Статус n8n контейнера (запущен ли)
- ✅ Доступность n8n webhook endpoint
- ✅ Логи Nginx (последние запросы и ошибки)
- ✅ Логи n8n (ошибки и предупреждения)
- ✅ Файрвол (открыты ли порты 80 и 443)
- ✅ Тестовый запрос к webhook
- ✅ Переменные окружения n8n

**Использование:**
```bash
cd /workspace
./check_rentprog_webhook_issues.sh
```

---

### 2. Подробное руководство по диагностике
**Файл:** `WEBHOOK_TROUBLESHOOTING.md`

**Содержание:**
- 📋 Общая информация о конфигурации
- 🚨 10 основных причин отсутствия вебхуков с подробными решениями
- 🔧 Инструкции по быстрой диагностике
- 📝 Пошаговая проверка всех компонентов
- 📊 Рекомендации по мониторингу
- ✅ Чеклист для проверки
- 🎯 Частые ошибки и их решения

---

### 3. Краткая сводка
**Файл:** `WEBHOOK_ISSUES_SUMMARY.md`

**Содержание:**
- Топ-5 самых частых причин отсутствия вебхуков
- Быстрые решения для каждой проблемы
- Пошаговая проверка
- Рекомендации по мониторингу

---

## 🔍 Архитектура вебхуков

```
RentProg → https://webhook.rentflow.rentals → Nginx → n8n (localhost:5678/webhook/rentprog-webhook)
```

**Правильный URL для RentProg:**
```
https://webhook.rentflow.rentals
```

**Важно:**
- ❌ Без слэша в конце (`/`)
- ❌ Без пути (`/webhook/rentprog-webhook`)
- ✅ Просто домен: `https://webhook.rentflow.rentals`

---

## 🚨 Топ-5 причин отсутствия вебхуков

### 1. ❌ Неправильный URL в RentProg (самая частая причина)
**Проблема:** В настройках RentProg указан неправильный адрес.

**Решение:** Убедитесь что URL: `https://webhook.rentflow.rentals` (без слэша и пути)

### 2. ❌ Workflow не активен в n8n
**Проблема:** Workflow "RentProg Webhooks Monitor" отключен.

**Решение:** Активируйте workflow в n8n UI (http://46.224.17.15:5678)

### 3. ❌ Nginx не запущен или не настроен
**Проблема:** Nginx не проксирует запросы к n8n.

**Решение:** `sudo systemctl start nginx && sudo systemctl reload nginx`

### 4. ❌ n8n контейнер не запущен
**Проблема:** n8n не работает.

**Решение:** `docker-compose up -d n8n`

### 5. ❌ DNS не резолвится или SSL сертификат истек
**Проблема:** Домен недоступен или SSL недействителен.

**Решение:** Проверьте DNS A-запись и обновите SSL сертификат через certbot

---

## 📋 Текущая конфигурация

### Nginx конфигурация
**Файл:** `/etc/nginx/sites-available/webhook.rentflow.rentals.conf`
- Проксирует на: `http://localhost:5678/webhook/rentprog-webhook`
- SSL сертификат: Let's Encrypt
- Логи: `/var/log/nginx/webhook-access.log` и `webhook-error.log`

### n8n Workflow
**Имя:** "RentProg Webhooks Monitor"
**ID:** gNXRKIQpNubEazH7
- Webhook path: `rentprog-webhook`
- Production URL: `https://webhook.rentflow.rentals`
- Webhook ID: `rentprog-webhook-monitor`

### Docker Compose
**n8n контейнер:**
- Порт: `5678:5678`
- Переменные окружения:
  - `WEBHOOK_URL=https://webhook.rentflow.rentals`
  - `N8N_WEBHOOK_URL=https://webhook.rentflow.rentals`

---

## 🔧 Как использовать диагностику

### Шаг 1: Запустите автоматическую диагностику
```bash
cd /workspace
./check_rentprog_webhook_issues.sh
```

Скрипт покажет:
- ✅ Что работает правильно
- ❌ Что требует исправления
- 💡 Рекомендации по исправлению

### Шаг 2: Проверьте настройки в RentProg
1. Войдите в RentProg UI для каждого филиала
2. Настройки компании → Webhooks
3. Убедитесь что URL: `https://webhook.rentflow.rentals` (без слэша и пути)
4. Метод: POST, Формат: JSON

### Шаг 3: Проверьте workflow в n8n
1. Откройте http://46.224.17.15:5678
2. Найдите workflow "RentProg Webhooks Monitor"
3. Убедитесь что он **Active**
4. Проверьте Webhook node:
   - Path: `rentprog-webhook`
   - Production URL: `https://webhook.rentflow.rentals`

### Шаг 4: Отправьте тестовый запрос
```bash
curl -X POST https://webhook.rentflow.rentals \
  -H "Content-Type: application/json" \
  -d '{"event":"test","payload":"{\"id\":\"test_123\"}"}'
```

### Шаг 5: Проверьте executions в n8n
1. Откройте workflow "RentProg Webhooks Monitor" → Executions
2. Если executions есть → вебхуки доходят ✅
3. Если executions нет → проблема в Nginx/DNS ❌

---

## 📊 Мониторинг вебхуков

### Логи Nginx
```bash
# Последние запросы
sudo tail -f /var/log/nginx/webhook-access.log

# Ошибки
sudo tail -f /var/log/nginx/webhook-error.log
```

### Логи n8n
```bash
# Последние логи
docker logs n8n --tail 50 -f

# Только ошибки
docker logs n8n --tail 100 2>&1 | grep -i error
```

### Executions в n8n
- Откройте workflow → Executions
- Проверьте последние executions
- Если executions есть → вебхуки доходят ✅

---

## 📚 Документация

### Созданные файлы:
1. **check_rentprog_webhook_issues.sh** - Скрипт автоматической диагностики
2. **WEBHOOK_TROUBLESHOOTING.md** - Подробное руководство по диагностике
3. **WEBHOOK_ISSUES_SUMMARY.md** - Краткая сводка основных проблем
4. **WEBHOOK_DIAGNOSTICS_REPORT.md** - Этот отчет

### Существующие файлы:
- `nginx/webhook.rentflow.rentals.conf` - Конфигурация Nginx
- `n8n-workflows/rentprog-webhooks-monitor.json` - Workflow n8n
- `RENTPROG_WEBHOOK_URLS.md` - Документация по настройке вебхуков
- `FINAL_WEBHOOKS_UPDATE_2025-01-15.md` - Отчет об обновлении вебхуков

---

## ✅ Следующие шаги

1. **Запустите диагностику:**
   ```bash
   cd /workspace
   ./check_rentprog_webhook_issues.sh
   ```

2. **Проверьте настройки в RentProg:**
   - URL должен быть: `https://webhook.rentflow.rentals` (без слэша и пути)
   - Проверьте для всех филиалов

3. **Проверьте workflow в n8n:**
   - Должен быть Active
   - Webhook node должен иметь правильный путь

4. **Мониторьте логи:**
   ```bash
   sudo tail -f /var/log/nginx/webhook-access.log
   docker logs n8n --tail 50 -f
   ```

5. **Отправьте тестовый запрос:**
   ```bash
   curl -X POST https://webhook.rentflow.rentals \
     -H "Content-Type: application/json" \
     -d '{"event":"test","payload":"{\"id\":\"test_123\"}"}'
   ```

---

## 🎯 Итог

Созданы инструменты для диагностики проблем с вебхуками RentProg:

✅ **Скрипт автоматической диагностики** - проверяет все компоненты системы  
✅ **Подробное руководство** - описание всех возможных проблем и решений  
✅ **Краткая сводка** - быстрый справочник по основным проблемам  
✅ **Итоговый отчет** - этот документ

**Самая частая причина отсутствия вебхуков:**
- ❌ Неправильный URL в RentProg (со слэшем, с путем, старый адрес)
- ✅ Решение: Убедитесь что URL: `https://webhook.rentflow.rentals` (без слэша и пути)

---

**Последнее обновление:** 2025-01-15
