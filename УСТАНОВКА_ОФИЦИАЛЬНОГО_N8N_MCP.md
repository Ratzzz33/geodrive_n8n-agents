# Установка официального MCP сервера n8n-mcp (резервный)

## 📦 О проекте

[**n8n-mcp**](https://github.com/czlonkowski/n8n-mcp) - это официальный MCP сервер для n8n от сообщества с **9.4k звёзд** на GitHub.

### Преимущества:

- ✅ **271+ инструментов** для работы с n8n
- ✅ **База данных 541 нод** n8n (100% покрытие)
- ✅ **2,709 шаблонов workflow** доступно для AI
- ✅ **Автоматическая генерация workflow** через AI
- ✅ **Валидация и автоисправление** workflows
- ✅ **Активно поддерживается** сообществом

### Сравнение:

| Функция | Наш сервер | Официальный n8n-mcp |
|---------|-----------|---------------------|
| Инструментов | 12 | 271+ |
| Шаблоны | - | 2,709 |
| Генерация workflow | Ручная | Автоматическая через AI |
| Валидация | Базовая | Продвинутая |
| База нод | - | 541 нод |

## 📋 Установка

### Вариант 1: Через npx (Рекомендуется)

Официальный сервер можно запустить через `npx`, что автоматически скачает и запустит последнюю версию:

**Добавьте в конфигурацию Cursor:**

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "C:\\nvm4w\\nodejs\\node.exe",
      "args": [
        "C:\\Users\\33pok\\geodrive_n8n-agents\\mcp-server\\chrome-devtools-server.js"
      ],
      "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents"
    },
    "n8n": {
      "command": "C:\\nvm4w\\nodejs\\node.exe",
      "args": [
        "C:\\Users\\33pok\\geodrive_n8n-agents\\mcp-server\\n8n-mcp-server.js"
      ],
      "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents",
      "env": {
        "N8N_BASE_URL": "https://n8n.rentflow.rentals",
        "N8N_API_KEY": "ваш_api_ключ"
      }
    },
    "n8n-mcp-official": {
      "command": "C:\\nvm4w\\nodejs\\node.exe",
      "args": ["-y", "n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "LOG_LEVEL": "error",
        "DISABLE_CONSOLE_OUTPUT": "true",
        "N8N_API_URL": "https://n8n.rentflow.rentals/api/v1",
        "N8N_API_KEY": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI3ZDYyYjM3My0yMDFiLTQ3ZjMtODU5YS1jZGM2OWRkZWE0NGEiLCJpc3MiOiJuOG4iLCJhdWQiOiJwdWJsaWMtYXBpIiwiaWF0IjoxNzYyMDg0MjY4LCJleHAiOjE3NjQ2NTE2MDB9.gsdxltowlQShNi9mil074-cMhnuJJLI5lN6MP7FQEcI"
      }
    }
  }
}
```

**Примечание:** `-y` флаг автоматически скачает пакет, если его нет локально.

### Вариант 2: Установка через npm (для локального использования)

Если хотите установить локально:

```bash
cd mcp-server
npm install n8n-mcp --save-dev
```

Но для MCP лучше использовать npx вариант.

## 🔧 Настройка

### Переменные окружения:

- `N8N_API_URL` - URL вашего n8n с `/api/v1` на конце
- `N8N_API_KEY` - API ключ n8n
- `MCP_MODE` - режим работы (stdio для Cursor)
- `LOG_LEVEL` - уровень логирования (error для минимальных логов)

### API ключ:

API ключ взят из вашей документации (`claude.md`):
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI3ZDYyYjM3My0yMDFiLTQ3ZjMtODU5YS1jZGM2OWRkZWE0NGEiLCJpc3MiOiJuOG4iLCJhdWQiOiJwdWJsaWMtYXBpIiwiaWF0IjoxNzYyMDg0MjY4LCJleHAiOjE3NjQ2NTE2MDB9.gsdxltowlQShNi9mil074-cMhnuJJLI5lN6MP7FQEcI
```

**Действителен до:** 2025-12-02

## 📋 После установки

1. **Добавьте конфигурацию в Cursor** (см. выше)
2. **Перезапустите Cursor полностью**
3. **Проверьте**: `Ctrl+,` → Tools & MCP

Должны появиться:
- ✅ **chrome-devtools** (9 tools)
- ✅ **n8n** (12 tools) - наш сервер
- ✅ **n8n-mcp-official** (271+ tools) - официальный резервный

## 🎯 Когда использовать какой сервер

### Наш сервер (`n8n`):
- Простые операции (список, создание, активация)
- Быстрый отклик
- Минимальные зависимости

### Официальный сервер (`n8n-mcp-official`):
- Генерация workflow через AI
- Работа с шаблонами (2,709 доступно)
- Продвинутая валидация
- Автоисправление ошибок
- Полная база всех нод n8n

## 📚 Документация

- **GitHub**: https://github.com/czlonkowski/n8n-mcp
- **Сайт**: https://www.n8n-mcp.com/
- **Документация**: https://github.com/czlonkowski/n8n-mcp#readme

## ✅ Готово!

Официальный MCP сервер готов к использованию как резервный вариант с расширенным функционалом!

