# 🔍 Анализ ошибок синхронизации Tbilisi

**Дата анализа:** 10.11.2025  
**Филиал:** Tbilisi  
**Всего ошибок:** 4

---

## 📊 Результаты проверки

### ✅ Успешно синхронизированы (2 из 4)

#### Бронирование #194178
- ✅ Найдено в `external_refs`
- ✅ Существует в таблице `bookings`
- ✅ Клиент существует в БД
- **Вывод:** Ошибка произошла при первой попытке, но при повторной попытке синхронизация прошла успешно

#### Бронирование #167505
- ✅ Найдено в `external_refs`
- ✅ Существует в таблице `bookings`
- ✅ Клиент существует в БД
- **Вывод:** Ошибка произошла при первой попытке, но при повторной попытке синхронизация прошла успешно

### ❌ Не синхронизированы (2 из 4)

#### Бронирование #450490
- ❌ НЕ найдено в `external_refs`
- ❌ НЕ существует в таблице `bookings`
- **Проблема:** Бронирование не было синхронизировано из-за ошибки FK constraint
- **Причина:** Клиент с указанным `client_id` не существует в БД

#### Бронирование #280489
- ❌ НЕ найдено в `external_refs`
- ❌ НЕ существует в таблице `bookings`
- **Проблема:** Бронирование не было синхронизировано из-за ошибки FK constraint
- **Причина:** Клиент с указанным `client_id` не существует в БД

---

## 🔍 Анализ проблемы

### Причина ошибок

Все 4 ошибки связаны с нарушением внешнего ключа `bookings_client_id_fkey`:
```
insert or update on table "bookings" violates foreign key constraint "bookings_client_id_fkey"
```

Это означает, что:
1. Бронирование ссылается на `client_id`, который не существует в таблице `clients`
2. Функция `resolveByExternalRef` не смогла найти клиента в БД
3. Клиент не был создан автоматически перед созданием бронирования

### Почему 2 из 4 были успешно синхронизированы?

Вероятно, произошло следующее:
1. При первой попытке клиент еще не был синхронизирован → ошибка FK
2. При повторной попытке (возможно, из другого процесса или вебхука) клиент уже был создан → успех

### Почему 2 из 4 не были синхронизированы?

Возможные причины:
1. Клиент был удален из RentProg после создания бронирования
2. Клиент принадлежит другому филиалу и не был найден при поиске
3. Ошибка при разрешении `client_id` через `resolveByExternalRef` (неправильный external_id)
4. Клиент не был синхронизирован ранее и не существует в `external_refs`

---

## 💡 Рекомендации по исправлению

### 1. Улучшить обработку ошибок в `upsertBookingFromRentProg`

**Текущее поведение:** При ошибке FK constraint бронирование не создается

**Предлагаемое решение:**
```typescript
// Если client_id не найден, попытаться загрузить клиента из RentProg
if (!clientId && payload.client_id) {
  try {
    // Загрузить клиента из RentProg API
    const clientData = await fetchClientFromRentProg(payload.client_id, branch);
    if (clientData) {
      // Создать клиента
      clientId = await upsertClientFromRentProg(clientData, branch);
    }
  } catch (error) {
    logger.warn(`Не удалось загрузить клиента ${payload.client_id} для бронирования ${rentprogId}`);
  }
}

// Если клиент все еще не найден, создать бронирование с client_id = NULL
// (если это допустимо бизнес-логикой)
```

### 2. Добавить автоматическую загрузку отсутствующих клиентов

При ошибке FK constraint:
1. Попытаться загрузить клиента из RentProg API по `client_id`
2. Если клиент найден → создать его в БД
3. Если клиент не найден → создать бронирование с `client_id = NULL` (с предупреждением)

### 3. Улучшить логирование

Добавить детальную информацию:
- RentProg ID клиента, который не был найден
- Причина ошибки (клиент не существует / клиент другого филиала / и т.д.)
- Предпринятые действия для исправления

### 4. Проверить проблемные бронирования в RentProg

Создать скрипт для:
- Запроса данных по бронированиям #450490 и #280489 из RentProg API
- Проверки существования указанных клиентов
- Определения причины отсутствия клиентов в БД

---

## 📋 Список проблемных бронирований

| # | RentProg ID | Статус | Причина |
|---|-------------|--------|---------|
| 1 | 450490 | ❌ Не синхронизировано | Клиент не найден в БД |
| 2 | 280489 | ❌ Не синхронизировано | Клиент не найден в БД |
| 3 | 194178 | ✅ Синхронизировано | Ошибка при первой попытке, успех при повторной |
| 4 | 167505 | ✅ Синхронизировано | Ошибка при первой попытке, успех при повторной |

---

## 🎯 Итоговый вывод

**Успешность синхронизации:** 99.6% (1074 из 1078 бронирований)

**Критичность ошибок:** Низкая
- Только 2 бронирования не были синхронизированы (0.2%)
- 2 других были успешно синхронизированы при повторной попытке

**Рекомендации:**
1. ✅ Улучшить обработку ошибок FK constraint
2. ✅ Добавить автоматическую загрузку отсутствующих клиентов
3. ✅ Проверить проблемные бронирования в RentProg API
4. ✅ Улучшить логирование для диагностики

---

**Отчеты сохранены:**
- `TBILISI_ERRORS_REPORT.md` - детальный отчет об ошибках
- `TBILISI_ERRORS_ANALYSIS.md` - анализ и рекомендации

