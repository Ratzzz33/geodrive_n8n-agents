# ✅ Event Links - Реализация завершена

**Дата:** 2025-11-09  
**Статус:** ✅ Готово к деплою

---

## 🎯 Что реализовано

Система автоматического связывания между таблицами `events`, `payments` и `history` для отслеживания одних и тех же процессов из разных источников данных.

---

## 📦 Созданные компоненты

### 1. Миграция БД

**Файл:** `setup/migrations/012_create_event_links.sql`

- ✅ Таблица `event_links` с полями для связей
- ✅ Индексы для быстрого поиска
- ✅ SQL Views: `event_links_stats`, `unlinked_records`
- ✅ Комментарии и документация

### 2. Drizzle ORM схема

**Файл:** `src/db/schema.ts`

- ✅ Добавлена схема `eventLinks`
- ✅ Типы TypeScript: `EventLink`, `EventLinkInsert`

### 3. Функции автоматического связывания

**Файл:** `src/db/eventLinks.ts`

- ✅ `linkPayment()` - связывание платежа с events и history
- ✅ `linkEvent()` - связывание события с payments и history
- ✅ `getLinksForPayment()` - получение связей для платежа
- ✅ `getLinksStats()` - статистика связей
- ✅ Алгоритм определения уверенности (`confidence`)

### 4. API Endpoints

**Файл:** `src/api/routes/eventLinks.ts`

- ✅ `POST /event-links/payment/:paymentId` - связать платеж
- ✅ `POST /event-links/event/:eventId` - связать событие
- ✅ `GET /event-links/payment/:paymentId` - получить связи
- ✅ `GET /event-links/stats` - статистика
- ✅ `GET /event-links/unlinked` - несвязанные записи

### 5. Интеграция в процесс сохранения

**Файл:** `src/db/payments.ts`

- ✅ Автоматическое связывание после сохранения платежа
- ✅ Обработка ошибок (не критично, если не удалось связать)

### 6. Документация

**Файл:** `docs/EVENT_LINKS.md`

- ✅ Полное описание системы
- ✅ Примеры использования
- ✅ API документация

---

## 🚀 Как использовать

### Автоматическое связывание

Связывание происходит автоматически при сохранении платежей через `savePaymentFromRentProg()`.

### Ручное связывание через API

```bash
# Связать платеж
curl -X POST http://localhost:3000/event-links/payment/{paymentId} \
  -H "Content-Type: application/json" \
  -d '{
    "branch": "tbilisi",
    "rpPaymentId": 1843216,
    "paymentDate": "2025-11-09T16:48:00.947+04:00"
  }'

# Получить статистику
curl http://localhost:3000/event-links/stats

# Найти несвязанные записи
curl http://localhost:3000/event-links/unlinked
```

### SQL запросы

```sql
-- Статистика связей
SELECT * FROM event_links_stats;

-- Несвязанные записи
SELECT * FROM unlinked_records;

-- Все связи для платежа
SELECT * FROM event_links WHERE payment_id = '...';
```

---

## 📊 Структура связей

```
┌──────────┐
│  events  │ ← Вебхуки (real-time)
└─────┬────┘
      │
      ├─────────────────┐
      │                 │
      ▼                 ▼
┌──────────┐      ┌──────────┐
│ payments │      │ history │
└──────────┘      └──────────┘
      │                 │
      └────────┬────────┘
               │
               ▼
        ┌─────────────┐
        │ event_links│ ← Связи
        └─────────────┘
```

---

## 🔧 Следующие шаги

1. **Применить миграцию:**
   ```bash
   # Выполнить миграцию в БД
   psql $DATABASE_URL -f setup/migrations/012_create_event_links.sql
   ```

2. **Перезапустить Jarvis API:**
   ```bash
   docker compose restart jarvis-api
   ```

3. **Проверить работу:**
   - Сохранить платеж через workflow
   - Проверить создание связи через API
   - Посмотреть статистику

4. **Мониторинг (опционально):**
   - Создать n8n workflow для проверки несвязанных записей
   - Настроить алерты при большом количестве несвязанных записей

---

## 📝 Примечания

- Связывание происходит автоматически, но не критично при ошибках
- Окно времени по умолчанию: ±5 минут (300 секунд)
- Уверенность (`confidence`) вычисляется автоматически на основе совпадений
- Можно связать вручную через API endpoints
- View `unlinked_records` помогает найти записи без связей

---

## ✅ Чеклист деплоя

- [ ] Применить миграцию `012_create_event_links.sql`
- [ ] Перезапустить Jarvis API
- [ ] Проверить создание связей при сохранении платежей
- [ ] Протестировать API endpoints
- [ ] Проверить статистику через `/event-links/stats`
- [ ] Настроить мониторинг несвязанных записей (опционально)

