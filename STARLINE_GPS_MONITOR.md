# Starline GPS Monitor

Система мониторинга GPS трекеров Starline для автопарка.

## 🎯 Функции

- ✅ Парсинг данных из Starline каждую минуту
- ✅ Автоматическое сопоставление машин по названию и последним 3 цифрам номера
- ✅ Хранение текущих и предыдущих координат (для определения движения)
- ✅ Определение статуса: едет/стоит/offline/GPS отключен
- ✅ Расчет расстояния перемещения
- ✅ История GPS треков для аналитики
- ✅ Telegram алерты при ошибках

---

## 📋 Архитектура

```
┌──────────────┐
│   Starline   │ (https://starline-online.ru)
└──────┬───────┘
       │ HTTP API (cookies авторизация)
       ↓
┌──────────────────┐
│ StarlineClient   │ (src/integrations/starline-client.ts)
└──────┬───────────┘
       │
       ↓
┌─────────────────────────┐
│ StarlineMonitorService  │ (src/services/starline-monitor.ts)
│ - Сопоставление машин   │
│ - Обновление GPS данных │
└──────┬──────────────────┘
       │
       ↓
┌────────────────┐         ┌───────────────┐
│  Jarvis API    │────────▶│   Neon DB     │
│  /starline/*   │         │  gps_tracking │
└───────┬────────┘         └───────────────┘
        │
        │ Вызов каждую минуту
        ↓
┌──────────────────────┐
│  n8n Workflow        │
│  "Starline GPS       │
│   Monitor"           │
└──────────────────────┘
```

---

## 🗄️ База данных

### Таблица `gps_tracking`

Хранит **только текущие и предыдущие координаты** для определения движения:

```sql
CREATE TABLE gps_tracking (
  id UUID PRIMARY KEY,
  car_id UUID REFERENCES cars(id),
  
  -- Starline info
  starline_device_id BIGINT,
  starline_alias TEXT,
  
  -- Текущее положение
  current_lat NUMERIC(10, 7),
  current_lng NUMERIC(10, 7),
  current_sat_qty INT,
  current_timestamp TIMESTAMPTZ,
  
  -- Предыдущее положение
  previous_lat NUMERIC(10, 7),
  previous_lng NUMERIC(10, 7),
  previous_sat_qty INT,
  previous_timestamp TIMESTAMPTZ,
  
  -- Статус и движение
  status TEXT, -- 'offline' | 'gps_offline' | 'moving' | 'parked_on' | 'parked_off'
  is_moving BOOLEAN,
  distance_moved NUMERIC(10, 2), -- метры
  
  -- Данные устройства
  gps_level INT,
  gsm_level INT,
  ignition_on BOOLEAN,
  engine_running BOOLEAN,
  parking_brake BOOLEAN,
  battery_voltage NUMERIC(4, 2),
  
  last_activity TIMESTAMPTZ,
  last_sync TIMESTAMPTZ
);
```

### Таблица `gps_tracking_history`

Автоматически сохраняет историю изменений координат (через триггер):

```sql
CREATE TABLE gps_tracking_history (
  id BIGSERIAL PRIMARY KEY,
  car_id UUID,
  lat NUMERIC(10, 7),
  lng NUMERIC(10, 7),
  status TEXT,
  is_moving BOOLEAN,
  tracked_at TIMESTAMPTZ
);
```

---

## 🔧 Настройка

### 1. Получение cookies Starline

**Метод 1: Через браузер (Chrome DevTools)**

1. Откройте https://starline-online.ru/
2. Залогиньтесь с учетными данными
3. Откройте DevTools (F12) → вкладка Network
4. Обновите страницу
5. Найдите любой запрос к `starline-online.ru`
6. Вкладка Headers → Request Headers → Cookie
7. Скопируйте значение `userAgentId=...`

**Метод 2: Через MCP Chrome (автоматически)**

Выполните в Cursor Agent:
```
Открой Starline через MCP Chrome и залогинься
```

### 2. Добавление в `.env`

```bash
# Starline GPS мониторинг
STARLINE_COOKIES=userAgentId=e2bfd67ca307f71034d05bf8158d9fb3; lang=ru
STARLINE_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
```

### 3. Применение миграции БД

```bash
# На сервере
cd /root/geodrive_n8n-agents
psql $DATABASE_URL -f drizzle/migrations/0012_gps_tracking.sql
```

### 4. Импорт n8n Workflow

```bash
# Импорт через n8n UI
1. Откройте https://n8n.rentflow.rentals
2. Workflows → Import from File
3. Выберите n8n-workflows/starline-gps-monitor.json
4. Активируйте workflow
```

---

## 🚀 Использование

### API Endpoints

#### POST `/starline/update-gps`
Обновить GPS данные всех сопоставленных машин:

```bash
curl -X POST http://46.224.17.15:3000/starline/update-gps
```

**Response:**
```json
{
  "ok": true,
  "updated": 25,
  "errors": [],
  "timestamp": "2025-11-08T20:30:00.000Z"
}
```

#### GET `/starline/match-cars`
Посмотреть сопоставление машин Starline ↔ таблица cars:

```bash
curl http://46.224.17.15:3000/starline/match-cars
```

**Response:**
```json
{
  "ok": true,
  "count": 25,
  "matches": [
    {
      "carId": "uuid",
      "licensePlate": "XX950DX",
      "brand": "Audi",
      "model": "Q7",
      "starlineDeviceId": 869573070871005,
      "starlineAlias": "Audi Q7 White XX950DX"
    }
  ]
}
```

### SQL Запросы

**Машины с отключенным GPS:**
```sql
SELECT 
  c.brand,
  c.model,
  c.license_plate,
  g.status,
  g.last_activity
FROM gps_tracking g
JOIN cars c ON c.id = g.car_id
WHERE g.status = 'gps_offline';
```

**Машины в движении:**
```sql
SELECT 
  c.brand,
  c.model,
  c.license_plate,
  g.is_moving,
  g.distance_moved,
  g.current_lat,
  g.current_lng
FROM gps_tracking g
JOIN cars c ON c.id = g.car_id
WHERE g.is_moving = TRUE;
```

**История GPS за последний час:**
```sql
SELECT 
  c.license_plate,
  h.lat,
  h.lng,
  h.status,
  h.tracked_at
FROM gps_tracking_history h
JOIN cars c ON c.id = h.car_id
WHERE h.tracked_at > NOW() - INTERVAL '1 hour'
ORDER BY h.tracked_at DESC;
```

---

## 📊 Логика сопоставления машин

### Алгоритм

1. **Извлечение из Starline alias:** `"BMW 3 587"` → модель: `"BMW 3"`, цифры: `"587"`
2. **Извлечение из license_plate:** `"WW587UU"` → `"587"`
3. **Сопоставление:**
   - Совпадение последних 3 цифр
   - Частичное совпадение модели

### Примеры

| Starline Alias | License Plate | Сопоставление |
|----------------|---------------|---------------|
| BMW 3 587 | WW587UU | ✅ (587 + BMW) |
| Audi Q7 White XX950DX | XX950DX | ✅ (950 + Audi Q7) |
| Countryman NEW 399 | WW399UU | ✅ (399 + Countryman) |
| РЕЗЕРВ | XX123DX | ❌ (нет цифр) |

---

## 🔍 Определение статусов

### Статусы машин

| Статус | Условие |
|--------|---------|
| **offline** | `device.status === 0` |
| **gps_offline** | `gps_lvl === 0` или `sat_qty === 0` |
| **moving** | `ignition_on && engine_running` |
| **parked_on** | `ignition_on && !engine_running` |
| **parked_off** | `!ignition_on` |

### Определение движения

Машина считается **движущейся** если:
- Координаты изменились **> 10 метров** с последней проверки
- ИЛИ `car_state.run === true` (двигатель работает)

Расстояние вычисляется по формуле Haversine (учитывает кривизну Земли).

---

## ⚙️ Конфигурация workflow

### Частота обновления

По умолчанию: **каждую 1 минуту**

Изменить в n8n workflow:
```json
{
  "parameters": {
    "rule": {
      "interval": [
        {
          "field": "minutes",
          "minutesInterval": 1  // ← измените здесь
        }
      ]
    }
  },
  "name": "Every 1 Minute",
  "type": "n8n-nodes-base.scheduleTrigger"
}
```

### Telegram алерты

Алерты отправляются при:
- ❌ Критической ошибке API
- ⚠️ Частичных ошибках (некоторые машины не обновились)

Настройка чата:
```bash
# В .env
TELEGRAM_ALERT_CHAT_ID=-5004140602
```

---

## 🛠️ Troubleshooting

### Проблема: Cookies устарели

**Симптомы:** 
- `401 Unauthorized`
- `Starline API error: 401`

**Решение:**
1. Заново залогиньтесь на https://starline-online.ru/
2. Обновите `STARLINE_COOKIES` в `.env`
3. Перезапустите Jarvis API:
```bash
docker compose restart jarvis-api
```

### Проблема: Машины не сопоставляются

**Симптомы:**
- `matchCars()` возвращает пустой массив
- В логах: "Не найдено совпадение для..."

**Решение:**
1. Проверьте алиас в Starline (должен содержать 3 цифры)
2. Проверьте license_plate в таблице cars
3. Запустите тестовое сопоставление:
```bash
curl http://46.224.17.15:3000/starline/match-cars
```

### Проблема: GPS не обновляется

**Проверка:**
```sql
SELECT 
  car_id,
  last_sync,
  status
FROM gps_tracking
ORDER BY last_sync DESC;
```

Если `last_sync` старше 5 минут:
1. Проверьте статус n8n workflow (должен быть Active)
2. Проверьте логи Jarvis API:
```bash
docker logs jarvis-api --tail 100
```

---

## 📈 Будущие улучшения

- [ ] Определение несанкционированных поездок (без брони)
- [ ] Геозоны и алерты при выезде из зоны
- [ ] Интеграция с RentProg (сверка броней)
- [ ] Dashboard для визуализации GPS треков
- [ ] Отчеты по пробегу
- [ ] Алерты при отключении GPS

---

## 📝 Changelog

### 2025-11-08
- ✅ Создан HTTP клиент для Starline API
- ✅ Создана таблица gps_tracking с историей
- ✅ Реализовано сопоставление машин по названию + 3 цифрам
- ✅ Создан n8n workflow для парсинга каждую минуту
- ✅ Добавлены Telegram алерты
- ✅ Реализовано определение движения по координатам

---

**Документация:** [STARLINE_GPS_MONITOR.md](./STARLINE_GPS_MONITOR.md)  
**API:** http://46.224.17.15:3000/starline  
**n8n Workflow:** `Starline GPS Monitor`

