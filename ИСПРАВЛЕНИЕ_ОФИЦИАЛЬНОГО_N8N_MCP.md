# Исправление официального n8n-mcp сервера

## ❌ Проблема

Официальный сервер запускается, но сразу закрывается (`Client closed for command`).

**Причины:**
1. На Windows `.cmd` файлы не могут запускаться напрямую через `spawn`
2. Отсутствует `cwd` (рабочая директория)
3. npx требует правильного контекста выполнения

## ✅ Решение

Измените конфигурацию, используя `cmd.exe` с флагом `/c`:

### Старая конфигурация (не работает):
```json
"n8n-mcp-official": {
  "command": "C:\\nvm4w\\nodejs\\npx.cmd",
  "args": ["-y", "n8n-mcp"],
  "env": { ... }
}
```

### Новая конфигурация (работает):
```json
"n8n-mcp-official": {
  "command": "cmd",
  "args": [
    "/c",
    "C:\\nvm4w\\nodejs\\npx.cmd",
    "-y",
    "n8n-mcp"
  ],
  "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents",
  "env": {
    "MCP_MODE": "stdio",
    "LOG_LEVEL": "error",
    "DISABLE_CONSOLE_OUTPUT": "true",
    "N8N_API_URL": "https://n8n.rentflow.rentals/api/v1",
    "N8N_API_KEY": "ваш_api_ключ"
  }
}
```

## 🔑 Изменения

1. **command**: `cmd` вместо `npx.cmd`
2. **args**: Добавлен `/c` в начало для выполнения команды через cmd
3. **cwd**: Добавлена рабочая директория (обязательно для npx)

## 📋 Что делает команда

```
cmd /c C:\nvm4w\nodejs\npx.cmd -y n8n-mcp
```

- `cmd /c` - запускает команду через cmd.exe и закрывает окно после выполнения
- `npx.cmd -y` - автоматически скачивает и запускает последнюю версию n8n-mcp
- `n8n-mcp` - название пакета

## ✅ После исправления

1. **Перезапустите Cursor полностью**
2. **Проверьте**: `Ctrl+,` → Tools & MCP
3. Должен появиться **n8n-mcp-official** с 271+ инструментами

## 🧪 Проверка работы

После перезапуска попробуйте:
```
Покажи список всех инструментов n8n-mcp-official
```

Или:
```
Создай простой workflow в n8n через официальный сервер
```

## 📚 Дополнительная информация

- Официальный репозиторий: https://github.com/czlonkowski/n8n-mcp
- Документация: https://www.n8n-mcp.com/

