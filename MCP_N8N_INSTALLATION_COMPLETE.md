# ✅ Установка MCP для n8n - Завершена

## 📦 Что установлено:

1. ✅ **MCP сервер для n8n** - `mcp-server/n8n-mcp-server.js`
2. ✅ **Все зависимости установлены** - `mcp-server/node_modules`
3. ✅ **Тестовый скрипт** - `mcp-server/test-n8n-connection.js`
4. ✅ **PowerShell скрипт для теста** - `QUICK_TEST_N8N_MCP.ps1`
5. ✅ **Документация** - `MCP_N8N_SETUP.md`

## 🔧 Доступные инструменты MCP:

После настройки в Cursor вы сможете использовать:

- `n8n_test_connection` - Проверка подключения
- `n8n_list_workflows` - Список всех workflows
- `n8n_get_workflow` - Получить workflow по ID/имени
- `n8n_create_workflow` - Создать новый workflow
- `n8n_update_workflow` - Обновить workflow
- `n8n_activate_workflow` - Активировать workflow
- `n8n_deactivate_workflow` - Деактивировать workflow
- `n8n_delete_workflow` - Удалить workflow
- `n8n_execute_workflow` - Запустить workflow
- `n8n_list_executions` - Список выполнений
- `n8n_get_execution` - Получить выполнение
- `n8n_list_credentials` - Список credentials

## 📋 Что нужно сделать ВАМ:

### 1. Добавить переменные в `.env`

Откройте `.env` файл (в корне проекта) и добавьте:

```env
N8N_BASE_URL=https://n8n.rentflow.rentals
N8N_API_KEY=ваш_api_ключ_здесь
```

**Как получить API ключ:**
- Откройте n8n: https://n8n.rentflow.rentals
- Settings → API → Create API Key
- Скопируйте ключ

### 2. Проверить подключение

Запустите тест:

```powershell
.\QUICK_TEST_N8N_MCP.ps1
```

Или вручную:

```bash
cd mcp-server
node test-n8n-connection.js
```

### 3. Настроить Cursor

1. `Ctrl+,` → Features → Model Context Protocol
2. Добавьте конфигурацию из `CURSOR_MCP_N8N_CONFIG.json`
3. Замените `ваш_api_ключ_здесь` на реальный ключ
4. Замените домен на ваш (если отличается)

### 4. Перезапустить Cursor

Полностью закройте и откройте Cursor снова.

## 🧪 Тестирование:

После настройки попробуйте в Cursor:

```
Проверь подключение к n8n
```

Или:

```
Покажи список всех workflows в n8n
```

## 📚 Файлы:

- `MCP_N8N_SETUP.md` - Полная документация
- `SETUP_MCP_N8N_COMPLETE.md` - Пошаговая инструкция
- `CURSOR_MCP_N8N_CONFIG.json` - Готовая конфигурация
- `QUICK_TEST_N8N_MCP.ps1` - Скрипт для быстрого теста

## ⚠️ Если не работает:

Проверьте:
1. ✅ Переменные в `.env` установлены
2. ✅ API ключ валиден
3. ✅ Домен доступен
4. ✅ Cursor перезапущен

Если всё равно не работает - используйте REST API (см. `README_N8N_API.md`).

---

**Готово к использованию! 🚀**

