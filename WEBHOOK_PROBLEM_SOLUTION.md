# ✅ РЕШЕНИЕ: Почему не приходят вебхуки от RentProg

**Дата:** 2025-11-04  
**Статус:** Проблема найдена и решение предложено

---

## 🎯 ГЛАВНЫЙ ВЫВОД

**Вебхуки ОТ RENTPROG ПРИХОДЯТ! ✅**

Проблема не в том, что вебхуки не доходят до системы.  
Проблема в том, что **86% вебхуков не распознаются** системой и помечаются как "unknown".

---

## 📊 Результаты проверки

### ✅ Что работает:

1. **SSL сертификат**: Валиден (Let's Encrypt R12)
2. **Домен**: `https://webhook.rentflow.rentals` доступен
3. **Nginx**: Успешно проксирует запросы на n8n
4. **n8n workflow**: Активен и обрабатывает вебхуки
5. **База данных**: События сохраняются корректно
6. **Вебхуки приходят**: 91 событие за последние 2 дня

### ⚠️ Проблема:

**78 из 91 событий (86%) имеют тип "unknown"**

```
События по типам:
- unknown: 78  ⚠️ ПРОБЛЕМА
- null: 3
- diagnostic: 3
- create: 2
- car_update: 1
- booking_update: 1
- client_update: 1
- update: 1
```

---

## 🔍 Почему события "unknown"?

В workflow файле `rentprog-webhooks-monitor.json` в ноде "Parse & Validate Format" определён **пустой** массив типов событий:

```javascript
const knownEventTypes = [];  // Пусто - все вебхуки считаются неизвестными
```

**Из комментария в коде:**
> "Пусто - все вебхуки считаются неизвестными"

**Что это означает:**
- RentProg отправляет вебхуки с разными типами событий
- n8n получает эти вебхуки
- Пытается найти тип события в списке `knownEventTypes`
- Не находит (список пуст!)
- Помечает как "unknown"
- Сохраняет в БД с типом "unknown"

---

## 💡 РЕШЕНИЕ

### Шаг 1: Найти реальные типы событий

Нужно посмотреть, какие именно события присылает RentProg.

**3 способа:**

#### Способ 1: Telegram уведомления (самый простой) ⭐

В workflow настроена нода "Debug: Unknown Format", которая должна отправлять уведомления в Telegram при каждом неизвестном вебхуке.

**Что делать:**
1. Откройте Telegram чат (указанный в `TELEGRAM_ALERT_CHAT_ID`)
2. Найдите сообщения вида: "Неизвестный формат вебхука от RentProg"
3. В сообщении будет поле "Тип события" (например: `booking_issue_planned`)
4. Скопируйте все уникальные типы событий

**Пример сообщения:**
```
Неизвестный формат вебхука от RentProg

Тип события: booking_issue_planned
RentProg ID: 470049
Company ID: 9247

Payload (Ruby hash):
{"id"=>470049, "state"=>["Новая", "Активная"]}
```

---

#### Способ 2: n8n UI (если нет Telegram)

1. Откройте https://n8n.rentflow.rentals
2. Workflow "RentProg Webhooks Monitor" → **Executions**
3. Найдите последние executions (за сегодня)
4. Откройте любой execution
5. Нажмите на ноду **"Parse & Validate Format"**
6. Посмотрите данные:
   - `rawEvent` - оригинальное название типа события
   - `parsedPayload` - данные вебхука
7. Запишите все уникальные `rawEvent`

**Пример данных:**
```json
{
  "rawEvent": "booking_issue_planned",
  "parsedPayload": {
    "id": 470049,
    "state": ["Новая", "Активная"]
  }
}
```

---

#### Способ 3: Создать событие в RentProg

1. Войдите в RentProg
2. Создайте или обновите бронирование
3. Сразу откройте n8n UI → Executions
4. Последний execution должен содержать данные вебхука
5. Посмотрите `rawEvent`

---

### Шаг 2: Обновить workflow

После того как получите список типов событий (например: `booking_issue_planned`, `booking_return_planned`, `car_moved`):

1. Откройте https://n8n.rentflow.rentals
2. Workflow "RentProg Webhooks Monitor"
3. Нажмите на ноду **"Parse & Validate Format"**
4. Найдите строку:
   ```javascript
   const knownEventTypes = [];
   ```
5. Замените на:
   ```javascript
   const knownEventTypes = [
     'booking_issue_planned',
     'booking_return_planned',
     'booking_update',
     'booking_create',
     'car_update',
     'car_moved',
     'client_update',
     'client_create',
     // добавьте остальные типы из Telegram/executions
   ];
   ```
6. Нажмите **Save**
7. Убедитесь что workflow остался **Active**

---

### Шаг 3: Проверить результат

1. Создайте событие в RentProg (например, обновите бронь)
2. Откройте n8n → Executions
3. Последний execution должен иметь:
   - `isKnownFormat: true` ✅
   - `eventType: "booking_update"` (не "unknown") ✅
4. Проверьте БД:
   ```bash
   node /workspace/check_recent_events.mjs
   ```
5. Должны видеть события с правильными типами (не "unknown")

---

## 📋 Список типов событий RentProg

**Типичные события по агентам (из AGENTS.md):**

### Бронирования:
- `booking.issue.planned` - запланирована выдача
- `booking.return.planned` - запланирован возврат
- `booking.created` - бронь создана
- `booking.updated` - бронь обновлена
- `booking.deleted` - бронь удалена

### Автомобили:
- `car.created` - авто добавлен
- `car.updated` - авто обновлён
- `car.moved` - авто перемещён в другой филиал
- `car.deleted` - авто архивирован

### Клиенты:
- `client.created` - клиент создан
- `client.updated` - клиент обновлён
- `client.deleted` - клиент архивирован

### Кассовые операции:
- `cash.collected` - инкассация проведена
- `payment.received` - платёж получен
- `payment.refunded` - возврат средств

**⚠️ Важно:** Точные названия могут отличаться! Нужно проверить реальные вебхуки из RentProg.

---

## 🛠️ Альтернативное решение

Если не хотите вручную добавлять типы событий, можно:

### Вариант 1: Принимать все события как valid

В ноде "Parse & Validate Format" изменить логику:

```javascript
// Вместо проверки в списке knownEventTypes
// Принимать любое событие с непустым rawEvent

if (rawEvent && rawEvent !== 'unknown' && rentprogId !== 'unknown') {
  isKnownFormat = true;
  eventType = rawEvent;
}
```

**Плюсы:**
- Автоматически принимает все форматы
- Не требует обслуживания

**Минусы:**
- Может пропустить некорректные вебхуки
- Сложнее отлаживать проблемы

---

### Вариант 2: Автоматическое обучение

Создать отдельный workflow, который:
1. Получает unknown события из БД
2. Анализирует их структуру
3. Автоматически добавляет в список `knownEventTypes`
4. Обновляет workflow

**Требует разработки отдельного механизма.**

---

## 📊 Текущая статистика

```
Всего событий: 91
Распознаны правильно: 13 (14%)
Неизвестный формат: 78 (86%)

События по филиалам:
- Tbilisi (9247): 3 события
- Service Center (11163): 3 события

Последние события:
1. [2025-11-04 13:34:28] create - Service Center ✅
2. [2025-11-04 12:10:08] update - Service Center ✅
3. [2025-11-04 06:57:57] null - Tbilisi ⚠️
```

---

## 🎯 Действия прямо сейчас

### 1️⃣ Найдите типы событий (5 минут)

**Откройте Telegram чат** или **n8n Executions** и скопируйте все уникальные названия событий.

### 2️⃣ Обновите workflow (2 минуты)

Добавьте типы в массив `knownEventTypes` в ноде "Parse & Validate Format".

### 3️⃣ Проверьте (1 минута)

Создайте событие в RentProg и убедитесь что оно распознаётся.

---

## 📝 Пример финального списка

После анализа вебхуков, ваш список может выглядеть так:

```javascript
const knownEventTypes = [
  // Бронирования
  'booking_issue_planned',
  'booking_return_planned',
  'booking_create',
  'booking_update',
  'booking_delete',
  
  // Автомобили
  'car_create',
  'car_update',
  'car_moved',
  'car_delete',
  
  // Клиенты
  'client_create',
  'client_update',
  'client_delete',
  
  // Кассовые операции
  'cash_collected',
  'payment_received',
  'payment_refunded'
];
```

---

## ✅ Результат после исправления

После добавления типов событий:
- **Вебхуки будут распознаваться** (type != "unknown") ✅
- **Auto Process** будет вызывать Jarvis API ✅
- **Upsert Processor** будет обновлять данные в БД ✅
- **Telegram alerts** будут приходить только для действительно неизвестных форматов ✅

---

## 🆘 Нужна помощь?

Если не можете найти типы событий:
1. Предоставьте скриншот из n8n Executions
2. Или скопируйте данные ноды "Parse & Validate Format"
3. Или покажите Telegram сообщения

Можно автоматически извлечь типы событий из БД и сгенерировать список.

---

**Документы для справки:**
- `WEBHOOK_INVESTIGATION_RESULTS.md` - полная диагностика
- `RENTPROG_WEBHOOK_DIAGNOSIS.md` - чеклист проверок
- `SESSION_SUMMARY_2025-11-03.md` - предыдущая сессия
- `n8n-workflows/rentprog-webhooks-monitor.json` - workflow файл

---

**Автор:** Claude Sonnet 4.5  
**Дата:** 2025-11-04  
**Статус:** ✅ Проблема идентифицирована, решение предложено
