# Сводка исправлений отслеживания изменений

**Дата:** 2025-01-20  
**Статус:** ✅ Исправления завершены

---

## 📋 Что было исправлено

### 1. ✅ Миграция 0041: Исправление триггеров

**Файл:** `setup/migrations/0041_fix_change_tracking_in_triggers.sql`

**Изменения:**

1. **Функция `apply_history_changes`:**
   - Добавлен параметр `user_name_value` для передачи автора изменения
   - При UPDATE в `cars` и `clients` заполняются все поля отслеживания:
     - `updated_by_source = 'rentprog_history'`
     - `updated_by_user = user_name_value` (автор из history)
     - `updated_by_execution_id = history_id::TEXT`
     - `updated_by_metadata` с информацией о записи history

2. **Триггер `auto_process_history_trigger`:**
   - Передает `user_name` в функцию `apply_history_changes`

3. **Функция `process_booking_nested_entities`:**
   - Исправлены поля дат: `start_date` → `start_at`, `end_date` → `end_at`
   - При INSERT/UPDATE в `cars` и `clients` заполняются все поля отслеживания:
     - `updated_by_source = 'trigger'`
     - `updated_by_function = 'process_booking_nested_entities'`
     - `updated_by_execution_id = NEW.id::TEXT` (ID брони)
     - `updated_by_metadata` с информацией о триггере

---

### 2. ✅ Обновление schema.ts

**Файл:** `src/db/schema.ts`

**Изменения:**
- Добавлены поля отслеживания изменений в таблицу `bookings`:
  - `updated_by_source`
  - `updated_by_workflow`
  - `updated_by_function`
  - `updated_by_execution_id`
  - `updated_by_user`
  - `updated_by_metadata`
- Добавлены индексы для быстрого поиска

---

### 3. ✅ Обновление функции dynamicUpsertEntity

**Файл:** `src/db/upsert.ts`

**Изменения:**
- При INSERT заполняются поля отслеживания:
  - `updated_by_source = 'jarvis_api'`
  - `updated_by_function = 'dynamicUpsertEntity'`
  - `updated_by_metadata` с информацией о rentprog_id и table_name

- При UPDATE заполняются поля отслеживания:
  - Те же поля, что и при INSERT
  - Исключены поля отслеживания из цикла обновления полей из data

---

## 🎯 Результат

Теперь **ВСЕ** изменения в таблицах `cars`, `clients` и `bookings` отслеживаются с указанием:

1. **Источника изменения:**
   - `rentprog_webhook` - через вебхуки
   - `rentprog_history` - через историю операций
   - `trigger` - через триггеры БД
   - `jarvis_api` - через API Jarvis
   - `n8n_workflow` - через n8n workflows
   - `snapshot_workflow` - через snapshot workflows
   - `manual` - ручные изменения

2. **Автора изменения:**
   - Для изменений через `history` - `user_name` из таблицы `history`
   - Для других источников - может быть передан через API

3. **Алгоритма/функции:**
   - Название функции, которая внесла изменение
   - ID execution в n8n (если применимо)
   - ID записи history (если применимо)

4. **Метаданных:**
   - Дополнительная информация об источнике изменения

---

## 📊 Примеры использования

### Найти все изменения конкретного сотрудника

```sql
SELECT 
  c.id,
  c.plate,
  c.model,
  c.updated_at,
  c.updated_by_source,
  c.updated_by_user,
  c.updated_by_function
FROM cars c
WHERE c.updated_by_user = 'Имя Фамилия'
ORDER BY c.updated_at DESC;
```

### Найти все изменения через history

```sql
SELECT 
  c.id,
  c.plate,
  c.updated_at,
  c.updated_by_user,
  c.updated_by_metadata->>'history_id' as history_id,
  h.description
FROM cars c
LEFT JOIN history h ON h.id::TEXT = c.updated_by_execution_id
WHERE c.updated_by_source = 'rentprog_history'
ORDER BY c.updated_at DESC;
```

### Найти все изменения через триггеры

```sql
SELECT 
  c.id,
  c.plate,
  c.updated_at,
  c.updated_by_function,
  c.updated_by_metadata->>'booking_id' as booking_id
FROM cars c
WHERE c.updated_by_source = 'trigger'
ORDER BY c.updated_at DESC;
```

### Полная история изменений машины

```sql
SELECT 
  c.id,
  c.plate,
  c.model,
  c.updated_at,
  c.updated_by_source,
  c.updated_by_user,
  c.updated_by_workflow,
  c.updated_by_function,
  c.updated_by_execution_id,
  c.updated_by_metadata
FROM cars c
WHERE c.plate = 'ABC-123'
ORDER BY c.updated_at DESC;
```

---

## 🚀 Следующие шаги

1. **Применить миграцию:**
   ```bash
   psql $DATABASE_URL -f setup/migrations/0041_fix_change_tracking_in_triggers.sql
   ```

2. **Проверить работу:**
   - Создать тестовую запись в `history` и проверить заполнение полей
   - Создать тестовую запись в `bookings` с вложенными `car`/`client` и проверить заполнение
   - Выполнить примеры запросов

3. **Мониторинг:**
   - Проверить, что новые изменения заполняют поля отслеживания
   - Убедиться, что можно найти изменения по сотруднику
   - Убедиться, что можно найти изменения по источнику

---

## ⚠️ Важные замечания

1. **Старые записи:**
   - Записи, созданные до применения миграции, не будут иметь заполненные поля отслеживания
   - Для поиска старых изменений можно использовать таблицу `history` напрямую

2. **Производительность:**
   - Индексы на `updated_by_source` и `updated_by_workflow` ускорят поиск
   - При большом объеме данных может потребоваться дополнительная оптимизация

3. **Дополнительные источники:**
   - Если появятся новые источники изменений, нужно будет добавить их в функции upsert
   - Типы источников определены в `src/db/change-tracking.ts`

---

**Статус:** ✅ Все исправления завершены, миграция готова к применению

