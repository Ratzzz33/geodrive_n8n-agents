# GPS Статусы — Краткая справка

## 🚦 5 статусов в БД (поле `status`)

| Статус | Значение | Условие | Кол-во машин |
|--------|----------|---------|--------------|
| **offline** 🔴 | Устройство не на связи | `device.status === 0` | ~15 |
| **gps_offline** 🟡 | Устройство онлайн, но слабый GPS | `gps_lvl === 0` | ~3 |
| **moving** 🟢 | Машина движется | `speed > 5` ИЛИ (`ign && run`) | ~6 |
| **parked_on** 🟠 | Стоит с зажиганием | `ign === true && run === false` | ~0 |
| **parked_off** ⚪ | Припаркована | Всё выключено | ~93 |

---

## ⚡ Быстрая проверка

```bash
# Статистика по статусам
node check_statuses_in_db.mjs

# SQL запрос
SELECT status, COUNT(*) FROM gps_tracking GROUP BY status;
```

---

## 🔄 Логика (порядок проверок)

```
1. status=0?           → offline
2. gps_lvl=0?          → gps_offline  
3. speed>5 или ign+run → moving
4. ign && !run         → parked_on
5. иначе               → parked_off
```

---

## ⚠️ Важно!

- **"в сети" ≠ `moving`** — это статус СВЯЗИ, не движения!
- **`is_moving` ≠ `status`** — определяется по изменению координат
- **Обновление:** каждые 2-5 минут
- **История:** НЕ сохраняется (только текущий статус)

---

**Полная документация:** [GPS_STATUSES.md](./GPS_STATUSES.md)

