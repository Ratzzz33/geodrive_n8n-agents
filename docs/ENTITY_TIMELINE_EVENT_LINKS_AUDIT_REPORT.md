# Отчет по Entity Timeline & Event Links

**Дата проверки:** 2025-11-10  
**Статус:** ✅ Система работает, требуется доработка автоматического связывания

---

## 📊 Текущее состояние

### Таблицы и данные

| Таблица | Количество записей | Статус |
|---------|-------------------|--------|
| `events` | 507 | ✅ Существует |
| `payments` | 3,804 | ✅ Существует |
| `history` | 215 | ✅ Существует |
| `entity_timeline` | 14,436 | ✅ Существует |
| `event_links` | 2 | ✅ Существует |

### Покрытие Entity Timeline

- **Платежи в timeline:** 3,633 из 3,804 (95.5%) ✅
- **GPS события:** 10,803 (Starline) ✅
- **Источники:**
  - `starline` (car): 10,803 событий
  - `rentprog_payment` (payment): 3,633 событий

### Обработка событий

- **Обработано:** 431 из 507 (85.0%)
- **Не обработано:** 76 событий ⚠️

---

## 🔍 Проблемы и анализ

### 1. Автоматическое связывание не работает ⚠️

**Проблема:** За последние 7 дней создано 3,804 новых платежа, но **0% связаны** с events/history.

**Причина:** 
- В таблице `events` **НЕТ событий с `entity_type = 'payment'`** (0 записей)
- Все события имеют `entity_type = 'car'`, `'client'`, `'booking'`
- Код в `linkPayment()` ищет события с `entity_type = 'payment'`, но таких не существует

**Код проблемы:**
```typescript
// src/db/eventLinks.ts, строка 150
WHERE 
  entity_type = 'payment'  // ❌ Таких событий нет!
  AND rentprog_id = ${String(rpPaymentId)}
```

**Решение:**
1. Изменить логику связывания - искать события о bookings, которые связаны с платежами через `booking_id`
2. Или добавить события о платежах в таблицу `events` (если RentProg отправляет такие вебхуки)

### 2. Необработанные события ⚠️

**Проблема:** 76 событий не обработаны (`processed = false`)

**Рекомендация:** Запустить обработку необработанных событий через workflow "RentProg Upsert Processor"

### 3. Низкое покрытие связей в event_links

**Текущее состояние:** Только 2 связи (исторические, созданы бэкфилом)

**Это нормально**, потому что:
- События о платежах не приходят через вебхуки
- Большинство booking не имеют платежей
- History - это activity log, а не события сущностей

**Для новых данных:** Связи будут создаваться автоматически, если:
- Появятся события о платежах в `events`
- Или изменится логика связывания (через booking_id)

---

## ✅ Что работает

### Entity Timeline

1. ✅ **Автоматическая запись платежей** - работает при сохранении через `savePaymentFromRentProg()`
2. ✅ **Автоматическая запись GPS** - работает при обновлении координат
3. ✅ **Автоматическая запись вебхуков** - работает при обработке событий
4. ✅ **API endpoints** - все работают:
   - `GET /entity-timeline/entity/:entityType/:entityId`
   - `POST /entity-timeline/related`
   - `GET /entity-timeline/stats`

### Event Links

1. ✅ **Код связывания готов** - функции `linkPayment()` и `linkEvent()` реализованы
2. ✅ **Интеграция в процесс сохранения** - вызывается при создании платежа
3. ✅ **API endpoints** - все работают:
   - `POST /event-links/payment/:paymentId`
   - `POST /event-links/event/:eventId`
   - `GET /event-links/payment/:paymentId`
   - `GET /event-links/stats`
   - `GET /event-links/unlinked`

---

## 🔧 Рекомендации

### 1. Исправить логику автоматического связывания

**Проблема:** Код ищет события с `entity_type = 'payment'`, но таких нет.

**Решение:** Изменить логику в `src/db/eventLinks.ts`:

```typescript
// Вместо поиска событий о платежах напрямую
// Искать события о bookings, связанных с платежом через booking_id

// 1. Найти booking_id платежа
const bookingId = payment.rawData.booking_id;

// 2. Найти события о booking
const eventQuery = sql`
  SELECT id, ts, rentprog_id, payload
  FROM events
  WHERE 
    entity_type = 'booking'
    AND rentprog_id = ${String(bookingId)}
    AND company_id = ${companyId}
    AND ABS(EXTRACT(EPOCH FROM (ts - ${paymentDate}::timestamptz))) < ${timeWindowSeconds}
  ORDER BY ABS(EXTRACT(EPOCH FROM (ts - ${paymentDate}::timestamptz)))
  LIMIT 1
`;
```

### 2. Обработать необработанные события

**Действие:** Запустить обработку 76 необработанных событий через workflow "RentProg Upsert Processor" или вручную через API.

### 3. Бэкфил для платежей (опционально)

**Текущее покрытие:** 95.5% (3,633 из 3,804)

**Рекомендация:** 
- ✅ Покрытие хорошее, бэкфил не критичен
- ⚠️ Можно добавить оставшиеся 171 платеж для 100% покрытия

**Скрипт:** `setup/backfill_entity_timeline.mjs` (уже существует)

### 4. Бэкфил для event_links (НЕ требуется)

**Причина:** 
- События о платежах не приходят через вебхуки
- Связи будут создаваться автоматически для новых данных после исправления логики
- Исторические данные не критичны

---

## 📋 Итоговый статус

### ✅ Работает

- Entity Timeline: 95.5% покрытие платежей, автоматическая запись работает
- Event Links: Код готов, API работает
- Интеграция: Автоматическая запись в timeline при сохранении платежей

### ⚠️ Требует исправления

- Автоматическое связывание: Не работает из-за отсутствия событий о платежах
- Необработанные события: 76 событий требуют обработки

### ❌ Бэкфил НЕ требуется

- Entity Timeline: Покрытие хорошее (95.5%)
- Event Links: Исторические связи не критичны, новые будут создаваться автоматически

---

## 🚀 План действий

1. **Исправить логику связывания** в `src/db/eventLinks.ts`:
   - Искать события о bookings вместо платежей
   - Связывать через `booking_id`

2. **Обработать необработанные события** (76 записей):
   - Через workflow "RentProg Upsert Processor"
   - Или вручную через API `/process-event`

3. **Опционально: Бэкфил оставшихся платежей** (171 платеж):
   - Запустить `setup/backfill_entity_timeline.mjs`
   - Для достижения 100% покрытия

4. **Мониторинг:**
   - Проверить работу автоматического связывания после исправления
   - Отслеживать создание новых связей в `event_links`

---

## 📝 Выводы

**Система Entity Timeline & Event Links работает корректно**, но требует доработки логики автоматического связывания.

**Основная проблема:** Отсутствие событий о платежах в таблице `events` (RentProg не отправляет такие вебхуки).

**Решение:** Изменить логику связывания для поиска событий о bookings, связанных с платежами.

**Бэкфил:** Не требуется, так как:
- Покрытие Entity Timeline хорошее (95.5%)
- Исторические связи в event_links не критичны
- Новые данные будут обрабатываться автоматически после исправления

---

**Подготовлено:** AI Agent  
**Дата:** 2025-11-10

