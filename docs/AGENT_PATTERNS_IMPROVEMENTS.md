# Улучшения архитектуры агентов на основе best practices

**Дата:** 2025-11-09  
**Источник:** Анализ паттернов агентов (Kaggle whitepaper + текущая архитектура)  
**Статус:** Рекомендации для внедрения

---

## 🎯 Цель

Применить проверенные паттерны работы с агентами к нашей системе **без усложнения** и **без затягивания разработки**. Фокус на практических улучшениях с быстрой отдачей.

---

## ✅ Что уже хорошо в нашей архитектуре

1. **Event-driven архитектура** — оркестратор + event bus
2. **Специализированные агенты** — каждый агент решает узкую задачу
3. **Централизованная маршрутизация** — `determineAgents()` в оркестраторе
4. **Дедупликация событий** — `events` таблица с `UNIQUE` constraint
5. **Разделение ответственности** — n8n для парсинга, Jarvis API для бизнес-логики

---

## 🚀 Рекомендуемые улучшения (по приоритету)

### 1. **Agent Memory/Context Management** ⭐ (высокий приоритет)

**Проблема:** Агенты не сохраняют контекст между вызовами. Например, агент "помощник по выдаче" не помнит, что уже проверил фото.

**Решение:** Добавить простую таблицу `agent_context` для хранения состояния агентов.

```sql
CREATE TABLE agent_context (
  id BIGSERIAL PRIMARY KEY,
  agent_id TEXT NOT NULL,
  entity_type TEXT,  -- 'car' | 'booking' | 'client'
  entity_id TEXT,     -- UUID или external_id
  context JSONB,      -- Состояние агента
  expires_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_agent_context_lookup 
  ON agent_context(agent_id, entity_type, entity_id) 
  WHERE expires_at > NOW();
```

**Использование:**
- Агент "помощник по выдаче" сохраняет: `{checked_photos: true, missing_items: ['tech_passport']}`
- При следующем вызове агент читает контекст и продолжает с того места, где остановился

**Время внедрения:** 2-3 часа  
**Польза:** Агенты становятся "умнее", не повторяют проверки

---

### 2. **ReAct Pattern для сложных агентов** ⭐ (средний приоритет)

**Проблема:** Сейчас агенты работают линейно. Нет возможности "подумать" перед действием.

**Решение:** Для агентов с LLM (ночной агент продаж, помощник по выдаче) использовать паттерн **ReAct** (Reasoning + Acting).

**Структура:**
```
1. Agent получает событие
2. Agent "думает" (LLM): "Что нужно сделать? Какие данные нужны?"
3. Agent "действует" (tool call): запрос к БД, API, проверка фото
4. Agent "думает" снова: "Достаточно ли данных? Что делать дальше?"
5. Agent "действует" финально: отправка уведомления, создание задачи
```

**Пример для ночного агента:**
```typescript
// Псевдокод
async function nightAgentReact(conversationId: string) {
  // Reasoning: анализируем диалог
  const analysis = await llm.think(`
    Проанализируй диалог ${conversationId}.
    Что хочет клиент? Какой интент?
  `);
  
  // Acting: получаем контекст
  const context = await getConversationContext(conversationId);
  const similarWins = await rag.search(analysis.intent);
  
  // Reasoning: формируем ответ
  const reply = await llm.think(`
    На основе контекста и победных диалогов:
    ${similarWins}
    Сформируй ответ клиенту.
  `);
  
  // Acting: отправляем
  await sendToAmoCRM(conversationId, reply);
}
```

**Время внедрения:** 4-6 часов (только для агентов с LLM)  
**Польза:** Более качественные ответы, меньше ошибок

---

### 3. **Agent Tool Registry** ⭐ (средний приоритет)

**Проблема:** Каждый агент сам реализует доступ к БД/API. Нет единого реестра инструментов.

**Решение:** Создать `AgentToolRegistry` с общими инструментами.

```typescript
// src/agents/tools/registry.ts
export class AgentToolRegistry {
  // БД инструменты
  async getEntityTimeline(entityType: string, entityId: string) { ... }
  async getEventLinks(entityId: string) { ... }
  async createTask(task: TaskInput) { ... }
  
  // API инструменты
  async fetchRentProgData(entityType: string, id: string) { ... }
  async sendTelegramMessage(chatId: string, text: string) { ... }
  
  // CV инструменты (будущее)
  async analyzePhoto(photoUrl: string, task: 'damage' | 'fuel' | 'tires') { ... }
}
```

**Использование:**
```typescript
// В агенте
const tools = new AgentToolRegistry();
const timeline = await tools.getEntityTimeline('car', carId);
const task = await tools.createTask({...});
```

**Время внедрения:** 3-4 часа  
**Польза:** Переиспользование кода, единообразие, легче тестировать

---

### 4. **Agent Execution Logging** ⭐ (высокий приоритет)

**Проблема:** Нет детального логирования действий агентов. Сложно отлаживать и анализировать.

**Решение:** Таблица `agent_executions` для трекинга всех вызовов агентов.

```sql
CREATE TABLE agent_executions (
  id BIGSERIAL PRIMARY KEY,
  agent_id TEXT NOT NULL,
  event_id BIGINT REFERENCES events(id),
  event_type TEXT,
  status TEXT,  -- 'started' | 'completed' | 'failed' | 'skipped'
  input_data JSONB,
  output_data JSONB,
  error_message TEXT,
  duration_ms INT,
  tools_used TEXT[],  -- ['getEntityTimeline', 'sendTelegram']
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_agent_executions_agent ON agent_executions(agent_id, created_at DESC);
CREATE INDEX idx_agent_executions_event ON agent_executions(event_id);
CREATE INDEX idx_agent_executions_status ON agent_executions(status, created_at DESC);
```

**Использование:**
- Метрики: сколько раз вызван агент, среднее время выполнения, % ошибок
- Отладка: что получил агент на вход, что вернул
- Аналитика: какие инструменты чаще всего используются

**Время внедрения:** 2-3 часа  
**Польза:** Прозрачность, отладка, метрики производительности

---

### 5. **Agent Retry Strategy** ⭐ (средний приоритет)

**Проблема:** Сейчас ошибки агентов просто логируются. Нет автоматических повторов.

**Решение:** Экспоненциальный backoff для временных ошибок.

```typescript
// src/agents/base-agent.ts
export abstract class BaseAgent {
  async executeWithRetry(
    event: SystemEvent,
    maxRetries = 3,
    baseDelay = 1000
  ): Promise<AgentResult> {
    for (let attempt = 0; attempt < maxRetries; attempt++) {
      try {
        return await this.handle(event);
      } catch (error) {
        if (this.isRetryable(error) && attempt < maxRetries - 1) {
          const delay = baseDelay * Math.pow(2, attempt);
          await sleep(delay);
          continue;
        }
        throw error;
      }
    }
  }
  
  private isRetryable(error: Error): boolean {
    // Временные ошибки: timeout, network, rate limit
    return error.message.includes('timeout') || 
           error.message.includes('ECONNRESET') ||
           error.message.includes('429');
  }
}
```

**Время внедрения:** 2 часа  
**Польза:** Устойчивость к временным сбоям (сеть, API лимиты)

---

### 6. **Agent Evaluation (для ночного агента)** ⭐ (низкий приоритет, но полезно)

**Проблема:** Нет способа оценить качество работы ночного агента.

**Решение:** Простая метрика "ответил ли клиент" + "перешёл ли в следующую стадию".

```sql
CREATE TABLE agent_evaluations (
  id BIGSERIAL PRIMARY KEY,
  agent_id TEXT NOT NULL,
  execution_id BIGINT REFERENCES agent_executions(id),
  metric_name TEXT,  -- 'client_replied' | 'stage_progress' | 'conversion'
  metric_value NUMERIC,
  context JSONB,     -- Дополнительный контекст
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

**Использование:**
- После отправки ответа ночным агентом → ждём 24 часа
- Проверяем: ответил ли клиент? Перешёл ли в стадию "Переписка" → "Оплата"?
- Сохраняем метрику для анализа

**Время внедрения:** 3-4 часа  
**Польза:** Понимание эффективности агента, возможность улучшать

---

## 📋 План внедрения (по приоритету)

### Фаза 1: Быстрые победы (1-2 дня)
1. ✅ **Agent Execution Logging** (2-3 часа) — сразу даст видимость
2. ✅ **Agent Memory/Context** (2-3 часа) — улучшит качество работы агентов

### Фаза 2: Улучшения (3-5 дней)
3. ✅ **Agent Tool Registry** (3-4 часа) — рефакторинг существующих агентов
4. ✅ **Agent Retry Strategy** (2 часа) — устойчивость к ошибкам

### Фаза 3: Продвинутые (по необходимости)
5. ⏳ **ReAct Pattern** (4-6 часов) — только для LLM-агентов
6. ⏳ **Agent Evaluation** (3-4 часа) — для ночного агента

---

## ⚠️ Что НЕ нужно делать (чтобы не усложнить)

1. ❌ **Сложные multi-agent frameworks** (LangGraph, AutoGen) — избыточно для нашей задачи
2. ❌ **Собственный LLM для каждого агента** — используем общий OpenAI/Anthropic
3. ❌ **Сложная система приоритетов/очередей** — пока достаточно простого event bus
4. ❌ **Agent-to-agent коммуникация** — все через оркестратор

---

## 🎯 Итоговые рекомендации

**Начать с:**
1. Agent Execution Logging — **сегодня** (2-3 часа)
2. Agent Memory/Context — **завтра** (2-3 часа)

**Эти два улучшения дадут:**
- ✅ Видимость работы агентов (логирование)
- ✅ "Умные" агенты с памятью (контекст)
- ✅ Минимальные изменения в коде
- ✅ Быстрая отдача

**Остальное — по мере необходимости**, когда появятся конкретные проблемы или требования.

---

## 📚 Ссылки

- Текущая архитектура: `ARCHITECTURE.md`, `ORCHESTRATOR.md`
- Список агентов: `AGENTS.md`
- Roadmap: `NEXT_STEPS.md`

