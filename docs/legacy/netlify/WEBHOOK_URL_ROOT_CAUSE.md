# 🔍 КОРНЕВАЯ ПРИЧИНА: Почему Webhook URL не обновился в n8n UI

## Что происходит

1. **В API n8n** ✅ Правильно:
   ```
   productionUrl: "https://webhook.rentflow.rentals"
   ```

2. **В UI n8n браузере** ❌ Неправильно:
   ```
   https://geodrive.netlify.app/webhook/rentprog-webhook
   ```

3. **Даже в приватном режиме браузера** ❌ Тот же неправильный URL

## Почему так происходит

n8n **НЕ использует** `productionUrl` из параметров workflow для отображения в UI!

n8n на **фронтенде (JavaScript)** генерирует webhook URL динамически по формуле:

```
Фактический webhook URL = WEBHOOK_URL (переменная окружения) + path
                        = [что-то] + "rentprog-webhook"
```

Значит на сервере в контейнере n8n переменная окружения `WEBHOOK_URL` содержит **ЕЩЕ СТАРОЕ значение**:
```
WEBHOOK_URL=https://geodrive.netlify.app/
```

Результат: `https://geodrive.netlify.app/ + rentprog-webhook = https://geodrive.netlify.app/rentprog-webhook`

## Почему мы не видели эту переменную раньше

1. Параметры workflow обновились правильно
2. API возвращает новые параметры
3. Но UI генерирует URL из переменной окружения контейнера
4. А она обновляется **ТОЛЬКО при перезагрузке контейнера**

## Что нужно сделать

На сервере 46.224.17.15:

### 1. Проверить текущее значение WEBHOOK_URL

```bash
ssh root@46.224.17.15
docker exec n8n printenv WEBHOOK_URL
```

**Ожидаемо:** 
```
https://geodrive.netlify.app/  ← СТАРОЕ
```

### 2. Найти и обновить docker-compose.yml

```bash
# Найти где лежит docker-compose
find / -name docker-compose.yml 2>/dev/null | head -5

# Обычно: /root/docker-compose.yml или /opt/docker-compose.yml
```

Найти строку:
```yaml
- WEBHOOK_URL=https://geodrive.netlify.app/
```

Заменить на:
```yaml
- WEBHOOK_URL=https://webhook.rentflow.rentals/
```

### 3. Перезапустить контейнер n8n

```bash
cd /path/to/docker-compose/directory
docker-compose down n8n
docker-compose up -d n8n
```

### 4. Подождать 1 минуту и проверить

```bash
docker exec n8n printenv WEBHOOK_URL
# Должно быть: https://webhook.rentflow.rentals/
```

## Проверка результата

После перезагрузки контейнера:

1. Откройте `https://n8n.rentflow.rentals` в браузере (в режиме приватного просмотра)
2. Откройте workflow "RentProg Webhooks Monitor"
3. Нажмите на Webhook ноду
4. Проверьте вкладку "Production URL" 
5. **Должно быть:** `https://webhook.rentflow.rentals/rentprog-webhook` ✅

## Итоговая схема

```
WEBHOOK_URL (переменная окружения на сервере)
    ↓
Контейнер n8n запускается и читает эту переменную
    ↓
Frontend n8n генерирует webhook URL = WEBHOOK_URL + path
    ↓
UI показывает: https://webhook.rentflow.rentals/rentprog-webhook
```

---

## Почему это не было видно раньше

1. API всегда возвращает то, что в БД (параметры workflow) — мы их обновили ✅
2. Но UI генерирует URL на фронтенде из переменной окружения сервера — она старая ❌
3. Это два разных источника данных!
