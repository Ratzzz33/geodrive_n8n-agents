# 🔐 Настройка SSH ключей и отключение пароля

## 📋 Описание

Это руководство поможет настроить безопасный доступ к серверу через SSH ключи и отключить аутентификацию по паролю.

**Преимущества:**
- ✅ Более безопасно (ключи сложнее подобрать, чем пароли)
- ✅ Удобнее (не нужно вводить пароль каждый раз)
- ✅ Подходит для автоматизации (скрипты, CI/CD)

---

## 🚀 Быстрый старт

### Шаг 1: Запуск скрипта настройки

```bash
# Из корня проекта
cd C:\Users\33pok\geodrive_n8n-agents
python setup/setup_ssh_keys.py
```

Скрипт автоматически:
1. ✅ Создаст SSH ключ (если его нет)
2. ✅ Скопирует публичный ключ на сервер
3. ✅ Протестирует подключение
4. ✅ Настроит SSH на сервере (отключит пароль)

### Шаг 2: Подтверждение отключения пароля

Скрипт спросит подтверждение перед отключением пароля. Это безопасно, если ключ уже работает.

---

## 📝 Подробная инструкция

### Вариант 1: Автоматическая настройка (рекомендуется)

#### 1.1. Запуск скрипта

```bash
python setup/setup_ssh_keys.py
```

#### 1.2. Что делает скрипт

1. **Проверка/создание SSH ключа:**
   - Проверяет наличие ключа в `~/.ssh/id_rsa`
   - Если нет - создает новый ключ (RSA 2048 бит)

2. **Копирование ключа на сервер:**
   - Подключается к серверу с паролем
   - Добавляет публичный ключ в `~/.ssh/authorized_keys`

3. **Тестирование:**
   - Пробует подключиться с ключом
   - Проверяет, что всё работает

4. **Настройка SSH на сервере:**
   - Создает резервную копию `/etc/ssh/sshd_config`
   - Отключает `PasswordAuthentication`
   - Включает `PubkeyAuthentication`
   - Перезагружает SSH сервис

#### 1.3. Результат

После успешной настройки:
- ✅ Подключение через ключ работает
- ✅ Пароль отключен
- ✅ `server_ssh.py` автоматически использует ключ

---

### Вариант 2: Ручная настройка

Если хотите настроить вручную:

#### 2.1. Создание SSH ключа

**Windows (Git Bash или PowerShell):**
```bash
# Создать ключ
ssh-keygen -t rsa -b 2048 -f ~/.ssh/id_rsa -N ""

# Проверить
ls ~/.ssh/
# Должны быть: id_rsa (приватный) и id_rsa.pub (публичный)
```

**Linux/Mac:**
```bash
ssh-keygen -t rsa -b 2048 -f ~/.ssh/id_rsa
# Нажмите Enter для пустого пароля (или введите пароль для ключа)
```

#### 2.2. Копирование ключа на сервер

**Способ 1: ssh-copy-id (Linux/Mac/Git Bash)**
```bash
ssh-copy-id root@46.224.17.15
# Введите пароль когда попросит
```

**Способ 2: Вручную (Windows)**
```bash
# 1. Прочитать публичный ключ
cat ~/.ssh/id_rsa.pub

# 2. Подключиться к серверу с паролем
ssh root@46.224.17.15

# 3. На сервере выполнить:
mkdir -p ~/.ssh
chmod 700 ~/.ssh
echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
exit
```

#### 2.3. Тестирование подключения

```bash
# Подключиться с ключом (без пароля)
ssh -i ~/.ssh/id_rsa root@46.224.17.15

# Если подключилось без пароля - ключ работает!
```

#### 2.4. Отключение пароля на сервере

**⚠️ ВАЖНО: Убедитесь, что ключ работает перед отключением пароля!**

```bash
# 1. Подключиться к серверу
ssh root@46.224.17.15

# 2. Создать резервную копию
cp /etc/ssh/sshd_config /etc/ssh/sshd_config.backup.$(date +%Y%m%d_%H%M%S)

# 3. Отредактировать конфиг
nano /etc/ssh/sshd_config

# 4. Найти и изменить:
# PasswordAuthentication yes → PasswordAuthentication no
# PubkeyAuthentication yes (убедиться что включено)

# 5. Проверить синтаксис
sshd -t

# 6. Перезагрузить SSH
systemctl reload sshd
# или
service sshd reload

# 7. Выйти и протестировать подключение с ключом
exit
ssh root@46.224.17.15  # Должно подключиться без пароля
```

---

## 🔧 Обновление server_ssh.py

После настройки ключей `server_ssh.py` автоматически:
- ✅ Сначала пробует подключиться с ключом (`~/.ssh/id_rsa`)
- ✅ Если ключ не работает - использует пароль (fallback)

**Использование:**
```python
from setup.server_ssh import ServerSSH

ssh = ServerSSH()
ssh.connect()  # Автоматически использует ключ или пароль
ssh.execute("docker ps")
ssh.close()
```

---

## 🛠️ Устранение проблем

### Проблема: "Permission denied (publickey)"

**Причина:** Ключ не добавлен на сервер или неправильные права доступа.

**Решение:**
```bash
# 1. Проверить права на ключ
chmod 600 ~/.ssh/id_rsa
chmod 644 ~/.ssh/id_rsa.pub

# 2. Проверить на сервере
ssh root@46.224.17.15 "ls -la ~/.ssh/"
# authorized_keys должен быть 600

# 3. Пересоздать ключ и скопировать заново
python setup/setup_ssh_keys.py
```

### Проблема: "Connection refused" после отключения пароля

**Причина:** SSH сервис не перезагрузился или ошибка в конфиге.

**Решение:**
```bash
# 1. Если есть доступ через консоль сервера (не SSH):
systemctl restart sshd

# 2. Если нет доступа - восстановить конфиг:
cp /etc/ssh/sshd_config.backup.* /etc/ssh/sshd_config
systemctl restart sshd
```

### Проблема: Ключ не работает, пароль отключен

**Решение:**
1. Подключитесь через консоль сервера (не SSH)
2. Восстановите пароль:
   ```bash
   # В /etc/ssh/sshd_config:
   PasswordAuthentication yes
   systemctl restart sshd
   ```
3. Пересоздайте ключ: `python setup/setup_ssh_keys.py`

---

## 📁 Расположение файлов

**Локально (Windows):**
- Приватный ключ: `C:\Users\33pok\.ssh\id_rsa`
- Публичный ключ: `C:\Users\33pok\.ssh\id_rsa.pub`

**На сервере:**
- Публичные ключи: `/root/.ssh/authorized_keys`
- Конфиг SSH: `/etc/ssh/sshd_config`
- Резервные копии: `/etc/ssh/sshd_config.backup.*`

---

## 🔒 Безопасность

### Рекомендации:

1. **Защита приватного ключа:**
   ```bash
   chmod 600 ~/.ssh/id_rsa  # Только владелец может читать
   ```

2. **Резервное копирование:**
   - Сохраните приватный ключ в безопасном месте (например, в менеджере паролей)
   - Если потеряете ключ - доступ к серверу будет невозможен!

3. **Дополнительная защита:**
   - Используйте пароль для ключа (при создании: `ssh-keygen -p`)
   - Ограничьте доступ по IP (в `/etc/ssh/sshd_config`: `AllowUsers root@IP`)

4. **Мониторинг:**
   - Проверяйте логи: `tail -f /var/log/auth.log` (Linux)
   - Настройте уведомления о неудачных попытках входа

---

## ✅ Проверка настройки

После настройки проверьте:

```bash
# 1. Подключение с ключом работает
ssh root@46.224.17.15
# Должно подключиться БЕЗ запроса пароля

# 2. Пароль отключен
ssh -o PreferredAuthentications=password root@46.224.17.15
# Должно выдать ошибку "Permission denied"

# 3. server_ssh.py использует ключ
python setup/server_ssh.py "echo 'test'"
# Должно подключиться и выполнить команду
```

---

## 📚 Дополнительные ресурсы

- [SSH Connection Guide](./SSH_CONNECTION_GUIDE.md) - Общее руководство по SSH
- [server_ssh.py](../setup/server_ssh.py) - Python SSH клиент
- [setup_ssh_keys.py](../setup/setup_ssh_keys.py) - Скрипт настройки ключей

---

**Готово!** Теперь у вас настроен безопасный доступ через SSH ключи. 🎉

