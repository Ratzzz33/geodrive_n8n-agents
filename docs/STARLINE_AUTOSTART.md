# Автозапуск Jarvis API + Starline GPS Monitor

**Дата:** 2025-11-09  
**Статус:** ✅ Готово к деплою

---

## 📋 Описание

Jarvis API с persistent Starline Scraper настроен как systemd сервис для автоматического запуска при загрузке сервера.

### Компоненты:
- **Jarvis API** - основной HTTP API сервер
- **Starline Scraper** - persistent Playwright browser session
- **Автоматический логин** - один раз при старте, далее переиспользуется сессия
- **Auto-restart** - автоматический перезапуск при падении

---

## 🚀 Установка

### 1. Деплой на сервер

```bash
cd C:\Users\33pok\geodrive_n8n-agents
git add -A
git commit -m "feat: добавлен автозапуск Jarvis API + Starline scraper"
git push origin master

# Деплой на сервер
python setup/server_ssh.py "cd /root/geodrive_n8n-agents && git pull && npm run build"
```

### 2. Установка systemd сервиса

```bash
# На сервере
cd /root/geodrive_n8n-agents
bash setup/install_jarvis_service.sh
```

Скрипт автоматически:
1. ✅ Копирует service файл в `/etc/systemd/system/`
2. ✅ Перезагружает systemd daemon
3. ✅ Включает автозапуск при загрузке
4. ✅ Останавливает старые процессы
5. ✅ Запускает новый сервис
6. ✅ Проверяет статус и логи

---

## 📊 Управление сервисом

### Основные команды:

```bash
# Статус сервиса
sudo systemctl status jarvis-api

# Перезапуск
sudo systemctl restart jarvis-api

# Остановка
sudo systemctl stop jarvis-api

# Запуск
sudo systemctl start jarvis-api

# Отключить автозапуск
sudo systemctl disable jarvis-api

# Включить автозапуск
sudo systemctl enable jarvis-api
```

---

## 📝 Логи

### Системные логи (journalctl):

```bash
# Логи в реальном времени
sudo journalctl -u jarvis-api -f

# Последние 100 строк
sudo journalctl -u jarvis-api -n 100

# Логи с определенного времени
sudo journalctl -u jarvis-api --since "2025-11-09 10:00:00"

# Логи с ошибками
sudo journalctl -u jarvis-api -p err
```

### Файловые логи:

```bash
# Stdout (информационные логи)
tail -f /var/log/jarvis-api.log

# Stderr (ошибки)
tail -f /var/log/jarvis-api-error.log

# Последние 200 строк
tail -200 /var/log/jarvis-api.log
```

---

## 🔄 Политика перезапуска

**Конфигурация:**
- `Restart=always` - перезапуск при любом падении
- `RestartSec=10` - пауза 10 секунд перед перезапуском
- `StartLimitBurst=5` - максимум 5 попыток перезапуска
- `StartLimitInterval=200` - в течение 200 секунд

**Сценарии:**
1. ✅ Падение из-за ошибки → автоматический перезапуск через 10 секунд
2. ✅ Перезагрузка сервера → автоматический запуск при загрузке
3. ✅ Падение Playwright браузера → Starline scraper переподключится автоматически
4. ❌ 5+ падений за 200 секунд → сервис останавливается (требуется ручное вмешательство)

---

## 🌐 Starline Scraper

### Инициализация при старте:

```
🚀 Starting Jarvis API Server (API only mode)...
✅ Database connected
🌐 Initializing Starline Scraper (persistent Playwright session)...
   [INFO] StarlineScraperService: Initializing persistent browser session...
   [INFO] StarlineScraperService: Logging in...
   [INFO] StarlineScraperService: ✅ Login successful
   [INFO] StarlineScraperService: ✅ Persistent browser session initialized and logged in
✅ Starline Scraper initialized successfully!
   Browser: Chromium (headless)
   Status: Logged in and ready
   Mode: Persistent session (no re-login needed)
✅ Starline Scraper health check passed
✅ Jarvis API Server is running on port 3000
```

### Автоматическое восстановление:

Если браузер падает, при следующем API запросе:
1. `ensureHealthy()` обнаруживает проблему
2. Автоматически вызывает `shutdown()` → `initialize()`
3. Браузер перезапускается и заново логинится
4. Запрос повторяется

---

## ✅ Проверка работоспособности

### 1. Проверить статус сервиса:

```bash
sudo systemctl status jarvis-api
# Должно быть: active (running)
```

### 2. Проверить health endpoint:

```bash
curl http://localhost:3000/health
# Ожидается: {"ok":true, ...}
```

### 3. Проверить Starline endpoints:

```bash
# Статус синхронизации
curl http://localhost:3000/starline/sync-status

# Список устройств (займет ~0.5-1 секунду)
curl http://localhost:3000/starline/sync-devices
```

### 4. Проверить логи:

```bash
sudo journalctl -u jarvis-api -n 50
# Должны быть логи инициализации Starline scraper
```

---

## 🐛 Troubleshooting

### Проблема: Сервис не запускается

**Проверка:**
```bash
sudo journalctl -u jarvis-api -n 100
```

**Частые причины:**
1. ❌ `.env` файл не найден → проверить `/root/geodrive_n8n-agents/.env`
2. ❌ Playwright браузеры не установлены → `npx playwright install chromium`
3. ❌ Порт 3000 занят → `lsof -i :3000` и убить процесс
4. ❌ Нет прав на `/var/log/` → `sudo chown root:root /var/log/jarvis-api*.log`

### Проблема: Starline scraper не инициализируется

**Проверка:**
```bash
# Проверить переменные окружения
sudo systemctl show jarvis-api | grep Environment
```

**Должны быть:**
- `STARLINE_USERNAME=YtZvrNYWR`
- `STARLINE_PASSWORD=7733Alex`

**Исправление:**
```bash
# Убедиться что .env содержит правильные значения
cat /root/geodrive_n8n-agents/.env | grep STARLINE

# Перезапустить сервис
sudo systemctl restart jarvis-api
```

### Проблема: Браузер падает постоянно

**Проверка:**
```bash
# Проверить установлены ли зависимости Playwright
npx playwright install-deps chromium

# Проверить доступность дисплея (для headless)
echo $DISPLAY  # Должно быть пусто или :0
```

### Проблема: Сервис перезапускается в цикле

**Проверка:**
```bash
sudo journalctl -u jarvis-api --since "5 minutes ago"
```

**Частые причины:**
1. ❌ Ошибка в коде → проверить stacktrace в логах
2. ❌ База данных недоступна → проверить Neon connection string
3. ❌ Недостаточно памяти → `free -h` и рестартовать сервер

**Временное решение:**
```bash
# Остановить автозапуск до исправления проблемы
sudo systemctl stop jarvis-api
sudo systemctl disable jarvis-api
```

---

## 🔧 Обновление конфигурации

### Изменить параметры сервиса:

```bash
# 1. Редактировать service файл
sudo nano /etc/systemd/system/jarvis-api.service

# 2. Перезагрузить конфигурацию
sudo systemctl daemon-reload

# 3. Перезапустить сервис
sudo systemctl restart jarvis-api
```

### Изменить переменные окружения:

```bash
# 1. Редактировать .env
nano /root/geodrive_n8n-agents/.env

# 2. Перезапустить сервис (он подхватит новые значения)
sudo systemctl restart jarvis-api
```

---

## 📈 Мониторинг

### CPU и Memory:

```bash
# Текущее потребление
ps aux | grep 'node.*dist/api'

# Постоянный мониторинг
top -p $(pgrep -f 'node.*dist/api')
```

### Uptime сервиса:

```bash
sudo systemctl status jarvis-api | grep "Active:"
# Покажет: Active: active (running) since ...
```

---

## 🎯 Итоги

- ✅ **Автозапуск** - Jarvis API запускается автоматически при загрузке сервера
- ✅ **Persistent Playwright** - Браузер остается открытым, логин только один раз
- ✅ **Auto-recovery** - Автоматическое восстановление при падении браузера
- ✅ **Service restart** - Автоматический перезапуск при ошибках
- ✅ **Логирование** - Все логи доступны через journalctl и файлы
- ✅ **Мониторинг** - Легко проверить статус и производительность

---

**Следующие шаги:**
1. Импортировать n8n workflow "Starline GPS Monitor"
2. Настроить cron для вызова `/starline/update-gps` каждую минуту
3. Создать Telegram алерты для критичных событий GPS

