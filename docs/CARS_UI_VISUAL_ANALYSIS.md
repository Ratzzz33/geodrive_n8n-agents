# 🎨 Визуальный анализ страницы автомобилей RentProg

**Дата:** 2025-11-14  
**URL:** https://web.rentprog.ru/cars  
**Метод:** MCP Chrome + детальный JavaScript анализ

---

## 📋 СТРУКТУРА ТАБЛИЦЫ

### Заголовки колонок (17 колонок):
1. **ID** - ID автомобиля
2. **Название** - марка и модель
3. **Код** - внутренний код
4. **Состояние** - 🎯 **ИКОНКА** (зеленый палец вверх)
5. **№** - государственный номер
6. **Цвет** - цвет кузова
7. **Год** - год выпуска
8. **1 - 2** - цена за 1-2 дня
9. **3 - 4** - цена за 3-4 дня
10. **5 - 7** - цена за 5-7 дней
11. **8 - 15** - цена за 8-15 дней
12. **16 - 30** - цена за 16-30 дней
13. **30+** - цена за 30+ дней
14. **Цена за час** - почасовая аренда
15. **Залог** - залог
16. **ТО** - дата последнего техосмотра
17. **Резина** - 🎯 **ИКОНКА** (сезон резины)

---

## 🎯 КРИТИЧНЫЕ ВИЗУАЛЬНЫЕ ЭЛЕМЕНТЫ

### 1. **Колонка "Состояние" (4-я колонка)**

#### 🟢 Зеленый палец вверх `mdi-thumb-up`
- **Класс:** `mdi mdi-thumb-up success--text`
- **Цвет:** `rgb(76, 175, 80)` (зеленый)
- **Значение:** Автомобиль доступен для аренды
- **Примеры:** Chevrolet Malibu (ID 47192), BMW X1 (ID 66415), Subaru Outback (ID 63899), Mini Countryman (ID 46224), MINI Hatch (ID 45440)
- **API поле:** `"state": 1` + `"active": true`

#### ❓ Другие возможные иконки (НЕ найдены в первых 5 машинах):
- 🟡 Желтая иконка предупреждения → `"state": 2` (требует внимания)
- 🔴 Красный крест/запрет → `"state": 3` или `"active": false` (нельзя выдавать)

**⚠️ ВАЖНО:** Мы **НЕ парсим** это состояние! У нас в БД нет полей `state` и `active`.

---

### 2. **Колонка "Резина" (17-я колонка)**

#### ❄️ Снежинка `mdi-snowflake` (зимняя резина)
- **Класс:** `mdi mdi-snowflake info--text`
- **Цвет:** `rgb(33, 150, 243)` (синий)
- **Значение:** Зимняя резина
- **Примеры:** Chevrolet Malibu (ID 47192), Mini Countryman (ID 46224)
- **API поле:** `"tire_type": 2` (предположительно: 1=летняя, 2=зимняя)

#### ☀️ Солнце `mdi-weather-sunny` (летняя резина)
- **Класс:** `mdi mdi-weather-sunny yellow--text`
- **Цвет:** `rgb(255, 235, 59)` (желтый)
- **Значение:** Летняя резина
- **Примеры:** BMW X1 (ID 66415), Subaru Outback (ID 63899)
- **API поле:** `"tire_type": 1` (предположительно)

#### 🔄 Луна-солнце `mdi-theme-light-dark` (всесезонная резина)
- **Класс:** `mdi mdi-theme-light-dark info--text`
- **Цвет:** `rgb(33, 150, 243)` (синий)
- **Значение:** Всесезонная резина
- **Пример:** MINI Hatch (ID 45440)
- **API поле:** `"tire_type": 3` (предположительно)

**⚠️ ВАЖНО:** Мы **ПАРСИМ** `tire_type` в поле `data` (JSONB), но **НЕ извлекаем** в отдельное поле БД!

---

### 3. **Цены по дням (колонки 8-13)**

#### Chevrolet Malibu (ID 47192):
- 1-2 дня: **107** лари
- 3-4 дня: **102** лари
- 5-7 дней: **96** лари
- 8-15 дней: **91** лари
- 16-30 дней: **77** лари
- 30+ дней: **63** лари

#### Mini Countryman (ID 46224):
- 1-2 дня: **179** лари
- 3-4 дня: **173** лари
- 5-7 дней: **168** лари
- 8-15 дней: **162** лари
- 16-30 дней: **138** лари
- 30+ дней: **110** лари

#### BMW X1, Subaru Outback, MINI Hatch:
- Все цены: **?** (не настроены)

**⚠️ ПРОБЛЕМА:** Эти цены **НЕ ДОСТУПНЫ** в API endpoint `/all_cars_with_bookings`!  
API возвращает только `relationships.prices.data` - массив ID, но не сами цены.

---

### 4. **Колонка "ТО" (16-я колонка)**

- **Mini Countryman (ID 46224):** `30 апр. 2024 г.`
- **MINI Hatch (ID 45440):** `30 апр. 2024 г.`
- **Остальные:** `-` (нет данных)

**API поле:** `"last_inspection"`

**⚠️ ВАЖНО:** Мы **ПАРСИМ** это поле в `data`, но **НЕ извлекаем** в отдельное поле БД!

---

## 📊 СРАВНЕНИЕ: UI vs БД vs API

| Что видим в UI | API поле | Есть в БД? | Парсим? |
|----------------|----------|------------|---------|
| 🟢 Зеленый палец (состояние) | `state: 1`, `active: true` | ❌ НЕТ | ❌ НЕТ |
| ❄️ Снежинка (зимняя резина) | `tire_type: 2` | ❌ НЕТ (только в `data`) | ⚠️ JSONB |
| ☀️ Солнце (летняя резина) | `tire_type: 1` | ❌ НЕТ (только в `data`) | ⚠️ JSONB |
| 🔄 Всесезонная резина | `tire_type: 3` | ❌ НЕТ (только в `data`) | ⚠️ JSONB |
| Цены 1-2 дня | ❌ НЕТ В API | ❌ НЕТ | ❌ НЕТ |
| Цены 3-4 дня | ❌ НЕТ В API | ❌ НЕТ | ❌ НЕТ |
| Цены 5-7 дней | ❌ НЕТ В API | ❌ НЕТ | ❌ НЕТ |
| Цены 8-15 дней | ❌ НЕТ В API | ❌ НЕТ | ❌ НЕТ |
| Цены 16-30 дней | ❌ НЕТ В API | ❌ НЕТ | ❌ НЕТ |
| Цены 30+ дней | ❌ НЕТ В API | ❌ НЕТ | ❌ НЕТ |
| Цена за час | `price_hour` | ❌ НЕТ (только в `data`) | ⚠️ JSONB |
| Залог | `deposit` | ✅ ДА | ✅ ДА |
| Дата ТО | `last_inspection` | ❌ НЕТ (только в `data`) | ⚠️ JSONB |

---

## 🚨 КРИТИЧНЫЕ ВЫВОДЫ

### 1. **Состояние автомобиля (state) - НЕ ПАРСИМ!**

**Что теряем:**
- 🟢 Зеленый = доступен (`state: 1`)
- 🟡 Желтый = требует внимания (`state: 2`)
- 🔴 Красный = нельзя выдавать (`state: 3` или `active: false`)

**Важность:** ⭐⭐⭐⭐⭐ **КРИТИЧНО**  
**Кто использует:** Агент контролер броней, Агент контроля ремонтов

---

### 2. **Цены по дням (1-2, 3-4, 5-7, ...) - НЕТ В API!**

**Проблема:**
- API `/all_cars_with_bookings` возвращает только `relationships.prices.data` - массив ID
- Сами цены **НЕ ВКЛЮЧЕНЫ** в ответ
- В UI они отображаются, значит есть другой endpoint или included-секция

**Решение:**
- Нужно либо найти другой endpoint
- Или парсить `included` секцию из JSON:API ответа

**Важность:** ⭐⭐⭐⭐⭐ **КРИТИЧНО**  
**Кто использует:** Отдел продаж, Калькулятор стоимости

---

### 3. **Тип резины (tire_type) - НЕ ИЗВЛЕКАЕМ В ОТДЕЛЬНОЕ ПОЛЕ**

**Что теряем:**
- ❄️ Зимняя = `tire_type: 2`
- ☀️ Летняя = `tire_type: 1`
- 🔄 Всесезонная = `tire_type: 3`

**Важность:** ⭐⭐⭐⭐ **ВАЖНО**  
**Кто использует:** Агент контроля ТО (сезонная замена)

---

### 4. **Дата ТО (last_inspection) - НЕ ИЗВЛЕКАЕМ**

**Важность:** ⭐⭐⭐⭐⭐ **КРИТИЧНО**  
**Кто использует:** Агент контроля ТО

---

## 📋 РЕКОМЕНДАЦИИ ПО ПАРСИНГУ

### P0 (СРОЧНО):

1. **Добавить в БД:**
```sql
ALTER TABLE cars ADD COLUMN IF NOT EXISTS
  active BOOLEAN DEFAULT TRUE,
  state INTEGER, -- 1=доступен, 2=внимание, 3=нельзя выдавать
  tire_type INTEGER, -- 1=лето, 2=зима, 3=всесезон
  last_inspection TIMESTAMPTZ,
  price_hour INTEGER;
```

2. **Обновить парсер (Merge & Process):**
```javascript
{
  branch: branch,
  rentprog_id: car.id,
  car_name: car.name || car.car_name,
  code: car.code,
  number: car.number,
  
  // ⬇️ ДОБАВИТЬ:
  active: car.active,
  state: car.state,
  tire_type: car.tire_type,
  last_inspection: car.last_inspection,
  price_hour: car.price_hour,
  
  color: car.color,
  year: car.year,
  deposit: car.deposit,
  data: car
}
```

3. **Найти endpoint для цен по дням:**
   - Проверить `included` секцию в `/all_cars_with_bookings`
   - Или использовать отдельный endpoint для цен

---

### P1 (ВАЖНО):

4. **Добавить другие критичные поля:**
   - `mileage` - пробег
   - `transmission` - коробка передач
   - `vin` - VIN номер
   - `investor_id` - инвестор

---

## 🎨 ЛЕГЕНДА ИКОНОК

| Иконка | Класс | Цвет | Значение | API поле |
|--------|-------|------|----------|----------|
| 🟢 👍 | `mdi-thumb-up success--text` | rgb(76, 175, 80) | Доступен | `state: 1`, `active: true` |
| ❄️ | `mdi-snowflake info--text` | rgb(33, 150, 243) | Зимняя резина | `tire_type: 2` |
| ☀️ | `mdi-weather-sunny yellow--text` | rgb(255, 235, 59) | Летняя резина | `tire_type: 1` |
| 🔄 | `mdi-theme-light-dark info--text` | rgb(33, 150, 243) | Всесезонная | `tire_type: 3` |

---

## 🔍 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Открыть детальную страницу автомобиля через MCP Chrome
2. ✅ Проанализировать вкладки (Брони, Цены, Обслуживание, и т.д.)
3. ✅ Сравнить данные детальной страницы с БД
4. ✅ Обновить парсер для извлечения критичных полей

---

**Браузер остается ОТКРЫТ. Готов к дальнейшему анализу!** 🚀

