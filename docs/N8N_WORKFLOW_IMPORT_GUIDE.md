# Инструкция по импорту и обновлению workflow в n8n

## ⚠️ ОБЯЗАТЕЛЬНО ДЛЯ АГЕНТА CURSOR

**Этот документ описывает единственный правильный способ работы с n8n workflow через API. Все импорты и обновления workflow ДОЛЖНЫ выполняться через PowerShell скрипт `setup/setup_n8n_via_curl.ps1`.**

### 🔑 Получение API ключа n8n

**ВАЖНО:** API ключи n8n имеют срок действия. При ошибке 401 получите новый ключ:

1. Откройте: http://46.224.17.15:5678
2. Войдите (33pokrov33@gmail.com)
3. Settings → API → Create API Key
4. Скопируйте ключ
5. Обновите переменную `$env:N8N_API_KEY` или в скрипте

**Текущий ключ (действителен до 2025-12-02):**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI3ZDYyYjM3My0yMDFiLTQ3ZjMtODU5YS1jZGM2OWRkZWE0NGEiLCJpc3MiOiJuOG4iLCJhdWQiOiJwdWJsaWMtYXBpIiwiaWF0IjoxNzYyMDg0MjY4LCJleHAiOjE3NjQ2NTE2MDB9.gsdxltowlQShNi9mil074-cMhnuJJLI5lN6MP7FQEcI
```

---

## Основной скрипт

**Файл:** `setup/setup_n8n_via_curl.ps1`

Этот скрипт является стандартным инструментом для:
- Создания credentials (PostgreSQL, Telegram Bot)
- Импорта workflow из JSON файлов
- Обновления существующих workflow
- Автоматического назначения credentials в нодах
- Активации workflow

---

## Как добавить новый workflow

### Шаг 1: Добавить workflow файл в репозиторий

1. Создайте JSON файл workflow в папке `n8n-workflows/`
2. Убедитесь, что файл валидный и содержит:
   - `name` - название workflow
   - `nodes` - массив нод
   - Правильные типы нод (`n8n-nodes-base.postgres`, `n8n-nodes-base.telegram`, etc.)
   - Credentials должны иметь структуру:
     ```json
     {
       "credentials": {
         "postgres": {
           "id": "postgres-credentials",
           "name": "PostgreSQL"
         }
       }
     }
     ```

### Шаг 2: Добавить в массив `$workflows` в скрипте

Откройте `setup/setup_n8n_via_curl.ps1` и найдите секцию (строка ~99):

```powershell
$workflows = @(
    "n8n-workflows/rentprog-webhooks-monitor.json",
    "n8n-workflows/sync-progress.json",
    "n8n-workflows/health-status.json"
)
```

**Добавьте новый workflow:**
```powershell
$workflows = @(
    "n8n-workflows/rentprog-webhooks-monitor.json",
    "n8n-workflows/sync-progress.json",
    "n8n-workflows/health-status.json",
    "n8n-workflows/your-new-workflow.json"  # <-- добавить здесь
)
```

### Шаг 3: Запустить скрипт

```powershell
powershell -ExecutionPolicy Bypass -File .\setup\setup_n8n_via_curl.ps1
```

---

## Как работает скрипт

### 1. Создание/получение credentials

**PostgreSQL credential:**
- Проверяет существование credential "PostgreSQL"
- Если существует - использует его ID
- Если нет - создает новый с подключением к Neon

**Telegram Bot credential:**
- Проверяет переменную окружения `TELEGRAM_BOT_TOKEN`
- Если токен указан - создает или использует существующий credential
- Если токена нет - пропускает создание

### 2. Импорт workflow

Для каждого файла из массива `$workflows`:

1. **Проверка существования:**
   ```powershell
   $existing = Invoke-RestMethod -Uri "$N8N_HOST/workflows" -Method GET -Headers $headers
   $existingWorkflow = $existing.data | Where-Object { $_.name -eq $workflowJson.name }
   ```

2. **Если workflow существует:**
   - Обновляет существующий через `PUT /workflows/{id}`
   - Сохраняет ID и статус `active`

3. **Если workflow не существует:**
   - Создает новый через `POST /workflows`
   - Получает новый ID

4. **Обновление credentials в нодах:**
   ```powershell
   foreach ($node in $response.nodes) {
       if ($node.credentials -and $node.credentials.postgres) {
           $node.credentials.postgres.id = $postgresCredId
           $updated = $true
       }
       if ($node.credentials -and $node.credentials.telegramApi -and $telegramCredId) {
           $node.credentials.telegramApi.id = $telegramCredId
           $updated = $true
       }
   }
   ```
   Затем отправляет обновленный workflow обратно через `PUT /workflows/{id}`

### 3. Активация workflow

Для каждого импортированного workflow:
- Проверяет статус `active`
- Если неактивен - активирует через `POST /workflows/{id}/activate`

---

## Структура API запросов

### Заголовки
```powershell
$headers = @{
    "X-N8N-API-KEY" = "YOUR_API_KEY"
    "Content-Type" = "application/json"
}
```

### Основные endpoints

1. **Получение списка workflow:**
   ```
   GET http://46.224.17.15:5678/api/v1/workflows
   ```

2. **Создание workflow:**
   ```
   POST http://46.224.17.15:5678/api/v1/workflows
   Body: JSON workflow
   ```

3. **Обновление workflow:**
   ```
   PUT http://46.224.17.15:5678/api/v1/workflows/{id}
   Body: JSON workflow
   ```

4. **Активация workflow:**
   ```
   POST http://46.224.17.15:5678/api/v1/workflows/{id}/activate
   ```

5. **Получение/создание credentials:**
   ```
   GET http://46.224.17.15:5678/api/v1/credentials
   POST http://46.224.17.15:5678/api/v1/credentials
   ```

---

## Важные правила

### ✅ ДЕЛАТЬ:

1. **Всегда используйте `setup_n8n_via_curl.ps1`** для импорта workflow
2. **Добавляйте новые workflow в массив `$workflows`** перед запуском
3. **Проверяйте существование workflow** перед созданием (скрипт делает это автоматически)
4. **Обновляйте credentials после импорта** (скрипт делает это автоматически)
5. **Активируйте workflow после импорта** (скрипт делает это автоматически)

### ❌ НЕ ДЕЛАТЬ:

1. **Не импортируйте workflow вручную через UI** - это обходит автоматизацию
2. **Не создавайте отдельные скрипты** для каждого workflow - используйте общий скрипт
3. **Не обновляйте workflow напрямую через API** без обновления credentials
4. **Не активируйте workflow вручную** - скрипт делает это автоматически
5. **Не используйте браузерную автоматизацию** для настройки workflow - это ненадежно

---

## Обновление существующего workflow

Если нужно обновить существующий workflow:

1. **Отредактируйте JSON файл** в `n8n-workflows/`
2. **Запустите скрипт** - он автоматически обновит существующий workflow
3. **Credentials сохранятся** - скрипт обновит их после получения существующего workflow

Важно: Скрипт получает существующий workflow через GET, сохраняет credentials из него, затем применяет обновления и сохраняет обратно.

---

## Переменные окружения

Скрипт использует:
- `$env:TELEGRAM_BOT_TOKEN` - токен бота для Telegram credential (опционально)

Если токен не указан, Telegram credential не создается, но workflow импортируются.

---

## Пример использования

### Добавление нового workflow "RentProg Upsert Processor"

1. Файл уже создан: `n8n-workflows/rentprog-upsert-processor.json`

2. Обновить скрипт:
   ```powershell
   $workflows = @(
       "n8n-workflows/rentprog-webhooks-monitor.json",
       "n8n-workflows/sync-progress.json",
       "n8n-workflows/health-status.json",
       "n8n-workflows/rentprog-upsert-processor.json"
   )
   ```

3. Запустить:
   ```powershell
   powershell -ExecutionPolicy Bypass -File .\setup\setup_n8n_via_curl.ps1
   ```

4. Скрипт автоматически:
   - Проверит/создаст credentials
   - Импортирует все 4 workflow
   - Назначит credentials в нодах
   - Активирует workflow

---

## Получение нового API ключа n8n

Если получаете ошибку 401 (Unauthorized):

1. Откройте n8n UI: `http://46.224.17.15:5678`
2. Войдите в систему
3. Перейдите в **Settings** → **API** (или **Personal Settings** → **API Keys**)
4. Нажмите **Create API Key**
5. Скопируйте новый ключ
6. Обновите `N8N_API_KEY` в файле `setup/setup_n8n_via_curl.ps1` (строка 5)

Или установите через переменную окружения перед запуском:
```powershell
$env:N8N_API_KEY = "your_new_api_key_here"
powershell -ExecutionPolicy Bypass -File .\setup\setup_n8n_via_curl.ps1
```

## Troubleshooting

### Ошибка 401 (Unauthorized)
- **Причина:** API ключ истек или неверный
- **Решение:** Получите новый API ключ через UI n8n (см. раздел выше)
- Обновите ключ в скрипте `setup_n8n_via_curl.ps1` или установите через `$env:N8N_API_KEY`

### Ошибка 409 (Conflict)
- Credential уже существует - это нормально, скрипт использует существующий

### Workflow не активируется
- Проверьте, что все ноды настроены правильно
- Проверьте, что credentials назначены
- Вручную откройте workflow в UI и проверьте ошибки

### Credentials не назначаются
- Убедитесь, что в JSON файле workflow указана правильная структура credentials
- Проверьте, что `postgresCredId` получен (PostgreSQL credential создан/найден)

---

## Дополнительные файлы

- `setup/setup_n8n_via_api.ts` - альтернативная версия на TypeScript (не рекомендуется использовать)
- `setup/import_n8n_workflows.ts` - упрощенная версия для импорта (использует тот же подход)

**Всегда используйте `setup_n8n_via_curl.ps1` как основной инструмент!**

---

## Контакты и поддержка

Если возникают проблемы:
1. Проверьте логи выполнения скрипта
2. Убедитесь, что n8n доступен: `http://46.224.17.15:5678`
3. Проверьте API ключ через UI n8n
4. Проверьте структуру JSON файла workflow

---

**Последнее обновление:** 2025-01-15  
**Версия документа:** 1.0  
**Статус:** Актуальная обязательная инструкция

