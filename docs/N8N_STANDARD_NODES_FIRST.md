# Приоритет стандартных нод n8n

**Дата:** 2025-01-16  
**Статус:** ✅ Принятый принцип разработки

---

## 🎯 Основной принцип

**При создании workflow в n8n отдавать предпочтение стандартным нодам перед Code нодами.**

---

## ✅ Преимущества стандартных нод

### 1. Надежность
- Протестированы командой n8n
- Регулярно обновляются
- Меньше багов

### 2. Удобство обслуживания
- Визуальная конфигурация
- Понятны для любого разработчика
- Легко модифицировать без программирования

### 3. Производительность
- Оптимизированы под n8n
- Встроенная обработка ошибок
- Автоматическое управление памятью

### 4. Документация
- Подробная документация в n8n UI
- Примеры использования
- Community support

---

## ❌ Когда НЕ использовать Code ноды

### 1. Простые преобразования данных
**Плохо:**
```javascript
// Code нода
const result = $input.item.json.value * 2;
return { json: { result } };
```

**Хорошо:**
Используйте **Set** или **Edit Fields** ноду

---

### 2. HTTP запросы
**Плохо:**
```javascript
// Code нода
const response = await fetch('https://api.example.com/data');
return { json: await response.json() };
```

**Хорошо:**
Используйте **HTTP Request** ноду

---

### 3. Фильтрация данных
**Плохо:**
```javascript
// Code нода
return $input.all().filter(item => item.json.status === 'active');
```

**Хорошо:**
Используйте **Filter** ноду

---

### 4. Условная логика
**Плохо:**
```javascript
// Code нода
if ($input.item.json.age > 18) {
  return { json: { allowed: true } };
}
```

**Хорошо:**
Используйте **If** или **Switch** ноду

---

## ✅ Когда Code нода оправдана

### 1. Сложная логика парсинга
**Пример:** `Parse & Validate Format` в `RentProg Webhooks Monitor`

**Почему:**
- Парсинг Ruby hash → JSON (нестандартный формат)
- Множественные условия валидации
- Динамическая функция `validateEventFormat()` с switch для 9 типов событий
- Невозможно реализовать стандартными нодами без 50+ нод

**Код:**
```javascript
// Парсинг Ruby hash
let jsonStr = payloadStr
  .replace(/=>/g, ':')
  .replace(/([{, ])([a-zA-Z_][a-zA-Z0-9_]*):/g, '$1"$2":')
  .replace(/nil/g, 'null');

// Валидация формата по типу события
function validateEventFormat(evType, payload, errors) {
  if (!payload.id) {
    errors.push(`${evType}: отсутствует обязательное поле 'id'`);
    return false;
  }
  
  switch (evType) {
    case 'car_update':
      const carFields = ['mileage', 'clean_state', 'status'];
      if (!carFields.some(f => payload[f] !== undefined)) {
        errors.push('car_update: нет полей для обновления');
        return false;
      }
      break;
    // ... еще 8 типов событий
  }
  
  return true;
}
```

---

### 2. Криптографические операции
**Пример:** Генерация `event_hash` для дедупликации

**Почему:**
- Требуется `crypto.createHash('sha256')`
- Нет стандартной ноды для SHA256

**Код:**
```javascript
const crypto = require('crypto');
const eventHash = crypto.createHash('sha256')
  .update(JSON.stringify(data))
  .digest('hex');
```

---

### 3. Работа с нестандартными форматами данных
**Пример:** Обработка массивов с вложенными объектами

**Почему:**
- Сложные трансформации данных
- Множественные уровни вложенности
- Итеративная обработка

---

### 4. Оптимизация производительности
**Пример:** Обработка больших объемов данных

**Почему:**
- Батчинг операций
- Кэширование результатов
- Минимизация количества нод

---

## 📋 Чеклист: Использовать ли Code ноду?

Перед созданием Code ноды, задайте себе вопросы:

- [ ] **Есть ли стандартная нода для этой задачи?**  
      → Если да, используйте её

- [ ] **Можно ли разбить логику на несколько стандартных нод?**  
      → Если да, разбейте

- [ ] **Логика слишком сложная для стандартных нод?**  
      → Code нода может быть оправдана

- [ ] **Требуются специфические библиотеки (crypto, regex)?**  
      → Code нода оправдана

- [ ] **Нестандартный формат данных?**  
      → Code нода оправдана

- [ ] **Оптимизация производительности критична?**  
      → Code нода оправдана

---

## 🔄 Рефакторинг: Code → Standard

### Пример 1: Умножение значения

**До (Code):**
```javascript
const value = $input.item.json.price;
return { json: { total: value * 1.2 } };
```

**После (Set):**
- Нода: **Set**
- Assignments:
  - `total = {{ $json.price * 1.2 }}`

---

### Пример 2: Фильтрация по условию

**До (Code):**
```javascript
const items = $input.all();
return items.filter(item => item.json.status === 'active');
```

**После (Filter):**
- Нода: **Filter**
- Condition: `status` equals `active`

---

### Пример 3: HTTP запрос с обработкой

**До (Code):**
```javascript
const response = await fetch('https://api.example.com/data', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ id: $input.item.json.id })
});
return { json: await response.json() };
```

**После (HTTP Request):**
- Нода: **HTTP Request**
- Method: `POST`
- URL: `https://api.example.com/data`
- Body Parameters: `id = {{ $json.id }}`

---

## 📊 Статистика по текущим workflow

### RentProg Webhooks Monitor

| Нода | Тип | Оправдана? | Причина |
|------|-----|------------|---------|
| Parse & Validate Format | Code | ✅ Да | Сложная логика парсинга Ruby hash + валидация 9 типов |
| If Known Format | Standard | ✅ Да | Простая условная логика |
| Auto Process | Standard | ✅ Да | HTTP запрос |
| Set Query Params | Standard | ✅ Да | Подготовка данных |
| Save Event | Standard | ✅ Да | PostgreSQL INSERT |

**Вывод:** Только 1 из 12 нод использует Code, и это **оправдано**.

---

## 🎓 Обучение

### Для новых разработчиков

1. **Изучите стандартные ноды:**
   - n8n UI → Nodes panel → Browse all nodes
   - Документация: https://docs.n8n.io/integrations/builtin/

2. **Практикуйтесь на простых задачах:**
   - Создайте workflow без Code нод
   - Используйте только Standard ноды

3. **Рефакторите существующие Code ноды:**
   - Найдите Code ноды в проекте
   - Попробуйте заменить их на Standard

---

## 📝 Связанные документы

- [WEBHOOK_EVENT_VALIDATION.md](./WEBHOOK_EVENT_VALIDATION.md) - Пример оправданного использования Code ноды
- [ARCHITECTURE.md](../ARCHITECTURE.md) - Общая архитектура
- [n8n Documentation](https://docs.n8n.io/) - Официальная документация n8n

---

**Последнее обновление:** 2025-01-16  
**Автор:** System Architecture  
**Статус:** ✅ Active Guideline

