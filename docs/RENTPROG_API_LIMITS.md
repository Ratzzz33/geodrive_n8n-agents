# RentProg API - Лимиты запросов

**Дата обновления:** 2025-11-09  
**Источник:** Официальная документация RentProg API

---

## 📊 Ограничения RentProg API

### Официальные лимиты:

| Тип операции | Лимит RentProg | Период |
|--------------|----------------|--------|
| **Чтение (GET)** | 120 запросов | 1 минута |
| **Запись (POST/PUT/PATCH)** | 60 запросов | 1 минута |
| **Поиск** | 30 запросов | 1 минута |
| **Авторизация** | 60 запросов | 1 минута |

### ⚠️ Наши безопасные лимиты (с запасом для других сервисов):

| Тип операции | Наш лимит | Задержка между запросами | Примечания |
|--------------|-----------|--------------------------|------------|
| **GET (чтение)** | **40 запросов/мин** | **~1.5 сек** | Оставляем 80/мин для других |
| **POST (запись)** | **20 запросов/мин** | **~3 сек** | Оставляем 40/мин для других |
| **Search (поиск)** | **10 запросов/мин** | **~6 сек** | Оставляем 20/мин для других |
| **Auth (токены)** | **Кэшировать!** | 1 токен на 4 мин | Max 4-8 токенов/мин (по филиалам) |

**Принцип:** Используем ~33% от лимита RentProg, оставляя 66% для других сервисов компании.

**При превышении лимита:** HTTP 429 → подождать 60 секунд

---

## ⚠️ Важные правила

### 1. Токены авторизации

- **Request token** выдается на ~240 секунд (4 минуты)
- Кэшируйте токены и переиспользуйте их
- **НЕ** запрашивайте новый токен на каждый запрос
- Лимит: 60 токенов/минуту (избыточно при правильном кэшировании)

### 2. Пагинация

- **Стандарт:** 10-20 записей за запрос
- Листайте страницы с задержками между запросами
- Для массовой выгрузки: ~5-10 филиалов × 20 авто = 100-200 запросов

### 3. Rate Limiting

При превышении лимитов:
- **HTTP 429 Too Many Requests**
- Подождите 60 секунд перед повтором
- Используйте exponential backoff

---

## 🎯 Рекомендации по реализации

### Кэширование токенов

```typescript
// ✅ ПРАВИЛЬНО - один токен на 4 минуты
const tokenCache = new Map<string, { token: string, expiresAt: Date }>();

async function getCachedToken(branch: BranchName): Promise<string> {
  const cached = tokenCache.get(branch);
  if (cached && cached.expiresAt > new Date()) {
    return cached.token;
  }
  
  // Запрашиваем новый токен только если истек
  const { token, expiresAt } = await getBranchToken(branch);
  tokenCache.set(branch, { token, expiresAt });
  return token;
}

// ❌ НЕПРАВИЛЬНО - токен на каждый запрос
async function getData() {
  const token = await getNewToken(); // 60 запросов/мин израсходуются быстро!
  return await fetch(url, { headers: { Authorization: token }});
}
```

### Задержки между запросами

```typescript
// ✅ ПРАВИЛЬНО - консервативный подход (с запасом для других сервисов)
const DELAY_BETWEEN_GET_REQUESTS = 1500;  // 1.5 сек = 40 запросов/мин
const DELAY_BETWEEN_POST_REQUESTS = 3000; // 3 сек = 20 запросов/мин
const DELAY_BETWEEN_SEARCH_REQUESTS = 6000; // 6 сек = 10 запросов/мин

for (const branch of branches) {
  await processBranch(branch);
  await sleep(DELAY_BETWEEN_GET_REQUESTS);
}

// ❌ НЕПРАВИЛЬНО - без задержек или слишком частые запросы
for (const branch of branches) {
  await processBranch(branch); // Rate limit за 5 секунд!
  // или
  await sleep(500); // Слишком быстро - 120 запросов/мин, не оставляет места другим!
}
```

### Пакетная обработка

```typescript
// ✅ ПРАВИЛЬНО - batch с консервативными задержками
async function syncAllCars() {
  const branches = ['tbilisi', 'batumi', 'kutaisi', 'service-center'];
  
  for (const branch of branches) {
    console.log(`Синхронизация ${branch}...`);
    const token = await getCachedToken(branch); // Кэшированный токен
    
    let page = 1;
    while (true) {
      const cars = await fetchCarsPage(branch, token, page);
      if (cars.length === 0) break;
      
      await processCars(cars);
      
      // Задержка между страницами: 1.5 сек (40 запросов/мин)
      await sleep(1500);
      page++;
    }
    
    // Задержка между филиалами
    await sleep(3000);
  }
}
```

### Retry с backoff

```typescript
// ✅ ПРАВИЛЬНО - exponential backoff
async function fetchWithRetry<T>(
  fn: () => Promise<T>,
  maxRetries = 3
): Promise<T> {
  for (let i = 0; i < maxRetries; i++) {
    try {
      return await fn();
    } catch (error) {
      if (error.status === 429) {
        const delay = Math.pow(2, i) * 1000; // 1s, 2s, 4s
        console.log(`Rate limit, ожидание ${delay}ms...`);
        await sleep(delay);
        continue;
      }
      throw error;
    }
  }
  throw new Error('Max retries exceeded');
}
```

---

## 📈 Расчет нагрузки

### Пример: Синхронизация всех автомобилей

**Исходные данные:**
- 4 филиала (tbilisi, batumi, kutaisi, service-center)
- ~50 автомобилей на филиал = 200 авто
- Пагинация: 20 авто/страницу = 10 страниц
- Всего: 4 филиала × 10 страниц = 40 запросов

**Расчет времени (с консервативными задержками):**
- С задержкой 1.5 сек: 40 × 1.5 = 60 секунд
- Задержки между филиалами: 3 × 3 = 9 секунд
- Резерв на токены (кэшируются): 4 запроса × 0.5s = 2 секунды
- **Итого:** ~70-75 секунд для полной синхронизации

**Вывод:** Используем 40 из 120 GET/мин, оставляя 80 для других сервисов компании! 👍

---

## 🚫 Анти-паттерны

### ❌ 1. Токен на каждый запрос
```typescript
// ПЛОХО: 40 запросов + 40 токенов = 80 запросов за минуту
for (const car of cars) {
  const token = await getNewToken(); // Избыточно!
  await updateCar(car, token);
}
```

### ❌ 2. Параллельные запросы без контроля
```typescript
// ПЛОХО: все запросы одновременно = rate limit
await Promise.all(
  branches.map(branch => syncBranch(branch))
);
```

### ❌ 3. Нет обработки 429 ошибок
```typescript
// ПЛОХО: упадет при rate limit
const data = await fetch(url);
if (!data.ok) throw new Error('Failed');
```

---

## ✅ Best Practices (обновлено для совместного использования с другими сервисами)

1. **Кэшируйте токены** - один токен на 4 минуты для каждого филиала (max 4-8 токенов/мин)
2. **Консервативные задержки:**
   - GET: 1.5 секунды (40 запросов/мин)
   - POST: 3 секунды (20 запросов/мин)
   - Search: 6 секунд (10 запросов/мин)
3. **Обрабатывайте 429** - exponential backoff, не паникуйте
4. **Пагинация** - не грузите все сразу, листайте с задержками
5. **Мониторинг** - логируйте количество запросов (не превышайте наши лимиты!)
6. **Batch операции** - группируйте обновления, не по одной записи
7. **Ночные синхронизации** - тяжелые операции выполняйте ночью (минимальная конкуренция)

---

## 📝 Логирование нагрузки

```typescript
// Полезно для мониторинга
class RateLimitTracker {
  private requests: number[] = [];
  
  recordRequest() {
    const now = Date.now();
    this.requests.push(now);
    
    // Очистка старых (> 1 минуты)
    this.requests = this.requests.filter(t => now - t < 60000);
  }
  
  getCurrentRate(): number {
    return this.requests.length;
  }
  
  canMakeRequest(limit: number): boolean {
    return this.getCurrentRate() < limit;
  }
}

const tracker = new RateLimitTracker();

async function safeApiFetch<T>(fn: () => Promise<T>): Promise<T> {
  if (!tracker.canMakeRequest(120)) {
    console.warn('Приближаемся к лимиту, задержка...');
    await sleep(1000);
  }
  
  tracker.recordRequest();
  return await fn();
}
```

---

## 🔍 Мониторинг в production

### Metrics для отслеживания:

1. **Requests per minute** (по типу: GET/POST/search)
2. **Token cache hit rate** (должен быть >90%)
3. **429 errors count** (должен быть 0 или близко к 0)
4. **Average request delay** (500-1000ms)
5. **Sync duration** (время полной синхронизации)

### Алерты:

- ⚠️ Warning: >35 GET запросов/мин (приближаемся к нашему лимиту 40)
- 🚨 Critical: >40 GET запросов/мин или любые 429 errors
- 📊 Info: Token cache miss rate >10%
- ℹ️ Notice: >15 POST запросов/мин или >8 Search запросов/мин

---

## 📚 См. также

- [RentProg Integration Guide](./RENTPROG_INTEGRATION.md)
- [Troubleshooting Rate Limits](./TROUBLESHOOTING.md)
- [Caching Strategy](../src/integrations/rentprog.ts)

---

**Важно:** Эти лимиты актуальны на ноябрь 2025. При изменениях со стороны RentProg обновите этот документ.

