# Статус Upsert Processor Workflow

**Дата:** 2025-11-04 01:20  
**Статус:** ✅ Очищено и готово к диагностике

---

## ✅ Выполнено

### 1. Очистка workflows
- **Удалено:** 6 дублирующихся Upsert Processor workflows
- **Осталось:** Только **ОДИН** workflow: `fijJpRlLjgpxSJE7`
- **Статус:** Активен ✅
- **URL:** https://n8n.rentflow.rentals/workflow/fijJpRlLjgpxSJE7
- **Webhook:** https://n8n.rentflow.rentals/webhook/upsert-processor

### 2. Перезапуск n8n
- **Выполнен:** Перезапуск контейнера n8n на сервере
- **Результат:** Webhooks перерегистрированы
- **Время:** 10 секунд downtime

### 3. Тестирование
- **Тест:** booking 501190
- **Результат:** Workflow выполняется (5 секунд)
- **Ответ:** `{"ok": false, "error": "Not found in any branch"}`

---

## ❌ Проблема

**Бронь 501190 не находится ни в одном филиале через RentProg API.**

### Возможные причины:

1. **Неправильный endpoint**  
   Используется: `https://rentprog.net/api/v1/public/bookings/501190`
   
   Fallback варианты из документации:
   - `/booking/501190` (единственное число)
   - `/booking?id=501190` (query параметр)

2. **Проблема с токеном**  
   Токен получается через `/get_token`, но может быть:
   - Недостаточно прав для public API
   - Токен истекает до проверки всех филиалов
   - Нужен другой формат авторизации

3. **Бронь недоступна через API**  
   Бронь создана через widget (`created_from_api: false`), возможно:
   - Этот тип броней не экспортируется через public API
   - Требуется другой endpoint (не public)

---

## 🔍 Следующие шаги для диагностики

### 1. Проверить execution logs в n8n UI
**URL:** https://n8n.rentflow.rentals/workflow/fijJpRlLjgpxSJE7

**Что проверить:**
1. **Нода "Get RentProg Tokens"**  
   - Получились ли токены для всех филиалов?
   - Какой формат токенов?

2. **Ноды "Try Tbilisi/Batumi/etc"**  
   - Какой HTTP status code? (200, 404, 401, 403?)
   - Что именно в ответе от API?
   - Есть ли error message?

3. **Console logs**  
   Должны быть сообщения:
   ```
   ✓ Получен токен для tbilisi
   ✓ Получен токен для batumi
   ...
   ```

### 2. Протестировать fallback endpoints
Добавить в workflow попытки альтернативных endpoints:
- `/api/v1/public/booking/501190` (единственное число)
- `/api/v1/public/booking?id=501190` (query параметр)

### 3. Проверить права токена
Возможно компания токены дают доступ только к определенным endpoints.

---

## 📋 Текущая конфигурация

### Workflow: RentProg Upsert Processor (Fixed)
- **ID:** `fijJpRlLjgpxSJE7`
- **Активен:** Да ✅
- **Webhook path:** `/webhook/upsert-processor`
- **Webhook ID:** `rentprog-upsert-processor`

### Логика workflow:
1. Получить временные токены для всех 4 филиалов
2. Последовательно запросить: Tbilisi → Batumi → Kutaisi → Service Center
3. При успехе - сохранить в БД через `external_refs`
4. При неудаче всех - Alert в Telegram + ответ "Not found"

### RentProg API:
- **Base URL:** `https://rentprog.net/api/v1/public`
- **Endpoint:** `/bookings/{id}` или `/{entity_type}s/{id}`
- **Авторизация:** `Authorization: Bearer {request_token}`
- **Company tokens:** Встроены в Code node "Get RentProg Tokens"

---

## 💡 Рекомендация

**Откройте n8n UI и посмотрите execution logs вручную:**

https://n8n.rentflow.rentals/workflow/fijJpRlLjgpxSJE7

Execution list → последний execution → детали нод

Это покажет **точную** причину, почему бронь не находится!

---

## 📝 Важное правило

**Больше НЕ создавать дублирующие workflows!**

- Для изменений используйте **ТОЛЬКО** `fijJpRlLjgpxSJE7`
- Обновление через MCP: `n8n_update_full_workflow`
- ID записан в `config/UPSERT_PROCESSOR_WORKFLOW.md`

