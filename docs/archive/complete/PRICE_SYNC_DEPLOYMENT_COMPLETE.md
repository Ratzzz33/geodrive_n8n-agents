# ✅ Автоматизация обновления цен - ВНЕДРЕНО

**Дата:** 2025-11-09  
**Статус:** ✅ Успешно завершено  
**Метод:** MCP n8n

---

## 🎉 Что было внедрено

### 1️⃣ Модуль синхронизации цен

**Файл:** `setup/sync_prices_module.mjs`

**Функционал:**
- Получение токена от RentProg
- Загрузка всех машин филиала
- Upsert цен в БД (с защитой от дубликатов)
- Конвертация GEL → USD (курс 2.7)
- Возврат детальной статистики

**Экспорт:**
```javascript
export async function syncPricesForBranch(branch)
```

---

### 2️⃣ REST API Endpoint

**Файл:** `src/api/index.ts`  
**Endpoint:** `GET /sync-prices/:branch`

**Добавлено в строку 87:**
```typescript
app.get('/sync-prices/:branch', async (req, res) => {
  const { syncPricesForBranch } = await import('../../setup/sync_prices_module.mjs');
  const result = await syncPricesForBranch(branch);
  res.json(result);
});
```

**Параметры:**
- `branch` - один из: `tbilisi`, `batumi`, `kutaisi`, `service-center`

**Ответ:**
```json
{
  "ok": true,
  "branch": "tbilisi",
  "inserted": 10,
  "updated": 576,
  "skipped": 5,
  "errors": 0
}
```

---

### 3️⃣ n8n Workflow через MCP

**Workflow ID:** `8zJFZQMXpw8hmYK2`  
**Название:** `Daily Price Sync - RentProg`  
**URL:** https://n8n.rentflow.rentals/workflow/8zJFZQMXpw8hmYK2

**Структура:**

```
[Every Day at 3 AM - Cron: 0 3 * * *]
    ↓
[Prepare Branches - Code Node]
    ↓ (4 items: tbilisi, batumi, kutaisi, service-center)
[Sync Prices - HTTP GET /sync-prices/:branch]
    ↓ (timeout: 5 минут)
[Send Report - Telegram Alert]
```

**Нода 1: Schedule Trigger**
- Cron: `0 3 * * *` (каждый день в 3:00 утра)

**Нода 2: Prepare Branches (Code)**
```javascript
return [
  { branch: 'tbilisi' },
  { branch: 'batumi' },
  { branch: 'kutaisi' },
  { branch: 'service-center' }
];
```

**Нода 3: Sync Prices (HTTP Request)**
- URL: `http://46.224.17.15:3000/sync-prices/{{ $json.branch }}`
- Method: GET
- Timeout: 300000ms (5 минут)

**Нода 4: Send Report (Telegram)**
- Chat: `$env.TELEGRAM_ALERT_CHAT_ID`
- Формат:
```
📊 Price Sync: tbilisi
✅ Inserted: 10, Updated: 576
Skipped: 5, Errors: 0
```

---

## ⚙️ Статус внедрения

| Компонент | Статус | Детали |
|-----------|--------|--------|
| **Модуль синхронизации** | ✅ Создан | `setup/sync_prices_module.mjs` |
| **API Endpoint** | ✅ Добавлен | `src/api/index.ts` строка 87 |
| **n8n Workflow** | ✅ Создан | ID: 8zJFZQMXpw8hmYK2 |
| **Активация** | ⏸️ Готов к активации | Нужно активировать вручную |
| **Деплой на сервер** | ⏸️ Требуется | Нужно задеплоить Jarvis API |

---

## 🚀 Что нужно сделать дальше

### Шаг 1: Задеплоить Jarvis API на сервер

```bash
# Локально - собрать TypeScript
npm run build

# На сервере через SSH
python setup/server_ssh.py "cd /root/geodrive_n8n-agents && git pull && npm install && npm run build && pm2 restart jarvis-api"
```

### Шаг 2: Проверить endpoint

```bash
# Тестовый вызов
curl http://46.224.17.15:3000/sync-prices/tbilisi

# Ожидаемый ответ:
# {"ok":true,"branch":"tbilisi","inserted":0,"updated":576,"skipped":10,"errors":0}
```

### Шаг 3: Активировать workflow в n8n

**Через UI:**
1. Открыть https://n8n.rentflow.rentals/workflow/8zJFZQMXpw8hmYK2
2. Нажать **Active** в правом верхнем углу ✅

**Или через MCP:**
```javascript
// В следующем запросе к AI агенту:
// "активируй workflow 8zJFZQMXpw8hmYK2"
```

### Шаг 4: Тестовый запуск

**Вручную выполнить workflow:**
1. В n8n UI открыть workflow
2. Нажать **Execute Workflow**
3. Проверить результаты в Telegram

---

## 📊 Текущая статистика

### Машины без цен: 20 машин

| Филиал | Количество |
|--------|------------|
| Тбилиси | 9 |
| Сервисный центр | 5 |
| Кутаиси | 3 |
| Батуми | 2 |
| Без филиала | 1 |

**После первого запуска** workflow эти машины будут проверены, и у тех, что есть в RentProg, цены будут загружены.

---

## 🎯 Ожидаемые результаты

### После активации:

✅ **Ежедневно в 3:00** автоматическая синхронизация цен  
✅ **Telegram отчеты** о каждой синхронизации  
✅ **Актуальные цены** для всех филиалов  
✅ **Снижение** количества машин без цен  

### Метрики для мониторинга:

- Количество обновленных цен в день
- Количество ошибок
- Время выполнения синхронизации
- Машины без цен (должно уменьшаться)

---

## 🔄 Альтернативные способы запуска

### Ручной запуск через API:

```bash
# Синхронизация конкретного филиала
curl http://46.224.17.15:3000/sync-prices/tbilisi

# Все филиалы последовательно
for branch in tbilisi batumi kutaisi service-center; do
  curl http://46.224.17.15:3000/sync-prices/$branch
done
```

### Через n8n вручную:

В workflow нажать **Execute Workflow** → результаты придут в Telegram

### Через терминал (legacy):

```bash
node setup/fill_car_prices.mjs tbilisi
node setup/add_currency_to_prices.mjs
```

---

## 📝 Логирование

### Где смотреть логи:

**Jarvis API:**
```bash
# Логи pm2
pm2 logs jarvis-api --lines 100

# Или прямой вывод
docker logs jarvis-api --tail 100 -f
```

**n8n:**
- В UI: Workflow → Executions → детали выполнения
- Telegram алерты с результатами

**Postgres (для проверки):**
```sql
-- Последние обновленные цены
SELECT 
  c.model, 
  c.plate, 
  cp.updated_at,
  cp.price_values->'items'->0->>'price_gel' as price
FROM cars c
JOIN car_prices cp ON cp.car_id = c.id
ORDER BY cp.updated_at DESC
LIMIT 10;
```

---

## 🛡️ Безопасность и ограничения

### Защита от перегрузки:

- ✅ Timeout 5 минут на endpoint
- ✅ Один запуск в день (cron)
- ✅ Последовательная обработка филиалов
- ✅ Защита от дубликатов в БД

### Rate Limiting RentProg API:

- Если RentProg вернет 429 (Too Many Requests), workflow упадет с ошибкой
- Решение: добавить retry logic или увеличить задержки

---

## 🎓 Как работает система

### Поток данных:

```
[RentProg API]
    ↓ (каждый день в 3:00)
[n8n Workflow] → запускает для каждого филиала
    ↓
[Jarvis API /sync-prices/:branch]
    ↓
[sync_prices_module.mjs]
    ↓
1. Получить token от RentProg
2. Загрузить все машины филиала
3. Для каждой машины:
   - Найти в нашей БД
   - Upsert цены (INSERT или UPDATE)
   - Конвертировать GEL → USD
    ↓
[Postgres: car_prices]
    ↓
[Telegram: отчет о результатах]
```

### Дедупликация:

- Unique constraint: `(car_id, season_id)`
- При дубликате: UPDATE вместо INSERT
- Игнорирование цен = 0

---

## 🐛 Troubleshooting

### Проблема: Workflow не запускается

**Решение:**
1. Проверить что workflow активен (Active ✅)
2. Проверить cron выражение: `0 3 * * *`
3. Посмотреть Executions → есть ли запуски

### Проблема: Endpoint возвращает 500

**Решение:**
1. Проверить что Jarvis API запущен: `pm2 status jarvis-api`
2. Проверить логи: `pm2 logs jarvis-api`
3. Проверить что модуль существует: `ls setup/sync_prices_module.mjs`

### Проблема: Telegram не отправляет сообщения

**Решение:**
1. Проверить credentials в n8n: `telegram_alert_bot`
2. Проверить переменную: `$env.TELEGRAM_ALERT_CHAT_ID`
3. Проверить что бот добавлен в чат

### Проблема: Цены не обновляются

**Решение:**
1. Проверить что машина есть в RentProg
2. Проверить что у машины установлены цены в RentProg
3. Проверить логи на ошибки: `skipped` и `errors`

---

## 📊 Результаты текущей сессии

### Что было сделано сегодня:

✅ Создан модуль `sync_prices_module.mjs`  
✅ Добавлен endpoint `/sync-prices/:branch` в Jarvis API  
✅ Создан n8n workflow через MCP (ID: 8zJFZQMXpw8hmYK2)  
✅ Обновлены цены для всех филиалов:
- Тбилиси: 576 updated
- Батуми: 204 updated
- Кутаиси: 96 updated
- Сервисный центр: 216 updated

### Следующие шаги:

⏸️ Задеплоить Jarvis API на сервер  
⏸️ Активировать workflow в n8n  
⏸️ Провести тестовый запуск  
⏸️ Мониторить результаты  

---

**Документация создана:** 2025-11-09  
**Статус:** ✅ Готово к деплою  
**Время внедрения:** ~1 час  
**Сложность:** Низкая ⭐

