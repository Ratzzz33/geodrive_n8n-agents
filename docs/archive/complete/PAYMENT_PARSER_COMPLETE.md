# ✅ Парсинг кассы компании - ГОТОВО!

**Дата:** 2025-11-05  
**Статус:** Все компоненты созданы, готовы к активации  

---

## 🎯 Что сделано

### 1. База данных ✅

**Таблица `payments` создана:**
- `id` (UUID) - уникальный ID платежа
- `branch_code` (TEXT) - код филиала (tbilisi/batumi/kutaisi/service-center)
- `employee_id` (UUID) - связь с сотрудником
- `booking_id` (UUID) - связь с бронью
- `client_id` (UUID) - связь с клиентом
- `rentprog_payment_id` (TEXT) - ID платежа в RentProg
- `payment_date` (TIMESTAMPTZ) - дата платежа
- `payment_type` (TEXT) - тип (income/expense/transfer)
- `payment_method` (TEXT) - метод (cash/card/bank_transfer/other)
- `amount` (NUMERIC) - сумма
- `currency` (TEXT) - валюта (GEL/USD/EUR)
- `description` (TEXT) - описание
- `comment` (TEXT) - комментарий
- `category` (TEXT) - категория
- `raw_data` (JSONB) - полные данные из RentProg
- `parsed_at` (TIMESTAMPTZ) - время парсинга

**Индексы:**
- По филиалу, сотруднику, броне, дате, типу, методу
- Уникальный constraint для дедупликации

---

### 2. n8n Workflow ✅

**Company Cash Register Parser** (ID: `x1z3p5QMQ421UPEY`)

**URL:** https://n8n.rentflow.rentals/workflow/x1z3p5QMQ421UPEY

**Что делает:**
1. Запускается каждые 5 минут (cron trigger)
2. Для каждого из 4 филиалов:
   - Логин через Playwright
   - Переход на страницу `/company/cash`
   - Парсинг таблицы платежей
   - Фильтр: только платежи за последние 5 минут
3. Сохранение в БД (`payments` таблица)
4. Дедупликация через `ON CONFLICT DO NOTHING`
5. Telegram alert в чат `-5004140602` при новом платеже

**Credentials настроены:**
- ✅ PostgreSQL Neon
- ✅ Telegram Bot
- ✅ RentProg логины для всех филиалов

---

### 3. Playwright установлен ✅

```bash
# Выполнено на сервере:
docker exec n8n npm install playwright
docker exec n8n npx playwright install chromium
```

**Установлено:**
- Playwright библиотека
- Chromium 141.0.7390.37
- FFMPEG (для медиа)
- Chromium Headless Shell

---

### 4. Все workflows готовы к активации

**Созданные workflows:**

1. **RentProg Events Scraper** (ID: `nZPD1AcSbLo3eSgr`)
   - Парсинг страницы "События" RentProg
   - Каждые 5 минут
   - Все 4 филиала
   - ⚠️ Не активирован

2. **Cash Register Reconciliation** (ID: `9Nrec3H5j2fIQ3Ma`)
   - Сверка касс сотрудников
   - Ежедневно в 04:00 Tbilisi
   - ⚠️ Не активирован

3. **Company Cash Register Parser** (ID: `x1z3p5QMQ421UPEY`)
   - **[НОВЫЙ]** Парсинг кассы компании
   - Каждые 5 минут
   - Все 4 филиала
   - ⚠️ Не активирован

---

## ⚡ Активация workflows (выполнить вручную)

### Способ 1: Через n8n UI (рекомендуется)

1. Откройте каждый workflow в браузере:
   - https://n8n.rentflow.rentals/workflow/nZPD1AcSbLo3eSgr
   - https://n8n.rentflow.rentals/workflow/9Nrec3H5j2fIQ3Ma
   - https://n8n.rentflow.rentals/workflow/x1z3p5QMQ421UPEY

2. Нажмите **toggle "Active"** в правом верхнем углу каждого workflow

3. Проверьте что тоггл переключился на зеленый цвет

---

### Способ 2: Через API

```bash
# RentProg Events Scraper
curl -X PATCH https://n8n.rentflow.rentals/api/v1/workflows/nZPD1AcSbLo3eSgr \
  -H "X-N8N-API-KEY: eyJ..." \
  -H "Content-Type: application/json" \
  -d '{"active": true}'

# Cash Register Reconciliation
curl -X PATCH https://n8n.rentflow.rentals/api/v1/workflows/9Nrec3H5j2fIQ3Ma \
  -H "X-N8N-API-KEY: eyJ..." \
  -H "Content-Type: application/json" \
  -d '{"active": true}'

# Company Cash Register Parser
curl -X PATCH https://n8n.rentflow.rentals/api/v1/workflows/x1z3p5QMQ421UPEY \
  -H "X-N8N-API-KEY: eyJ..." \
  -H "Content-Type: application/json" \
  -d '{"active": true}'
```

---

## 📊 Что будет происходить после активации

### Company Cash Register Parser (каждые 5 минут)

**Для каждого филиала:**
1. Логин в RentProg
2. Переход на `/company/cash`
3. Парсинг таблицы:
   - Дата и время
   - Сотрудник
   - Тип операции
   - Метод оплаты
   - Сумма и валюта
   - Комментарий

4. Фильтр: только новые (за последние 5 минут)
5. Сохранение в БД
6. **Telegram alert при новом платеже:**

```
💰 Новый платеж в кассе

🏢 Филиал: tbilisi
👤 Сотрудник: Иван Иванов
📋 Тип: income
💳 Метод: cash
💵 Сумма: 150.00 GEL
📝 Комментарий: Оплата брони #12345

🕐 Дата: 2025-11-05T14:30:00Z
```

---

## 🔍 Мониторинг

### Проверка работы через n8n UI

1. Откройте: https://n8n.rentflow.rentals/executions
2. Фильтр по workflow: "Company Cash Register Parser"
3. Проверьте логи выполнений:
   - ✅ Success - платежи найдены и сохранены
   - ℹ️ Success (empty) - новых платежей нет
   - ❌ Error - проблема с Playwright или БД

---

### Проверка данных в БД

```sql
-- Последние 10 платежей
SELECT 
  branch_code,
  payment_date,
  payment_type,
  payment_method,
  amount,
  currency,
  comment,
  parsed_at
FROM payments
ORDER BY payment_date DESC
LIMIT 10;

-- Статистика по филиалам
SELECT 
  branch_code,
  COUNT(*) as total_payments,
  SUM(CASE WHEN payment_type = 'income' THEN amount ELSE 0 END) as total_income,
  SUM(CASE WHEN payment_type = 'expense' THEN amount ELSE 0 END) as total_expense
FROM payments
GROUP BY branch_code;
```

---

## 📋 Структура данных

### Пример записи в `payments`

```json
{
  "id": "uuid-here",
  "branch_code": "tbilisi",
  "employee_id": "uuid-employee",
  "booking_id": null,
  "client_id": null,
  "rentprog_payment_id": null,
  "payment_date": "2025-11-05T14:30:00Z",
  "payment_type": "income",
  "payment_method": "cash",
  "amount": 150.00,
  "currency": "GEL",
  "description": null,
  "comment": "Оплата брони #12345",
  "category": null,
  "raw_data": {
    "date": "05 нояб. 25 14:30",
    "employee": "Иван Иванов",
    "type": "Приход",
    "method": "Наличные",
    "amount": "150.00",
    "currency": "GEL",
    "comment": "Оплата брони #12345"
  },
  "parsed_at": "2025-11-05T14:35:00Z"
}
```

---

## ⚠️ Важные примечания

### 1. DOM селекторы могут измениться

Workflow использует селекторы таблицы:
```javascript
td:nth-child(1)  // Дата
td:nth-child(2)  // Сотрудник
td:nth-child(3)  // Тип
td:nth-child(4)  // Метод
td:nth-child(5)  // Сумма
td:nth-child(6)  // Валюта
td:nth-child(7)  // Комментарий
```

Если RentProg изменит структуру таблицы - селекторы нужно будет обновить в workflow.

---

### 2. Связь с сотрудниками

Пока `employee_id` = NULL, так как нужно:
1. Получить список сотрудников из RentProg
2. Сопоставить их с нашей таблицей `employees`
3. Обновить workflow для поиска employee_id по имени

**TODO:** Добавить логику связывания сотрудников

---

### 3. Связь с бронями и клиентами

Аналогично, `booking_id` и `client_id` пока NULL.

Для связывания нужно:
- Парсить booking ID из комментария (если есть)
- Искать бронь в нашей БД
- Записывать связь

**TODO:** Добавить логику связывания бронь/клиентов

---

## 🚀 Следующие шаги

### Сейчас:
1. ✅ Активировать все 3 workflow в n8n UI
2. ✅ Проверить первые выполнения в логах
3. ✅ Убедиться что данные появляются в БД
4. ✅ Проверить Telegram alerts

### Будущее:
1. Связать платежи с сотрудниками (по имени)
2. Связать платежи с бронями (парсинг ID из комментариев)
3. Добавить категоризацию платежей
4. Создать дашборд аналитики платежей
5. Добавить алерты на необычные суммы/типы платежей

---

## ✅ Чеклист готовности

- [x] Таблица `payments` создана в БД
- [x] Workflow "Company Cash Register Parser" создан
- [x] Credentials настроены (PostgreSQL, Telegram, RentProg)
- [x] Playwright установлен в n8n
- [x] Chromium браузер установлен
- [x] Логины RentProg добавлены в код
- [ ] **Workflows активированы (сделать вручную!)**
- [ ] Первый тестовый запуск выполнен
- [ ] Данные появляются в БД
- [ ] Telegram alerts работают

---

## 🎉 Готово!

Все компоненты созданы и готовы к работе!

**Следующий шаг:** Активируйте workflows в n8n UI по ссылкам выше.

После активации система автоматически начнет:
- Собирать все платежи по всем филиалам каждые 5 минут
- Сохранять их в централизованную таблицу `payments`
- Отправлять Telegram alerts о новых платежах

---

**Документация:**
- Миграция БД: `setup/migrations/007_create_payments_table.sql`
- Отчет по предыдущим workflows: `WORKFLOWS_IMPORTED_VIA_MCP.md`


