# Установка MCP для n8n - Выполнено ✅

## ✅ Что сделано:

1. **MCP сервер создан**: `mcp-server/n8n-mcp-server.js`
2. **Зависимости установлены**: Все пакеты установлены в `mcp-server/node_modules`
3. **Тестовый скрипт создан**: `mcp-server/test-n8n-connection.js`
4. **Документация готова**: `MCP_N8N_SETUP.md`

## 📋 Следующие шаги:

### Шаг 1: Настройте переменные окружения

Добавьте в файл `.env` (в корне проекта):

```env
# URL вашего n8n (ваш домен)
N8N_BASE_URL=https://n8n.rentflow.rentals

# API ключ (получить в n8n: Settings → API → Create API Key)
N8N_API_KEY=ваш_api_ключ_здесь
```

**Получить API ключ:**
1. Откройте n8n: `https://n8n.rentflow.rentals`
2. Войдите в систему
3. Перейдите: **Settings** → **API**
4. Нажмите **Create API Key**
5. Скопируйте ключ

### Шаг 2: Проверьте подключение

Запустите тестовый скрипт:

```bash
cd mcp-server
node test-n8n-connection.js
```

Если видите "✅ Подключение успешно!" - всё готово!

### Шаг 3: Настройте Cursor

1. Откройте настройки Cursor: `Ctrl+,`
2. Перейдите: **Features** → **Model Context Protocol**
3. Найдите секцию `mcpServers`
4. Добавьте конфигурацию:

```json
{
  "mcpServers": {
    "n8n": {
      "command": "C:\\Program Files\\nodejs\\node.exe",
      "args": [
        "C:\\Users\\33pok\\geodrive_n8n-agents\\mcp-server\\n8n-mcp-server.js"
      ],
      "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents",
      "env": {
        "N8N_BASE_URL": "https://n8n.rentflow.rentals",
        "N8N_API_KEY": "ваш_api_ключ"
      }
    }
  }
}
```

**Или**, если переменные в `.env` файле, можно так:

```json
{
  "mcpServers": {
    "n8n": {
      "command": "C:\\Program Files\\nodejs\\node.exe",
      "args": [
        "C:\\Users\\33pok\\geodrive_n8n-agents\\mcp-server\\n8n-mcp-server.js"
      ],
      "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents"
    }
  }
}
```

### Шаг 4: Перезапустите Cursor

**Важно:** Полностью закройте и снова откройте Cursor.

### Шаг 5: Проверьте работу

После перезапуска попробуйте в чате с AI:

```
Проверь подключение к n8n
```

Или:

```
Покажи список всех workflows в n8n
```

## 🔍 Если не работает:

### Проблема: "N8N_API_KEY не настроен"

**Решение:**
1. Проверьте файл `.env` - есть ли там `N8N_API_KEY`
2. Или добавьте `env` секцию в конфигурацию Cursor (см. выше)

### Проблема: Ошибка подключения

**Решение:**
1. Проверьте доступность домена: `curl https://n8n.rentflow.rentals/api/v1/workflows`
2. Убедитесь, что SSL сертификат валиден
3. Проверьте правильность URL (без `/api/v1` на конце)

### Проблема: MCP сервер не подключается

**Решение:**
1. Проверьте логи в Cursor: View → Output → "MCP" или "n8n"
2. Убедитесь, что путь к `node.exe` правильный
3. Перезапустите Cursor полностью

## 📚 Документация

- [MCP_N8N_SETUP.md](./MCP_N8N_SETUP.md) - Полная документация
- [CURSOR_MCP_N8N_CONFIG.json](./CURSOR_MCP_N8N_CONFIG.json) - Готовая конфигурация

## ✅ Готово к использованию!

После выполнения всех шагов вы сможете управлять n8n напрямую из Cursor через MCP.

