# Отчет: Telegram алерты и исправления (2025-11-05)

**Дата:** 5 ноября 2025  
**Статус:** ✅ Завершено  
**Ответственный:** Cursor Agent

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Выполненные задачи](#выполненные-задачи)
3. [Архитектура решения](#архитектура-решения)
4. [Технические детали](#технические-детали)
5. [Примеры уведомлений](#примеры-уведомлений)
6. [Инструкции по использованию](#инструкции-по-использованию)
7. [Troubleshooting](#troubleshooting)

---

## Обзор

### Цели сессии

1. ✅ Исправить токен API для филиала Kutaisi
2. ✅ Завершить обработку 3449 броней (распарсинг nested data)
3. ✅ Добавить Telegram уведомления для всех 4 processor workflows
4. ✅ Настроить правильное форматирование алертов

### Результаты

- **4 workflows обновлены** с Telegram алертами
- **3449 броней обработаны** (100% completion)
- **Триггер БД исправлен** для автоматической обработки
- **Переменные окружения настроены** на production сервере

---

## Выполненные задачи

### 1. Исправление токена RentProg для Kutaisi ✅

**Проблема:**  
Workflow "Kutaisi Processor Rentprog" использовал неверный `company_token`, что вызывало ошибку 404 при получении токена от RentProg API.

**Причина:**  
В коде был устаревший токен:
```javascript
// Неправильный токен
'5599ebb7a1f0a1e5f6a5d4e3c2b1a0f9e8d7c6b5a4f3e2d1c0b9a8f7e6d5c4b'
```

**Решение:**
- Обновлён токен в workflow "Kutaisi Processor Rentprog" (ID: `gJPvJwGQSi8455s9`)
- Правильный токен: `5599ebb7b94827fdfd49ca3a5b7e259cfa99d8ea78edeb50`
- Исправлен также в `setup/create_all_branch_workflows.mjs` для будущих операций

**Скрипт:** `setup/fix_kutaisi_token.mjs`

**Проверка:**
```bash
node setup/find_kutaisi_workflow.mjs
# ✅ Токен ПРАВИЛЬНЫЙ!
```

---

### 2. Завершение обработки данных броней ✅

**Проблема:**  
- 3449 броней в таблице `bookings` имели nested данные в поле `data`
- Требовалось распарсить `car` и `client` из `data` и создать связи

**Решение:**

#### 2.1. Исправление триггера БД

**Файл:** `setup/fix_trigger_on_conflict.mjs`

Обновлён триггер `process_booking_nested_entities` для:
- Использования `ON CONFLICT (system, external_id) DO NOTHING` для `external_refs`
- Парсинга JSONB строк в объекты
- Извлечения полей брони (`start_date`, `end_date`, `price`, etc.)
- Очистки поля `data` до `{}` после обработки

**Ключевой фрагмент триггера:**
```sql
-- Если NEW.data является JSONB-строкой, преобразуем её в JSONB-объект
IF jsonb_typeof(NEW.data) = 'string' THEN
  NEW.data := (NEW.data->>0)::JSONB;
END IF;

-- Обработка car и client...

-- ВАЖНО: Очищаем data после обработки
NEW.data := '{}'::jsonb;
```

#### 2.2. Исправление структуры таблицы

**Файл:** `setup/fix_bookings_table.mjs`

- Добавлен `PRIMARY KEY (id)` для таблицы `bookings`
- Изменён тип полей `price`, `days`, `total`, `deposit` с `BIGINT` на `NUMERIC`

#### 2.3. Конвертация JSONB строк в объекты

**Файл:** `setup/fix_jsonb_strings_to_objects.mjs`

Преобразовал все JSONB строки вида `"{\"id\":...}"` в JSONB объекты `{"id":...}`:
```sql
UPDATE bookings
SET data = (data#>>'{}')::JSONB
WHERE jsonb_typeof(data) = 'string'
```

#### 2.4. Обработка всех существующих броней

**Файл:** `setup/process_all_bookings_with_data.mjs`

Итерация по всем бронях с непустым `data` и принудительный вызов триггера через `UPDATE`.

**Результат:**
```
📦 Всего броней: 3449
✅ Обработано (data = {}): 3449 (100.0%)
⏳ Осталось: 0

🚗 С привязкой к машине: 3446
👤 С привязкой к клиенту: 3446
📅 С датами: 3449
```

---

### 3. Добавление Telegram уведомлений ✅

**Требование:**  
Отправлять в Telegram чат уведомления о всех событиях от RentProg (car.update, booking.create, etc.) для всех 4 филиалов.

#### 3.1. Архитектура уведомлений

**Workflows:**
- ✅ Tbilisi Processor Rentprog (ID: `P65bXE5Xhupkxxw6`)
- ✅ Batumi Processor Rentprog (ID: `YsBma7qYsdsDykTq`)
- ✅ Kutaisi Processor Rentprog (ID: `gJPvJwGQSi8455s9`)
- ✅ Service Center Processor Rentprog (ID: `PbDKuU06H7s2Oem8`)

**Добавленные ноды в каждый workflow:**

1. **Format Telegram Alert** (Code node)
   - Парсит данные вебхука из `payload`
   - Определяет тип события (`car.update`, `booking.create`, etc.)
   - Форматирует сообщение с изменениями

2. **Send Telegram Alert** (Telegram node)
   - Использует credential "Telegram account"
   - Отправляет в чат `$env.TELEGRAM_ALERT_CHAT_ID`

**Точка подключения:**  
`Parse Webhook` → `Format Telegram Alert` → `Send Telegram Alert`

**Почему именно от Parse Webhook?**  
Чтобы получить **оригинальный payload** с массивами `[old, new]` ДО преобразования в `Prepare Update`.

#### 3.2. Скрипты создания

**Файл:** `setup/add_telegram_alerts_to_processors.mjs`
- Добавление нод в workflows

**Файл:** `setup/fix_telegram_nodes_use_credential.mjs`
- Замена HTTP Request на стандартную Telegram ноду с credential

**Файл:** `setup/fix_alert_connection_point.mjs`
- Исправление точки подключения (от Parse Webhook)

**Файл:** `setup/fix_alert_format_code.mjs`
- Обновление кода форматирования с правильным парсингом payload

---

### 4. Настройка переменных окружения ✅

**Проблема:**  
Переменная `TELEGRAM_ALERT_CHAT_ID` не была настроена в `docker-compose.yml` на сервере, что приводило к `undefined` в workflows.

**Решение:**

**Файл:** `setup/add_telegram_chat_id.py`

Добавил переменную в `docker-compose.yml` на production сервере:
```yaml
environment:
  - TELEGRAM_ALERT_CHAT_ID=-5004140602
```

**Перезапуск контейнера:**
```bash
docker compose stop n8n
docker compose up -d n8n
```

**Проверка:**
```bash
docker exec n8n printenv TELEGRAM_ALERT_CHAT_ID
# Output: -5004140602
```

---

## Архитектура решения

### Поток обработки вебхуков с алертами

```
RentProg → Webhook → n8n
                      ↓
           [Parse Webhook] ─┬→ [Check Exists]
                             │         ↓
                             │   [Switch by Operation]
                             │         ↓
                             │   [Prepare Update/Create]
                             │         ↓
                             │   [Insert Fetched Entity]
                             │         ↓
                             │   [Respond Success]
                             │
                             └→ [Format Telegram Alert]
                                        ↓
                                 [Send Telegram Alert]
                                        ↓
                                    Telegram Chat
```

### Структура данных алерта

**INPUT (от Parse Webhook):**
```json
{
  "entitytype": "booking",
  "operation": "update",
  "rentprogid": "507616",
  "companyid": 9247,
  "payload": {
    "id": 507616,
    "state": ["Новая", "Активная"],
    "start_worker_id": [null, 11852],
    "start_date": ["2025-11-10 12:00", "2025-11-10 14:00"]
  }
}
```

**OUTPUT (Telegram message):**
```
📅 BATUMI | booking.update
🆔 ID: 507616

📝 Изменения:
• state: Новая → Активная
• start_worker_id: null → 11852
• start_date: 2025-11-10 12:00 → 2025-11-10 14:00
```

---

## Технические детали

### Code node "Format Telegram Alert"

**Язык:** JavaScript  
**Mode:** Run Once for All Items

**Ключевые функции:**

1. **Парсинг payload:**
```javascript
const input = $input.first().json;
const entityType = input.entitytype || 'unknown';
const operation = input.operation || 'unknown';
const rentprogId = input.rentprogid || 'N/A';
const payload = input.payload || {};
```

2. **Обработка UPDATE событий:**
```javascript
if (operation === 'update') {
  message += `📝 Изменения:\n`;
  
  for (const [key, value] of Object.entries(payload)) {
    if (Array.isArray(value) && value.length === 2) {
      const oldVal = value[0] === null ? 'null' : value[0];
      const newVal = value[1] === null ? 'null' : value[1];
      message += `• ${key}: ${oldVal} → ${newVal}\n`;
    }
  }
}
```

3. **Обработка CREATE событий:**
```javascript
if (operation === 'create') {
  message += `✨ Новый объект:\n`;
  
  const keyFields = {
    car: ['car_name', 'number', 'vin', 'year', 'transmission', 'mileage', 'active', 'state'],
    booking: ['car_id', 'car_name', 'client_id', 'first_name', 'last_name', 'start_date', 'end_date', 'days', 'state', 'price', 'total'],
    client: ['name', 'lastname', 'phone', 'email', 'category', 'passport_number', 'driver_number']
  };
  
  const fieldsToShow = keyFields[entityType] || [];
  
  for (const field of fieldsToShow) {
    if (payload[field] !== undefined) {
      const value = payload[field] === null ? 'null' : payload[field];
      message += `• ${field}: ${value}\n`;
    }
  }
}
```

### Telegram node "Send Telegram Alert"

**Type:** n8n-nodes-base.telegram  
**Version:** 1.2

**Параметры:**
- **Resource:** message
- **Operation:** sendMessage
- **Chat ID:** `={{ $env.TELEGRAM_ALERT_CHAT_ID }}`
- **Text:** `={{ $json.message }}`
- **Credential:** Telegram account

**Опции:**
- **Continue On Fail:** true (не блокирует основной workflow при ошибке)

---

## Примеры уведомлений

### 1. Обновление машины (car.update)

```
🚗 SERVICE-CENTER | car.update
🆔 ID: 52138

📝 Изменения:
• company_id: 9247 → 11163
• branch_name: Batumi Office → GeoDrive Auto Service
• mileage: 197000 → 200500
```

### 2. Создание брони (booking.create)

```
📅 KUTAISI | booking.create
🆔 ID: 486033

✨ Новый объект:
• car_id: 37407
• car_name: Volkswagen Tiguan
• client_id: 368848
• first_name: Yelyzaveta
• last_name: Futorianska
• start_date: 2025-11-10 01:30:00
• end_date: 2025-11-18 05:00:00
• days: 8
• state: Новая
• price: 105
• total: 840
```

### 3. Обновление брони (booking.update)

```
📅 BATUMI | booking.update
🆔 ID: 507616

📝 Изменения:
• state: Новая → Активная
• start_worker_id: null → 11852
• start_date: 2025-11-10 12:00 → 2025-11-10 14:00
• start_mileage: 189700 → 189750
```

### 4. Обновление клиента (client.update)

```
👤 TBILISI | client.update
🆔 ID: 368848

📝 Изменения:
• phone: +353852852351 → +353999888777
• email: null → yelyzaveta@example.com
• category: Новый → Постоянный
```

### 5. Создание машины (car.create)

```
🚗 TBILISI | car.create
🆔 ID: 49999

✨ Новый объект:
• car_name: Toyota RAV4
• number: AA123BB
• vin: JTMRFREV00J123456
• year: 2024
• transmission: Автомат
• mileage: 5000
• active: true
• state: 1
```

---

## Инструкции по использованию

### Как работает система

1. **RentProg отправляет вебхук** на `https://n8n.rentflow.rentals/webhook/{branch}-webhook`
2. **n8n получает событие** в соответствующий workflow филиала
3. **Parse Webhook** распаковывает данные
4. **Параллельно:**
   - Основная ветка обрабатывает событие и сохраняет в БД
   - Алерт-ветка форматирует и отправляет уведомление в Telegram

### Настройка нового филиала

Если нужно добавить новый филиал:

1. **Создать workflow:**
```bash
# Отредактировать setup/create_all_branch_workflows.mjs
# Добавить новый филиал в массив branches
{
  name: 'New Branch',
  code: 'new-branch',
  company_id: 12345,
  company_token: 'your_token_here',
  webhook_path: 'new-branch-webhook'
}

# Запустить
node setup/create_all_branch_workflows.mjs
```

2. **Добавить Telegram алерты:**
```bash
# Отредактировать setup/add_telegram_alerts_to_processors.mjs
# Добавить ID нового workflow в WORKFLOWS

node setup/add_telegram_alerts_to_processors.mjs
node setup/fix_telegram_nodes_use_credential.mjs
node setup/fix_alert_connection_point.mjs
node setup/fix_alert_format_code.mjs
```

3. **Настроить вебхук в RentProg:**
- URL: `https://n8n.rentflow.rentals/webhook/new-branch-webhook`
- Events: car.*, booking.*, client.*

### Изменение формата уведомлений

**Файл для редактирования:** `setup/fix_alert_format_code.mjs`

**Константа:** `NEW_FORMAT_CODE`

**Примеры изменений:**

1. **Добавить новые ключевые поля:**
```javascript
const keyFields = {
  car: ['car_name', 'number', 'vin', 'year', 'new_field'], // добавить new_field
  booking: [...],
  client: [...]
};
```

2. **Изменить эмодзи:**
```javascript
const emojiMap = {
  car: '🚙',      // было 🚗
  booking: '📆',  // было 📅
  client: '👥'    // было 👤
};
```

3. **Применить изменения:**
```bash
node setup/fix_alert_format_code.mjs
```

### Отключение алертов

**Временно (для одного workflow):**
1. Открыть workflow в n8n UI
2. Деактивировать ноды "Format Telegram Alert" и "Send Telegram Alert"
3. Save

**Полностью (удалить ноды):**
```javascript
// Создать скрипт для удаления нод
// Или удалить вручную через UI
```

---

## Troubleshooting

### Проблема: Уведомления не приходят

**Проверка 1: Переменная окружения**
```bash
python setup/server_ssh.py "docker exec n8n printenv TELEGRAM_ALERT_CHAT_ID"
# Должно вернуть: -5004140602
```

**Решение:**
```bash
python setup/add_telegram_chat_id.py
```

**Проверка 2: Telegram credential**
- Открыть n8n UI → Credentials
- Найти "Telegram account"
- Проверить, что токен настроен

**Проверка 3: Chat ID правильный**
- Отправить тестовое сообщение в чат
- Проверить, что бот добавлен в чат

### Проблема: Сообщения приходят пустые или неправильные

**Причина:** Неправильная точка подключения или код форматирования

**Проверка:**
```bash
# Проверить структуру workflow
node setup/list_processor_workflows.mjs

# Проверить connections
# Format Telegram Alert должен получать данные от Parse Webhook
```

**Решение:**
```bash
node setup/fix_alert_connection_point.mjs
node setup/fix_alert_format_code.mjs
```

### Проблема: Ошибка 404 в Get RentProg Token

**Причина:** Неправильный company_token

**Проверка:**
```bash
node setup/find_kutaisi_workflow.mjs
# Проверит токен для Kutaisi
```

**Решение:**
```bash
node setup/fix_kutaisi_token.mjs
```

### Проблема: undefined в Chat ID

**Причина:** Переменная `TELEGRAM_ALERT_CHAT_ID` не настроена

**Решение:**
```bash
python setup/add_telegram_chat_id.py
```

### Проблема: Дублирование уведомлений

**Причина:** Несколько connection к Format Alert node

**Решение:**
1. Открыть workflow в n8n UI
2. Проверить connections к "Format Telegram Alert"
3. Удалить дублирующие connections
4. Должен быть только один: `Parse Webhook` → `Format Telegram Alert`

---

## Файлы и скрипты

### Созданные скрипты

| Файл | Назначение |
|------|-----------|
| `setup/fix_kutaisi_token.mjs` | Исправление токена Kutaisi |
| `setup/find_kutaisi_workflow.mjs` | Поиск и проверка workflow Kutaisi |
| `setup/fix_trigger_on_conflict.mjs` | Исправление триггера БД |
| `setup/fix_bookings_table.mjs` | Исправление структуры таблицы bookings |
| `setup/fix_jsonb_strings_to_objects.mjs` | Конвертация JSONB строк в объекты |
| `setup/process_all_bookings_with_data.mjs` | Обработка всех существующих броней |
| `setup/force_process_remaining.mjs` | Принудительная обработка необработанных броней |
| `setup/check_processing_progress.mjs` | Проверка прогресса обработки |
| `setup/add_telegram_alerts_to_processors.mjs` | Добавление Telegram нод в workflows |
| `setup/fix_telegram_nodes_use_credential.mjs` | Замена HTTP Request на Telegram ноды |
| `setup/fix_alert_connection_point.mjs` | Исправление точки подключения алертов |
| `setup/fix_alert_format_code.mjs` | Обновление кода форматирования |
| `setup/add_telegram_chat_id.py` | Добавление Chat ID на сервер |
| `setup/list_processor_workflows.mjs` | Список всех processor workflows |

### Обновлённые файлы

| Файл | Изменения |
|------|-----------|
| `setup/create_all_branch_workflows.mjs` | Исправлен токен Kutaisi |
| `config/n8n-variables.yaml` | Добавлен TELEGRAM_ALERT_CHAT_ID |
| `docker-compose.yml` (на сервере) | Добавлен TELEGRAM_ALERT_CHAT_ID |

### Документация

| Файл | Назначение |
|------|-----------|
| `SESSION_REPORT_TELEGRAM_ALERTS_2025-11-05.md` | Этот отчет |
| `TELEGRAM_ALERTS_GUIDE.md` | Руководство по использованию алертов |
| `RENTPROG_PROCESSORS.md` | Документация по processor workflows |

---

## Статистика

### Обработано данных

- **Брони:** 3449 (100%)
- **Машины:** 3446 связей создано
- **Клиенты:** 3446 связей создано
- **Workflows:** 4 обновлено

### Время выполнения

- **Исправление токена:** ~2 минуты
- **Обработка броней:** ~15 минут
- **Добавление алертов:** ~5 минут
- **Исправления и тесты:** ~10 минут
- **Общее время:** ~32 минуты

### Результаты тестов

✅ Все 4 workflows активны  
✅ Триггер БД работает корректно  
✅ Telegram алерты приходят  
✅ Формат сообщений правильный  
✅ Переменные окружения настроены  

---

## Следующие шаги

### Краткосрочные (необходимо)

1. **Создать словарь переводов полей** для человекочитаемых названий
   - Сейчас: `company_id: 9247 → 11163`
   - Будущее: `Филиал: Batumi Office → GeoDrive Auto Service`

2. **Добавить фильтрацию событий** (опционально)
   - Не отправлять алерты для незначительных изменений
   - Настраиваемый список важных полей

3. **Мониторинг алертов**
   - Счётчик отправленных уведомлений
   - Статистика по типам событий

### Среднесрочные (желательно)

1. **Группировка уведомлений**
   - Batch обработка (несколько событий в одном сообщении)
   - Защита от спама при массовых обновлениях

2. **Интерактивные кнопки**
   - Быстрые действия из Telegram (открыть в RentProg, и т.д.)

3. **Настраиваемые алерты по филиалам**
   - Разные чаты для разных филиалов
   - Персональные уведомления для ответственных

### Долгосрочные (планирование)

1. **AI-анализ событий**
   - Определение аномалий (необычные изменения)
   - Предиктивные алерты

2. **Dashboard**
   - Визуализация событий в реальном времени
   - Статистика по филиалам

---

## Заключение

### Достигнутые цели

✅ **Все 4 цели выполнены:**
1. Токен Kutaisi исправлен
2. 3449 броней обработаны (100%)
3. Telegram алерты добавлены во все workflows
4. Переменные окружения настроены

### Качество решения

- **Надёжность:** Все компоненты протестированы и работают
- **Масштабируемость:** Легко добавить новые филиалы
- **Поддерживаемость:** Документирован код и процессы
- **Производительность:** Алерты не блокируют основной workflow

### Документация

- ✅ Отчет о сессии (этот документ)
- ✅ Руководство по использованию
- ✅ Примеры уведомлений
- ✅ Troubleshooting guide

---

**Автор:** Cursor Agent  
**Дата:** 5 ноября 2025  
**Версия:** 1.0  
**Статус:** ✅ Production Ready

