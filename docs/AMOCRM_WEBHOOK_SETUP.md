# Инструкция по настройке вебхуков и API в AmoCRM

## 📋 Обзор

Эта инструкция поможет настроить интеграцию с AmoCRM через **вебхуки и API**:
1. **Вебхуки AmoCRM** → n8n (для получения событий в реальном времени)
2. **API через Playwright Service** (для получения деталей сделок/контактов по запросу)
3. **Jarvis API** (для обработки и сохранения данных в БД)

**Важно:** Мы работаем **только через API**, без массового парсинга истории. Все данные получаем в реальном времени через вебхуки.

---

## 🎯 Шаг 1: Регистрация вебхука в AmoCRM

### 1.1. Войдите в AmoCRM

1. Откройте: https://geodrive.amocrm.ru
2. Войдите с учетными данными: `geodrive.ge@gmail.com` / `wnr3c4%UqN@jY23`

### 1.2. Откройте настройки вебхуков

**Вариант A (через настройки аккаунта):**
1. Нажмите на **аватар** в правом верхнем углу
2. Выберите **"Настройки"** (Settings)
3. В левом меню найдите **"Вебхуки"** (Webhooks) или **"Интеграции"** (Integrations)

**Вариант B (прямая ссылка):**
```
https://geodrive.amocrm.ru/settings/webhooks
```

### 1.3. Добавьте новый вебхук

1. Нажмите кнопку **"+ Добавить хук"** или **"Создать вебхук"**
2. В поле **"URL"** введите:
   ```
   https://webhook.rentflow.rentals/amocrm
   ```
3. Выберите события для отслеживания:

   **Обязательные события (рекомендуется выбрать все):**
   - ✅ **Сделка добавлена** (`lead.add`)
   - ✅ **Сделка изменена** (`lead.update`)
   - ✅ **Статус сделки изменен** (`lead.status`)
   - ✅ **Контакт добавлен** (`contact.add`)
   - ✅ **Контакт изменен** (`contact.update`)
   - ✅ **Неразобранное добавлено** (`unsorted.add`)
   - ✅ **Неразобранное удалено** (`unsorted.delete`)
   - ✅ **Примечание добавлено** (`note.add`)
   - ✅ **Входящее сообщение добавлено** (`message.add`)
   - ✅ **Беседа добавлена** (`conversation.add`)

   **Опциональные события:**
   - Компания добавлена/изменена
   - Покупатель добавлен/изменен
   - Задача добавлена/изменена

4. Нажмите **"Сохранить"** или **"Создать вебхук"**

### 1.4. Проверка вебхука

После сохранения AmoCRM отправит тестовый запрос на ваш URL. Проверьте:
- В n8n workflow "Обработка вебхуков AmoCRM" должно появиться новое выполнение
- В таблице `amocrm_webhook_events` должна появиться запись

---

## 🔑 Шаг 2: Настройка AmoCRM API (OAuth 2.0)

**AmoCRM API v4** используется напрямую для получения деталей сделок/контактов по запросу.

### 2.1. Создание интеграции в AmoCRM

**Для получения Access Token нужно создать интеграцию:**

1. Откройте: https://geodrive.amocrm.ru/amo-market
2. Нажмите на **троеточие** (⋮) в левом верхнем углу
3. Выберите **"Создать интеграцию"**

**Или прямая ссылка:**
```
https://geodrive.amocrm.ru/amo-market/integrations/create
```

### 2.2. Заполните форму интеграции

**Основные поля:**

1. **Название интеграции:**
   ```
   Jarvis Bot - API Integration
   ```

2. **Описание:**
   ```
   Автоматизация получения данных из AmoCRM через API для аналитики и синхронизации с RentProg
   ```

3. **Ссылка для перенаправления (Redirect URI):**
   ```
   https://n8n.rentflow.rentals/oauth/amocrm/callback
   ```
   *(Или любой другой URL для получения кода авторизации)*

4. **Права доступа:**
   - ✅ **"Предоставить доступ: Все"** (Grant access: All)

5. Нажмите **"Сохранить"**

### 2.3. Получение Access Token

**Подробная инструкция:** См. `docs/AMOCRM_API_AUTH.md`

**Краткая версия:**
1. После создания интеграции скопируйте **Client ID** и **Client Secret**
2. Получите Authorization Code через OAuth flow
3. Обменяйте Code на Access Token
4. Сохраните Access Token в n8n переменных окружения: `AMOCRM_ACCESS_TOKEN`

### 2.4. Как это работает

1. **Вебхук приходит** в n8n с базовой информацией о сделке/контакте
2. **n8n запрашивает детали** напрямую через AmoCRM API v4:
   - `GET https://geodrive.amocrm.ru/api/v4/leads/:id?with=contacts,companies`
   - `GET https://geodrive.amocrm.ru/api/v4/contacts/:id`
3. **AmoCRM API** возвращает полные данные
4. **Данные отправляются** в Jarvis API для обработки и сохранения в БД

**Важно:** API запросы делаются **только по запросу** (когда приходит вебхук), не массово.

---

## ✅ Шаг 3: Проверка работы

### 4.1. Проверка вебхуков

1. Создайте тестовую сделку в AmoCRM
2. Проверьте в n8n:
   - Workflow "Обработка вебхуков AmoCRM" должен выполниться
   - В логах должно быть событие `lead.add`

3. Проверьте в БД:
```sql
SELECT * FROM amocrm_webhook_events 
ORDER BY ts DESC 
LIMIT 10;
```

### 4.2. Проверка API

1. Проверьте получение сделки через AmoCRM API:
```bash
curl -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  "https://geodrive.amocrm.ru/api/v4/leads/32249167?with=contacts,companies"
```

2. Проверьте получение контакта:
```bash
curl -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  "https://geodrive.amocrm.ru/api/v4/contacts/123456"
```

---

## 📊 Шаг 4: Мониторинг лимитов API

### Лимиты AmoCRM API:

- **7,000 запросов/час** (базовый тариф)
- **250 запросов/минуту**

### Наше потребление (работа через вебхуки + API):

- **Вебхуки:** 0 запросов/час (только прием данных, не расходует лимиты)
- **API запросы:** ~10-100 запросов/час (только когда приходят вебхуки)
  - Каждый вебхук → 1 запрос на получение деталей через Playwright Service
  - Если вебхуков нет → 0 запросов

**Итого:** Остается **~6,900+ запросов/час** для других сервисов 🎉

**Преимущества:**
- ✅ Не расходуем лимиты на polling
- ✅ Получаем данные в реальном времени
- ✅ Запросы только по необходимости (когда есть изменения)

---

## 🔧 Шаг 5: Настройка Nginx (если нужно)

Если вебхук не работает, проверьте конфигурацию Nginx:

```nginx
# /etc/nginx/sites-available/webhook.rentflow.rentals
location /amocrm {
    proxy_pass http://localhost:5678/webhook/amocrm;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

После изменений:
```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

## 🚨 Troubleshooting

### Проблема: Вебхук не приходит

1. Проверьте URL в настройках AmoCRM
2. Проверьте, что workflow активен в n8n
3. Проверьте логи n8n:
```bash
docker logs n8n --tail 100
```

### Проблема: API возвращает 401 Unauthorized

1. Проверьте, что Access Token актуален (токены AmoCRM истекают через определенное время)
2. Обновите Access Token через OAuth 2.0 flow
3. Проверьте переменную окружения `AMOCRM_ACCESS_TOKEN` в n8n
4. Убедитесь, что токен имеет нужные права доступа

### Проблема: Достигнут лимит API

**Маловероятно при работе через вебхуки**, но если произошло:

1. Проверьте количество вебхуков (каждый вебхук = 1 API запрос)
2. Убедитесь, что нет дублирующихся вебхуков
3. Проверьте логи n8n на наличие ошибок, вызывающих повторные запросы

---

## 📝 Чеклист настройки

- [ ] Вебхук создан в AmoCRM
- [ ] URL вебхука: `https://webhook.rentflow.rentals/amocrm`
- [ ] Выбраны все нужные события
- [ ] Workflow "Обработка вебхуков AmoCRM" активен в n8n
- [ ] Таблица `amocrm_webhook_events` создана в БД
- [ ] AmoCRM API интеграция создана
- [ ] Access Token получен и сохранен в n8n (`AMOCRM_ACCESS_TOKEN`)
- [ ] Jarvis API работает (проверка: `curl http://localhost:3000/health`)
- [ ] Тестовая сделка создана и получена через вебхук
- [ ] Данные сохраняются в таблицу `amocrm_deals`
- [ ] Лимиты API проверены (должно быть ~6,900+ запросов/час свободно)

---

## 🔗 Полезные ссылки

- **Документация AmoCRM API:** https://www.amocrm.ru/developers/content/api/auth
- **Документация вебхуков:** https://www.amocrm.ru/developers/content/api/webhooks
- **n8n UI:** https://n8n.rentflow.rentals
- **Вебхуки URL:** https://webhook.rentflow.rentals

---

**Дата создания:** 2025-11-11  
**Статус:** ✅ Готово к использованию

