# Анализ пробелов в парсинге автомобилей

**Дата:** 2025-11-14  
**Источник:** Сравнение JSON API `/all_cars_with_bookings` vs БД

---

## 🔍 ЧТО МЫ ПАРСИМ СЕЙЧАС

### Текущие поля в workflow (Process Cars):

```javascript
{
  branch: branch,
  rentprog_id: car.id,
  car_name: car.name || car.car_name,
  code: car.code,
  number: car.number,
  color: car.color,
  year: car.year,
  price: car.price || 0,
  deposit: car.deposit || 0,
  data: car  // полный JSON
}
```

**Итого: 9 полей + полный JSON**

---

## ❌ ЧТО МЫ НЕ ПАРСИМ (но есть в API)

### 1. **Статусы и состояния автомобиля**

#### `state` (числовое значение)
- **Пример:** `"state": 1`
- **Значение:** Состояние автомобиля (вероятно: 1=отличное, 2=хорошее, 3=требует ремонта)
- **Важность:** ⭐⭐⭐⭐⭐ Критично для понимания доступности авто
- **Используется в UI:** Вероятно цветной кружок/иконка

#### `active` (boolean)
- **Пример:** `"active": true`
- **Значение:** Активен ли автомобиль для аренды
- **Важность:** ⭐⭐⭐⭐⭐ Критично
- **Используется в UI:** Показывает доступность

#### `tank_state` (boolean)
- **Пример:** `"tank_state": false`
- **Значение:** Состояние бака (полный/пустой?)
- **Важность:** ⭐⭐⭐

#### `clean_state` (boolean)
- **Пример:** `"clean_state": true`
- **Значение:** Чистота автомобиля
- **Важность:** ⭐⭐⭐

---

### 2. **Технические характеристики**

#### `transmission` (string)
- **Пример:** `"transmission": "Автомат"`
- **Значение:** Тип коробки передач
- **Важность:** ⭐⭐⭐⭐⭐ Часто используется клиентами

#### `fuel` (string)
- **Пример:** `"fuel": "Бензин 95"`
- **Важность:** ⭐⭐⭐⭐

#### `engine_capacity` (string)
- **Пример:** `"engine_capacity": "1.5"`
- **Важность:** ⭐⭐⭐

#### `car_class` (string)
- **Пример:** `"car_class": "Средний"`
- **Важность:** ⭐⭐⭐⭐

#### `car_type` (string)
- **Пример:** `"car_type": "Седан"`
- **Важность:** ⭐⭐⭐⭐

#### `number_doors` (integer)
- **Пример:** `"number_doors": 4`
- **Важность:** ⭐⭐

---

### 3. **Опции и оборудование**

#### Климат и комфорт:
- `is_air` (boolean) - кондиционер
- `climate_control` (boolean) - климат-контроль
- `heated_seats` (boolean) - подогрев сидений
- `heated_seats_front` (boolean) - подогрев передних сидений
- `heated_windshield` (boolean) - подогрев лобового стекла

#### Безопасность:
- `parktronic` (boolean) - парктроник
- `parktronic_back` (boolean) - задний парктроник
- `parktronic_camera` (boolean) - камера заднего вида
- `rain_sensor` (boolean) - датчик дождя

#### Медиа:
- `audio_system` (boolean)
- `video_system` (boolean)
- `tv_system` (boolean)
- `cd_system` (boolean)
- `usb_system` (boolean)

#### Другое:
- `wheel_adjustment` (boolean) - регулировка руля
- `wheel_adjustment_full` (boolean) - полная регулировка
- `folding_seats` (boolean) - складные сиденья

**Важность всех опций:** ⭐⭐⭐⭐ (влияют на выбор клиента)

---

### 4. **Цены (критично!)**

#### `price_hour` (integer)
- **Пример:** `"price_hour": 22`
- **Важность:** ⭐⭐⭐⭐⭐
- **Проблема:** Мы парсим только `price`, которое может быть ценой за день

#### Залоги:
- `deposit` - основной залог ✅ (парсим)
- `hourly_deposit` - залог для почасовой аренды
- `weekly_deposit` - залог для недельной аренды
- `monthly_deposit` - залог для месячной аренды

#### Relationships → prices (массив ID цен)
- **Проблема:** Мы НЕ парсим связанные prices (12 объектов)
- **Содержит:** Цены для разных периодов (1-2 дня, 3-4 дня, 5-7 дней, и т.д.)
- **Важность:** ⭐⭐⭐⭐⭐ Критично для ценообразования

---

### 5. **Пробег и ТО**

#### `mileage` (integer)
- **Пример:** `"mileage": 132985`
- **Важность:** ⭐⭐⭐⭐⭐ Критично для планирования ТО

#### ТО масла двигателя:
- `oil_engine` (string)
- `oil_engine_km` (integer) - при каком пробеге
- `oil_engine_date` (date) - когда сделано

#### ТО КПП:
- `oil_transmission` (string)
- `oil_transmission_km` (integer)
- `oil_transmission_date` (date)

#### ГРМ:
- `grm` (string)
- `grm_km` (integer)
- `grm_date` (date)

#### Другие жидкости:
- `antifreeze`, `antifreeze_km`, `antifreeze_date`
- `brake_fluid`, `brake_fluid_km`, `brake_fluid_date`
- `candle`, `candle_km`, `candle_date` (свечи)
- `power_steering`, `power_steering_km`, `power_steering_date`

#### Фильтры и прочее:
- `salon_filter_renew`, `salon_filter_renew_km`, `salon_filter_renew_date`
- `fuel_filter_renew`, `fuel_filter_renew_km`, `fuel_filter_renew_date`
- `akb_renew`, `akb_renew_km`, `akb_renew_date` (аккумулятор)

**Важность:** ⭐⭐⭐⭐⭐ Критично для агента контроля ТО

---

### 6. **Финансы**

#### `purchase_price` (float)
- **Пример:** `"purchase_price": 16890`
- **Важность:** ⭐⭐⭐⭐

#### `purchase_date` (date)
- **Пример:** `"purchase_date": "2024-05-14"`
- **Важность:** ⭐⭐⭐

#### `sale_price` (float/null)
#### `sale_date` (date/null)

#### `investor_id` (integer)
- **Пример:** `"investor_id": 776`
- **Важность:** ⭐⭐⭐⭐ (для финансовой отчетности)

---

### 7. **Франшизы и страховки**

#### `franchise` (integer)
- **Пример:** `"franchise": 540`
- **Важность:** ⭐⭐⭐⭐

#### `max_fine` (integer)
- **Пример:** `"max_fine": 25`
- **Важность:** ⭐⭐⭐

#### `repair_cost` (integer)
- **Пример:** `"repair_cost": 300`
- **Важность:** ⭐⭐⭐⭐

#### `insurance` (string/null)

---

### 8. **Агрегаторы и интеграции**

#### `ready_for_agregator` (boolean)
- **Пример:** `"ready_for_agregator": false`

#### `in_agregator` (boolean)
#### `in_agregator_date` (date/null)
#### `in_localrent` (boolean)

**Важность:** ⭐⭐⭐

---

### 9. **Документы и идентификаторы**

#### `vin` (string)
- **Пример:** `"vin": "1G1ZB5STXHF279609"`
- **Важность:** ⭐⭐⭐⭐⭐ Уникальный идентификатор

#### `body_number` (string)
#### `pts` (string) - паспорт ТС
#### `registration_certificate` (string) - свидетельство о регистрации
#### `diagnostic_card` (string/null)
#### `last_inspection` (date/null)

**Важность:** ⭐⭐⭐⭐

---

### 10. **Дополнительные поля**

#### `sort` (integer)
- **Пример:** `"sort": 0`
- **Значение:** Порядок сортировки
- **Важность:** ⭐⭐

#### `age_limit` (integer)
- **Пример:** `"age_limit": 21`
- **Значение:** Минимальный возраст водителя
- **Важность:** ⭐⭐⭐⭐

#### `driver_year_limit` (integer)
- **Пример:** `"driver_year_limit": 1`
- **Значение:** Минимальный стаж вождения
- **Важность:** ⭐⭐⭐⭐

#### `store_place` (string/null)
- **Значение:** Место хранения

#### `description` (string/null)
- **Важность:** ⭐⭐⭐

#### Custom fields:
- `custom_field_1` - например "Premium/95" (тип топлива?)
- `custom_field_2` - например "50" (расход?)
- `custom_field_3` - например "PSP +995(0322)185555" (контакт?)

**Важность:** ⭐⭐⭐

---

### 11. **Beacon (маяк GPS)**

#### `beacon_email` (string/null)
#### `beacon_phone` (string/null)
#### `beacon_pin` (string/null)

**Важность:** ⭐⭐⭐ (для агента служебных поездок)

---

### 12. **Компания и филиал**

#### `main_company_id` (integer)
- **Пример:** `"main_company_id": 9110`

#### `company_id` (integer)
- **Пример:** `"company_id": 9247`
- **Важность:** ⭐⭐⭐⭐ (для определения филиала)

---

## 📊 ИТОГОВАЯ СТАТИСТИКА

### Что парсим:
- ✅ **9 основных полей** + полный JSON в `data`

### Что НЕ парсим явно:
- ❌ **~80+ полей** из JSON не извлечены в отдельные колонки БД
- ❌ **Relationships** (prices, hourly_prices, weekly_prices, monthly_prices) - вообще не сохраняем
- ❌ **Визуальные индикаторы** из UI (иконки состояния, цветные кружки)

---

## ⚠️ КРИТИЧНЫЕ ПРОПУСКИ

### Top-5 самых важных:

1. **`active`** - доступен ли автомобиль
2. **`state`** - состояние автомобиля (1/2/3)
3. **`mileage`** - пробег (для ТО)
4. **`transmission`** - АКПП/МКПП (часто ищут клиенты)
5. **`relationships.prices`** - ВСЕ цены для разных периодов

---

## 🎯 РЕКОМЕНДАЦИИ

### 1. Добавить в БД таблицу `car_prices`

```sql
CREATE TABLE car_prices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  car_id UUID REFERENCES cars(id),
  price_type TEXT, -- '1-2days', '3-4days', 'hourly', etc.
  price INTEGER,
  rentprog_price_id TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 2. Добавить важные поля в таблицу `cars`

```sql
ALTER TABLE cars ADD COLUMN IF NOT EXISTS
  active BOOLEAN DEFAULT TRUE,
  state INTEGER, -- 1/2/3
  mileage INTEGER,
  transmission TEXT,
  fuel TEXT,
  car_class TEXT,
  car_type TEXT,
  vin TEXT,
  investor_id INTEGER,
  age_limit INTEGER,
  driver_year_limit INTEGER;
```

### 3. Обновить workflow для парсинга этих полей

### 4. Визуальные элементы из UI
- Ждем результатов анализа через Puppeteer...

---

**Следующий шаг:** Дождаться результатов браузерного анализа для выявления визуальных индикаторов (иконки, цвета, статусы).

