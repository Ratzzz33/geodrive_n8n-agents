# 🔐 Полный список GitHub Secrets для CI/CD

**Последнее обновление:** 2025-01-XX  
**Статус:** ✅ Актуально

---

## 📋 Быстрый чеклист

### ✅ Обязательные секреты (минимум для деплоя):

- [ ] `SERVER_IP` - IP адрес сервера
- [ ] `SERVER_SSH_KEY` или `SERVER_PASSWORD` - для SSH подключения
- [ ] `NEON_DATABASE_URL` - строка подключения к БД
- [ ] `N8N_API_KEY` - API ключ n8n

### ⚙️ Опциональные (для расширенной функциональности):

- [ ] `SERVER_USER` - пользователь SSH (по умолчанию `root`)
- [ ] `N8N_HOST` - URL n8n
- [ ] `TELEGRAM_BOT_TOKEN` - токен основного бота
- [ ] `N8N_ALERTS_TELEGRAM_BOT_TOKEN` - токен бота для алертов
- [ ] `TELEGRAM_ALERT_CHAT_ID` - ID чата для алертов
- [ ] `RENTPROG_TBILISI_TOKEN` - токен RentProg (Тбилиси)
- [ ] `RENTPROG_BATUMI_TOKEN` - токен RentProg (Батуми)
- [ ] `RENTPROG_KUTAISI_TOKEN` - токен RentProg (Кутаиси)
- [ ] `RENTPROG_SERVICE_CENTER_TOKEN` - токен RentProg (Сервис центр)
- [ ] `HCLOUD_TOKEN` - API токен Hetzner Cloud

---

## 🔑 Детальное описание секретов

### 1. SSH подключение к серверу

#### `SERVER_IP` ⭐ ОБЯЗАТЕЛЬНО
```
46.224.17.15
```
- **Описание:** IP адрес сервера Hetzner Cloud
- **Используется в:** Всех workflows для деплоя и SSH подключения

#### `SERVER_SSH_KEY` ⭐ РЕКОМЕНДУЕТСЯ (вместо пароля)
```
-----BEGIN RSA PRIVATE KEY-----
MIIEpAIBAAKCAQEA...
(весь содержимое файла ~/.ssh/id_rsa)
-----END RSA PRIVATE KEY-----
```
- **Описание:** Приватный SSH ключ для безопасного подключения
- **Как получить:**
  1. После настройки SSH ключей: `cat ~/.ssh/id_rsa`
  2. Скопируйте весь файл (включая BEGIN/END строки)
- **Преимущества:** Безопаснее пароля, не нужно обновлять
- **Важно:** Если используете SSH ключ, `SERVER_PASSWORD` не нужен

#### `SERVER_PASSWORD` ⚠️ Альтернатива SSH ключу
```
WNHeg7U7aiKw
```
- **Описание:** Пароль root пользователя на сервере
- **Когда использовать:** Если SSH ключ не настроен
- **Безопасность:** Менее безопасно, чем SSH ключ
- **Обновление:** Может измениться, нужно обновлять в secrets

#### `SERVER_USER` (опционально)
```
root
```
- **Описание:** Пользователь для SSH подключения
- **По умолчанию:** `root` (если не указан)
- **Когда менять:** Если используете другого пользователя

---

### 2. База данных (Neon PostgreSQL)

#### `NEON_DATABASE_URL` ⭐ ОБЯЗАТЕЛЬНО
```
postgresql://neondb_owner:npg_cHIT9Kxfk1Am@ep-rough-heart-ahnybmq0-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require
```
- **Описание:** Полная строка подключения к PostgreSQL
- **Используется в:**
  - Миграциях БД
  - Тестах подключения
  - CI/CD проверках
- **Где взять:** Neon Console → Connection String

---

### 3. n8n API

#### `N8N_API_KEY` ⭐ ОБЯЗАТЕЛЬНО
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI3ZDYyYjM3My0yMDFiLTQ3ZjMtODU5YS1jZGM2OWRkZWE0NGEiLCJpc3MiOiJuOG4iLCJhdWQiOiJwdWJsaWMtYXBpIiwiaWF0IjoxNzYyMDg0MjY4LCJleHAiOjE3NjQ2NTE2MDB9.gsdxltowlQShNi9mil074-cMhnuJJLI5lN6MP7FQEcI
```
- **Срок действия:** до 2025-12-02
- **Описание:** API ключ для доступа к n8n REST API
- **Используется в:**
  - Автоматическом импорте workflow
  - Синхронизации workflow
  - Проверках работоспособности
- **Как обновить при истечении:**
  1. Откройте: https://n8n.rentflow.rentals
  2. Settings → API → Create API Key
  3. Обновите secret в GitHub

#### `N8N_HOST` (опционально)
```
https://n8n.rentflow.rentals
```
или
```
http://46.224.17.15:5678
```
- **Описание:** URL n8n сервера
- **Используется в:** Некоторых workflows для проверок

---

### 4. Telegram Боты

#### `TELEGRAM_BOT_TOKEN` (опционально)
```
Токен основного бота @test_geodrive_check_bot
```
- **Описание:** Токен основного Telegram бота
- **Используется в:** Workflows для отправки уведомлений

#### `N8N_ALERTS_TELEGRAM_BOT_TOKEN` (опционально)
```
Токен бота для алертов @n8n_alert_geodrive_bot
```
- **Описание:** Токен бота для системных алертов
- **Используется в:** Workflows для критических уведомлений

#### `TELEGRAM_ALERT_CHAT_ID` (опционально)
```
-5004140602
```
- **Описание:** ID чата для отправки алертов
- **Используется в:** Workflows для алертов

---

### 5. RentProg API (по филиалам)

#### `RENTPROG_TBILISI_TOKEN` (опционально)
```
Токен для филиала Тбилиси
```

#### `RENTPROG_BATUMI_TOKEN` (опционально)
```
Токен для филиала Батуми
```

#### `RENTPROG_KUTAISI_TOKEN` (опционально)
```
Токен для филиала Кутаиси
```

#### `RENTPROG_SERVICE_CENTER_TOKEN` (опционально)
```
Токен для сервисного центра
```

- **Описание:** Токены для доступа к RentProg API по филиалам
- **Используется в:** Workflows для синхронизации данных из RentProg
- **Где взять:** RentProg → Настройки → API → Токены

---

### 6. Hetzner Cloud

#### `HCLOUD_TOKEN` (опционально)
```
2S6Lm5H2OcnEVRDXBRScemVxmFi0EkqCAqYGfVTCIsENYlqHJqo4HNpHaO2djqE4
```
- **Описание:** API токен Hetzner Cloud
- **Используется в:** Управлении серверами через Hetzner CLI
- **Где взять:** Hetzner Cloud Console → API Tokens

---

## 🚀 Как добавить Secrets в GitHub

### Способ 1: Через веб-интерфейс (рекомендуется)

1. Откройте репозиторий: https://github.com/Ratzzz33/geodrive_n8n-agents
2. Перейдите: **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Для каждого секрета:
   - **Name:** название (например, `SERVER_IP`)
   - **Secret:** значение секрета
   - Нажмите **"Add secret"**

### Способ 2: Через GitHub CLI

```bash
# Установите GitHub CLI если нет
# https://cli.github.com/

# Добавьте секреты
gh secret set SERVER_IP --body "46.224.17.15"
gh secret set SERVER_USER --body "root"
gh secret set SERVER_SSH_KEY < ~/.ssh/id_rsa
gh secret set NEON_DATABASE_URL --body "postgresql://..."
gh secret set N8N_API_KEY --body "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

---

## 📝 Минимальный набор для работы

Для базового деплоя достаточно:

```yaml
SERVER_IP: 46.224.17.15
SERVER_SSH_KEY: (приватный SSH ключ)
NEON_DATABASE_URL: postgresql://...
N8N_API_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

Или вместо SSH ключа:
```yaml
SERVER_IP: 46.224.17.15
SERVER_PASSWORD: WNHeg7U7aiKw
NEON_DATABASE_URL: postgresql://...
N8N_API_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

## 🔒 Безопасность

### ✅ Правила:

1. **Никогда не коммитьте секреты в код!**
   - Используйте `.env` локально (в `.gitignore`)
   - Используйте GitHub Secrets в CI/CD
   - Используйте `.env.example` с плейсхолдерами

2. **Приоритет SSH ключа над паролем:**
   - ✅ Используйте `SERVER_SSH_KEY` вместо `SERVER_PASSWORD`
   - ✅ SSH ключ безопаснее и не нужно обновлять
   - ⚠️ Если используете ключ, пароль не нужен

3. **Регулярное обновление:**
   - Обновляйте `N8N_API_KEY` при истечении (сейчас до 2025-12-02)
   - Меняйте пароли при необходимости
   - Ротация SSH ключей (если нужно)

4. **Ограничение доступа:**
   - Secrets видны только GitHub Actions во время выполнения
   - После добавления секреты нельзя прочитать обратно (только изменить)
   - Используйте минимально необходимые секреты

---

## 🧪 Проверка работы

### Проверка наличия секретов

После добавления всех секретов:

1. Откройте: **Settings** → **Secrets and variables** → **Actions**
2. Проверьте список - должны быть все необходимые секреты
3. Запустите workflow вручную:
   - **Actions** tab → выберите workflow → **Run workflow**
4. Проверьте логи - не должно быть ошибок "secret not set"

### Тестовый workflow

Создайте простой workflow для проверки:

```yaml
name: Test Secrets
on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check secrets
        run: |
          echo "Checking secrets..."
          if [ -z "${{ secrets.SERVER_IP }}" ]; then
            echo "❌ SERVER_IP not set"
            exit 1
          fi
          if [ -z "${{ secrets.NEON_DATABASE_URL }}" ]; then
            echo "❌ NEON_DATABASE_URL not set"
            exit 1
          fi
          echo "✅ All required secrets are set"
```

---

## 📚 Использование в GitHub Actions

### Пример workflow с секретами

```yaml
name: Deploy
on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts
      
      - name: Deploy to server
        env:
          SERVER_IP: ${{ secrets.SERVER_IP }}
          SERVER_USER: ${{ secrets.SERVER_USER || 'root' }}
        run: |
          ssh ${{ secrets.SERVER_USER || 'root' }}@${{ secrets.SERVER_IP }} "
            cd /root/geodrive_n8n-agents &&
            git pull &&
            docker compose restart
          "
```

---

## 🔄 Обновление секретов

### Когда обновлять:

1. **N8N_API_KEY** - при истечении (сейчас до 2025-12-02)
2. **SERVER_PASSWORD** - если изменили пароль на сервере
3. **NEON_DATABASE_URL** - если изменили пароль БД
4. **RentProg токены** - если обновили токены в RentProg

### Как обновить:

1. Откройте: **Settings** → **Secrets and variables** → **Actions**
2. Найдите нужный secret
3. Нажмите **Update** (карандаш)
4. Введите новое значение
5. Сохраните

---

## ⚠️ Важные заметки

1. **SSH ключ vs Пароль:**
   - После настройки SSH ключей рекомендуется использовать `SERVER_SSH_KEY`
   - Пароль можно оставить как fallback, но лучше отключить на сервере

2. **Истечение ключей:**
   - `N8N_API_KEY` истекает 2025-12-02 - нужно обновить до этого срока
   - Настройте напоминание за неделю до истечения

3. **Проверка перед деплоем:**
   - GitHub Actions автоматически пропустит деплой, если secrets не настроены
   - Тесты запускаются всегда, независимо от secrets
   - Деплой запускается только после успешных тестов

---

## 📖 Дополнительные ресурсы

- [SSH Keys Setup](./SSH_KEYS_SETUP.md) - Настройка SSH ключей
- [.github/secrets.md](../.github/secrets.md) - Дополнительная информация
- [GITHUB_SECRETS_SETUP.md](../GITHUB_SECRETS_SETUP.md) - Альтернативная документация

---

**Готово!** Настройте все необходимые секреты и ваш CI/CD будет работать. 🎉

