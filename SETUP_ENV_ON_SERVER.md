# Настройка .env файла на сервере

## Проблема
Бот не отвечает, потому что на сервере отсутствует файл `.env` или в нем нет правильного токена `TELEGRAM_BOT_TOKEN`.

## Решение

### Вариант 1: Через SSH (вручную)

1. Подключитесь к серверу:
```bash
ssh root@46.224.17.15
# Пароль: enebit7Lschwrkb93vnm
```

2. Перейдите в директорию проекта:
```bash
cd /root/geodrive_n8n-agents
```

3. Проверьте наличие `.env` файла:
```bash
ls -la .env
```

4. Если файла нет или он пустой, создайте его:
```bash
# Если есть .env.example
cp .env.example .env

# Или создайте новый
nano .env
```

5. Добавьте токен бота:
```bash
TELEGRAM_BOT_TOKEN=your_token_here
```

**ВАЖНО:** Замените `your_token_here` на реальный токен бота `@test_geodrive_check_bot`.

6. Сохраните файл (Ctrl+O, Enter, Ctrl+X в nano)

7. Проверьте, что токен установлен:
```bash
grep TELEGRAM_BOT_TOKEN .env
```

8. Перезапустите бота:
```bash
pkill -9 -f "tsx.*index.ts" || pkill -9 -f "node.*dist/index.js" || true
sleep 3
npm run dev > /root/bot.log 2>&1 &
tail -f /root/bot.log
```

### Вариант 2: Через GitHub Actions

После добавления `.env` на сервере с правильным токеном, запустите workflow "Restart Bot on Server" в GitHub Actions.

## Проверка

После настройки `.env`, бот должен запуститься без ошибок. Проверьте логи:
```bash
tail -f /root/bot.log
```

Должно быть сообщение типа:
```
🤖 Bot started (polling mode)
📱 Bot @test_geodrive_check_bot connected (ID: xxxxx)
```

## Где взять токен?

1. Откройте Telegram
2. Найдите бота @BotFather
3. Отправьте команду `/mybots`
4. Выберите бота `@test_geodrive_check_bot`
5. Выберите "API Token"
6. Скопируйте токен

Или создайте нового бота через `/newbot` в @BotFather.

