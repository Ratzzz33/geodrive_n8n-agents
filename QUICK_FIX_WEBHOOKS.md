# БЫСТРОЕ ИСПРАВЛЕНИЕ: Вебхуки service-center не работают

## 🔴 ПРОБЛЕМА

В RentProg прописан URL `https://n8n.rentflow.rentals/webhook/service-center-webhook`, но в n8n **НЕТ** воркфлоу, который слушает на этом пути!

## ✅ БЫСТРОЕ РЕШЕНИЕ (5 минут)

### Измените URL вебхука в RentProg:

**Было:**
```
https://n8n.rentflow.rentals/webhook/service-center-webhook
```

**Должно быть:**
```
https://webhook.rentflow.rentals/
```

Этот endpoint уже настроен и работает!

## 📋 ШАГ ЗА ШАГОМ

### 1. Добавьте логирование (на сервере)

```bash
# Скопируйте новый конфиг nginx
cd /путь/к/workspace
sudo cp nginx/n8n.rentflow.rentals.conf /etc/nginx/sites-available/

# Проверьте конфиг
sudo nginx -t

# Перезагрузите nginx
sudo systemctl reload nginx
```

### 2. Установите скрипт анализа логов

```bash
sudo cp scripts/analyze-webhook-logs.sh /usr/local/bin/
sudo chmod +x /usr/local/bin/analyze-webhook-logs.sh
```

### 3. Измените URL в RentProg

1. Зайдите в админку RentProg
2. Найдите раздел Webhooks / API / Интеграции
3. Найдите webhook для сервисного центра
4. Измените URL на: `https://webhook.rentflow.rentals/`
5. Сохраните

### 4. Протестируйте

```bash
# Запустите мониторинг логов
sudo /usr/local/bin/analyze-webhook-logs.sh --monitor

# В другом терминале отправьте тестовый webhook из RentProg
# или дождитесь реального события
```

### 5. Проверьте результат

```bash
# Полная статистика
sudo /usr/local/bin/analyze-webhook-logs.sh

# Или специфично для service center
sudo /usr/local/bin/analyze-webhook-logs.sh --service-center
```

## 🔍 ПРОВЕРКА ДО ИЗМЕНЕНИЙ

Убедитесь, что проблема именно в отсутствии воркфлоу:

```bash
# Протестируйте существующий endpoint (должен работать)
curl -X POST https://webhook.rentflow.rentals/ \
  -H "Content-Type: application/json" \
  -d '{"test": "ping", "event": "test.ping", "payload": "{\"id\": 1}"}'

# Протестируйте несуществующий (должен вернуть 404)
curl -X POST https://n8n.rentflow.rentals/webhook/service-center-webhook \
  -H "Content-Type: application/json" \
  -d '{"test": "ping"}'
```

## 📚 ПОДРОБНАЯ ДОКУМЕНТАЦИЯ

- **Полный диагноз:** `WEBHOOK_DIAGNOSIS.md`
- **Инструкция по настройке логов:** `WEBHOOK_DEBUG_SETUP.md`
- **Скрипт анализа:** `scripts/analyze-webhook-logs.sh`

## ⚡ АЛЬТЕРНАТИВА (если нельзя изменить URL)

Если URL в RentProg нельзя изменить, создайте дубликат воркфлоу:

1. Зайдите в n8n: `https://n8n.rentflow.rentals`
2. Откройте воркфлоу `RentProg Webhooks Monitor`
3. Сделайте дубликат (Duplicate)
4. В новом воркфлоу измените webhook path на `service-center-webhook`
5. Сохраните и активируйте

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После изменения URL:
- ✅ Вебхуки от RentProg будут стабильно приходить
- ✅ События будут сохраняться в таблицу `events`
- ✅ Данные будут передаваться в Jarvis API для обработки
- ✅ В логах будет видно все входящие запросы

## ❓ ВОПРОСЫ

**В: Почему вебхуки "иногда работали"?**
О: На самом деле они НЕ работали. Это могли быть события на другом endpoint или ошибка наблюдения.

**В: Почему нельзя создать воркфлоу для нового пути?**
О: Можно, но нет смысла дублировать логику. Лучше использовать существующий.

**В: Нужно ли менять nginx конфигурацию?**
О: Да, для добавления детального логирования. Это поможет отладить любые будущие проблемы.

**В: Что делать, если после изменения URL всё равно не работает?**
О: Проверьте логи: `sudo /usr/local/bin/analyze-webhook-logs.sh --full > report.txt`
И проверьте логи n8n: `docker logs n8n --tail 100`

## 📞 КОНТАКТЫ

При возникновении проблем:
1. Соберите логи (см. выше)
2. Проверьте статус n8n: `docker ps | grep n8n`
3. Проверьте логи nginx: `sudo tail -100 /var/log/nginx/webhook-debug.log`
