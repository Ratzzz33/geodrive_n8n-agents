# 🔍 Основные причины отсутствия вебхуков от RentProg

## ✅ Правильная конфигурация

**URL который должен быть указан в RentProg:**
```
https://webhook.rentflow.rentals
```

**Важно:**
- ❌ Без слэша в конце (`/`)
- ❌ Без пути (`/webhook/rentprog-webhook`)
- ✅ Просто домен: `https://webhook.rentflow.rentals`

---

## 🚨 Топ-5 причин отсутствия вебхуков

### 1. ❌ Неправильный URL в RentProg (самая частая причина)

**Проблема:** В настройках RentProg указан неправильный адрес.

**Проверка:**
1. Войдите в RentProg UI для каждого филиала
2. Настройки компании → Webhooks
3. Проверьте указанный URL

**Решение:**
- Убедитесь что URL: `https://webhook.rentflow.rentals` (без слэша и пути)
- Метод: `POST`, Формат: `JSON`
- Проверьте для всех филиалов (Тбилиси, Батуми, Кутаиси, Сервисный центр)

---

### 2. ❌ Workflow не активен в n8n

**Проблема:** Workflow "RentProg Webhooks Monitor" отключен.

**Проверка:**
1. Откройте http://46.224.17.15:5678
2. Найдите workflow "RentProg Webhooks Monitor"
3. Проверьте что он **Active** (зеленая кнопка)

**Решение:**
- Активируйте workflow (нажмите кнопку "Active")
- Проверьте Webhook node:
  - Path: `rentprog-webhook`
  - Production URL: `https://webhook.rentflow.rentals`

---

### 3. ❌ Nginx не запущен или не настроен

**Проблема:** Nginx не проксирует запросы к n8n.

**Проверка:**
```bash
sudo systemctl status nginx
sudo nginx -t
```

**Решение:**
```bash
# Запустить Nginx
sudo systemctl start nginx

# Проверить конфигурацию
sudo nginx -t

# Перезагрузить
sudo systemctl reload nginx
```

---

### 4. ❌ n8n контейнер не запущен

**Проблема:** n8n не работает.

**Проверка:**
```bash
docker ps | grep n8n
```

**Решение:**
```bash
cd /workspace
docker-compose up -d n8n
```

---

### 5. ❌ DNS не резолвится или SSL сертификат истек

**Проблема:** Домен недоступен или SSL недействителен.

**Проверка:**
```bash
# DNS
dig +short webhook.rentflow.rentals
# Должно вернуть: 46.224.17.15

# SSL
curl -I https://webhook.rentflow.rentals
```

**Решение:**
- Проверьте DNS A-запись для `webhook.rentflow.rentals` → `46.224.17.15`
- Обновите SSL: `sudo certbot --nginx -d webhook.rentflow.rentals --non-interactive --agree-tos --email admin@rentflow.rentals --redirect`

---

## 🔧 Быстрая диагностика

Запустите скрипт проверки:
```bash
cd /workspace
./check_rentprog_webhook_issues.sh
```

Скрипт автоматически проверит все компоненты и покажет проблемы.

---

## 📝 Пошаговая проверка

### Шаг 1: Проверка в RentProg
- [ ] URL: `https://webhook.rentflow.rentals` (без слэша и пути)
- [ ] Метод: POST
- [ ] Формат: JSON
- [ ] Проверено для всех филиалов

### Шаг 2: Проверка на сервере
```bash
# Запустите диагностику
./check_rentprog_webhook_issues.sh
```

### Шаг 3: Проверка workflow в n8n
- [ ] Workflow "RentProg Webhooks Monitor" активен
- [ ] Webhook node путь: `rentprog-webhook`
- [ ] Production URL: `https://webhook.rentflow.rentals`

### Шаг 4: Тестовый запрос
```bash
curl -X POST https://webhook.rentflow.rentals \
  -H "Content-Type: application/json" \
  -d '{"event":"test","payload":"{\"id\":\"test_123\"}"}'
```

### Шаг 5: Проверка executions
1. Откройте n8n UI → Workflow "RentProg Webhooks Monitor"
2. Перейдите в "Executions"
3. Должны быть новые executions после каждого вебхука

---

## 📊 Мониторинг

### Логи Nginx
```bash
sudo tail -f /var/log/nginx/webhook-access.log
```

### Логи n8n
```bash
docker logs n8n --tail 50 -f
```

### Executions в n8n
- Откройте workflow → Executions
- Если executions есть → вебхуки доходят ✅
- Если executions нет → проблема в Nginx/DNS ❌

---

## 🎯 Что делать если вебхуки не приходят

1. **Проверьте URL в RentProg** (самая частая причина)
   - Должен быть: `https://webhook.rentflow.rentals` (без слэша и пути)

2. **Проверьте workflow в n8n**
   - Должен быть Active
   - Webhook node должен иметь правильный путь

3. **Запустите диагностику**
   ```bash
   ./check_rentprog_webhook_issues.sh
   ```

4. **Проверьте логи**
   ```bash
   # Nginx
   sudo tail -f /var/log/nginx/webhook-access.log
   
   # n8n
   docker logs n8n --tail 50 -f
   ```

5. **Отправьте тестовый запрос**
   ```bash
   curl -X POST https://webhook.rentflow.rentals \
     -H "Content-Type: application/json" \
     -d '{"event":"test","payload":"{\"id\":\"test_123\"}"}'
   ```

---

## 📚 Дополнительная документация

- [WEBHOOK_TROUBLESHOOTING.md](./WEBHOOK_TROUBLESHOOTING.md) - Подробная диагностика всех проблем
- [check_rentprog_webhook_issues.sh](./check_rentprog_webhook_issues.sh) - Скрипт автоматической диагностики

---

**Последнее обновление:** 2025-01-15
