# Диагностика проблемы: Вебхуки от RentProg не приходят

**Дата:** 2025-11-02  
**Статус:** 🔍 Диагностика завершена

---

## ✅ Что проверено

1. **Workflow активен** - ✅ Да
2. **Webhook endpoint доступен** - ✅ 200 OK
3. **Тестовый запрос проходит** - ✅ Успешно
4. **Конфигурация n8n** - ✅ Правильная

---

## 🔍 Возможные причины

### 1. Проблема с HTTPS в Nginx (наиболее вероятно)

**Проблема:** В локальной конфигурации `nginx/webhook.rentflow.rentals.conf` указан только HTTP (порт 80), но RentProg отправляет на HTTPS (443).

**Решение:** Обновить конфигурацию на сервере для поддержки HTTPS.

**Проверка на сервере:**
```bash
ssh root@46.224.17.15

# Проверить реальную конфигурацию
cat /etc/nginx/sites-available/webhook.rentflow.rentals.conf | grep -E "listen|ssl"

# Должно быть:
# listen 443 ssl http2;
```

**Если HTTPS не настроен**, обновить конфигурацию на сервере используя обновленный файл `nginx/webhook.rentflow.rentals.conf` из этого репозитория.

### 2. RentProg не отправляет вебхуки

**Проверка:**
- Убедитесь что в RentProg вебхуки **активированы** для каждого филиала
- Проверьте логи RentProg (если доступны)
- Попробуйте отправить тестовый вебхук из интерфейса RentProg

### 3. Nginx не проксирует правильно

**Проверка логов на сервере:**
```bash
ssh root@46.224.17.15

# Проверить последние запросы
tail -50 /var/log/nginx/webhook-access.log

# Проверить ошибки
tail -50 /var/log/nginx/webhook-error.log
```

Если в логах **нет запросов** от RentProg - проблема на стороне RentProg или DNS.

Если в логах **есть запросы, но ошибки** - проблема с проксированием или n8n.

### 4. Firewall блокирует запросы

**Проверка:**
```bash
# Проверить открытые порты
ufw status | grep -E "443|80"
# или
iptables -L -n | grep -E "443|80"
```

---

## 🔧 Пошаговое исправление

### Шаг 1: Обновить конфигурацию Nginx на сервере

1. Скопировать обновленный файл на сервер:
```bash
scp nginx/webhook.rentflow.rentals.conf root@46.224.17.15:/tmp/
```

2. На сервере:
```bash
ssh root@46.224.17.15

# Создать бэкап текущей конфигурации
cp /etc/nginx/sites-available/webhook.rentflow.rentals.conf /etc/nginx/sites-available/webhook.rentflow.rentals.conf.backup

# Скопировать новую конфигурацию
cp /tmp/webhook.rentflow.rentals.conf /etc/nginx/sites-available/webhook.rentflow.rentals.conf

# Проверить конфигурацию
nginx -t

# Если OK, перезагрузить
systemctl reload nginx
```

### Шаг 2: Проверить SSL сертификат

```bash
# Проверить наличие
ls -la /etc/letsencrypt/live/webhook.rentflow.rentals/

# Если нет, получить:
certbot --nginx -d webhook.rentflow.rentals --non-interactive --agree-tos --email admin@rentflow.rentals --redirect
```

### Шаг 3: Проверить логи

```bash
# Запросы
tail -f /var/log/nginx/webhook-access.log

# Ошибки
tail -f /var/log/nginx/webhook-error.log
```

### Шаг 4: Отправить тестовый вебхук

```bash
curl -X POST "https://webhook.rentflow.rentals/" \
  -H "Content-Type: application/json" \
  -d '{"ts": "2025-11-02T19:00:00Z", "branch": "tbilisi", "type": "test", "ok": true}'
```

**Ожидается:**
- В Telegram: debug сообщение
- В БД: новая запись в таблице `events`
- В n8n: новое execution

---

## 📋 Чек-лист после исправления

- [ ] Nginx конфигурация обновлена (HTTPS добавлен)
- [ ] `nginx -t` проходит без ошибок
- [ ] Nginx перезагружен
- [ ] SSL сертификат существует и действителен
- [ ] HTTPS endpoint доступен (curl возвращает 200)
- [ ] Тестовый вебхук проходит успешно
- [ ] Debug сообщения приходят в Telegram
- [ ] Записи появляются в таблице `events`
- [ ] В логах nginx видны запросы от RentProg

---

## 🚨 Если проблема сохраняется

1. **Проверить RentProg:**
   - Действительно ли вебхуки отправляются? (проверить логи RentProg)
   - Правильный ли URL указан? (должен быть `https://webhook.rentflow.rentals` без пути)

2. **Проверить DNS:**
   ```bash
   dig webhook.rentflow.rentals
   # Должно указывать на 46.224.17.15
   ```

3. **Проверить доступность изнутри сети RentProg:**
   - Возможно firewall блокирует исходящие HTTPS соединения

4. **Проверить логи n8n:**
   ```bash
   docker logs n8n --tail 100 | grep -i webhook
   ```

---

## 📝 Примечания

1. **Локальный файл обновлен:** `nginx/webhook.rentflow.rentals.conf` теперь содержит HTTPS конфигурацию
2. **Нужно обновить на сервере:** Скопировать обновленную конфигурацию на сервер и перезагрузить nginx
3. **SSL сертификат:** По документации уже настроен до 2026-01-31, но нужно проверить наличие на сервере

