# Настройка MCP Chrome DevTools - Пошаговая инструкция

## ✅ Проверено: Всё готово к работе

- ✅ Node.js установлен: `C:\Program Files\nodejs\node.exe`
- ✅ Скрипт существует: `C:\Users\33pok\geodrive_n8n-agents\mcp-server\chrome-devtools-server.js`
- ✅ Зависимости установлены

## 📋 Шаг за шагом

### Шаг 1: Откройте настройки Cursor

1. Нажмите **`Ctrl+,`** (или `Cmd+,` на Mac)
2. В поиске введите: **`MCP`** или **`Model Context Protocol`**
3. Откройте **Features → Model Context Protocol**

### Шаг 2: Найдите секцию "MCP Servers"

В настройках должна быть секция для конфигурации MCP серверов. Обычно это JSON конфигурация.

### Шаг 3: Скопируйте и вставьте эту конфигурацию

**ВАЖНО:** Если там уже есть другие серверы, добавьте `chrome-devtools` в существующий объект `mcpServers`.

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "C:\\Program Files\\nodejs\\node.exe",
      "args": [
        "C:\\Users\\33pok\\geodrive_n8n-agents\\mcp-server\\chrome-devtools-server.js"
      ],
      "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents"
    }
  }
}
```

**Если у вас уже есть другие серверы, добавьте `chrome-devtools` так:**

```json
{
  "mcpServers": {
    "ваш-другой-сервер": { ... },
    "chrome-devtools": {
      "command": "C:\\Program Files\\nodejs\\node.exe",
      "args": [
        "C:\\Users\\33pok\\geodrive_n8n-agents\\mcp-server\\chrome-devtools-server.js"
      ],
      "cwd": "C:\\Users\\33pok\\geodrive_n8n-agents"
    }
  }
}
```

### Шаг 4: Сохраните настройки

Сохраните изменения (обычно автоматически, но проверьте).

### Шаг 5: Полностью перезапустите Cursor

**КРИТИЧЕСКИ ВАЖНО:** 
- Закройте ВСЕ окна Cursor
- Убедитесь, что процесс полностью завершён
- Откройте Cursor снова

### Шаг 6: Проверьте подключение

После перезапуска:

1. Откройте чат с AI в Cursor
2. Попробуйте команду:
   ```
   Открой страницу https://google.com и сделай скриншот
   ```

Если AI может использовать инструменты браузера - всё работает! ✅

## 🔍 Где смотреть логи

Если что-то не работает:

1. В Cursor: **View → Output** (или `Ctrl+Shift+U`)
2. В выпадающем списке выберите: **"MCP"** или **"chrome-devtools"**
3. Проверьте, нет ли ошибок

## ❌ Если всё ещё не работает

### Проверка 1: Путь к Node.js

Откройте командную строку и выполните:
```cmd
"C:\Program Files\nodejs\node.exe" --version
```

Должно вывести версию (например, `v24.11.0`).

### Проверка 2: Путь к скрипту

В Проводнике Windows перейдите:
```
C:\Users\33pok\geodrive_n8n-agents\mcp-server\
```

Должен быть файл `chrome-devtools-server.js`.

### Проверка 3: Тест запуска сервера

В командной строке (в папке проекта):
```cmd
cd C:\Users\33pok\geodrive_n8n-agents
"C:\Program Files\nodejs\node.exe" mcp-server\chrome-devtools-server.js
```

Если выводит ошибки - исправьте их.
Если ничего не выводит (просто висит) - это нормально (MCP работает через stdio).

**Нажмите Ctrl+C для выхода.**

### Проверка 4: Формат JSON

Убедитесь, что JSON валидный:
- Все строки в кавычках
- Двойные обратные слеши `\\` в путях Windows
- Правильные запятые между элементами

## ✅ Готовая конфигурация

Готовая конфигурация сохранена в файле: **`CURSOR_MCP_FINAL_CONFIG.json`**

Скопируйте содержимое оттуда в настройки Cursor.

## 📚 Дополнительная помощь

- [FIX_MCP_CONFIG.md](./FIX_MCP_CONFIG.md) - Другие варианты решения
- [MCP_CHROME_DEVTOOLS_SETUP.md](./MCP_CHROME_DEVTOOLS_SETUP.md) - Полная документация
- [QUICK_SETUP_MCP.md](./QUICK_SETUP_MCP.md) - Быстрая настройка

