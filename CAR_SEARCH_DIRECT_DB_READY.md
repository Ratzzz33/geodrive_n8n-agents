# 🚗 Car Search AI Assistant (Прямой запрос к БД) - Готово!

**Дата:** 2025-11-08  
**Статус:** ✅ Готов к тестированию

---

## 🎉 Что сделано:

### Query Car Search Tool (Direct DB) ✅
- **ID:** `kI4mXx8MuOBa37lp`
- **URL:** https://n8n.rentflow.rentals/workflow/kI4mXx8MuOBa37lp
- **Тип:** Прямой SQL запрос к PostgreSQL (Neon)
- **Валидация:** ✅ Пройдена, 0 ошибок
- **Преимущества:**
  - ✅ Не требует Jarvis API
  - ✅ Прямое подключение к БД
  - ✅ Быстрее чем HTTP запросы
  - ✅ Работает сразу после активации

### Car Search AI Assistant ✅
- **ID:** `z1b7wIj17ppMuU7a`
- **URL:** https://n8n.rentflow.rentals/workflow/z1b7wIj17ppMuU7a
- **Статус:** Подключен к новому Tool
- **Готов к активации**

---

## 🏗️ Архитектура:

```
Telegram Message
    ↓
[AI Agent] ← [OpenAI GPT-4o-mini]
    ↓
[Tool: Query Car Search]
    ↓
[SQL Query] → PostgreSQL (Neon)
    ↓
SELECT cars, prices, branches
WHERE filters...
    ↓
[Format Response]
    ↓
Formatted message → Telegram
```

---

## 📋 SQL запрос в Tool:

```sql
SELECT 
  c.id,
  c.model,
  c.plate,
  c.year,
  c.transmission,
  c.seats,
  c.car_type,
  b.name as branch_name,
  b.code as branch_code,
  cp.price_gel,
  ROUND(cp.price_gel / 2.7, 2) as price_usd
FROM cars c
JOIN branches b ON c.branch_id = b.id
LEFT JOIN car_prices cp ON c.id = cp.car_id AND cp.period_days = 1
WHERE c.state = 1
  AND b.code = 'batumi'
  AND c.year >= 2010
  AND cp.price_gel <= (50 * 2.7)
  AND c.transmission = 'Автомат' -- если указано
ORDER BY cp.price_gel ASC 
LIMIT 10
```

**Параметры фильтрации:**
- `branch` - код филиала (tbilisi, batumi, kutaisi)
- `maxPriceUSD` - максимальная цена в долларах (конвертируется в GEL)
- `transmission` - коробка передач (Автомат, Механика)
- `yearFrom` - год выпуска от

---

## 🚀 Тестирование:

### Шаг 1: Открыть Tool workflow

```
https://n8n.rentflow.rentals/workflow/kI4mXx8MuOBa37lp
```

### Шаг 2: Тестировать Tool вручную

1. Нажмите **"Execute workflow"** (внизу справа)
2. В появившемся окне введите тестовые данные:

```json
{
  "branch": "batumi",
  "maxPriceUSD": 50,
  "transmission": "Автомат",
  "yearFrom": 2015
}
```

3. Нажмите **"Execute"**
4. Проверьте результат

**Ожидаемый вывод:**
```json
{
  "message": "🚗 Нашел 3 машины в Батуми:\n\n1. Ford Fiesta (2015)\n   📍 Батуми\n   ⚙️ Автомат | 5 мест\n   💰 96 GEL/день (≈$35.56)\n...",
  "count": 3,
  "results": [...]
}
```

---

### Шаг 3: Активировать Car Search AI Assistant

1. Откройте: https://n8n.rentflow.rentals/workflow/z1b7wIj17ppMuU7a
2. Нажмите toggle **"Active"**
3. **Красный восклицательный знак должен исчезнуть!**

---

### Шаг 4: Настроить Telegram webhook

1. Кликните на **"Telegram Trigger"** ноду
2. Скопируйте **"Webhook URL"**
3. Установите webhook:

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=<WEBHOOK_URL>"
```

**Проверьте:**
```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

---

### Шаг 5: Тестировать через Telegram

Отправьте боту:

**Простые запросы:**
- "Покажи машины в Батуми"
- "Что есть до 50 долларов?"
- "Найди автомат"

**С фильтрами:**
- "Машины в Батуми до 50 долларов с автоматом"
- "Покажи машины от 2015 года"
- "Что есть в Тбилиси до 40 долларов?"

**Ожидаемый ответ:**
```
🚗 Нашел 3 машины в Батуми:

1. Ford Fiesta (2015)
   📍 Батуми
   ⚙️ Автомат | 5 мест
   💰 96 GEL/день (≈$35.56)

2. Honda Fit EX (2015)
   📍 Батуми
   ⚙️ Автомат | 5 мест
   💰 107 GEL/день (≈$39.63)

📊 Макс. цена: $50
⚙️ Коробка: Автомат
```

---

## 🔍 Отладка:

### Если Tool не возвращает результаты:

1. **Проверить данные в БД:**
   ```sql
   SELECT COUNT(*) FROM cars WHERE state = 1;
   SELECT COUNT(*) FROM car_prices;
   SELECT * FROM branches;
   ```

2. **Проверить SQL запрос в Tool:**
   - Откройте Query Database ноду
   - Посмотрите сгенерированный SQL
   - Проверьте что поля существуют

3. **Проверить credentials:**
   - Postgres account должен быть подключен
   - Database: `neondb`
   - Host: `ep-rough-heart-ahnybmq0-pooler.c-3.us-east-1.aws.neon.tech`

### Если AI Agent не вызывает Tool:

1. **Проверить toolDescription:**
   - Должно быть: "Используй этот инструмент ДЛЯ ЛЮБОГО запроса..."

2. **Проверить system message в AI Agent:**
   - Должны быть четкие инструкции
   - Примеры запросов

3. **Проверить OpenAI credentials:**
   - Model: `gpt-4o-mini`
   - API key валиден

### Если Telegram не отвечает:

1. **Проверить webhook:**
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
   ```

2. **Проверить workflow активирован:**
   - https://n8n.rentflow.rentals/workflow/z1b7wIj17ppMuU7a
   - Toggle должен быть зеленым

3. **Проверить executions:**
   - https://n8n.rentflow.rentals/workflow/z1b7wIj17ppMuU7a/executions
   - Посмотреть ошибки

---

## 📊 Доступные фильтры:

| Параметр | Тип | Описание | Пример |
|----------|-----|----------|--------|
| `branch` | string | Код филиала | tbilisi, batumi, kutaisi |
| `maxPriceUSD` | number | Макс. цена в $ | 50 |
| `transmission` | string | Коробка передач | Автомат, Механика |
| `yearFrom` | number | Год выпуска от | 2015 |

**По умолчанию:**
- branch: `batumi`
- maxPriceUSD: `999` (без ограничения)
- yearFrom: `2010`

---

## 🎯 Преимущества прямого запроса к БД:

✅ **Не нужен Jarvis API** - работает сразу  
✅ **Быстрее** - нет HTTP запросов  
✅ **Проще отладка** - видно SQL запрос  
✅ **Меньше точек отказа** - только n8n + PostgreSQL  
✅ **Гибкость** - легко добавить новые фильтры  

---

## 📝 Как добавить новые фильтры:

### 1. Добавить в Tool Description:

Откройте **"Tool: Query Car Search"** ноду → **fields** → **Add Field**:

```json
{
  "name": "carType",
  "type": "string",
  "description": "Тип кузова: Седан, Кроссовер, Хэтчбек"
}
```

### 2. Добавить в SQL запрос:

В **"Query Database"** ноде добавьте:

```javascript
const carType = $json.carType;

// В WHERE добавить:
if (carType) {
  query += ' AND c.car_type = \'' + carType + '\'';
}
```

### 3. Обновить System Message в AI Agent:

Добавьте примеры с новым фильтром:
```
Примеры:
- Найди кроссоверы в Батуми
- Покажи седаны до 40 долларов
```

---

## ✅ Чек-лист:

### Создание:
- [x] Query Car Search Tool (Direct DB) - создан ✅
- [x] SQL запрос работает ✅
- [x] Format Response готов ✅
- [x] Валидация пройдена ✅

### Тестирование:
- [ ] Tool протестирован вручную (Execute workflow)
- [ ] Car Search AI Assistant активирован
- [ ] Telegram webhook настроен
- [ ] Тест через Telegram пройден

---

## 🎉 Готово!

**Car Search AI Assistant** с **прямым запросом к БД** готов к использованию!

**Преимущества:**
- Работает без Jarvis API
- Быстрее и проще
- Легко расширяется

**Следующие шаги:**
1. Откройте Tool workflow и протестируйте вручную
2. Активируйте AI Assistant workflow
3. Настройте Telegram webhook
4. Тестируйте!

---

**Последнее обновление:** 2025-11-08 12:45  
**Версия:** 2.0.0 (Direct DB)  
**Статус:** ✅ Готов к использованию

